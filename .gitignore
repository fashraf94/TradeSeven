# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env*.local

# Environment variables
.env
.env.local
.env*.local
.env.local.backup
.env.backup
*.env.backup

# Refresh script output
scripts/output/
scripts/test-results/*
!scripts/test-results/20260427-224157Z/
!scripts/test-results/20260427-224157Z/**

# Sprint 1 — local-only verification utility (do not commit)
scripts/preview-consolidation-prompt.js

# Sprint 1 — local-only investigation report (do not commit)
REFLECTION_WRITER_INVESTIGATION.md
REFLECTION_WRITER_INVESTIGATION_V2.md

# Sprint 2 — local-only film room audit (do not commit)
FILM_ROOM_AUDIT.md

# Sprint 2 — local-only technical context exposure audit (do not commit)
TECHNICAL_CONTEXT_EXPOSURE_AUDIT.md

.vercel

# Claude Code agent harness — local worktrees for parallel agent runs
.claude/worktrees/

# WS1 observe-walk — local-only GCS service-account key for the read-back (never commit)
.gcs-credentials.json
