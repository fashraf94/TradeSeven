# Phase 3 Design Gate — Review Round 1 Triage Matrix

**Date:** July 23, 2026 · **Reviewer:** ChatGPT · **Triage:** Claude (author)
**Verdict confirmed:** NOT LOCK-READY. **Tally:** 10 BLOCKER + 3 MAJOR → 13 accepted in substance (4 AMENDED, 4 VERIFICATION-DEPENDENT), 0 rejected.
**Reviewer caveat honored (founder-flagged):** ChatGPT has no repo access; findings marked ⧗ rest on wire semantics inferred from provenance summaries — fix direction locked now, final text lands after CC's wire read.

| # | Sev | Target | Disposition | Resolution |
|---|---|---|---|---|
| 1 | BLK | Guide §3/§7 | **ACCEPT** | The pipeline-relative-reinterpretation sentence is DELETED — it contradicted Spec §5.1 (one global `intendedMode` per ruleId). Rule: mode is immutable per ruleId; archetype-relative *demotion* happens ONLY via the ratified tension treatment `advisoryDowngrade`; where two archetypes need genuinely different semantics of one condition, they are two ruleIds (same `family`). Highest-risk catch of the round. |
| 2 | BLK ⧗ | FI constitution | **ACCEPT-AMENDED** | The two-tier structure becomes explicit: `<40` = live hard exclusion (wire) · `≥5 names >70` = the quality-core cohort (wire) · 40–70 residuals = mechanically eligible, and the archetype's refusal is the founder-pinned **chart-motivated entry** into that band ("won't let a hot chart talk it into a mediocre business") — an identity refusal about *choice*, not a false claim of mechanical exclusion. Kernel, render, and rubric rewritten around the two thresholds. ⧗ exact constraint text + whether the floor binds at swap-time verified by CC first. |
| 3 | BLK ⧗ | Speculator constitution | **ACCEPT-AMENDED** | Same identity-vs-mechanics distinction: the ≥3-above-0.80 wire is a quota; "never buys boring" is the definition-doc refusal of *instructed safety* (founder-locked), not a claim of universal mechanical exclusion. Fixes: label the refusal definition-derived; soften "a calmer name loses every time" to the weight-dominance claim the 0.60 wire supports; rubric step 1 narrowed — a rule *requiring* low-vol/stable names stays core_conflict; a rule *permitting* a residual calmer holding is tension. ⧗ constraint text verified by CC. |
| 4 | BLK | TF constitution | **ACCEPT** | True and embarrassing: TF was authored before the provenance-header pattern existed and never retrofitted. Fix: full live-wire header added (technical 0.40 / fundamental 0.05, top-3-sector constraint, forcedRotation ON brisk 8/60min, temp 0.3); every kernel clause labeled fired-wire / definition-derived / PENDING — with one honest downgrade: **macro regime (priority 4) is my insertion, not doc-grounded** → labeled PENDING-VERIFICATION. Founder approval stands for grounded clauses; the macro clause is re-presented at revision. |
| 5 | BLK | Guide header/§10 | **ACCEPT** | Two fixes: (a) status honesty — constitution status lines updated to FOUNDER-APPROVED (content) with lock-state a separate field; the guide stops saying "locked." (b) The substantive one: **cell schema gains `{kernelIdentityVersion, kernelIdentityHash, status: provisional\|locked}`**; the activation gate counts only `locked` cells whose hash matches the current registry — the §3.3 exact-parent-compile discipline applied to authored content. |
| 6 | BLK ⧗ | CP constitution | **ACCEPT-AMENDED** | Absence-based identity, correctly caught. Kernel narrowed: loss-side claims stand (no stall/noise/opportunity-cost exits — wire + definition grounded); profit-side becomes a *disposition* ("winners are not scalped; no eager profit-taking; its allowlist carries no profit-taking adjustment — by design"), NOT an exit ontology. Rubric: profit-target-style rules → **tension**, not core_conflict. ⧗ CC enumerates every runtime path that can exit a guardian winner (trailing stops, swap cadence, circuit breaker) before final text. |
| 7 | BLK | Guide §2 + CP | **ACCEPT** | `noise_discounted` gets an operational definition: every use must name an executable threshold source — for CP, the **patient-stop value is the authoritative risk line** (sub-stop price action = noise) plus fundamental deterioration. Interim rule adopted verbatim: short-term-price rules are **tension pending a cited threshold**, never core_conflict on vibes. Connects to F8: "fires on noise" is defined via paramBounds, not adjectives. |
| 8 | BLK | Guide §1/§4/§8 | **ACCEPT** | Workflow amended: for parameterized rules, **cell + treatment + paramBounds author atomically**, and the decision basis is defined per ChatGPT's rule — judge the full template domain; if the mechanism is valid only within a subset → tension + narrowedParams with the admitted domain recorded in the cell. |
| 9 | MAJ | TF render | **ACCEPT** | Render inclusion test defined for all six: any kernel clause that (a) distinguishes core_conflict from tension or (b) can collide with rendered rules MUST appear in the identity block. TF's render gains tape-tempo/rotation and the two-leg hold-and-surface clause; its 110 cap is retired for the uniform 175 regime. |
| 10 | BLK | Guide §7 + FI | **ACCEPT-AMENDED** | The inverse display-agreement catch. Split ratified: **exact matches to the live shortlist floor** (e.g. "exclude fundamentalScore <40") are deterministic-in-fact and must not author as advisory — they are **held out of authoring until the substrate is ratified**; parameterized modifications (a custom 80 bar) stay `prompt_advisory`. Amendment: rather than wait for a distant Sheet C, the **shortlist-substrate ratification is pulled forward** into this revision cycle as Amendment Sheet C item 1 (one paragraph: the archetype-owned shortlist layer is a third deterministic substrate class with `detectorSource` citing the live floor). |
| 11 | MAJ | Guide §5/§8/§10 | **ACCEPT** | The contradiction resolves in the strict direction: **verification-first** — §10's "cells may be authored meanwhile" clause is deleted (the CC read is one small session; a provisional workspace isn't worth its bookkeeping). Signal-table rows gain a machine-readable `verificationId/status`; no cell may cite an unverified row. |
| 12 | BLK | Guide §9 + Contrarian | **ACCEPT** | The controlling rule is the definition doc's own: explicit conversation grants authority. `hold_line` is defined as a shared interaction kind: **trigger** = a request contradicting a standing risk line · **allowed action** = one in-character advisory pushback, then bounded compliance on explicit confirmation **through the directive gate** (which GameModePolicy tiers already govern) · **receipt** = the directive logged + the pushback noted. Neither silent compliance nor open-ended resistance. Kernel text was already consistent; the guide §9 wording is corrected. |
| 13 | MAJ | Sheet B-2 | **ACCEPT** | Transitional pin added verbatim: both sites pinned to `'balanced'`; neither changes unless both change in one §7-gated commit; parity test asserts battle default == manifest default. B-1 stands RATIFIED → Sheet B closes with this edit. |

**Reviewer meta-answers, logged:** highest-risk document = the guide (confirmed by F1); rubric most likely inconsistent at scale = **Diversifier** — accepted, and it compounds the existing cap/weights verification priority: Diversifier authors LAST, after its wires are verified and after the gate/ranking fix (F1) and pipeline-position facts (CC read) are in hand.

**Round 2 scope:** consolidated revisions (Guide V1.1, four constitution patches, Sheet B final, Amendment C item 1) return with this matrix for delta review + blocker-closure verification only.
