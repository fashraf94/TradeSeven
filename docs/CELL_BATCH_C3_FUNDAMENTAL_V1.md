# Compatibility Cells — Batch C3: FUNDAMENTAL FAMILY (6 rules × 5 archetypes, less two deferrals = 29 cells) — V1.2

**Date:** July 29, 2026 · **Authority:** the six locked kernels · ADJUDICATION_RULINGS **V1.6** — **citable:** R-2…R-8, R-10, R-11, R-13…R-33 (R-22 as amended), and the C3 mints **R-34 (as amended), R-35, R-37, R-38 (as amended), R-39, R-41**, each binding on closure confirmation + founder acceptance · **DEFERRED and UNCITABLE:** R-9, **R-36**, **R-40** · **NONPRECEDENTIAL:** R-1, R-12 · conventions C-13…C-18 · Signal Inventory V2 · `ruleSupportStatus.js` @ HEAD.
**Scope note (the batch's defining fact):** these are **the seven rules the fundamental mirror wire un-hid**. Every cell below rests on data the agent provably receives — `trailingPE {value, sectorMedian}`, `revenueGrowthPct`, `priceBookMRQ`, `marketCapClass`, `beatRate` (real-only), `surpriseMagPercentile`, `earningsRevisions30d`, `fundamentalScore` — verified rendering in production July 29. The six re-triaged rules are `hidden_absent_substrate`; no cells authored (R-5 binds `f-10` on return under semantic identity). Diversifier held.
**⚠ f-12 WITHDRAWN FROM THE BATCH (R1-F1).** Its `days` parameter (14–60) is **inert** — every setting reads the same fixed `earningsRevisions30d` field, so R-36's narrowing distinguished settings the substrate cannot distinguish. All five f-12 cells are DEFERRED and R-36 is DEFERRED (uncitable) until either window-specific revision data exists or the corpus rule is re-predicated to a fixed 30-day window. *This was a substrate fiction recorded in V1.0's own data-honesty note and then narrowed anyway — the exact failure class the program exists to catch.*
**Standards in force:** natives cite a named kernel element (adjacency ≠ corroboration; a rejected demotion is not proof) · every verdict is full-domain · adapted case law mints new rulings with exact bounds · **every tension cell carries its own complete advisory sentence** (no cross-references).
*Paths (C-16): draft board + battle-eval swap candidates; the fundamentals block renders on both eval and draft surfaces post-wire. Emergency (C-17, family-wide): the emergency replacement path is signal-free and ignores every screen and preference below.*

## C3 mints (exact bounds of record; full reasoning in-cell)
**R-34 (AMENDED at review) — ENUMERATED SCORING-PREFERENCE CLASS RULING · degen:** stored core_conflict → **TENSION** for the three true scoring-preference clients **and only these: `fund-revenue-growth`, `fund-value-pe`, `fund-bank-pb`.** Doctrine: **preference/tiebreak → tension · requirement/gate → core_conflict · truly unrelated evidence → neutral.** The friction is that the rule asks the model to weigh evidence the kernel says is not evidence — not that the evidence is forbidden. *R-34 no longer governs market cap (own ruling, R-39), f-12 (deferred), or tv-10 (a gate — R-41).*
**R-35 (RATIFIED, rationale corrected)** f-07/degen → **TENSION**: the verdict carrier is the evidence relationship (a preference on excluded evidence); that surprise names often *are* volatile is mitigating context, not the reason.
**R-36 — DEFERRED, UNCITABLE** (f-12/contrarian): the `days` bound cannot repair an inert parameter. Unblocking: window-specific data, or re-predication to a fixed window.
**R-37 (RATIFIED)** tv-10/momentum_chaser → **TENSION + narrowedParams {fund_score ∈ [40,55]}**.
**R-38 (AMENDED at review)** tv-10/analyst → **TENSION + narrowedParams {fund_score ∈ [71,85]}**: the FI kernel refuses below 40, reaches but does not *choose into* the 40–70 band on chart heat, and cores above 70 — so a bound of 65 admitted the mediocre-business range the ruling claimed to prevent. Integer form recorded.
**R-39 (NEW)** fund-market-cap/degen → **TENSION**: market cap is **not fundamental quality evidence** (so R-34's class does not reach it); the friction is that the `large` default selects the structurally calmest cohort on the archetype's dominant axis while remaining a preference, never an override.
**R-40 — DEFERRED, UNCITABLE** (tv-10/contrarian): the review took the stated fallback, and correctly — **a prominently labeled substrate fiction is still substrate fiction.** `technicalScore` has no verified semantic contract, so nothing establishes that 60 is early-turn evidence or that 61 is established momentum; the R-36 standing check applies to this cell as much as to f-12's. The tension *mechanism* analysis survives as non-binding input for re-adjudication. **Unblocking:** the technicalScore contract mints an evidence-based boundary.
**R-41 (NEW)** tv-10/degen → **CORE_CONFLICT**: stored core_conflict SUSTAINED. A fundamental-score floor conditioning *tier eligibility* makes company quality a requirement that determines exposure — the SP kernel's named conflict, not a tiebreak. R-34's preference doctrine expressly does not reach it.

---

## C3-1 · fund-revenue-growth — prefer revenue growth above {pct}% (5–30)
*Signal: `revenueGrowthPct` [V, wired] — ×100-normalized, verified in production.*
- **momentum_chaser: neutral (stored)** — a growth preference on deprioritized evidence; it reorders among candidates and never claims to override trend evidence (R-4's line exactly). Full domain: every value is a threshold on the same preference.
- **contrarian: neutral (stored)** — growth and dislocation are orthogonal; a washed-out grower is a common contrarian setup, and the rule imposes no crowd-following.
- **degen: TENSION (R-34 class client)** — a scoring preference on evidence the kernel excludes at zero weight; the friction is being asked to weigh non-evidence, not a forced low-volatility selection. Guidance (advisory): the agent is instructed to preserve volatility as the ranking basis; the growth preference may be considered only after volatility-qualified candidates are otherwise indistinguishable.
- **guardian: neutral (stored)** — a selection preference; protective exits untouched.
- **analyst: NATIVE (stored, corroborated — kernel element: business-quality evidence as the primary standard; revenue growth is named quality evidence)**; full domain (the threshold sets strictness, not mechanism).

## C3-2 · fund-value-pe — prefer P/E below {level} (sector median / 20 / 15)
*Signal: `trailingPE {value, sectorMedian}` [V, wired] — the enriched pair carrying the comparison basis.*
- **momentum_chaser: TENSION (R-4, already binding — stored core_conflict superseded)** — a valuation preference is a scoring modifier among trend-qualified candidates, never a fundamental override of trend evidence; the audit's forward flag independently reached the same result. Guidance (advisory): the agent is instructed to apply the discount preference only among names already satisfying its trend criteria, and not to use the discount preference to admit a below-trend name. Full-domain check passes (all three levels are comparison thresholds on one mechanism).
- **contrarian: NATIVE (stored, corroborated — kernel element: the dislocation thesis; a sector-relative discount is the valuation face of a washed-out name)**; full domain. *Note (R-2 discipline): valuation evidences neither dislocation nor recovery by itself — the cell records that the preference is subordinate to those legs.*
- **degen: TENSION (R-34 class client)** — a valuation preference on excluded evidence. *(That cheap names are often volatile is mitigating context, not the verdict's basis — the evidence relationship carries it.)* Guidance (advisory): the agent is instructed to preserve volatility as the ranking basis; the valuation preference may be considered only after volatility-qualified candidates are otherwise indistinguishable.
- **guardian: neutral (stored)** — valuation preference; protection untouched.
- **analyst: neutral (stored)** — *not* native despite being quality-flavoured: the FI kernel's element is the two-tier quality *standard*, and a single-metric cheapness preference is one input to it, not the standard itself (the C1-6 adjacency lesson applied in the column where it is most tempting).

## C3-3 · fund-bank-pb — use P/B instead of P/E for banks; flag above {threshold} (1–3) as expensive
*Signal: `priceBookMRQ` [V, wired] + `industryName` [V, wired]. The corpus's only single-sector screen.*
- **momentum_chaser: neutral (stored)** — a sector-scoped valuation methodology note; no trend interaction at any threshold.
- **contrarian: neutral (stored)** — P/B discipline in one sector neither requires nor forbids dislocation hunting.
- **degen: TENSION (R-34 class client)** — the same preference mechanism at sector scope. Guidance (advisory): the agent is instructed to preserve volatility as the ranking basis; the bank-valuation preference may be considered only after volatility-qualified financials are otherwise indistinguishable.
- **guardian: neutral (stored)** — valuation methodology; protection untouched.
- **analyst: NATIVE (stored, corroborated — kernel element: sector-appropriate quality assessment; using P/B for banks *is* the doctrine's own worked example)**; full domain.

## C3-4 · fund-market-cap — prefer {size} cap stocks (large / mid / small)
*Signal: `marketCapClass` [V, wired] — derived and rendered post-wire (SIG-028 resolved). **All five stored cells are fallthrough — no explicit verdict has ever existed for this rule; these are the first.***
- **momentum_chaser: neutral** — strength exists in every cap class; a size preference reorders without touching trend evidence.
- **contrarian: neutral** — dislocation is cap-agnostic across the domain.
- **degen: TENSION (R-39 — its own ruling; market cap is NOT fundamental quality evidence, so the R-34 class does not reach it)** — fallthrough-neutral was silent permission on the archetype's dominant axis: the `large` default selects the structurally calmest cohort. A preference, never an override, so tension not conflict. Guidance (advisory): the agent is instructed to preserve volatility as the ranking basis; the size preference may be considered only after volatility-qualified candidates are otherwise indistinguishable. **paramBounds lean (non-binding): `small` and `mid` sit closest to the archetype's axis.**
- **guardian: neutral (R1-F6 — V1.0's tension WITHDRAWN)** — the V1.0 rationale graded this cell against *another optional card* (r-08's anchors) rather than against the kernel, and reintroduced the capitalization adjacency C1-6 already rejected: capitalization is not a CP kernel element, so an optional size preference cannot be identity tension. **The r-08 × fund-market-cap collision is real but belongs in rule-conflict/precedence metadata, not a compatibility cell** — filed there.
- **analyst: neutral** — cap class is orthogonal to the quality standard; quality spans sizes.

## C3-5 · f-07 — prioritize beat rate ≥{beat_pct}% (50–100) + surprise magnitude {decile}
*Signals: `beatRate` [V, wired — **real-only**; the D2 suppression means sector-default values never render] + `surpriseMagPercentile` [V, wired — gated on the same ≥4-quarter condition per F1].*
- **momentum_chaser: neutral (stored)** — earnings-surprise history is deprioritized evidence used as a preference; no trend override.
- **contrarian: neutral (stored)** — beat history is orthogonal to dislocation; a beaten-down serial beater is a coherent contrarian name.
- **degen: TENSION (R-35, rationale corrected)** — the verdict rests on the evidence relationship: a preference asking the model to weigh earnings evidence the kernel excludes at zero weight. *(That post-earnings-surprise names are frequently the volatility cohort is mitigating context, not the carrier.)* Guidance (advisory): the agent is instructed to preserve volatility as the ranking basis; the earnings-surprise preference may be considered only after volatility-qualified candidates are otherwise indistinguishable.
- **guardian: neutral (stored)** — a selection preference; protection untouched.
- **analyst: NATIVE (stored, corroborated — kernel element: business-performance evidence; execution consistency is named quality evidence)**; full domain (both params set strictness). *Data-honesty note: at the corpus max (beat_pct=100) the real-only suppression means thin-history names simply carry no beat rate and fall out of the preference — the cell records this rather than assuming coverage.*

## C3-6 · f-12 — **DEFERRED, NOT AUTHORED (R1-F1)**
All five cells withdrawn. The rule's `days` parameter (14–60) is inert: every setting reads the same fixed `earningsRevisions30d` field, so no verdict can distinguish the settings the substrate does not distinguish, and R-36's narrowing was a bound on a dial that does not turn. **Unblocking condition:** window-specific revision data lands, OR the corpus rule is re-predicated to a fixed 30-day window — then all five cells author fresh (the contrarian cell's *mechanism* analysis — name-level avoidance of the out-of-favour, the shape the map scores CC one scope-level out — survives as reasoning input, not as a binding bound). Filed to the honest-copy/producer worklist.

## C3-7 · tv-10 — Star/Core tier requires fund ≥{fund_score} (40–85) AND tech ≥{tech_score} (40–80)
*Signals: `fundamentalScore` [V, SIG-003] + `technicalScore` [U] — the tech leg's composite is unminted; the cell binds it to the rendered composite and says so (C-10).*
- **momentum_chaser: TENSION + narrowedParams: {fund_score ∈ [40,55]} (R-37 — stored neutral NOT SUSTAINED; audit flag accepted)** — a fundamental floor that restricts a chart-strong name to Core vetoes tier placement earned by priority-1 evidence with priority-6 evidence, which the kernel's ordering forbids; at fund_score 40–55 the gate is nominal and the dual requirement functions as confirmation stacking rather than a quality veto. Settings above 55 are not offered. Guidance (advisory): the agent is instructed that trend and momentum evidence governs tier placement and the fundamental floor operates only as a minimum sanity check, never as the ranking basis.
- **contrarian: DEFERRED, NOT AUTHORED (R2-F2; R-40 deferred).** The mechanism reading stands as non-binding input — at high technical floors a dislocated name with real recovery evidence but only an early turn is denied the higher tier until momentum is established, which the kernel rejects as lateness — but no bound can be authored while `technicalScore` lacks a verified contract, and V1.0's neutral-with-a-lean was equally insufficient under R1-8. **Unblocking:** the technicalScore contract mints an evidence-based early-turn/established-momentum boundary; the cell then authors fresh.
- **degen: CORE_CONFLICT (R-41 — stored core_conflict SUSTAINED; V1.0's tension WITHDRAWN)** — this is not a preference: a fundamental-score floor *conditions tier eligibility*, so company quality determines exposure. That is the SP kernel's named conflict (quality as a requirement), and R-34's preference doctrine expressly does not reach a gate. V1.0's guidance was also dishonest on its face — a floor cannot be "a formality it does not weigh" when the equipped rule requires the floor to change the result.
- **guardian: neutral (stored)** — a dual-confirmation tier gate is compatible caution; not a CP kernel element (the capitalization lesson), and protection is unaffected.
- **analyst: TENSION + narrowedParams: {fund_score ∈ [71,85]} (R-38 AS AMENDED; stored native NOT SUSTAINED)** — at fund_score 40 the "dual confirmation" bar sits at the outright-refusal floor, admitting Star names on chart heat with a nominal quality check, and the trailing clause still admits technical-only names at Core. The kernel refuses below 40, *reaches but does not choose into* the 40–70 band on chart heat, and cores above 70 — so the offered range begins at **71**, where the floor is a real standard and the rule is the two-tier quality test mechanized. *(V1.1 carried the amended bound in the registry but left 65 in this cell — the exact-bound control failing where it matters most, since the cell is what a consumer reads.)* Guidance (advisory): the agent is instructed the fundamental floor is the admission standard and the technical leg times entry within it — never the reverse.

---

## Batch findings (V1.1)
1. **29 cells: 4 native · 1 core_conflict · 8 tension (3 with binding narrowedParams) · 16 neutral.** Six cells deferred and not counted: all five f-12 cells (inert `days` parameter) and tv-10/contrarian (no verified `technicalScore` contract).
2. **The batch's own failures are its most important findings — and the standing check caught a second case on its first outing.** V1.0 narrowed f-12's inert `days` dial after recording that it was inert. V1.1 then labeled tv-10/contrarian's bound "provisional" and counted it anyway — **a prominently labeled substrate fiction is still substrate fiction.** Both are now deferred. **Standing check (adopted, and immediately load-bearing):** *before narrowing any parameter, confirm the substrate distinguishes the settings the narrowing separates — and if it cannot, defer rather than annotate.*
3. **The excluded-at-zero doctrine survives and is now precise** (R-34 as amended): preference/tiebreak → tension · **requirement/gate → core_conflict** · unrelated evidence → neutral. tv-10/degen is the gate case and returns to core_conflict (R-41); market cap is the unrelated-evidence case and gets its own ruling (R-39) because cap size is not quality evidence at all. R-34 is now an **enumerated class ruling** naming its three clients — informal "family reasoning" is no longer sufficient authority under the exact-citation control.
4. **The tv-10 mirror narrowing holds on the verified dial:** TF 40–55 (R-37) and FI 71–85 (R-38 amended above the kernel's actual core-quality line) — one parameter, two kernels, complementary bounds for opposite reasons. The contrarian narrowing on the *other* dial is deferred with its unverified signal.
5. **Guardian's market-cap cell corrected to neutral:** V1.0 graded it against another optional card rather than the kernel, reviving the capitalization adjacency C1-6 rejected. The r-08 collision is real and filed as rule-precedence metadata, which is where cross-rule conflicts belong.
6. **Speculator guidance rewritten across all six degen cells:** "carries no weight … as a tiebreak" was self-contradictory (a tiebreak carries weight). Every such cell now instructs volatility as the ranking basis with the fundamental preference considered only among otherwise-indistinguishable qualified candidates — and correlation facts ("cheap names are often volatile") are demoted to mitigating context, never verdict carriers.
7. Gate math: **45 + 105 + 29 = 179 / 475 authorable** (six deferred cells rejoin on their unblocking conditions).

## Review directives — STAMP ROUND
The three R2 residuals are closed: the analyst cell now carries the amended bound `{fund_score ∈ [71,85]}` in the verdict line, its reasoning, and every reference · R-40 is DEFERRED and uncitable with the tv-10/contrarian cell withdrawn (29 cells) · the authority header names V1.6 and lists citable vs deferred explicitly. Per the R2 verdict's own terms no further adjudication is required. Verdict: BATCH ACCEPTED (179/475) or the residual.