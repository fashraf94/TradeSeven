# FantasyTrades — Governing Documents (`docs/`)

This directory is the version-controlled source of truth for the League Tournament build and the contracts that govern it. It exists because the June 10 implementation discovery was blocked by exactly one thing: none of these documents lived in the repo. Every implementation chat reads from here, not from pasted context.

## Reading order for an implementation session

1. **`BUILD_RULES.md`** — the operating rules. Read first, always. (Also referenced from the repo-root `CLAUDE.md` so Claude Code loads it automatically.)
2. **`FANTASYTRADES_LEAGUE_TOURNAMENT_IMPLEMENTATION_SPEC_V1.md`** — the binding build spec. Where it differs from the framework, the spec wins; its §0 decisions register is the delta log.
3. **`FANTASYTRADES_LEAGUE_TOURNAMENT_DESIGN_FRAMEWORK_V2_1_AGENTIC.md`** — the design of record (the *why* behind the spec).
4. **`audits/2026-06-10_IMPLEMENTATION_DISCOVERY.md`** — the citation baseline for the build (file:line anchors, fence-contact register, verdicts).
5. As needed: the remaining documents below.

## Contents

| File | What it is |
|---|---|
| `BUILD_RULES.md` | Operating rules distillate: fence, branches, discovery protocol, import rule, signal capture, cron constraints. |
| `FANTASYTRADES_LEAGUE_TOURNAMENT_IMPLEMENTATION_SPEC_V1.md` | The binding implementation spec (June 11, 2026). Phase plan in §4. |
| `FANTASYTRADES_LEAGUE_TOURNAMENT_DESIGN_FRAMEWORK_V2_1_AGENTIC.md` | Design framework V2.1 (June 10, 2026) — design-complete record. |
| `FANTASYTRADES_PRELAUNCH_SEQUENCE.md` | The parent execution-sequence contract (May 12, 2026). |
| `FANTASYTRADES_PRELAUNCH_SEQUENCE_AMENDMENT_A_JUN10_2026.md` | **Added June 11, 2026 (verbatim founder paste)** — Amendment A: revised sequence of record, strikes Item 5, binds the Signal Capture Rider. |
| `VISION_PROGRAM_POST_LAUNCH_PLACEMENT_ADDENDUM_A_JUN10_2026.md` | **Added June 11, 2026 (founder upload `0beadef`)** — holds the signal-event catalog (§4) the Capture Rider binds to. |
| `audits/2026-06-10_TOURNAMENT_DESIGN_AUDIT.md` | The first read-only audit (@ `e3b4011`): P1 duplicate GO/NO-GO, tier seam, claims machinery, surprises incl. DST. *Transcribed — see provenance note.* |
| `audits/2026-06-10_IMPLEMENTATION_DISCOVERY.md` | The implementation discovery (@ `f12f852`): ledger viability, sourcing, orchestrator mechanics, fence-contact register. **The build's citation baseline.** *Transcribed — see provenance note.* |
| `audits/2026-06-10_V4_SCORER_TRIAGE.md` | The V4 nightly-scorer triage: stale-copy verdict, dual-writer severity model, §7-rider context. Fix merged (commit `5432c7f6`). *Transcribed — see provenance note.* |
| `audits/2026-06-12_P3_STAGE0_DISCOVERY_REPORT.md` | P3 Stage 0 discovery (@ `53d892e`): orchestrator topology, CPU model, board-prompt route, P3a/P3b split — proposals A–D as ratified. *Founder-supplied original, added byte-exact at P5.* |
| `audits/2026-06-12_P3A_PHASE_REPORT.md` | P3a phase report (@ `5c8a524`): agent board production, ledger-aware agent draft + acquisition, riders #2/#3 + #6 board-time half, P4 contract register. *Founder-supplied original, added byte-exact at P5.* |
| `audits/2026-06-12_P3B_PHASE_REPORT.md` | P3b phase report: cron + ET dispatcher, Monday pipeline, advancement + champion, CPU system agents, P4-gated deploy step. *Founder-supplied original, added byte-exact at P5.* |
| `audits/2026-06-12_P4_STAGE0_FENCE_EDIT_MAP.md` | P4 Stage 0 fence-edit map (founder-approved): the sanctioned fence entry's edit-by-edit contract. *Relocated from the repo-root upload (PR #491) at P5 — R100 rename, content untouched.* |
| `audits/2026-06-12_P4_PHASE_REPORT.md` | P4 phase report: the fence entry executed — 7-item contract, tiered-mode invariant battery, gate flip, founder-signed calibration table. *Relocated from the repo-root upload (PR #491) at P5 — R100 rename, content untouched.* |
| `audits/2026-07-12_LEAGUE_DEPARTED_POINTS_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the departed-position-points arc: departed-point sources (agent swaps → `trades[].lockedPoints`, user drops → `droppedPicks`), double-count map, `bankedBadgePoints`, Flat6 render delta, and the route-removal-only retirement verdict. Founder-approved to the record; folded into the Phase 1 commit. |
| `audits/20260712_COMPETITIVE_DRAFT_LIFECYCLE_DISCOVERY.md` | Read-only discovery for the Competitive Live Draft (slot lobbies) arc: the training-lobby map (state machine, writer, data model), the lift-and-adapt reuse verdict (one draft room for both modes), and the canonical-open blast-radius analysis (a DRAFTING/AWAITING_OPEN pod sits in front of the `status==='battle'` firewall). The build baseline for that arc; anchors verified at HEAD `8aeb377f`. *Founder-supplied original, added byte-exact.* |
| `audits/20260718_ENTRY_FLOW_CONSOLIDATION_DISCOVERY.md` | Read-only discovery for the Entry-Flow Consolidation arc: the five-entry-surface map (League center empty-state, `MyGameBar`, the "Pick your mode" fixtures stub, Quick Play, the slot picker's single mount), the conditional-`MyGameBar` verdict (signal exists via the widened `selectMyGroup`, needs a small lobby plumb), the status-surface routing-not-pages verdict, the donor-branch delete call, and the mirror-guard site + battle-week key normalization. Four load-bearing claims adversarially CONFIRMED; anchors verified at HEAD `51bc50ad`. |
| `audits/20260719_SEATED_WAITING_ROOM_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the Seated Waiting Room ("While you wait") arc: the seated-center render sites (desktop `.ld-center`, mobile `BracketFunnelSection`), the signal-availability + prop-pass matrix (`activeGroup`/`activeTrainingPod`), the reuse map for the training-start / return / Spectate / BaggerBomb actions, the mirror-guard + P4b confirmation, the honest-display test surface (all lobby smokes render the no-game path only), and the shared-token/house-CTA patterns. Anchors verified at HEAD `7b28d15e`; includes the founder disposition (§11). |
| `audits/ARCHETYPE_MASTERY_PHASE0_DISCOVERY_20260720.md` | Phase 0 read-only discovery for the Archetype Mastery arc (spec V2 §11, @ `39efa66`): the three-STOP verdict (completion write non-transactional + two fenced `decide.js` expiry-completions; Forge bundle keying vs the §6.1 wording contradiction), the retention/cardinality/rules-posture CLEARs, the terminal-status enumeration (only `completed` exists) + proposed §4 matrix, the ALL-ASSUMED constants proposal, the no-concurrency-control platform finding, and the residual net-new write-host census. STOPs ruled by the Jul 21 memo below. *Founder-approved to the record.* |
| `ARCHETYPE_MASTERY_SPEC_V2_1_STOP_RULINGS_JUL21_2026.md` | **Added July 21, 2026 (verbatim founder relay)** — the V2.1 delta to `ARCHETYPE_MASTERY_SPEC_V2_LOCKED.md`: STOP-A ruling (completeBattle → guarded transaction; fenced expiry-completions structurally outside mastery), STOP-B ruling (customization bundles per-archetype; Forge rule bundles account-scoped by design — §6 replacement text), the §8/§9/§10 deliverable rulings (fail-closed matrix, provisional `formulaVersion: 1` constants, write-once-guard constraint), and the P1 greenlight. Where it conflicts with V2, this memo wins. |
| `ARCHETYPE_MASTERY_SPEC_V2_2_P2_GREENLIGHT_JUL21_2026.md` | **Added July 21, 2026 (verbatim founder relay)** — the V2.2 delta: the P2 greenlight directive and the post-review ratifications (reforge-anchor deviation with the A8-footnote-relocation rider; dial invalidation on archetype switch with the P3 notice rider; the §5 award-shape delta of record — `placementInputs?` on paying receipts). Where it conflicts with V2/V2.1, this document wins. |
| `audits/20260720_G2_ACTIVEBATTLEID_CONFLICT_DISCOVERY.md` | Read-only discovery for the G2 `activeBattleId` conflict (`LAUNCH_READINESS_WATCH_LEDGER` G2): a casual vs-CPU deploy during a pre-battle competitive slot pod blocks the pod's Monday agent deploy (`runPrescribedTournamentDeploy` early-returns `battleCreated:false`). Severity verdict — **day-one gap, self-heals Tuesday** via the draft-stream catch-up (`tournamentOrchestrator.js:657`), user layer still scores at 1.5×; the dangerous property is **silence** (no battle, no shadow-log, orchestrator counts the skip as `deployed++`). Covers the `'fullday'` expiry/collision window (Fri close → first Monday tick), all four `activeBattleId` setters (fenced `decide.js`) + two clearers, the training-is-not-a-second-source + reverse-direction-safe confirmations, a dangling-pointer edge, and the fenced-vs-non-fenced fix-site map. 5/5 adversarial verifiers CONFIRMED; anchors verified at HEAD `6802670c`. |
| `audits/20260722_TRAINING_POD_STATUS_P0_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the Training-Pod Status-Transition P0 (@ `3f8ee0f`): the **premise-reversal** finding — the training status machine is already built, wired into the orchestrator morning tick, flag-on, and test-locked at HEAD, so training pods DO advance `FORMING→DRAFTING→(AWAITING_OPEN→)BATTLE→COMPLETE` (spec's "never advances" is stale; mastery-report E2 sub-claim 4 was the current truth). Covers the full status-writer map, the 3-lens adversarial divergence verdict (H1–H4 ruled out; only the FORMING-orphan edge + pre-wiring residue strand), the baselinePolicy STAMP-vs-VALUE-capture confirmation (both present, no gap), the complete bank/settle gate (`status===BATTLE && players.length===4`, nothing else), settlement continuity through the same `completeBattle`/eligibility-stamp/`MODE_MULT 0.6`, the fence clear (no fix point on the fence), and the D1 terminal-disposition gap (`LEGAL_TRANSITIONS` has no pre-BATTLE terminal edge). 12-agent workflow, all 5 crux verdicts CONFIRMED. Ruled by the memo below. |
| `TRAINING_POD_STATUS_P0_PHASE0_RULINGS_JUL22_2026.md` | **Added July 22, 2026 (verbatim founder relay)** — accepts the Phase 0 premise reversal and re-scopes the P0, superseding `TRAINING_POD_STATUS_P0_FIX_SPEC_V1`: R1 verification (orchestrator-cron registration confirmed `vercel.json:162` + founder-side dashboard/census), R2 the unified `EXPIRED` terminal-disposition mechanism (new status + `FORMING/DRAFTING/AWAITING_OPEN→EXPIRED` edges + `{expiredAt,expiredReason,expiredBy}` markers + mandatory status-consumer census + transactional state+version precondition; never hard-delete), R3 its two callers (founder-gated one-time cleanup + rolling `POD_EXPIRY_SWEEP_ENABLED` backstop riding the existing tick), R4 the surviving §4.3 regression-lock test. No transition code, no flag on the working path. Where it conflicts with Spec V1, this memo wins. |
| `FANTASYTIMES_WIRE_AGENT_FIRST_NEWS_SPEC_V1_5.md` | **Added July 24, 2026 (verbatim founder paste at build kickoff)** — the FantasyTimes Wire arc's final-lock spec: four adversarial review rounds + the Phase 0 discovery (`audits/20260724_FANTASYTIMES_WIRE_PHASE0_DISCOVERY.md`) + founder rulings D1–D8. Phase 1 (the typed channel) built from this document on `claude/fantasytimes-wire-news-spec-m5side`; Phases 2–4 outlined, Phase 3 fence-gated. Where it conflicts with earlier Wire versions, this document wins. |
| `audits/20260724_FANTASYTIMES_WIRE_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the FantasyTimes Wire arc (@ `dd28eedf`): Q1–Q15 with 11 discovery agents + 7 adversarial verifiers (all PARTIAL — corrected numbers carried). 5 STOPs (news already reaches agents incl. live S5 directive; no injectionClass registry; six ticker universes; 2026-only holiday data / no walker; cleanup.js is Mon+Thu not daily), 6 AMENDs, 4 CLEARs; 8 pre-existing defects filed for separate tasking. Basis for rulings D6–D8 and spec V1.3→V1.5. |
| `audits/20260724_WIRE_PHASE1_BUILD_REPORT.md` | Phase 1 build report for the Wire arc: the typed channel (validator, digest renderer, uniform envelopes, receipts, replay sweep, session walker, continuity, metrics) built flag-gated and dark per spec V1.5, with the §9 matrix as the test contract. File:line map, flag posture, coverage statement, founder pre-flip checklist. *Two claims corrected Jul 25 post-review — see the review report below.* |
| `audits/20260725_WIRE_PHASE1_CODE_REVIEW.md` | BUILD_RULES §2 code review of the Phase 1 diff: seven independent adversarial reviewers, 41 executed repros, **53 findings (3 critical, 16 high, 23 medium, 11 low)**. Criticals: the replay sweep was unreachable on the common cron tick; the B6 concurrency test could not fail; unvalidated `primaryTicker` let model free text reach the agent-facing digest. 27 fixed on-branch, 26 dispositioned; one inline-receipt semantics question referred to the founder (ruled D9 → spec V1.6). Includes the probed-and-confirmed-correct register. |
| `FANTASYTIMES_WIRE_SPEC_V1_6_POINT_AMENDMENT.md` | **Added July 25, 2026 (verbatim founder paste, r2)** — the point amendment to spec V1.5 (which remains governing): founder ruling D9 (superseded-attempt receipt semantics, unified inline+sweep; `idempotencyConflicts` retired), `subjectRef` (Neta server-stamped / `index_move` model-required + ETF→index remap), the narrow `figures[]` sign rule, per-seam eventType pinning, and the A5 remediation deliverables (rules-suite first execution, host-integration test, m6 hunk→finding map, A6 standing rule). Governs together with V1.5; where they conflict, V1.6 wins. |
| `audits/20260729_DELIGHT_THEMING_FOUNDATION_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the Delight Layer arc, Task 1 (Theming Foundation) (@ `db6f5ebc`): the four spec-premise corrections (pilot surfaces `DashboardLoop`/`DashboardDesktop` are dead code behind `COMMAND_DASHBOARD_*` flags; `src/theme/tokens.js` already exists and feeds `ThemeContext` → 64 files; two `:root` blocks already ship, one of them 19/22 dead; the `colors` object is at `App.jsx:1083`, not ~9819), the empirically-proven finding that acceptance rows A1/A2/A3 are unbuildable as specified (vitest `css` default + `@layer base` defeating jsdom) together with a demonstrated 7/7-passing alternative, the scope inversion (four **mutually disjoint** token systems; `colors` prop = 24 live receivers vs 362 inline-hex files), the hex census (7,207 colour literals / 306 distinct / 457 of 999 files), the dE2000 drift analysis (93 pairs under dE 3.0), the D7 rgba evidence, and 10 STOPs + 12 pre-existing defects filed for separate tasking. 14-agent workflow (9 census + 5 adversarial verifiers, 1 CONFIRMED / 4 PARTIAL); all 47 verifier corrections folded in. **Awaiting founder rulings — no code written.** |
| `audits/20260729_DELIGHT_THEMING_PHASE2_PREAMBLE_HAZARD_SCAN.md` | Phase 2 preamble hazard re-scan for the Delight Layer arc, Task 1 (@ `6c37ae93`), required because the discovery hazard map was built on the dead pilot files. Re-scans the three LIVE surfaces (`CommandDashboard.jsx`, `CommandDashboardDesktop.jsx`, `DesktopBackground.jsx`) against H1–H7: **no hard-stop condition**, 7 migratable literals. H2 CLEAR in this tree (motion animates scale/opacity/y only); H1 present but ruled by R-S9, with `alpha()` → teal and `readableOn()` → near-black documented as silent failures; adds **new hazard class H8** (SVG presentation attributes don't substitute `var()`, ratified as R-H8). Records that `readableOn` widens defect D-6 to three helper families, and that both shipping dashboards are already tokenized via `CMD` rather than raw hex. |
| `audits/20260731_DELIGHT_MOTION_TOKENS_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the Delight Layer arc, Task 3 (Motion Token Foundation) (@ `20de9ed9`): the transition census (motion analogue of Task 1's hex census) establishing the de-facto **spring standard `stiffness:300/damping:25`** (28 sites; 300-family = 51% of springs, declared by no module) and **tween plurality bare `duration:0.2`** (93) / `0.3-easeOut` entrance / `easeOut`-dominant; the §2 premise calibration (**~34%, not "most,"** of animated elements inherit the framer default — 228 of 672); the D1 answer (**seven** import-disjoint motion-constant modules with **zero** identical springs — 3 newly catalogued: `shockwaveUtils.js`, `challengeDefinitions.js`, non-framer `faceEngineCore.js` — none a superset, `ui/motion.jsx` **dead**, so a **new sibling `src/theme/motion.js`** is correct); the reduced-motion sprawl (5 accessor styles, all honoring RM, grounding D2's injected-argument accessor); the gesture inventory (6 true-drag sites, 3 velocity-reading — the `gesture`-token target); the CSS-side D4 quantification (index.css = live 76-keyframe source, `animations.js` = **72/73 byte-identical dead duplication**); the guard verdict (**FEASIBLE-WITH-CAVEATS**, opener-regex over a curated ratchet list, count-based A5); the proposed 5-value vocabulary table + pilot candidates; and 11 pre-existing defects filed for separate tasking. 11-agent workflow (8 census + 3 adversarial verifiers, 2 CONFIRMED / 1 PARTIAL); all corrections folded, headline counts lead-verified. **Founder locked the rulings July 31, 2026** (a NEW sixth token `quick` added — tween 0.2; `snappy` anchored to the plurality 300/25); **Phase 1 built `src/theme/motion.js` + `motion.test.js` inert** (six tokens + the D2 `motionToken` accessor, rows A1–A4 green). |
| `audits/20260801_DELIGHT_DEPLOY_SKY_COUPLING_PHASE0_DISCOVERY.md` | Phase 0 read-only discovery for the Delight Layer arc, Task 4 (Signature Deploy — hold-to-deploy sky coupling) (@ `66a4b099`): the six-item map (hold anatomy in `useHoldToDeploy.js`, the deploy-CTA census, the deploy-success signal, accessibility, reduced motion, event feasibility), the NON-FENCED verdict, and the finding that `ft-deploy-intent` would be the app's **first production `CustomEvent` dispatch** (the cited `ft-accent-changed` precedent is listener-only, with no dispatcher anywhere). Carries the load-bearing architecture correction — the intent `max()` must decorate the sky's OUTPUT at the consumption read, never `advanceWarp`'s `state.speed`, which would corrupt the tier-ease anchor — plus 8 pre-existing defects filed for separate tasking (2 medium, incl. a poll `limit(5)`-before-clone-filter case that can hide a live battle from the sky). 13-agent adversarial workflow, all five crux lanes upheld. **Ruled by the founder Aug 1, 2026** (R-T4-ARCH, R-T4-S1–S5). |
| `audits/20260801_DELIGHT_DEPLOY_SKY_COUPLING_PHASE1_BUILD_REPORT.md` | Phase 1 build report: the intent channel (dispatch in the hold hook, the pure curve/exhale overlay in `warpStateMachine.js`, the starfield listener) built dark behind `DEPLOY_SKY_COUPLING_ENABLED`. Records R-T4-ARCH as executed, the four mutation checks, and the founder feel-pass brief. **Its §2 tuning table is superseded by the round-1 record below.** |
| `audits/20260801_DELIGHT_DEPLOY_SKY_COUPLING_TUNING_ROUND1.md` | Feel-pass round 1 (founder T1/T2/T3): `INTENT_CURVE_EXPONENT` 2.5→1.2, `INTENT_PEAK` 1.4→1.8, and the Task-2 tier widening `SPEED_RESTING` 0.12→0.08 / `SPEED_LIVE` 0.5→0.7. Carries the measured crossover table (how much of the press elapses before the sky visibly moves: 487ms→97ms at rest) and the pins moved alongside. **Holds the current values.** Note: its BATTLE LIVE crossover row describes a state production cannot reach — see the Phase 3 report. |
| `audits/20260801_DELIGHT_DEPLOY_SKY_COUPLING_PHASE2_BUILD_REPORT.md` | Phase 2 build report: the commit surge timed into the ~450ms lock beat (R-T4-S3 option ii), the post-deploy settle via optimistic append (R-T4-S1), and the contained button cosmetic pass. Leads with the item-3 terminal-collision finding — the ceremony does not unmount the deploy button, **the settle does**, so this phase creates the hazard rather than inheriting it, and the commit-authoritative precedence is therefore structural rather than timing-dependent. Records the BUILD_RULES §2 review position honestly (10 files; `/code-review` unavailable; inline review performed and its coverage written down) — one of the two occurrences that prompted the §2 amendment. |
| `audits/20260801_DELIGHT_DEPLOY_SKY_COUPLING_PHASE3_REPORT.md` | Phase 3 closing report for Task 4: the A1–A7 acceptance matrix closed, State Map Amendment C recorded, the event contract documented at both ends (and the house pattern for the next custom event), the **known-unexercised** rulings (R-T4-S3b and the BATTLE LIVE crossover analysis describe states no user can reach, per the `b236660f` reachability invariant), and the Phase 0 census correction (six hold sites in code, four live at current flags). Also carries the BUILD_RULES §2 amendment rationale. |
| `audits/20260725_WIRE_V16_REMEDIATION_REPORT.md` | V1.6 remediation-pass report on `claude/fantasytimes-wire-news-spec-m5side` (pre-merge): A1–A4 implementation with the named tests (176 Wire vitest tests across 14 suites), the A5-1 rules-suite **first execution in-harness** (111 emulator tests passed incl. all 12 Wire denials), the A5-2 host-integration/metrics/cleanup suites, the m6 hunk→finding-ID map over `9d1c11d7..HEAD`, and the A5-5 lookback-threading disposition (nontrivial → named post-merge follow-up). Gate for the single dark merge (A7). |
| `audits/20260725_FANTASYTIMES_WIRE_PHASE2_DISCOVERY.md` | Phase 2 read-only discovery (@ `e7d541cc`, rebase target `db6f5ebc`): §5's 16 items across 11 discovery agents + 11 adversarial verifiers (9 of 11 PARTIAL/REFUTED — corrected numbers carried). **3 CLEAR · 8 AMEND · 5 STOP.** STOPs: the adapters' stated input (the generating snapshot) is never persisted; `promptVersion` does not exist; the "nothing consumes the Wire" premise is false (`buildContinuityContext` feeds digests into live prompts at seven sites, fail-open); `digestRendererVersion` has never been written; the Phase 2 spec was absent from the repo so A6 had nothing to bind to. Also: the F-B1 re-derivation class is already live for four fields, and the figures↔symbol binding gap on multi-ticker rows. 40-item separate-tasking register. The grounding record cited by Spec V1.3. |
| `FANTASYTIMES_WIRE_PHASE2_SPEC_V1_2.md` | **Founder upload (byte-exact), July 25, 2026** — Phase 2 base text: the Consumption & Quality Arc after the Fable adversarial round (F-B1/F-B2 + F-M1…M13). Carries acceptance rows **P2-1…P2-28** and decisions D-P2-1…7, both of which the later versions amend rather than restate. Superseded by V1.3→V1.5; retained because V1.3 §3 ("V1.2 text governs where not amended") and V1.4 §6 ("P2-1…P2-28 as V1.2 except as noted") both govern from it. |
| `FANTASYTIMES_WIRE_PHASE2_SPEC_V1_3.md` | **Founder upload (byte-exact), July 25, 2026** — the discovery-amendment pass: all five discovery STOPs resolved, founder rulings **D-P2-8…16** + the F-B1 companion ruling, discovery amendments A–L folded, and the N6 build-enabling fixes introduced. Adds rows **P2-29…P2-43** and the P0–P3 prerequisite ladder. Where V1.4/V1.5 do not amend it, this document governs. |
| `FANTASYTIMES_WIRE_PHASE2_SPEC_V1_4.md` | **Founder upload (byte-exact), July 25, 2026** — the ChatGPT diff-scoped round (R3): frozen execution object binding provenance to execution (P11), the all-or-none legacy/stamped/malformed state machine, the three-part figure-binding rule, structural dependency enforcement replacing the bypassable source-text tripwire, and 13 matrix tightenings. Adds rows **P2-44…P2-46** and the `fantasyTimesConsensus` retention binding constraint (≥90 days). |
| `FANTASYTIMES_WIRE_PHASE2_SPEC_V1_5.md` | **Founder upload (byte-exact), July 25, 2026** — the Fable diff-scoped round (R4) and the **FINAL-LOCK candidate**: Doug's provenance clocks separated by what each value governs (no digest carried from submit), the `wireModelCall` single-constructor wrapper with the two-direction deny-unknown assertion, typed-only binding entity set, ordered legacy/stamped/malformed evaluation, and the reflections→sweep→editorial budget hierarchy with an inviolable sweep floor. Adds rows **P2-47…P2-49**, completing the 49-row matrix. **Governing for Phase 2**, together with V1.3 (which governs everything V1.4/V1.5 do not amend) and Addendum A. |
| `PHASE2_N2_PARTIAL_EXEMPLAR_RULING_JUL29_2026.md` | **Founder ruling, July 29, 2026** — the exemplar shortlist returned three zero groups; ruling embeds exemplars for the **four** types with candidates now (kai market_pulse, alex market_mover, kim sector_column, doug earnings_preview) and **defers three** post-gate (neta econ_recap, doug earnings_recap, neta econ_preview), the first two gated on live production defects. N2.1 qualification still governs the four; the 4/3 split is confirmed against the founder's re-run `considered` counts. Diagnosis: `audits/20260729_WIRE_EXEMPLAR_ZERO_GROUPS_DIAGNOSIS.md`. |
| `audits/20260729_WIRE_EXEMPLAR_ZERO_GROUPS_DIAGNOSIS.md` | Read-only code diagnosis of the three exemplar-shortlist zero groups: the **wiper hypothesis REFUTED** for Neta story production (both Neta paths source Sonar, never the wiped consensus `economics[]`); `econ_recap` most likely never written (Sonar-`actual` production gate); **doug earnings_recap a live defect** (`actual_eps` availability timing, UTC-date firing secondary); the retention ceiling (~31-44d/reporter) bounding the window; script filters ruled out as a false-zero cause; the `considered` count as the founder-side disambiguator. Five separate-tasking items filed. |
| `PHASE2_CALIBRATION_ADDENDUM_V1_1.md` | **Added July 29, 2026 — the FINAL-LOCK artifact (D-P2-11/D-P2-16), founder-confirmed.** The P2 calibration record under the §7 firewall (coverage and shape statistics only; no error rate existed or was consulted — zero production Wire entries): the executed 48/16-slot enumeration, the seven persisted shapes + consensus-bucket operands at HEAD, the 48-slot coverage map (**5 STRICT · 8 PROXY · 4 CIRCULAR · 31 dead**), the shape→adapter mapping with the R4-M1 binding precondition and the UTC-keyed consensus join rule, and the RECORDED numbers: **threshold 5% over VERIFIED** (inherited from the §6.1 gate line), **floor ≥5 VERIFIED + ≥2 shapes**, per-basis-class tolerances (F-M4: fixed; changes bump `adapterVersion` and reset the window). §9 carries the ratification record, including the TICKER_TO_SECTOR caveat (validator bump alongside generation bump — enforced mechanically by `assessTickerUniverseCaveat`). **Phase 2 is locked; P3 builds against the 49-row matrix.** |
| `RECAP_RESTORATION_MINIARC_SPEC_V1.md` | **Founder upload (byte-exact), July 30, 2026** — the mini-arc's spec-lite base text: the two-defect diagnosis grounding (S3 Sonar actual-null gate, S5 AMC `actual_eps` timing + UTC window), fixes F1/F2, constraints C1–C7, discovery-lite items 1–5, acceptance R1–R4 + the R9 production-observation runbook line, and the process/merge-order note. Superseded by V1.1 (which carries the changelog against this text); retained as the base the V1.1 deltas amend. |
| `RECAP_RESTORATION_MINIARC_SPEC_V1_1.md` | **Added July 30, 2026 (verbatim founder paste at discovery kickoff)** — the Recap Restoration mini-arc spec V1.1 (supersedes V1, above): restores the two structurally-silent Wire seams S3 (`neta_econ_recap`) and S5 (`doug_earnings_recap`). F1 dual-count log + skip-log taxonomy, F2 AMC prose honesty, C2 prior-session fixtures, C8 referent identity/pre-call dedup, R2 verifier battery, R9 liveness rows, epoch-sequencing law. |
| `RECAP_RESTORATION_DISCOVERY_RULINGS_JUL30_2026.md` | **Added July 30, 2026 (verbatim founder relay)** — the discovery rulings on the mini-arc: R-B1 EODHD econ-events fetch as econ operand source (capture script = founder deliverable; HARD STOP if plan-unavailable), R-B1a plausibility gate + settle delay (publication gates, NOT adapter tolerances), R-A1 `macroCalendar` arrays as Tier-1 authority + jobless claims added, R-B3 C8(c) superseded (FINAL-LOCK §3 UTC join stands), R-B2 C2 re-scope (ET derivations + cron re-aim), R-B5 generalized F2 labeling, R-B4 top-level `referentDate` dedup schema, R-B6 F1 amendments, R-B7 one pre-window epoch reset. Where it conflicts with spec V1.1, this memo wins. |
| `audits/20260730_RECAP_RESTORATION_DISCOVERY_LITE.md` | CC discovery-lite for the Recap Restoration mini-arc (@ `9747085`): 14-agent read-only workflow (7 lanes, each adversarially verified — 5 CONFIRMED / 2 PARTIAL). Executive verdict table; the six premise corrections (econ is Sonar-not-EODHD, no econPrint verifier exists, jobless-claims not Tier-1 today, C8(c)-vs-locked-join collision, C2 never reached / real fault is UTC-today, F2 targets the current-session operand); +23 Haiku/day budget ceiling; fence CLEAR; 7 residual defects filed. Ruled by the memo above. |
| `audits/20260730_RECAP_RESTORATION_BUILD_REPORT.md` | Build report for the mini-arc (rulings R-B1…R-B7/R-A1 executed): file:line map, the R-B1a band table, the R-B2(i)-vs-R-B3 lock-tension resolution (consensus keys keep the locked UTC expression on the publish instant), the adversarial §2 review table (2 high / 5 medium / 4 low — 9 fixed in the fix-forward commit, 1 deferred to the capture gate), register additions, the R9 runbook line, and the capture-gate closure addendum. |
| `ECON_CAPTURE_FINDINGS_AND_MATCHER_RULINGS_JUL30_2026.md` | **Added July 30, 2026 (verbatim founder relay)** — the capture-gate rulings closing R-B1: provenance for the two capture artifacts (committed at `api/_utils/__fixtures__/econCapture20260730.json` + `econCaptureJulyFull.json`, filenames normalized from the memo's `econ-capture-*.json`), the §1 Tier-1→feed-type mapping table (CPI→`Inflation Rate` YoY, exact-type + comparison keying), the §2 absent-category rulings (JOLTS dropped after an any-name search; Productivity unmapped pending an August window), the §3 disambiguation hazards, and the §5 build actions. HARD STOP does not fire; the feed is numeric with counts in thousands. Where it conflicts with the Jul 30 discovery rulings, this memo wins. |
| `PHASE2_SPEC_V1_5_ADDENDUM_A.md` | **Added July 25, 2026 (verbatim founder relay)** — the P1-gate rulings on V1.5: **R-A1** scopes P2-48's "sole Anthropic-client importer" to the **Wire context** (FantasyTimes generation seams + Wire `_utils` + the N3 editorial judge, so `judgeModelId` is execution-bound rather than declared), leaving fenced `decide.js` and every other repo importer out of scope — the unscoped V1.5 text was the Amendment H defect class reintroduced one row over, surfaced by the build session's importer census. **R-A2** rules that committed SHAs in specs are historical grounding, never build targets: the rebase target is always current `origin/main` at build start. **R-A3** requires the BUILD_RULES §1 flag-split prose rule be quoted verbatim at P1 kickoff and applied to the letter — `voiceLayerPrompt.js`'s flag-conditional newsLine rendering registers in `promptHonestyRegistry.js` in the same commit regardless of whether the rule triggers. Governs together with V1.5. |
| `../CONSTITUTION_TREND_FOLLOWER_V1.md` | **Founder upload, July 23, 2026 (repo root)** — Archetype constitution for `momentum_chaser` / Trend Follower: kernel, DR-13 golden render + render contract (carries the R1-9 format ruling), compat rubric. |
| `../CONSTITUTION_CONTRARIAN_V1.md` | **Founder upload, July 23, 2026 (repo root)** — Archetype constitution for `contrarian` / Contrarian. |
| `CONSTITUTION_DIVERSIFIER_V1.md` | **Founder upload, July 24, 2026** — Archetype constitution for `diversifier` / Diversifier (the one constitution living in `docs/`). |
| `../CONSTITUTION_SPECULATOR_V1.md` | **Founder upload, July 23, 2026 (repo root)** — Archetype constitution for `degen` / Speculator. |
| `../CONSTITUTION_FUNDAMENTAL_INVESTOR_V1.md` | **Founder upload, July 23, 2026 (repo root)** — Archetype constitution for `analyst` / Fundamental Investor. |
| `../CONSTITUTION_CAPITAL_PRESERVER_V1.md` | **Founder upload, July 23, 2026 (repo root)** — Archetype constitution for `guardian` / Capital Preserver. |

**Constitution path binding (DR-13 arc):** the six golden renders above are doc-parity-locked byte-exact to the frozen constants in `api/_utils/evalIdentityBlocks.js` — CI parses each constitution's blockquote and asserts equality (`api/_utils/evalIdentityBlocks.test.js`). The tests bind to the exact paths listed here via `EVAL_IDENTITY_CONSTITUTION_PATHS`; relocating a constitution requires updating that path map (and this table) in the same commit. Per the July 24 founder ruling (Flag E), the five root-level constitutions stay at the repo root.

**Founder-to-add note:** the two marked files exist as original documents in the founder's design workspace and are added by uploading those originals directly (GitHub upload UI) — never regenerated by an AI session.

**Audit provenance note:** the three audit reports were delivered as session output (never saved as files at the time). The files here are **transcriptions of the founder's verbatim pastes** of that output, produced with the founder's approval, each carrying a transcription-note header and verified by anchor spot-checks (HEAD SHAs, commit hashes, verdict strings, section counts). They are the best extant source and are treated as the citation baseline; their in-text `file:line` anchors refer to repo code at the named HEAD, and the standing rule holds — re-verify anchors at your HEAD before relying on them. True regeneration of any record from model memory remains forbidden: if a record is missing, report it, don't reconstruct it.

## Maintenance rules

- Documents here are **records**, not living files: superseding happens by adding a new versioned document, never by editing a locked one. (Exception: this README's table and `BUILD_RULES.md`, which track current state.)
- Audit reports are immutable once added — their line anchors are tied to the HEAD they name.
- When a build phase changes a rule of record (e.g., a fence change), `BUILD_RULES.md` is updated **in the same PR**, with the founder decision cited.
