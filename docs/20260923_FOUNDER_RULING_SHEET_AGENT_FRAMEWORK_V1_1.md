# Founder Ruling Sheet — Agent Framework Arc

**Version:** 1.1 — supersedes V1.0 · **Date:** 23 September 2026 · **Author:** Fable
**Status: ALL RULINGS ADOPTED BY FOUNDER, 23 Sep 2026** ("yes to all, agree with the leans"). Each ruling below carries the formal decision and a plain-terms layer. Source: Expanded Capability and Reuse Audit (HEAD `f6728ffc`, graded trusted); its §8 dispositions are adopted and recorded in the Integrity Findings Register V2.
**Changes from V1.0:** every ruling stamped adopted; R8 ruled at **week scope**; plain-language layer folded under each ruling; cross-arc incorporation section added (exit dials, called shots, pick directive); R3 gains the sequencing constraint that it lands before the first dial ships.

**Settled — not reopened:** BaggerBomb-embedded pilot; flat-six Trend Follower as the offline qualification slice; D-24 and the J1→J2→J3 JEV ordering; Pi/Hermes independence under the standing firewall (adapter target stays UNDECIDED); passive-user standing-consent constraints.

---

**R1 — ADOPTED.** Every archetype declaration must have enforcement evidence or come off the declaration. The item-by-item hard list is deferred to the identity-contract conformance pass, which maps each declared field to its prompt, gate, or test.
*Plain terms: what the character sheet says becomes true — no promises without a referee behind them.*

**R2 — ADOPTED.** Distressed entry becomes per-archetype: the Contrarian is permitted within its existing risk gates; the veto remains the default for all other archetypes. Resolves G07's declaration/gate contradiction and the JEV adjudication's FR-2 in one ruling.
*Plain terms: the Contrarian gets to actually be a contrarian — buying beaten-down names inside its risk limits — accepting that it can genuinely lose points doing so. That's the character.*

**R3 — ADOPTED.** A player's protective instruction is never silently deferred: deterministic stops fire on the always-run guardrail phase regardless of LOCK. Slot consequences follow mode physics with typed outcomes only — fired-and-replaced, fired-and-slot-empty, blocked-by-mode — never hedge language the mode lacks. Fable drafts the mode-truth language tables; founder blesses wording. **Sequencing: this ruling and the always-run phase land before the first exit dial ships.** Resolves the JEV adjudication's FR-3 and governs G02/G08 remediation.
*Plain terms: your stop-loss always fires, LOCK or no LOCK, checked by a referee every tick rather than waiting for the agent to wake up — fixed before the dials make stops common.*

**R4 — ADOPTED.** Mandate sector caps are **position counts**. The comparator is fixed to count positions per sector; invalid domains are rejected at vintage build. New vintages only; historical vintages are preserved untouched and examined separately with authorized book-exposure evidence. No silent unit conversion, no rewriting pinned history. Resolves G03.
*Plain terms: "max 2 per sector" now actually means two positions — the smoke detector gets its battery, on new books only.*

**R5 — ADOPTED (process).** Fable authors the completed-bar Trend Follower setup definition with the Regime Taxonomy method, on daily/completed-bar evidence, with explicit invalidation and unknown criteria; the founder blesses the numeric thresholds when the drafted artifact is in front of him.
*Plain terms: I write the measuring stick for the test track; you approve the numbers.*

**R6 — ADOPTED.** The horizon enum is carried through save, equip, and capture; each mode's setup contract defines the enum→window translation; reaching horizon end moves the hypothesis to review-due — never silent deletion, never an automatic forced sell beyond existing mode rules; unspecified carries as unspecified.
*Plain terms: a two-week idea stays a two-week idea, and when its time runs out the agent flags it for review instead of letting a March idea sit there looking fresh in September.*

**R7 — ADOPTED.** Deploy freezes the full hypothesis version into the battle context. Edits while equipped are rejected with an explicit saved-as-new-version-for-next-deploy path; a queue is deferred. The active battle and its review always cite the pinned version.
*Plain terms: the battle and the post-game review show exactly what you believed when the battle started, even if you changed your mind Wednesday.*

**R8 — ADOPTED at WEEK scope.** For backed play, the agent's brain — memory, lessons, consolidated insight, model and prompt versions — is snapshotted when the backing window opens, and every battle in that window deploys from the snapshot, whether the week is one weekly battle or several daily ones. Dial and loadout changes remain permitted-and-disclosed exactly per Backing V1.3; nothing in V1.3 is rewritten. The voice layer reads the frozen snapshot during backed weeks so narration cannot drift from the frozen brain. Platform-forced model changes mid-window are logged and disclosed as exceptions.
*Plain terms: backers always finish the week with the brain they staked on; your hands stay free and disclosed.*

**R9 — ADOPTED.** Intraday is not a pilot prerequisite. The pilot speaks daily/completed-bar vocabulary only; intraday capabilities join the journey when the Intraday arc's own stage gates pass. The arcs stay decoupled.
*Plain terms: the pilot never claims to be watching something it can't see; intraday talk switches on when that data has earned it.*

---

## Cross-arc incorporation (Command Center / cockpit arc)

Delivered to the arc-authority chat 23 Sep; nothing was in the codebase, so these bind the builds from birth:

1. **Exit dials (stop + profit-taking):** the always-run deterministic guardrail pass is the dials' precondition (the arc's existing G-1 transfer); R3's precedence and typed outcomes are baked into the dials spec; dial marker states on the board are the typed outcomes only.
2. **Called shots (agent's stated intentions):** the record carries from v1 a stable shot id, the pinned hypothesis version (R7), the horizon with expiry state (R6), and the evidence-cutoff id — and joins capture/receipts so the Film Room replays called-vs-happened.
3. **Symbol-scoped pick directive:** ships with a Speculator-style four-layer mismatch fixture (selection / commitment / narration / effect) in its acceptance set from the first build.
4. **R8 mechanics:** dial and loadout changes are the player's sanctioned, disclosed hands during backed weeks; the brain snapshot is what freezes.

Clean alignments, recorded: the arc's C-1/C-3 capture requests are the evidence G02/G04 need (register cross-links, no duplicate design); the cockpit's build gates (grounding on, fit-check flip, capture flip, held-parity) match the audit's prerequisites.

---

**What follows this sheet:** the framework-closeout docs PR (this sheet, Integrity Findings Register V2 with the G-series section, the audit report committed); then the pilot spec drafted from the audit's eight-contract reuse table with these rulings applied; the R3 mode-truth language tables and the R5 setup definition ride with the pilot spec.

*Adopted rulings take D-numbers on the arc ledger at the arc authority's next session per convention.*
