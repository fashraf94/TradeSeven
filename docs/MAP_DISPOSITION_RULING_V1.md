# Map Disposition Ruling — V1.0 (responding to the Reverse-Direction Map Audit)

**Date:** July 29, 2026 · **Status:** RECOMMENDED — founder approval converts each item to a ruling of record.
**Input:** `20260729_REVERSE_DIRECTION_MAP_AUDIT.md` (153 surviving flags: 141 reverse / 12 forward; structural findings S1–S7; close-out order §10.5).

## D1 — The schema question (S1/V2): the runtime gains `tension` AT REGISTRY COMPOSITION, not by patching the legacy map.
The authored cell matrix has always been the plan-of-record replacement for the stored map. The audit makes the decision concrete: the registry-composition arc extends the runtime verdict schema to four states, with tension carrying its Guide-§4 treatment fields (guidance ref, narrowedParams) — and requires the product build that V12 proved missing (narrowedParams/paramBounds have zero code occurrences; offer surfaces must learn to restrict parameter domains per archetype). The legacy 3-state map is NOT patched to hold tension — it cannot, and piecemeal edits cascade per S3.

## D2 — Batching (V5): ONE deliberate identity-version event, at composition.
No cell-at-a-time fixes. The composition event carries: the authored matrix as registry content, the schema extension, the 17 core_conflict-class corrections, the four family re-filings (V10), and the seed-map reconciliation the V6 invariant forces — all under one ARCHETYPE_IDENTITY_VERSION bump with the full ceremony (founder-gated, adversarially reviewed). Interim risk is acceptable: pre-launch, the wrong-permission window has no users in it; the 17 dangerous cells are named and tracked.

## D3 — The 121 unchallenged tension-class flags: the merits pass IS cell authoring.
Each family batch adjudicates its own column's flags as authoring inputs, with the ⚑ override line-item process C1 established (cells never silently override the map; every override is ratified in the batch review). The audit's 13 named low-confidence rows get priority adversarial attention in their batches. No separate mega-docket round.

## D4 — The 288 fallthrough cells (S2): closed by construction.
Cell authoring produces an explicit verdict for every offerable pair (A-4: absence is not a verdict; the activation gate counts explicit cells only). No separate sweep.

## D5 — Diversifier column: unchanged — out of authoring until SECTOR_CAP_MODE='enforce' is live and verified.
Its 46 flags are recorded and disposition when the window closes. (Standing founder item: review the sector-cap observe telemetry when ready — it now gates two things.)

## D6 — Found-defect taskings (audit §10.4, separate small CC session):
(a) **compileOnSettingsChange key-space defect** — doc id vs template id; every compat lookup silently falls through and the "verbatim" comment is untrue. Real fix + test. (b) **Default-trait seeding hygiene** — diversifier seeds four hidden rules, guardian seeds the deprecated single-stock-limit; re-seed from supported natives. (c) **INVARIANT R header** — update the stale positive claim (docs-only). (d) The audit report itself commits to `docs/audits/`.

## D7 — Repo record (the V9 miss): upload to docs/ —
`ADJUDICATION_RULINGS_V1.md` (V1.1 contents — the audit proved it was never committed), `CELL_BATCH_C1_RISK_V1.md`, this ruling, and the audit report. The case law must exist where the tools can read it.

## What does NOT change
Cell authoring continues on the C1 pattern, family by family (technical next), flags-as-inputs per D3. The legacy map keeps running enforce-mode as-is until the composition event — its known errors are mostly silent-lenient, which pre-launch harms no one, and the loud direction (over-blocks) got five of its twelve forward flags confirmed toward softer verdicts that authoring will deliver.
