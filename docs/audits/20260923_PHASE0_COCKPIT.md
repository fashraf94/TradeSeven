# Phase 0 — Cockpit discovery (read-only)

**Date:** 2026-09-23 · **Runner:** Claude Code (read-only on source; one report file; no PR) · **For:** the cockpit spec V1 (Fable), Astra's review, the founder.
**HEAD:** `2a1a16b1ccb56baa63fb4fa8e8d92f79b3a6199d` — `origin/main` after `git fetch origin` (the first command of the session, BUILD_RULES §3), the merge of PR #894 ("docs/framework-closeout"), 2026-09-23. **Branch:** `claude/phase0-cockpit`, cut from `origin/main` at HEAD; working tree clean at cut. **Every `file:line` below is at that SHA and was read in this session** (VERIFIED) unless marked ASSUMED, *production* (a live Firestore value, not re-verifiable from the tree) or *input* (one of the four attached documents, which are not in the tree — §0.2). No source file changed; no test run; no PR.
**Inputs read first:** `CALL_RECORD_FIELD_CONTRACT_V1_0.md`, `COCKPIT_DESIGN_DIRECTION_V4.md` (tile spec by reference to V3 §1, §3–§6), `COCKPIT_DESIGN_DIRECTION_V3.md`, `COCKPIT_DESIGN_BRIEF_V1.md` (the four *inputs*); `docs/BUILD_RULES.md` (§1 fence list, lines 10-32); `docs/specs/CAPTURE_BUILD_SPEC_V1_4.md`; `docs/2026-09-22_CC_ARC_ANSWERS_TO_FILM_ROOM.md` (the Sep 22 HEAD-verified answer set — **on `main`**, §0.2).
**Markers:** **VERIFIED** — read at HEAD · **CORRECTED** — the contract, V4/V3, the Brief or the Sep 22 answer set says otherwise; the truth follows · **NOT FOUND** — does not exist at HEAD · **ASSUMED** — inferred, not read.

---

## 0. Executive verdict

**Buildable, in three builds, with no §1 fence contact on the default routes.** The contract's shape survives HEAD; its words about the fence, the exchange id, the directive id, the evidence cutoff and the horizon table do not. The largest corrections: the evaluation schema and the chat prompt are **not** fenced (contract §2, §7, §10); `chatExchanges[]` entries have **no id** (contract §3 `render.chatExchangeId`); a filing's identity is `directiveThreadId`, and `battle.directive` is **one latest-wins slot**, so two divergent answers in one fifteen-minute interval overwrite each other (contract §6); there is **no per-symbol evidence cutoff** on any record (contract §3 `evidence`); and the R6 horizon translation table **does not exist** (contract §3 `horizon`). The Brief's §8 gates are all still closed except the capture flip.

| # | Target | What HEAD says | Size | Fenced | Blocker |
|---|---|---|---|---|---|
| 1 | `declarations` schema | Tool schema `api/_utils/agentEvalToolSchema.js` is **not fenced**; `swap_type` prose-vs-`required` is deliberate and pinned; the validator checks top-level only, no cap; exactly **one** reader of `candidates[]` (`selectBench.js:203`); teaching the model in the system prompt is the only fence contact (two fenced copies, DR-13 route). Mint from the tool result, never stamp the entry, and no golden moves. | S–M (M with a prompt section) | no · **yes** on the prompt route (`agentEvalPromptAssembly.js` ×2) | route ruling; `m7e2eBudget` margin; the schema is model-visible at `off` unless flag-conditional (§13) |
| 2 | Anticipation pipeline | Writer NF; prose is a **Gemma** call, dispatched in the tick's `finally` after the final battle update, before capture; lint `'off'` (`:2566`), receipts console + GCS only; the chat pane reads only `direction`; exchanges are index-keyed — **no exchange id exists**; `chatExchanges[]` uncapped. | S–M | no | the exchange-id decision |
| 3 | `intradayViews` pattern | Rules `firestore.rules:448-452` copy verbatim as `match /calls/{callId}`; write is a separate awaited `set(merge)` with a natural key; client is a one-shot `getDoc` — **no `agentBattles/*/…` subcollection listener exists anywhere**; no subcollection index. | S | no | doc-id scheme; composite index |
| 4 | Directive write path | Chip route: one transaction, membership by allowlist helpers (**never `gateDirective`**), charges one message, **404 for everyone at `'shadow'`**; chat: plain `update`, no transaction; the gate reads fixed strings — **no parameterised adjustment**; 46 archetype-wide ids, none symbol-scoped, none says ask/wait/pick; a fifth gate status `no_archetype`; **`directiveId` does not exist** (`directiveThreadId`); **`api/agent-battle/` does not exist**; `persisted`/`charged` absent (B2 unmerged). | M | no (single slot) · **STOP** if multi-directive | latest-wins collision; ack exchange + charge ruling |
| 5 | Capture tick | Allowlist is default-deny by path and shape; `callId` needs new entries (`id` kind already admitted; **no test pins the list**); **no per-symbol as-of timestamp anywhere** — the cutoff is tick-level `callEnvelope.promptBuiltAt` + per-source vintage labels; `evalId` is minted before the model call; a write before `:4058` joins both ways. | S–M | no | `TICK_CAPTURE_SCHEMA_VERSION` ruling |
| 6 | Close sweep | `agent-daily-scores` is the wrong host (active-only, one UTC slot, price precondition); the Film Room close pass is **unbuilt**; **41/100 confirmed**, BUILD_RULES §6 "19 of 21" is stale (23 of 25); the deferred-beat PR is **on no branch**; completion runs in one transaction — `ended_with_battle` joins it; the queue-flag template covers the rest; **the R6 translation table does not exist**. | S–M | no | deferred-beat PR; R6 table for BaggerBomb |
| 7 | Voice Layer prompt | **Not fenced** (contract §7 CORRECTED): one `buildVoiceLayerPrompt` with a `grounded` boolean; copy Block 5.4 (Show It) — grounded-only, flag-gated, beside `:3250`/`:3316`; the `calls` read joins the `Promise.all` at `chat.js:569`; no char cap, 800 output tokens. | S–M | no | grounding `'on'` |
| 8 | Battle View seams | Pane tab: five insertion lines, but **one tab list serves both shells**; mobile header and turn row are **not pinned today**; **no `drag="x"` in `src/`**, no thumb component; **V4's 320 ms bezier and 180 ms fade match no motion token** (§11); CSS transitions are neutered under reduced motion; client is read-only on `agentBattles` beyond ten allowlisted keys. | pane S · pills S · mobile track M–L | no | motion ruling; a layout change on a shipped screen |
| 9 | Research surfaces | `AssetResearchModal` takes a symbol but has **no framing prop** and is off the token system; the screener has **no prefill**; **Show It is gated by `isShowItOn()`, not grounding** (CORRECTED); the watchlist is server-only, edited by whole-array PATCH, no add-ticker endpoint, the Equip door unbuilt; the ask-in-chat prefill exists. | S + M + M | no (the fenced deploy read is unchanged) | Show It flip order; mid-battle watchlist semantics |
| 10 | Tokens | All 15 `CMD` names exist — **but `CMD` is legacy**; `--ft-*` is canonical, has no `ink*` tokens, and no alias for any of the seven states; **the shipped player colour is teal, copper is the CPU** (V4 §4 CORRECTED); no fuse exists in the Battle View; `DESIGN_TOKENS.md` is deprecated (Brief §7 CORRECTED). | S | no | purple / copper ruling |
| 11 | Flags | All values as the Sep 22 doc, lines after `:2530` moved +3; `TICK_CAPTURE_ENABLED = true`; `cronState.evalSeq` present. | — | — | — |
| 12 | Branches | B2 unmerged at `5ce73eb9` (one JSON conflict, not `chat.js`); six unmerged docs branches; **no branch touches any target**; the answer set is **merged** (CORRECTED); shallow clone caveat. | — | — | — |

### 0.1 Fence intersection at a glance

| BUILD_RULES §1 file | Touched by | How |
|---|---|---|
| `api/_utils/agentEvalPromptAssembly.js` | **Build 0, prompt-section route only** | two one-import/one-call splices (tiered `:485-511`, flat-six `:688-714`) via the DR-13 flag-split + a `PROMPT_CONTRIBUTING_MODULES` entry in the same commit; **none** on the schema-description route |
| `api/_utils/agentBattleService.js` | **none** — as long as `calls` stays a subcollection and no new top-level battle key is written | a `render`/`calls` field on the battle document would be `createAgentBattle` doc-shape contact → STOP |
| `api/agent/decide.js` | none (reads the watchlist at deploy; unchanged) | — |
| `api/_utils/agentPromptAssembly.js` | none (not in the chat path) | — |
| `agentSwapExecution.js`, `agentScoring.js`, `agentRiskManager.js`, `agentGuardrails.js`, `archetypeScoring.js`, `agentArchetypeConfig.js`, `tournamentUserScoring.js` | none | — |
| **Multi-directive support** (two divergent answers heard in one interval) | Build 1 **only if chosen** | changes the argument at `agentEvalPromptAssembly.js:1234` and the single-directive contracts of `resolveControls` / `renderDirectiveBlock` / `deriveHeardStamp` → **STOP-class**; the default (latest-wins, or refuse a second divergent answer while the slot is unheard) has no contact |

**Non-fence ratchets every build meets in the same commit:** the §2.3 import-boundary baseline (`api/_utils/archetypeImportBoundaryBaseline.json`) for any new importer of the menu; the deny-by-default protected-stores write scan (`api/_utils/compositionProtectedStoresAllowlist.json`) for every new unresolved `set`/`update`; `src/theme/tokens.guard.test.js` `GUARDED_FILES` for every new file under `src/screens/battleView/`; `src/theme/motion.guard.test.js` for any new `transition={{`; `PROMPT_CONTRIBUTING_MODULES` for any new prompt render module; `TICK_CAPTURE_SCHEMA_VERSION` for the tick's new paths; `flagPinGuard`'s `DARK_BY_DESIGN` + a pin for `CALL_RECORDS_MODE`.

### 0.2 The inputs — three corrections of record before any target

1. **The answer set is on `main`, not on an unmerged branch — CORRECTED.** The task says to read `docs/2026-09-22_CC_ARC_ANSWERS_TO_FILM_ROOM.md` from `origin/claude/cc-arc-answers-film-room-tz3wch`. That branch (tip `539abfa0`) is an ancestor of `origin/main` (merged as PR #888, `47802e3d`); the file at `docs/2026-09-22_CC_ARC_ANSWERS_TO_FILM_ROOM.md` on `main` is byte-identical to the branch copy (`git diff origin/main origin/claude/cc-arc-answers-film-room-tz3wch -- <path>` is empty). Its anchors are at `463a375f`; between that SHA and HEAD **no file under `api/`, `src/screens/battleView/`, `src/hooks/`, `src/data/` or `src/components/Dashboard/commandUI.jsx` changed** (`git diff --stat 463a375f HEAD` over those paths lists only backing-beta files under `api/backing/`, `api/team/`, `api/tournament/`, plus `src/components/Dashboard/EquipStation.jsx` and `desktop/IdentityPanel.jsx`); `src/config/featureFlags.js` gained +3 net lines inside the `BACKING_BETA_ENABLED` docstring (`:2496-2530`), so every flag defined **after** `:2530` sits 3 lines lower than the Sep 22 doc says; `firestore.rules` gained 17 lines and `firestore.indexes.json` 14 (backing). Every Sep 22 anchor this report depends on was re-read at HEAD; where it moved the new line is given.
2. **A different document already occupies the Brief's natural filename — CORRECTED (name collision).** `docs/design/COMMAND_CENTER_COCKPIT_DESIGN_BRIEF_V1.md` exists on `main` (added in `c35f9a59`, Sep 12) but it is the **Sep 1** brief — "The Command Center is the **cockpit**: the page a player lands on to deploy…" (`:15`) — the hub-page concept that Direction V3 §2 / V4 §1 explicitly leave untouched. The attached Brief V1 (Sep 22, "the place where the player coaches the agent during a live game") is a different document with a colliding title. None of the four inputs is in the tree (`docs/inputs/` does not exist; no file under `docs/` matches `CALL_RECORD_FIELD` or `COCKPIT_DESIGN_DIRECTION`). When they are committed, they need distinct names (e.g. `docs/design/COCKPIT_DESIGN_BRIEF_V1_SEP22.md`) or the Sep 1 file will be cited as the brief.
3. **The framework rulings the contract leans on are in the tree, under `docs/`, not `docs/audits/`.** `docs/20260923_FOUNDER_RULING_SHEET_AGENT_FRAMEWORK_V1_1.md` — all rulings adopted Sep 23 (`:4`); R3 always-run guardrail pass before the first dial (`:17`); R6 horizon enum → per-mode translation, review-due at horizon end (`:26`); R7 pinned hypothesis version (`:29`); R8 backed week at week scope (`:32`); the cross-arc incorporation for the cockpit — stable shot id, pinned hypothesis version, horizon with expiry state, evidence-cutoff id, and the four-layer mismatch fixture (`:40-49`). The four-layer fixture's origin is the Expanded Capability Reuse Audit's named Speculator case — "selection, canonical commitment, narrative truth and executed effect require separate evidence" (`docs/audits/FantasyTrades_Expanded_Capability_Reuse_Audit_2026-09-23.md:575`, `:597`). **No such fixture exists at HEAD**: the only "four-layer" in code is the unrelated constraint-precedence wording in the fenced `api/_utils/agentEvalPromptAssembly.js:75`, `:83`, `:122`, `:829`.

---

## 1. The evaluation output schema (the `declarations` block)

**1.1 Where the schema lives — not fenced.** The Haiku tool the decider fills is `TRADE_DECISION_TOOL` in `api/_utils/agentEvalToolSchema.js:4-5` (`name: 'submit_trade_decision'`), a file **absent from the BUILD_RULES §1 list**. The cron passes it at `api/cron/agent-evaluate.js:2499-2500` (`tools: [TRADE_DECISION_TOOL]`, `tool_choice: { type: 'tool', name: 'submit_trade_decision' }`). Top-level `required` is `['decision', 'rationale', 'conviction', 'hypothesis', 'riskAssessment']` (`:10`); `anticipationCandidates` is `:157-192` — `type: 'array'` (`:158`), items `required: ['symbol', 'direction', 'signalSummary', 'threshold']` (`:163`), `direction` enum `['potential_entry', 'potential_exit']` (`:171`), plus optional `rationale`, `signalSource`. No `additionalProperties` anywhere in the file. VERIFIED.

**1.2 The `swap_type` prose-vs-`required` disagreement — real, deliberate, pinned.** `:27-31`: `swap_type` enum `['defensive_cut', 'profit_take', 'momentum_rotation', 'upgrade']` with the description "Required if SWAP; omit on HOLD", while `required` (`:10`) omits it. That is intentional: `api/_utils/agentEvalToolSchema.test.js:25-26` pins `expect(TRADE_DECISION_TOOL.input_schema.required).not.toContain('swap_type')` ("must not be forced on HOLD"), the validator header says the same (`api/_utils/agentEvalToolResultValidation.js:44-47`), and the finding is recorded at `docs/audits/20260919_BUILD_EVAL_FIX_T3_tool-result-validation.md:118`. **For the cockpit build:** the "reviewed once" schema edit (contract §2) must add `declarations` beside `anticipationCandidates` and leave `required` alone; a `declarations` that is `required` would reject every HOLD that declares nothing — the same trap. VERIFIED.

**1.3 Validation — top-level only, no cap, consumers drop.** `validateTradeToolResult` (`agentEvalToolResultValidation.js:92`, NF) checks `required` presence (`:97-101`), then every top-level property's type and enum (`:103-127`; null/absent skipped at `:112`; type `:113-115`; enum `:116-118`), then the SWAP pair (`:129-136`). Nested rows are **not** validated by design — `:39-43` "Only TOP-LEVEL properties are checked. Nested item shapes (the `anticipationCandidates` rows …) are left to the consumers that already drop malformed entries individually" — pinned by `api/cron/agent-evaluate.astraFindings.test.js:495-499` (a candidate with `symbol: 42` passes). No length cap exists anywhere (no `.slice`, no `maxItems`). The two consumers apply the same admission rule, "object with a truthy `symbol`": the anticipation queue `agent-evaluate.js:2849` and the stamp composer `api/_utils/tickStamps.js:297`. Call site `:2541`; an invalid result becomes `failureClass: INVALID_TOOL_RESULT_CLASS` + `fallbackHold = true` (`:2566-2580`). **Consequence:** a `declarations` property typed `['object','null']` is type-checked by the generic loop with zero validator edits; its inner shape (the contract's `condition: { side, level }` typed and numeric) is validated by nobody unless the call writer does it — which the contract's "a malformed declaration is not born" requires. VERIFIED.

**1.4 `anticipationCandidates` → `evaluations[].candidates[]`.** `tickStamps.js:87` `TICK_STAMP_KEYS = ['heard', 'evidence', 'vintages', 'candidates']`; `:103-105` `CANDIDATE_FIELDS = ['symbol', 'direction', 'signalSummary', 'threshold', 'signalSource']`; composer `composeCandidatesStamp` `:293-308` (`threshold` via `strOrNull` — prose); spread onto the entry at `agent-evaluate.js:3802-3806` under `TICK_STAMPS_ENABLED && promptBuilt` (`:3762`) from the **linted** array (`:3806`). **No cap** on the array; only a per-item ≤300-byte pin (`api/_utils/tickStamps.test.js:465-466`). The entry itself is capped at 150 (`evaluations.slice(-150)`, Sep 22 §A2 / `:3938`). VERIFIED.

**1.5 Every reader of `candidates[]`.** A property-access grep for `.candidates` across `api/` and `src/` (non-test) finds **one reader**: `src/screens/battleView/selectBench.js:203` → `selectFlagged(candidates, roster)` `:157-169`, which keeps `potential_entry` names on the roster (`:162`) and returns them **uncapped** (`:168`); `FLAGGED_DISPLAY_CAP` has zero hits in `api/` or `src/` — the Sep 22 doc's "B2 cap not at HEAD" holds. `src/screens/battleView/PaneBench.jsx:259-261` renders the chip; `src/screens/battleView/battleViewCopy.js` renders two strings only — `benchFlaggedChip: 'Flagged'` (`:802`) and the eyebrow `tapeKindEyebrow` (`:426`, `:451-454`: `potential_entry` → `'Bench note'`, `potential_exit` → `'Holding note'` only when grounded) — the eyebrow reads the **exchange's** `anticipationContext.direction`, not the entry. The four prompt-feeding readers named by `docs/specs/INTRADAY_DATA_BUILD_1_CONTRACT_V1_1.md:25` and tested together in `api/_utils/intradayPromptExclusions.test.js:1-3` read **none of it** by allowlist: `formatRecentEvals` (`api/_utils/agentEvalPromptAssembly.js:1389-1402`, **fenced**, key-explicit, pinned by `api/cron/agent-evaluate.tickStamps.pins.test.js:253-256` — no fenced path may read any `TICK_STAMP_KEY` off an entry); `RECORD_ENTRY_FIELDS` (`api/_utils/voiceLayerGrounding.js:357-361`); `ANTICIPATION_ENTRY_FIELDS = ['evalId', 'timestamp']` (`api/_utils/voiceLayerAnticipation.js:61`); `REFLECTION_EVALUATION_FIELDS` (`api/_utils/agentReflectionUtils.js:161`). `api/cron/agent-batch-review.js:189` reads entries wholesale; `deriveHeard.js`, `WhyPanel.jsx`, `decisionRecord.js`, `reflect.js:127` (count only) and `shadowAssemblyCapture.js:371` (`candidatesTested`, unrelated) are not readers. **Nothing in `src/` reads `anticipationContext.symbol`, `.threshold` or `.evaluationId`** — only `.direction` (`src/components/Agent/deriveChatMessages.js:92`). VERIFIED.

**1.6 Where the model learns about it — fenced prose, two copies.** The instruction block "ANTICIPATION CANDIDATES — WHEN TO POPULATE" lives in the **fenced** `api/_utils/agentEvalPromptAssembly.js` twice: the tiered builder `:485-511` and the flat-six builder `:688-714` (read-only here). `agentPromptAssembly.js` and `decide.js` have zero mentions. The schema's own `description` (`agentEvalToolSchema.js:160`) is also delivered to the model. So `declarations` can be taught **without fence contact** (schema descriptions only, NF) or **with** it (a system-prompt section at both builders — the sanctioned route is the DR-13 flag-split, `docs/BUILD_RULES.md:30`: a dark NF render module, a one-import/one-call splice at each builder, and a `PROMPT_CONTRIBUTING_MODULES` entry in `api/_utils/__fixtures__/promptHonestyRegistry.js:46-54` in the same commit). VERIFIED.

**1.7 The goldens and pins a `declarations` block meets.**

| Pin | Fails if… |
|---|---|
| Frozen entry + whole-update golden — `api/cron/agent-evaluate.tickStamps.flagOff.test.js:196-197`, exact new-key list `:204-205` (`[...TIMING_ENTRY_KEYS, ...FAIL_CLOSED_ENTRY_KEYS]`); golden `api/_utils/__fixtures__/tickStampsEntryGolden.flagOff.json`; **never regenerated** (spec V1.4 `:67`) | a new field is written on the **entry** unconditionally |
| Flag-on shape — `api/cron/agent-evaluate.tickStamps.flagOn.test.js:180`, `:386` exact key lists | a new stamp key rides the entry |
| Composer pins — `api/_utils/tickStamps.test.js:89`, `:92`, `:388` | `TICK_STAMP_KEYS` / `CANDIDATE_FIELDS` grow |
| Source pins — `api/cron/agent-evaluate.tickStamps.pins.test.js:179` (`toContain`, additive OK), `:253-256` (no fenced `.key` read) | fenced code reads `.declarations` |
| Prompt byte goldens — `api/_utils/evalIdentityBlocks.ask2.test.js:87` (`ask2PromptGoldens.json`, 12× `anticipationCandidates`), `agentEvalPromptAssembly.ask1.test.js`, `evalIdentityBlock.injection.test.js:74-75` (12 `__dr13_snapshots__`), `p4Equivalence.battery.test.js:410`, `:435`; honesty sweep `agentEvalPromptAssembly.honesty.test.js:22-31` | any **unconditional** prose edit to the fenced assembler; a flag-gated dark splice keeps flag-off bytes |
| Request budget — `api/_utils/composition.m7e2eBudget.test.js:152`, `:193`, `:198` against `EVAL_FULL_REQUEST_INPUT_BUDGET = 12_000` (`:62`) | the larger schema tips the budget; margin **unmeasured** (ASSUMED room — measure in the build) |
| Threshold copy pin — `api/_utils/thresholdLintCopy.test.js:100-102` | a new description contains `20-day` |
| Schema unit test `agentEvalToolSchema.test.js:12-42`; validator N1 `astraFindings.test.js:488-515`; phase-3 script `api/scripts/test-voice-layer-phase-3.js:405-406` | no (unless the sub-`required` literal is reformatted) |

**The sizing insight:** the contract puts the record in `calls/{callId}`, not on the entry. If the call writer mints **directly from `haikuResult.declarations`** and the entry never carries a `declarations` stamp, the first four rows above are untouched — no golden moves. Only the pins that fire on a stamped entry key are avoided; the schema, validator-as-is and writer remain.

**1.8 Harness fixtures and pre-flight cases.** `vitest.eval.config.mjs:15` includes only `api/scripts/archetype-integrity-eval/**/*.eval.mjs` — the Gemma directive-gate reliability corpus (`corpus.js:3-6`), whose "preflight" is a reachability probe (`runEval.eval.mjs:252`); it carries **no** `TRADE_DECISION_TOOL` or anticipation case. `scripts/paired-eval-harness.js:65`, `:123` reads the **live** schema (no anticipation fixture). `scripts/preflight-capture-check.js:4-9` is the L1 learning-receipt gate. **No pre-flight harness carries an anticipation case.** Fixture factories to extend: `api/_utils/__fixtures__/tickStampsHarness.js:346-363` (`makeHoldResult`, no `anticipationCandidates` by default) and `:382-399` (`makeAnticipationCandidates`); `api/_utils/__fixtures__/voiceGroundingFixtures.js:493-504` (`makeCandidate`); `api/_utils/intradayPromptExclusions.test.js:26-33` (an exclusion row proving the four allowlists never admit `declarations`). VERIFIED.

**1.9 What `declarations: { calledShots[], watching[], playerAsk, fork }` touches.** `agentEvalToolSchema.js` (NF, one property beside `:157-192`); `agentEvalToolResultValidation.js` (NF, no edit for a top-level `['object','null']`; nested validation is the writer's); the call writer (new NF module — §13); `agent-evaluate.js` (NF, a call after `:2541`/`:2836` on the linted result — the anticipation queue at `:2847-2853` is the model); the honesty registry **only** under the prompt-section route. Fenced: **no** on the schema-description route; **yes — `agentEvalPromptAssembly.js` at two splice points** on the prompt-section route (the one BUILD_RULES §1 permits only through the flag-split, §7-gated). The contract's own words need correcting here: §2 "reviewed once" and §10 "`declarations` schema + call writer (fenced, founder-reviewed)" — **CORRECTED: the schema file is not fenced.** What is fenced is teaching the decider about the block in the system prompt; a schema-description-only build has zero §1 contact. **Size:** S–M (schema route) · M (prompt route: dark flag, two splices, registry, honesty sweep, byte goldens flag-off, budget margin). **Blockers:** the founder's choice of route; the `m7e2eBudget` margin; the `FAIL_CLOSED`/`TIMING` key lists if any entry field is added.

## 2. The anticipation pipeline as shipped

**2.1 The writer.** `api/_utils/voiceLayerAnticipation.js:167` `generateAnticipation` (NF). The model-path exchange `:351-369`: `userMessage: null`, `agentResponse`, `scratchpad`, `hasDirective: false`, `directive: null`, `suggestedActions: null`, `elicitationTarget: 'anticipation'`, `timestamp` (ISO), `mode: 'battle'`, `messageType: 'anticipation'` (`:361`), `anticipationSource: 'haiku'` (`:362`), `anticipationContext: { symbol, direction, threshold, evaluationId }` (`:363-368`). **No id field of any kind.** Written by one `battleRef.update({ chatExchanges: FieldValue.arrayUnion(exchange) })` (`:379-381`), never the status feed. The grounded composer `composeGroundedAnticipation` (`:83-165`) writes `{ symbol, direction, evaluationId, slot }` with `groundingVersion` and **no `threshold`** (`:117-139`, write `:141`), and runs only when `getVoiceGroundingMode(ownerId) === 'on'` (`:200-201`; fallback `:273`). The prose is a **Gemma** call, not a second Haiku call — `callGemmaVoiceWithRetry` (`:26`, `:310`; `api/_utils/gemmaClient.js:38-39`, OpenRouter `google/gemma-4-26b-a4b-it`) under a 10 s abort (`:306-307`), prompt `buildAnticipationPrompt` (`api/_utils/voiceLayerPrompt.js:3942`). **Cron call site:** queue `pendingAnticipations` (`agent-evaluate.js:737`), enqueue from the linted array `:2847-2853`, dispatch in the tick's `finally` (`:4075`) **after** narrations settle (`:4090-4106`) and **after** the final battle update (`:4164-4167` comment; the update statement itself ASSUMED from the comment), gated only by remaining budget `> 12_000` ms of `TIME_BUDGET_MS = 290_000` (`:4122-4124`, `:170`) — no dedicated flag; the skip path logs `cron_budget_skip` (`:4143-4161`). Capture follows it: `:4167` "TICK CAPTURE — THE LAST THING THE TICK DOES (spec C-9)". VERIFIED.

**2.2 The threshold lint — two persistence sites.** Lead-in `:2743-2747`; `lintEnforcing = ANTICIPATION_THRESHOLD_LINT_MODE === 'on'` (`:2769`), `lintActive` (`:2770`); present-signals built from the tick's own in-memory objects `:2773-2779` (`buildPresentSignals`, `lintThreshold` from `api/_utils/anticipationThresholdLint.js`, NF); verdict `:2787-2795`; the owed-receipt comment `:2797-2803` ("The 'on' flip PR owes a durable receipt on the battle doc itself"); console receipt `:2804-2807`; GCS `logAnticipation({ errorStep: 'threshold_absent_signal', … dropped: lintEnforcing … })` `:2808-2833` (`api/_utils/shadowLogger.js:154`, `:44-46` — no-op without GCS); the drop `:2834-2836`; **site 1** the queue `:2847-2853`; **site 2** the stamp `:3806`. **`ANTICIPATION_THRESHOLD_LINT_MODE = 'off'` at `src/config/featureFlags.js:2566`** (moved from `:2563`; modes `:2569`; pin `src/config/anticipationThresholdLintFlags.test.js:39`). Contract §8 retires the `'on'` flip in favour of mint-time `invalidated` — consistent with HEAD: the lint's evidence check (`buildPresentSignals` + `lintThreshold`) is the reusable part, but it **parses signal names out of prose**; a typed `condition.level` needs a numeric presence check that does not exist. VERIFIED.

**2.3 How the chat pane renders an anticipation line.** `src/components/Agent/AgentChat.jsx` `MessageBubble`: `resolveMessageType` `:306-307` (config `src/utils/renderMessageWithEntities.jsx:111` — accent `#5EEAD4`, no label), the eyebrow `:332-339` → `BATTLE_VIEW_COPY.tapeKindEyebrow(messageType, hasUserHalf, _anticipationDirection, grounded)` → `battleViewCopy.js:451-454`; rendered at `:366-388` (`data-tape-kind-eyebrow`); `showKindEyebrow={Array.isArray(tapeEntries)}` (`:1556`). Same eyebrow in the character bubble (`src/screens/battleView/deriveBubble.js:164`). Pinned by `src/components/Agent/AgentChat.tapeKinds.render.test.jsx:203-210`, `:230-240`. **Keys are index-based:** `deriveChatMessages.js:64-65` `id: \`exchange-${i}-agent\`` (`_serverIndex: i`); `exchangeId` has **zero hits** in `api/` or `src/`. **Scroll-to-line exists for one target only:** the `openCheck` door — `AgentChat.jsx:901-914` queries `[data-tape-entry-id="${openCheckId}"]` and `scrollIntoView`; that attribute is emitted only by the tape's check/trade cards (`src/screens/battleView/TapeCards.jsx:285`, `:321`), never by `MessageBubble`. **What rendering from a call record with a "→ cockpit" chip takes:** (a) a join key the exchange does not have — either the anticipation writer stamps `callId` into `anticipationContext` (`:363-368`, NF) or the record carries the exchange's identity; the only joins today are `anticipationContext.evaluationId` → `evaluations[].evalId` and the ISO `timestamp`; (b) a `data-…-id` on `MessageBubble` plus a nonce-keyed layout effect mirroring `:901-914`; (c) the chip beside the eyebrow `:366-388`. **CORRECTED vs contract §3:** `render.chatExchangeId` and `playerResponse.exchangeId` reference an exchange id that **does not exist**; either the anticipation writer mints one (a new field on every anticipation exchange — NF, no cap, harmless) or the join is `(timestamp, symbol)`. Size S (client) + S (writer). VERIFIED.

**2.4 `chatExchanges[]` is uncapped.** Every server write is `FieldValue.arrayUnion` with no `slice`: `api/agent/chat.js:1071-1072`; `api/agent/file-directive.js:282-286` (inside its transaction, with `directive: slot`); `api/agent/research.js:354`; `api/agent/ensure-opener.js:310`; `api/_utils/voiceLayerTradeNarration.js:274`; `voiceLayerAnticipation.js:141`, `:380`; `api/cron/agent-batch-review.js:419`; the fenced opener `api/agent/decide.js:1659` and the fenced init `api/_utils/agentBattleService.js:262` (`chatExchanges: []`). Read-side **windows**, not caps: `chat.js:664-667` `slice(-10)`; `voiceLayerGrounding.js:577` `slice(-window)`; `src/` renders the whole array (`deriveChatMessages.js:38`, `OpenChatPanel.jsx:169`, `FilmRoomChat.jsx:146`). Contrast `evaluations[]` at 150. VERIFIED (confirms Sep 22 §F2).

**2.5 Where the pane is fed.** `src/hooks/useAgentBattle.js:26-32` (`doc(db, 'agentBattles', id)` + `onSnapshot`, whole document spread at `:32`), `chatExchanges` derived `:54`, returned `:58` → `src/screens/AgentBattleScreen.jsx:655-664` → `AgentChat` prop → `deriveChatMessages` (`AgentChat.jsx:709`) and the tape builder (`:1517`, `buildTape.js:325`). VERIFIED.

**Target 2 — size S–M · fenced: no · blockers:** the exchange-id decision (2.3); the Gemma path is unchanged by the record (the record supplies structure, the narrator still writes the sentence).

## 3. The `intradayViews` pattern, to mirror for `calls`

**3.1 The rules block.** `firestore.rules:429` `match /agentBattles/{battleId}` — owner read `:430-431`; the client update allowlist `:434-437` (`executionMode, pendingProposal, battleLedger, updatedAt, strategyPreset, gameplanMeeting, gameplanMeetingHistory, dailyGrades, feedBookmarks, reviewDecisions`) — a client can never write `calls`; `create, delete: if false` `:439`. The nested block, still at **`:448-452`** (the 17 backing lines landed after it): `match /intradayViews/{evalId} { allow read: if request.auth != null && get(/databases/$(database)/documents/agentBattles/$(battleId)).data.ownerId == request.auth.uid; allow write: if false; }` — `allow read` (covers `get` and `list`), the owner proved by an inline `get()` of the parent (no helper function exists; the same form is the `voiceLayerCache` block `:893-894`; a missing parent makes `get()` null and denies — the safe direction, `:441-447` comment). Root default deny `:1261-1263`. **No `ticks`/`tickBodies` match exists** (zero grep hits), pinned by `test/rules/tickCaptureDenials.rules.mjs:140-142` — its regexes are ticks-specific, so a new `calls` match does not trip it. **A `calls` block is `:448-452` copied with `match /calls/{callId}`.** The rules test to copy is `test/rules/intradayViewsDenials.rules.mjs` (111 lines; cases `:78-109`: owner reads parent and view; another user, a privileged-claims context and anon are denied; orphan view fails closed; every principal's create/update/merge/delete fails; the sibling execution-control update still works), auto-included by `vitest.rules.config.mjs:15` and run under the emulator by `npm run test:rules`. VERIFIED.

**3.2 The server write path.** `api/_utils/intraday/evaluatorHook.js:45-51` (NF) `writeIntradayView(db, { battleId, view })` → `db.collection('agentBattles').doc(battleId).collection('intradayViews').doc(view.evalId)` → `ref.set(view, { merge: true })` under a 2 s timeout, never throws; **doc id = `evalId`, a natural key**; no flag inside the hook — the cron gates: `agent-evaluate.js:3696` `if (INTRADAY_DIAGNOSTIC_ENABLED)`, the write `:3713`, the eight pointer fields onto the entry `:3728-3738`. Placement: after the entry object is composed, before the tick stamps (`:3802`) and before the authoritative `battleRef.update(finalUpdate)` (`:4058`) — **a separate awaited write, not atomic with the battle update**. `db` is the Admin singleton (`getFirebaseAdmin()`, `api/_utils/firebaseAdmin.js:26-33`). VERIFIED.

**3.3 The client and the Why? panel.** `src/screens/battleView/useIntradayView.js` (44 lines): **one `getDoc` when the panel opens, "Never a subscription"** (`:4`, `:31`); `wanted` (`:26`) = flag ∧ panel open ∧ battleId ∧ entry `evalId` ∧ `intradayViewRef === evalId`. Consumer `src/screens/AgentBattleScreen.jsx:1489` → `WhyPanel` prop `intradayDiagnostic` (`:1851`) → `WhyPanel.jsx:549-556` renders its own section under the header "Diagnostic · recorded at the check · not seen by the agent" (`src/data/intradayDiagnosticCopy.js:17`); states are **absent or rendered** only — no loading, no error label. VERIFIED.

**3.4 What does not transfer to an uncapped, per-record, server-only subcollection.** (i) *Read shape:* a one-shot `getDoc` on open versus a live listener over a collection — **no client subscribes to any `agentBattles/*/…` subcollection today**; the nearest precedents are `src/services/tournamentGroupService.js:91-103` (`query(collection(db, …, 'claims'), orderBy('createdAt', 'desc'), limit(20))` + `onSnapshot`) and `src/services/claimFreeAgencyService.js:424-433`. (ii) *Identity:* a natural key versus a minted id — mint precedents are Firestore auto-ids, `randomUUID()` (`chat.js:1009`, `file-directive.js:266`) and the capture build's deterministic `${battleId}:${tickSeq}` / `${tickId}:${n}` (`captureContext.js:130`, `:249`); a deterministic `${tickId}:call:${n}` is idempotent under a re-run transaction callback (`agent-evaluate.js:684-687`), `randomUUID` is not. (iii) *Atomicity:* the capture batch pins exactly two paths (`captureWriter.test.js:191-194`), so a `calls` write cannot ride it; it is its own awaited write like `intradayViews`, or joins the completion transaction (6.6). (iv) *Indexes:* `firestore.indexes.json` holds six root `agentBattles` composites and **no index for any subcollection** (`ticks`/`tickBodies` appear only under `fieldOverrides`); a `calls` query filtered by `state` and ordered by `mintedAt` needs a composite (Firestore semantics — ASSUMED; the template is `:220-236`), a bare `orderBy('mintedAt')` does not. (v) *Cost:* PLC-008 (`docs/audits/2026-09-20_FANTASYTRADES_PERFORMANCE_LATENCY_COST_AUDIT.md:526-556`) is about whole-document subscription bytes; a history subcollection is precisely its conditional remedy, and no document records a per-battle document-count concern — one call per triggered check keeps `calls` ≤ `ticks`. (vi) *The deny-by-default write scan:* every new unresolved `tx.set`/`set` site needs an allowlist row (§4.10). VERIFIED (except the index semantics).

**Target 3 — size S · fenced: no · blockers:** none in code; the doc-id scheme and the composite index are spec decisions.

## 4. The directive write path at HEAD (B2 never merged)

**4.1 A chip tap, client side.** Chips are minted **server-side, on grounded turns only**: `api/_utils/voiceLayerGrounding.js:1088-1094` mints `{ kind: 'directive', id, text }` from the archetype's allowlist (`id` = the adjustment id, `text` = the canonical text); `api/agent/chat.js:901` builds `suggestedActions` only when `grounded` (`:480` `grounded = groundingMode === 'on' && mode === 'battle'`). `src/components/Agent/AgentChat.jsx` renders them (`:444-458`), the tap hands the payload to `handleActionClick` (`:1143`) → `fileDirective(action.id)` (`:1153`, fn `:1059`) → `fetch('/api/agent/file-directive', { method: 'POST' … })` (`:1070-1071`) with `{ agentId, battleId, adjustmentId, expectedDirectiveThreadId: directiveBelief }` (`:1076`; belief `:654-656` from the subscribed `battle.directive.directiveThreadId`). The response is consumed for **one field** — `data.directive.directiveThreadId` (`:1086-1088`); nothing from it is rendered (`:1083-1085` — the receipt is the exchange the route wrote). The League arena (`src/components/League/battleArena/useArenaEngine.js:131-158`, `CommandDock.jsx:41`, `:346-349`) calls the same route and renders its receipt from the response instead. VERIFIED.

**4.2 A chip tap, server side — `api/agent/file-directive.js` (338 lines, NF).** `requireAuth` (`:149`; `api/_utils/authMiddleware.js:52-62` → `verifyIdToken`), rate limit `:142`, `maxDuration: 10` (`:97`). **The 404 gate:** `:172-174` `if (getVoiceGroundingMode(user.uid) !== 'on') return res.status(404)` — at `'shadow'` the route is dark for every user. **One `runTransaction`** (`:196`): owner `:203`, `status === 'active'` `:205`, agent binding `:207`, belief → 409 `conflict` `:212-213`, archetype + **membership through the allowlist helpers directly** (`:222-224`; import `:79` — the route **never calls `gateDirective`**), canonical text `:227-228`, budget re-read (battle `:257-258` `chatBudgetUsed` vs limit 10; League `agentChatBudget` `:240-254`), `directiveThreadId = randomUUID()` `:266`, `buildDirectiveSlot` `:274`, one `tx.update(battleRef, { chatExchanges: arrayUnion(exchange), directive: slot, ...battleBudgetUpdate })` `:282-286`, `commitBudget()` `:287`. **A filing charges one message** (`:285-287`; header `:41-42`). Response 200 `{ status: 'filed' | 'replaced-prior', directive: <slot>, replacedDirectiveThreadId, remaining }` (`:325-330`); 409/422/429 bodies at `:301-337`. **`persisted` / `charged`: zero hits** in `file-directive.js`; in `chat.js` only comments and the budget helper's internal return (`api/_utils/agentChatBudget.js:106`, `:119`), never a response key — the Sep 22 correction holds. The chip exchange (`:121-137`) is `messageType: 'directive_filed'`, `source: 'chip'`, `elicitationTarget: 'directive_filed'`, `groundingVersion: 1`, **no `archetypeGate`**. VERIFIED.

**4.3 A chat directive — `api/agent/chat.js` (1179 lines, NF).** `gateDirective({ parsed, effectiveArchetype, mode, callGemmaVoice, systemPrompt, conversationHistory, userMessage, signal, deadlineMs })` at `:843-852` when `ARCHETYPE_INTEGRITY_MODE !== 'off'` (live value `'enforce'`, `src/config/featureFlags.js:770`); `normalizedDirective = enforcing ? gate.directive : null` `:857-858`; research-lint wrap `:937-938`; the mint `:1009` `directiveThreadId = (lintedHasDirective && lintedDirective) ? randomUUID() : null`. The exchange `:1025-1067` (`userMessage` `:1026`, `directive: buildDirectiveRecord(...)` `:1033-1035`, `directiveThreadId` `:1036`, `archetypeGate: gateOutcome` `:1056`, `researchLint` `:1061`, `groundingVersion` `:1066` — Sep 22 said `:1027-1067`; the object starts at `:1025`). **A plain `battleRef.update`** `:1071-1083` — `chatExchanges` arrayUnion `:1072`, `FieldValue.increment(1)` on the budget `:1077`, `directive: buildDirectiveSlot(...)` only when a thread id was minted `:1080-1082`; `runTransaction` has **no match** in `chat.js`. Response keys `:941-964`: `agentMessage, suggestedActions, hasDirective, directive, …` plus `directiveStatus` / `directiveStatusLine` from `renderDirectiveStatus` (`api/_utils/directiveGate.js:69-73`; the line is `src/data/decisionRecord.js:666` "No change made to your strategy this turn."); the client shows that line only when `_grounded && _gateRan` (`AgentChat.jsx:429`; `deriveChatMessages.js:97-98`). VERIFIED.

**4.4 The gate — `api/_utils/directiveGate.js` (361 lines, NF).** Export `gateDirective` `:307`; classifications `:75` `in_archetype | flex | core_conflict | user_lever | research_only`; the menu is imported **directly from `src/data/archetypeAdjustments.js`** (`:21`; dependency-surface guard `api/_utils/directiveGate.test.js:3-7`); membership = `isValidAdjustmentId` (`archetypeAdjustments.js:278-279`); the fit check `:124-129` (whitespace-normalized, **case-sensitive** `includes`) fires only under `DIRECTIVE_FIT_CHECK_ENABLED` `:152-161` → `status: 'fit_mismatch'`; the committed verdict `:163-177` (`text: canonical` **`:165`**, `expiry: 'end_of_battle'` `:166`, `adjustmentId` `:172`, `canonicalTextVersion` `:173` — Sep 22's `:166-176` is off by one); one-shot repair `:140`, `:179`, `:353` (`no_proposal` / `invalid_id`); forensics `:240-290`. **A fifth status exists that the Sep 22 card list omits:** `'no_archetype'` — `:319-324`, when the archetype is missing or its allowlist is empty. **Parameterization: none.** The gate reads only `classification` and `selectedAdjustmentId`; canonical text is a fixed string per id (`getCanonicalText`, `archetypeAdjustments.js:283-286`); no `${`, template or params exist in the gate or the menu. VERIFIED.

**4.5 The menu — `src/data/archetypeAdjustments.js` (441 lines, NF, zero imports).** `ARCHETYPE_ADJUSTMENTS` `:71`, six code-ids; each entry `{ id, canonical, canonicalTextVersion, policy: { riskDirection, concentrationDirection, timeHorizonDirection, coreAlignment, forbiddenOpposite } }` (e.g. TF-02 `:92` "Require stronger confirmation before entering") — **no `classification`, `expiry`, `params` or symbol on any entry**; `expiry` is hard-coded `'end_of_battle'` by both writers. `cautiousRegister` is per archetype at `:89`, `:116`, `:143`, `:175`, `:203` **and `:230`** (analyst — Sep 22 listed five). 46 ids, pinned (`src/data/archetypeAdjustments.test.js:74-77`, `:92`). Every "hold / before / confirm" canonical is an archetype-wide execution tuning conditioned on chart events ("Hold longer through noise before considering an exit", CP-03 `:179`); **no canonical says ask, wait or pick, and none names a symbol** — no existing adjustment can carry "hold off on HOOD" or "ask me before acting on HOOD". Content lock: `api/_utils/archetypeRegistry.test.js:117` fails when menu content changes without an `ARCHETYPE_IDENTITY_VERSION` bump; the menu renders into the chat system prompt (`api/_utils/voiceLayerPrompt.js:2796-2799`), so **any new id moves the prompt goldens**. **§2.3 import-boundary ratchet:** `api/_utils/archetypeImportBoundaryBaseline.json:11` (`directiveGate.js`), `:31` (`file-directive.js`) — a new route importing the menu must be added in the same commit (`archetypeRegistry.test.js:248`). VERIFIED.

**4.6 The filing helpers — `api/_utils/directiveFiling.js` (69 lines, NF).** `buildDirectiveRecord` `:29-43` and `buildDirectiveSlot` `:50-61` → `{ text, expiry, directiveThreadId, createdAt, adjustmentId?, canonicalTextVersion? }`; latest-wins by construction — the whole object is set (`:46-48`); `BATTLE_CHAT_BUDGET = { field: 'chatBudgetUsed', limit: 10 }` `:69`. VERIFIED.

**4.7 The flag.** `DIRECTIVE_FIT_CHECK_ENABLED = false` (`src/config/featureFlags.js:887`; pin `src/config/directiveFitCheckFlags.test.js:23`; DARK_BY_DESIGN `src/config/flagPinGuard.test.js:172-173`). Flip order `:813-832`: never lit while grounding is `'canary'` or `'on'` because the grounded prompt carries no quote instruction. The Sep 18 flip was **halted** (`docs/audits/20260918_FLIP_DIRECTIVE_FIT_CHECK.md:10-20`); its §7 (`:192-208`) leaves a founder decision open — gate `applyFitCheckOutputRule` (`api/_utils/voiceLayerPrompt.js:981-984`) on a resolved archetype so an agent without a menu is not told to quote one (§5.2, `:135-168`). `getVoiceGroundingMode(uid)` (`featureFlags.js:2322-2326`) resolves `'canary'` per user from `VOICE_GROUNDING_CANARY_UIDS` (`:2297-2304`) and returns `'shadow'` for everyone today. VERIFIED.

**4.8 How the cron hears a directive.** `resolveControls` (`api/_utils/controlPromptRenderer.js:104`, pure): well-formed test `:123-127` (→ `malformed`), `integrityMode !== 'enforce'` `:134` (→ `mode_not_enforce`), the epoch kill set `:140` (→ `epoch_killed`), else effective `:147`. Expiry is the caller's: `isDirectiveActive` (`api/_utils/directiveUtils.js:75-77` → `isDirectiveActiveOnDay` `:38-72`) knows **`end_of_battle | 3_games | permanent`** only, day-granular (`:45-48`), and treats an **unknown expiry as active** (`:71-72`) — a hazard for any new expiry value. The rendered block is in the **fenced** `buildLiveContextBlock` (`api/_utils/agentEvalPromptAssembly.js:1228-1240`, read-only: `directive: isDirectiveActive(battle?.directive, battle) ? battle.directive : null` `:1234`; text from `controlPromptRenderer.js:213-219` "ACTIVE DIRECTIVE (from your Coach)"). `deriveHeardStamp` (`api/_utils/tickStamps.js:138-151`) → `evaluations[].heard = { directiveThreadId, suppressed }` from the same resolution the prompt was rendered from (`agent-evaluate.js:3772-3777`, `:3802`). **`battle.directive` is one object, latest wins** — both writers set it whole (`chat.js:1081`, `file-directive.js:284`); no clearer exists; `directives[]` survives only as write-dead legacy. **So two calls answered divergently in one fifteen-minute interval collide:** the second slot overwrites the first before any check hears it; on the chip pattern the second tap is a 409 until the client's belief catches up, then `replaced-prior`. Real multi-directive support changes the argument the fenced assembler passes at `:1234` and the single-directive contracts of `resolveControls` / `renderDirectiveBlock` / `deriveHeardStamp` — **fence contact, STOP-class**. VERIFIED.

**4.9 What a symbol-scoped `pick` and a call-scoped divergent-answer kind require.** (a) **A new adjustment family**, per archetype (hold-off · ask-first · pick), proved by `isValidAdjustmentId` — on a deterministic tap route that *is* "the canonical gate" as the chip route practises it (4.2); no model classification is involved. (b) **A templated canonical with a validated symbol slot, rendered server-side** — no template mechanism exists; the symbol is proved against the battle's universe the way the research route does (`api/agent/research.js:349` `canonicalUniverseSymbol`; `src/data/battleUniverse.js:10-16`) and the rendered sentence goes into `normalized.text` before `buildDirectiveSlot`. (c) **The fit check is not reached** on a deterministic route and would fail every filing on a template via chat (verbatim rule `:126-129`); it is dark regardless. (d) **Expiry:** "hold off until the horizon" needs a new value in `directiveUtils.js` **and** registration, or `:71-72` makes it end-of-battle silently; "one check" does not exist. (e) **P1:** "hold" is expressible as directive text; "re-ask" is **not a directive mechanism** — no cron path re-asks (anticipation only writes exchanges) — so "Ask me first" lives as call-record state (`outcome.reasked`) plus a hold directive, never as a "wait". (f) **Files:** `archetypeAdjustments.js` (family + renderer), the new route, `archetypeImportBoundaryBaseline.json` (same commit), `src/data/decisionRecord.js` (message type / copy), optionally `directiveUtils.js` (expiry) and `directiveFiling.js` (additive `symbol`/`kind` on the slot — re-pin `file-directive.test.js:401-405`). **Fenced: none — if `battle.directive` keeps its single-object shape and no new top-level battle key is added** (a new key is `createAgentBattle` doc-shape contact: `chat.js:1043-1046`, `file-directive.js:65-68`). Tests: the 46-count and per-archetype pins, the identity hash + version bump, the `voiceLayerPrompt.fitCheck` / `grounding.goldens` prompt goldens (the menu renders into the prompt). The four-layer mismatch fixture (§0.2) is new. VERIFIED.

**4.10 The answer endpoint's shape (contract §6).** **Path — CORRECTED:** no `api/agent-battle/` directory exists (`ls` fails); Vercel routing is file-based, `vercel.json` has no `rewrites` and its `functions` key covers `api/**/*.js` (`:3-7`); the repo's convention is hyphenated **files** under `api/agent/` (`file-directive.js`, `ensure-opener.js`, `set-tempo-dial.js`) → `POST /api/agent/call-response`. **Mirror from `file-directive.js`:** security middleware `:142`, 405 `:145`, `requireAuth` `:149`, a **route flag of its own** (`CALL_RECORDS_MODE`, dark + `DARK_BY_DESIGN` + pin — not `getVoiceGroundingMode === 'on'`, which is bound to chip minting), body validation with a nullable-and-required belief `:176-189`, **one `runTransaction`** `:196` carrying owner / active / binding / belief / archetype / membership / budget, `randomUUID` `:266`, slot + record `:274-280`, `tx.update` on existing keys `:282-286`, response after commit `:323-330`, the HTTP-status client contract (`decisionRecord.js:786-791`). **New inside the same transaction:** `tx.get(callRef)` for `calls/{callId}`, the `open` / `hit`-awaiting check, per-`kind` legality of `answer`, the `playerResponse` write, `stateChangedAt`/`stateSource`. **Ack answers:** the chip route always writes an exchange because both chat surfaces render receipts from `chatExchanges`; an `ack` with no exchange shows **nothing** in the Battle View — write an agent-initiated exchange (`decide.js:1638` / `voiceLayerTradeNarration.js:241` shape, `hasDirective: false`), which by precedent does **not** charge (`decide.js:1655-1657`), whereas a chip filing does (`:285-287`) — a founder call. **`directiveId` does not exist — CORRECTED:** the identity of a filing is `directiveThreadId` (`directiveFiling.js:27`, `:33`, `:54`; `tickStamps.js:140`; `controlPromptRenderer.js:47`, `:180`); the record's link field must be `directiveThreadId`. The new `tx.set`/`tx.update` sites need rows in the deny-by-default protected-stores allowlist (`api/_utils/compositionProtectedStoresAllowlist.json:214-215` are `file-directive.js`'s; the scan `api/_utils/compositionProtectedStoresScan.js:72-76` protects `agents`, `rules`, `bundles`, … and treats every **unresolved** ref write as protected — `compositionProtectedStores.scan.test.js:51`). VERIFIED.

**4.11 Every other writer.** `battle.directive` (non-null): exactly two — `chat.js:1081`, `file-directive.js:284`; no clearer. `chatExchanges` writers (all `arrayUnion`): `chat.js:1072`; `file-directive.js:283`; `research.js:354`; `ensure-opener.js:310`; `decide.js:1659` (**fenced**); `voiceLayerTradeNarration.js:274`; `voiceLayerAnticipation.js:141`, `:380`; `agent-batch-review.js:419`; migration init `agent-evaluate.js:750`; initial shape `agentBattleService.js:262` (**fenced**). VERIFIED.

**Target 4 — size M · fenced: no (single slot) / STOP if multi-directive · blockers:** the latest-wins collision between two divergent answers in one interval (4.8); the ack-exchange and charge decisions (4.10); the fit-check flip is a Brief §8 gate but is **irrelevant to the deterministic route** — it matters only to the chat path's honesty for C4.

## 5. The capture tick, for the `callId` reference

**5.1 The two shapes.** `api/_utils/tickCapture/captureWriter.js` (NF): the body `:174-204` (`schemaVersion, tickId, battleId, tickSeq, evalId, capturedAt, expireAt, request, response, copyError, originalToolResult, finalToolResult, controlsAsRendered, controlSourceText, faults, validationErrors, rejectedFields`), the permanent record `:207-337` (the Sep 22 §F2 field list re-verified; `manifest` `:287-292`, `callEnvelope.promptBuiltAt` `:303`, `scores` `:314-320`, `capture` `:321-336`), written as one batch to `ticks/{tickId}` and `tickBodies/{tickId}` (`:533-534`). `TICK_CAPTURE_SCHEMA_VERSION = 1` — "Bump on any shape change" (`captureConfig.js:37-38`); 120-day body TTL (`:63`); subcollection names `:66-67`. VERIFIED.

**5.2 The C-3 allowlist.** `api/_utils/tickCapture/captureSerializer.js:63-185` `PERMANENT_FIELD_KINDS` — `'<dot.path>': '<kind>'`, `*` = one segment (`:60-61`); kinds: `symbol` (must be in the tick's universe), `enum:<NAME>`, `id` (no whitespace, ≤ 120 chars), `hash`, `timestamp`, `number | bool | null`; containers in `PERMANENT_CONTAINER_KINDS` `:214-241` (root `'': 'object'` `:215`). **Default deny** (`:27-31`): an undeclared leaf is nulled on the record and lands, with its value, in `tickBodies.rejectedFields[]` (`:396`; `captureWriter.js:388-396`) — so an unregistered `callId` would silently live only in the TTL'd body. An **array of ids is two entries**: `'controls.standingLeanIds': 'array'` + `'controls.standingLeanIds.*': 'id'` (`:227`, `:140`; likewise `activeRuleIds` `:230`/`:145`, and `actions` `:224-225` + `'actions.*.actionId': 'id'` `:120`); `tickId`/`evalId` are `id` (`:65`, `:68`). **No test pins the path count or list** — `captureSerializer.test.js` only checks each `enum:` names a real list; adding entries breaks nothing. VERIFIED.

**5.3 Where a `callId` reference attaches — CORRECTED nuance on "without widening".** Three shapes: (a) a scalar `'callId': 'id'` — one call per tick only, too narrow for a `declarations` block with several shots; (b) **`calls[]` mirroring `actions[]`** — `'calls': 'array'`, `'calls.*': 'object'`, `'calls.*.callId': 'id'` (+ `'calls.*.n': 'number'`, a `kind` enum), a `call` mutator sized like `action` (`captureContext.js:246-261`, sixteen lines) and `calls: []` beside `actions: []` (`:150`); (c) a keyed `declarations{}` object. Every shape **adds paths**, so by V1.4 amendment 4 — "C-3's allowlist is by path and shape" (`docs/specs/CAPTURE_BUILD_SPEC_V1_4.md:107`) — it *is* an allowlist change reviewed as such; it loosens no existing path, `id` is already an admitted kind, and a `TICK_CAPTURE_SCHEMA_VERSION` bump is the founder's call. The contract's "the tick carries `callId` as a reference" (§1) is right about the direction and wrong that it costs nothing: it is a §2-reviewed capture-writer extension, exactly as contract §8 says. The cron's capture call sites (`tickCapture.*`): create `:705`; stage/exit at every exit; `scores :1039`; `universe :1046`; `risk :1571`; `action :1814, :3279`; `model :2384-2607`; `originalToolResult :2547`; `finalToolResult :2996`; checks `:3017-3447`; `identify :3837`; `manifest :3858`; finalize gate `:4180`. VERIFIED.

**5.4 The evidence-cutoff field the contract's `evidence.tickId` join relies on — CORRECTED.** The permanent `manifest` (`captureWriter.js:287-292`) holds `evidenceKeys[]` (the held symbols), `vintages{}`, `vintageLabels{}`, `notRenderedFields[]`, written at `agent-evaluate.js:3858-3866` from the entry's `vintages`: numeric entries → `manifest.vintages`, string entries → `manifest.vintageLabels`. At HEAD **every vintage is a string** (`tickStamps.js:266-270`: `quote: 'tick'`, `vwap: 'tick'`, `techAt` ISO, `fundAsOf` date, `rankingsAt` ISO; `VINTAGE_FIELDS` `:93`), so `manifest.vintages{}` is empty and `manifest.vintageLabels{}` carries the five — **per source, never per symbol** ("ONE block per entry"). The entry's per-symbol `evidence[sym] = { px, chg, atrX, vwapDev, bbPct, nr7, regime, risk }` (`:192-208`) has **no timestamp**, and it lives on the 150-capped entry, not the record. **There is no per-symbol as-of timestamp anywhere.** The tick-level cutoff is `callEnvelope.promptBuiltAt` (`captureWriter.js:303`; set `agent-evaluate.js:2458`; on the entry `:3664`; admitted as `timestamp`, `captureSerializer.js:162`). So the contract's `evidence: { tickId, intradaySweepId?, priceAsOf }` joins through `tickId` to a **tick-level** `promptBuiltAt` plus per-source vintage labels; `priceAsOf` per symbol is derived display data equal to the tick's build time (the quote vintage is `'tick'`), which is what the contract already labels it. VERIFIED.

**5.5 Identity.** `tickId = ${battleId}:${tickSeq}` (`captureContext.js:130`); `tickSeq` minted inside the admission transaction (`agent-evaluate.js:679-682`; a callback may re-run, `:684-687`); **`evalId` is minted before the model call** (`:2740-2741`) and stamped on the record only at `identify` (`:3837`; early exits leave it null, `captureContext.js:135`) — so a call minted at the check can carry both `tickId` and `evalId`; `actionId = ${tickId}:${n}` (`:249`). VERIFIED.

**5.6 Placement.** The authoritative battle update is `:4058`; capture finalizes at `:4180-4184` in the `finally`, after narrations (`:4091`) and anticipations (`:4122-4131`). A call minted from `haikuResult.declarations` right after validation (`:2541`) and the lint (`:2836`) — the same place the anticipation queue is built (`:2847-2853`) — can be written with a bounded timeout like `writeIntradayView` **before `:4058`**, and `tickCapture.call({ callId })` recorded before the record composes; the call → tick join is then two-way. If the writer runs later than `:4184` the join is call → `tickId` only. VERIFIED.

**Target 5 — size S–M · fenced: no · blockers:** the schema-version ruling; C-9 wall time (the call write is one more awaited write before the final update).

## 6. The close-of-day sweep and the deferred-beat PR

**6.1 `agent-daily-scores`.** `api/cron/agent-daily-scores.js` (309 lines, NF). Schedule `vercel.json:65-66` `45 1 * * 2-6` — one UTC entry, no dual-hour DST pair, so 21:45 ET during EDT (the header `:4` "8:45 PM" is the EST reading); the only guard is `isTradingDay()` (`:219`; `Intl`-backed via `marketSchedule.js:96-98`) — no ET-minute guard. **Active battles only:** `findActiveAgentBattles` (`:230`; fenced `api/_utils/agentBattleService.js:43-49`, `where('status', '==', 'active')`, read-only). Per battle it writes badge points and `scoreState.dailyScores.{dayKey}` (`:171-194`, `:182`), idempotent per battle × trading-day index (`:54-57`). A rider could hang in the loop or after `:194`, but three early returns skip it (no active battles, price fetch failed → 500, not a trading day) and **a battle that completed before 01:45 UTC is invisible to it** — the Sep 22 §A4 caveat holds. VERIFIED.

**6.2 The Film Room close-pass cron (Build A) — unbuilt.** No film-room, tape or close-pass handler exists under `api/cron/` (25 non-test handlers); the only "close pass" in `api/` is the mandate close pass (`api/_utils/mandateClosePass.js`). VERIFIED.

**6.3 `vercel.json` — 41 entries CONFIRMED; one stale line in BUILD_RULES.** `"crons"` `:43-208` holds **41** `"path"` entries (24 `/api/cron/*`, 16 `/api/fantasytimes/*`, 1 `/api/lobbies/*`); BUILD_RULES §6 `:78` "41/100" is right. **CORRECTED:** §6 `:82` "(19 of 21 are registered)" is stale — **23 of 25** handlers are registered; the two unregistered remain `season-daily-evaluate.js` and `season-pit-stop-manage.js`. VERIFIED.

**6.4 The deferred-beat / instrumentation PR — on no branch.** The scaling Phase 0 (`docs/audits/20260918_PHASE0_EVAL_CRON_SCALING.md:271` a per-battle wall-time pair; `:293` R2 — `summary.skipped` conflates lock-skips and deferrals and is not persisted; `:298` R7 — `tickMs` on the entry) remains unbuilt: none of the 19 history-connected unmerged branches touches `agent-evaluate.js`, and a content grep of that file's blob across every other branch finds no deferred-beat token (§12). At HEAD: `TIME_BUDGET_MS = 290_000` (`:170`; `maxDuration: 300` `:166`); the break `:389-396` (`summary.skipped += remaining`, "deferred to next tick"); the lock-skip `:691-695`; `summary` (`:225`) leaves only through the HTTP response (`:446`). **A deferred battle still gets no tick, no lock, no record.** VERIFIED.

**6.5 The queue-flag template (BUILD_RULES §5).** `pendingReflection` is written at completion inside the completion transaction (`:5812-5819`; `t.update` `:5895`), drained by `api/cron/process-pending-reflections.js:47-53` (`where status == completed`, `where pendingReflection == true`, `orderBy completedAt` — served by the composite `firestore.indexes.json:220-236`), cleared at `:87-90` or left true to retry. This is the sanctioned shape for a backstop that may never be fire-and-forget. VERIFIED.

**6.6 The candidate hosts, judged.** (A) An `agent-daily-scores` rider — the wrong host: active-only, one UTC slot, and a price-fetch precondition it would inherit. (B) The Film Room close pass — unbuilt; sequencing the cockpit behind it is a dependency the contract does not need. (C) A new cron entry — 41/100 leaves room, but §6 prefers branching in existing handlers. (D) **What the contract already prescribes covers most of it without a sweep:** `expired_unresolved` flips on encounter at the next check (the evaluation cron), and `ended_with_battle` belongs to **completion**, which runs inside one transaction (`completeBattle` `:5692`, `db.runTransaction` `:5699` … `:5955`) — a `calls` write can join it (extra `t.set` on the subcollection documents; all reads before the first write; deterministic under a re-run) or follow it as the mastery award does (`:5988-5999`) with a repair marker. **What is left for a sweep** is a horizon that passes on a day the battle never triggers again — and the honest host for that is the queue-flag pattern: a per-battle flag drained by an existing cron (`process-pending-reflections` is the shape), no new schedule entry. **The dependency the contract names is real:** a deferred battle gets no tick (6.4), so on-encounter flips never run for it and only the sweep would expire its calls — the deferred-beat PR is unbuilt. And the contract's `horizon.expiresAt` "computed at mint from `horizonPhrase` via R6's translation table" — **NOT FOUND:** no translation table exists; the only horizon enum in code is the Signal Drop prompt's `timeHorizon` (`api/_utils/signalDropPrompt.js:83-87`: `intraday | swing | positional | longterm | unspecified`), and R6 (`docs/20260923_FOUNDER_RULING_SHEET_AGENT_FRAMEWORK_V1_1.md:26`) says each mode's setup contract *defines* the translation — nobody has written BaggerBomb's. VERIFIED.

**Target 6 — size S–M · fenced: no · blockers:** the deferred-beat PR; the R6 translation table for BaggerBomb; whether completion's transaction takes the `calls` writes or a follow-on does.

## 7. The Voice Layer prompt path (the calls block)

**7.1 Where the chat prompt is assembled — not in the fence.** `api/agent/chat.js` imports `buildVoiceLayerPrompt` (`:4`) from `api/_utils/voiceLayerPrompt.js:2834` (NF) and the grounded blocks from `api/_utils/voiceLayerGrounding.js` (NF, `:41-52`); the fenced `agentPromptAssembly.js` is the **deploy/strategy** assembler (`buildStrategySystemPrompt :37`, `buildPortfolioSystemPrompt :200`) and is not imported by `chat.js`. Legacy and grounded are **one function with a boolean**: `chat.js:479-480` `grounded = getVoiceGroundingMode(uid) === 'on' && mode === 'battle'`; `:697-710` `buildPrompt(grounded)`; `:717` the shadow counterpart `buildPrompt(!grounded)`; `voiceLayerPrompt.js:2875` `grounded = false` ("the untouched assembly, byte for byte"). **CORRECTED vs contract §7 ("rides the same fenced review as the evaluation schema"):** neither the chat prompt nor the evaluation tool schema is in the §1 fence. The fence is met only if the block's prose is spliced into a fenced assembler — which the chat path never touches. VERIFIED.

**7.2 The DR-13 registry.** `PROMPT_CONTRIBUTING_MODULES` (`api/_utils/__fixtures__/promptHonestyRegistry.js:46-54`) already lists `voiceLayerPrompt.js` (`:51`) and `voiceLayerGrounding.js` (`:53`), so their source is swept for forbidden signal names (`agentEvalPromptAssembly.honesty.test.js:86-94`); the import tripwire (`:104-116`) scans **only the two fenced assemblers** (`:110`), so a new chat render module is registered by discipline, not caught by CI. VERIFIED.

**7.3 The current size budget.** No character cap on the assembled system prompt (`voiceLayerPrompt.js:3329` joins the blocks). History: legacy last 10 user-initiated exchanges (`chat.js:664-667`), grounded `HISTORY_WINDOW = 10` (`voiceLayerGrounding.js:139`). Record: grounded only, `RECORD_WINDOW = 3` newest-first (`:110`, `:372`; "~442 tokens" `:109`), `EVIDENCE_TOKEN_BUDGET = 300` (`:133`); legacy renders trades only (`voiceLayerPrompt.js:1121-1157`). Model `google/gemma-4-26b-a4b-it`, temperature 0.7, **800 output tokens** (`api/_utils/gemmaClient.js:39-41`); turn time 19 s Gemma / 24 s turn / 30 s function (`chat.js:101-102`, `:62`). The legacy prompt measured ~4,515 tokens (`VOICE_LAYER_PROMPT_CONSTRUCTION_GUIDE.md:684`, at the **repo root**, not `docs/`). A bounded calls block — open · hit-awaiting · last N resolved, one line each — is a few hundred tokens against a 262K-context model; the binding budget is the 24 s turn, not tokens. VERIFIED.

**7.4 Where the block enters.** The pattern to copy is **Block 5.4 (Show It)**: `voiceLayerPrompt.js:3250-3252` `const platformResearch = grounded && SHOW_IT_ENABLED ? buildPlatformResearchBlock(battle?.chatExchanges) : null;` pushed at `:3316` between `battleState` (`:3315`) and `earlierMessages` (`:3317`). A `buildCallsBlock(calls)` lands in `voiceLayerGrounding.js` (already registered) or a new module registered in the same commit; `const callsBlock = grounded && CALL_RECORDS_MODE === 'on' ? buildCallsBlock(calls) : null;` beside `:3250`; pushed adjacent to `:3315-3317`. `buildVoiceLayerPrompt` is **synchronous and reads only what it is handed**, so `chat.js` must read the `calls` subcollection and pass it as a new named parameter (precedent `capabilitiesManifest`, `voiceLayerPrompt.js:2856-2861`). The read slots into the existing `Promise.all` at `chat.js:569-586` — which already holds the route's one subcollection read, `groupRef.collection('claims')…count().get().catch(→ null)` (`:580-583`) — not as a new sequential await (the prologue is already "4 sequential Firestore round trips", `:735-745`). The grounded YOUR RECORD block already renders **heard** status (`RECORD_ENTRY_FIELDS` `voiceLayerGrounding.js:357-361` includes `'heard'`; `renderCurrentDirective` `:310-336`), so the calls block's "heard at" column has a precedent renderer. VERIFIED.

**7.5 Grounding interplay.** `VOICE_GROUNDING_MODE = 'shadow'` (`featureFlags.js:2271`; walk `['off', 'shadow', 'canary', 'on']` `:2274`; pin `src/config/voiceGroundingFlags.test.js:54`). At `'shadow'` the **legacy** prompt is sent and the grounded counterpart is only recorded to the GCS conversation stream (`chat.js:717-730`, `:190` → `shadowLogger.js:71`). The **twenty-pair read** is the grounding spec's walk: ≥ 20 real shadow turns replayed old-vs-new → founder read of the 20 pairs → one canary day → `'on'` (`docs/design/VOICE_LAYER_GROUNDING_SPEC_V1_3.md:127-131`); the pair count is *production*. The fit-check flip order (`featureFlags.js:813-832`) forbids lighting the fit check while grounding is canary/on. **Consequence:** a calls block should be **grounded-only**; it is then invisible until `'on'` — exactly Brief §8's gate — and the flag-off goldens (`voiceLayerPrompt.grounding.goldens.test.js`, `voiceLayerPrompt.js:35`) stay byte-identical. Review mode is never grounded (`chat.js:476-480`), so the block is battle-only. VERIFIED.

**Target 7 — size S–M · fenced: no · blockers:** grounding `'on'` (Brief §8); `CALL_RECORDS_MODE`; the block must be grounded-only or the goldens move; the `calls` read must be parallel, not sequential.

## 8. Battle View seams (desktop and mobile)

**8.1 The container.** `src/screens/AgentBattleScreen.jsx` (2,819 lines, NF) is the route-level host — `src/screens/BattleViewScreen.jsx:43-47` mounts it when `agentDeployed === true`, `src/App.jsx:9435` renders that when `screen === 'battle'`; there is no router. The live path is controller **on** (`BATTLE_VIEW_CONTROLLER_ENABLED = true`, `featureFlags.js:2142`) and pane **on** (`BATTLE_VIEW_CHARACTER_PANE_ENABLED = true`, `:2204`; `isCharacterPaneOn` `:2213-2214`), read at render (`:531`, `:535`, `:539`). `src/screens/battleView/` is 70 files, 15,706 lines; every one of its 35 non-test files is token-guarded (§10.3). VERIFIED.

**8.2 The character pane and its tab model — where Cockpit lands.** `src/screens/battleView/useCharacterPane.js:39-49` `PANE_SECTION = { CHAT, BENCH, TAPE }` and `PANE_SECTIONS` in segmented order; default `useState(PANE_SECTION.CHAT)` (`:77`), reset `:130`, fallback `:148`; **local state, never persisted**. Labels `battleViewCopy.js:745-747`. `CharacterPane.jsx`: `SegmentedControl` `:119-178` (a `role="tablist"` `:132`, `PANE_SECTIONS.map` `:146`, selected = teal wash `:168-169`, **no sliding thumb**); the header `:274-384` — mark (36 px) · name · archetype · controls `:359-362` (segmented · overflow · collapse/close); panels `:388-390`, hidden but never unmounted. `PANE_HEADER_FIXED_PX = 327` (`:92-93`, "243 controls") feeds `ARCHETYPE_MIN_VIEWPORT_PX` (`:107`) — a fourth tab widens the controls figure and the derivation must be redone. **Insertion:** `COCKPIT: 'cockpit'` at `:40`, first in `PANE_SECTIONS` `:46`, default at `:77`/`:130`/`:148`; a label at `CharacterPane.jsx:61` + copy at `:745`; a `cockpit` prop and `panel(PANE_SECTION.COCKPIT, cockpit)` at `:388`; `cockpit={<PaneCockpit …/>}` beside `bench=`/`tape=` at `AgentBattleScreen.jsx:2011-2012`. Dependents keyed on CHAT: `chatOpen` (`:614-616` — unread clears only while Chat is the visible section, so a Cockpit default opens the pane with unread **not** cleared, a design decision) and the named doors `pane.openPane(PANE_SECTION.CHAT, …)` (`:1229`, `:1250`, `:1382`, `:1423`). Tests: `useCharacterPane.test.jsx`, `showItDoor.jsdom.test.jsx`. VERIFIED.

**8.3 The mobile pane and the floating mark — one tab list for both shells (CORRECTED nuance on V4 §1).** `CharacterAvatar.jsx` (315): fixed to the viewport on mobile, absolute in the board column on desktop (`:137-143`); the 48 px mark button `:235`; the unread badge `:287-311` (teal); built once at `AgentBattleScreen.jsx:1976`, placed at the root on mobile (`:2701`) and in the board column on desktop (`:2446`). A tap opens the pane on the **remembered** section (`pane.openPane(null, …)`, `useCharacterPane.js:95-97`); the bubble opens Chat. The mobile pane is a **full-screen fixed overlay** (`:2702-2703`), the board dimmed and body scroll locked — not a sheet; it shows all sections. V4 wants Cockpit as a desktop pane tab (default) but **not** a tab behind the mobile mark (Chat · Bench · Tape stay) — today the same `CharacterPane` and `PANE_SECTIONS` serve both shells, so the section list must become shell-aware (a `sections` prop or an `isDesktop` filter) for V4 to hold. `paneUnread` (`:1726-1735`) counts non-user tape entries since last seen; a **calls badge** folds into it or adds a second prop at `CharacterAvatar.jsx:102` with a sibling span. VERIFIED.

**8.4 The "THIS TURN" row.** `ThisTurnStrip.jsx` (105 lines): a column (`:39-48`), the label row `:49-57` is a lone block with the **right end free**; empty text `Nothing queued · next check ~{t}` (`battleViewCopy.js:545`). Mounted `AgentBattleScreen.jsx:1739-1746`, rendered `:2428` **inside the board column** in both shells. The two pills land by making `:49-57` a `space-between` row after `{COPY.thisTurn}` (`:56`). **No vintage / as-of line exists in the header or the turn row today** — the only provenance line is the Why? panel's (`WhyPanel.jsx:234-236`; `decisionRecord.js:1069-1099`). VERIFIED.

**8.5 The seams for Board ⇄ Cockpit.** *(i) Animation:* 35 framer uses across eight `battleView/` files, all through `motionToken` (`src/theme/motion.js:155-156`; `snappy :103`, `fade = 0.2 :106`, `smooth = 0.3 easeOut :109`, `instant :127`). **V4 §3's 320 ms `cubic-bezier(.2,.9,.2,1)` and 180 ms cross-fade match no token — BUILD_RULES §11 requires the named vocabulary**; either the slide adopts `smooth`/`fade`, or the founder tunes the LOCKED table (`motion.test.js`) in the same commit. *(ii) Gesture:* the only drag in the Battle View is `ChatSheet.jsx:182-186` `drag="y"` (pane-off only); **no framer `drag="x"` exists anywhere in `src/`**; the horizontal precedent is native scroll-snap (`src/components/Dashboard/ArchetypePicker.jsx:213-214`, `:262-264`: `touch-action: pan-x`, `scroll-snap-type: x mandatory`, reduced motion falls back to native snap). *(iii) Segmented control:* no shared component; the pane's tablist has no thumb; the sliding-thumb idiom is framer `layoutId` (`src/components/Forge/SeasonModeToggle.jsx:2-3`, `:44-46` — a raw transition literal, not a token). *(iv) Reduced motion:* `useReducedMotion()` threaded as a prop (`AgentBattleScreen.jsx:540-541`), `motionToken(name, { reducedMotion })` → `instant`; **`src/index.css:550-555` neuters every CSS transition under reduced motion**, so the slide must be framer- or scroll-snap-driven, never a CSS transition. *(v) Layout — the header and turn row are NOT pinned on mobile today:* the root is `minHeight: '100vh'` (`:2095`), the top section is not sticky (`:2122`), the board column is `flex: '1 1 auto'` (`:2398`) with **the page as the scroller**; only `TierHeader` is sticky (`:199-201`); desktop alone has the bounded `overflowY: 'auto'` scroller (`:2424`). V4 §1's pinned header + turn row is therefore a layout change on a shipped screen: a bounded mobile board column with its own scroller (mirroring `:2424`) and `ThisTurnStrip` lifted out of the column (`:2428`) to sit above the track. VERIFIED.

**8.6 Desktop versus mobile.** `useMinWidth(768)` via `matchMedia` (`:159-174`; `isDesktop` `:517`); one component set with an `isDesktop` prop (`:2333`, `:2381`, `:2465`, `:2683`); no Desktop*/Mobile* pairs. VERIFIED.

**8.7 The styling anchors V4 §4 names — two are legacy.** The tier header `TierHeader` (`AgentBattleScreen.jsx:187-234`; wordmark `:209-220`; `TIER_HEADER_COLORS` `:116`) is a **raw-hex gradient in the non-guarded host file** — "styled like ⭐ STAR PICKS · 2X EACH" means re-expressing the look in `--ft-*`, not reusing it. The glowing fuse dot is `src/components/BaggerBomb/ChamberFuse.jsx` (`HOLO_COLORS`, `:9`; needle `:437`), mounted via `TacticalRow.jsx:487` — legacy tokens (§10.4). The teal-outline chip `WhyPanel.jsx:590-605` (`cssVar('teal')`) and the tug-of-war `ArenaHeader.jsx:294-340` (guarded, `--ft-*-rgb`) are the right precedents. The starfield is the `BaggerBombBackground` canvas (`:2104-2111`). VERIFIED.

**8.8 `useAgentBattle.js` and what the cockpit adds.** Whole-document `onSnapshot` (`:26-28`), spread `:32`, `statusFeed` `:49` plus six derivations, returned `:58`; consumed at `AgentBattleScreen.jsx:650-664`. **No client subscribes to any `agentBattles/*/…` subcollection**; the cockpit adds a second listener — `onSnapshot(query(collection(db, 'agentBattles', id, 'calls'), orderBy('mintedAt')))` — in a new hook beside `useAgentBattle`, with the `match /calls/{callId}` rules block (parent rules do not cascade, `firestore.rules:441-443`). VERIFIED.

**8.9 Every client write reachable from the Battle View (Brief §6.7).** `POST /api/agent/chat` (`AgentChat.jsx:999`), `/api/agent/file-directive` (`:1070`), `/api/agent/research` (`:1120`; the screen's `:1310` is dark), `/api/agent/ensure-opener` (`:770`), `submitDailyGrades` → `updateDoc dailyGrades.*` (`:1345`; `agentService.js:629-631`), `/api/agent/debate` (`DebateModal.jsx:38`); pane-off only: feed bookmarks. `battleView/` and `useAgentBattle.js` write nothing. The client rules allowlist (`firestore.rules:434-437`) cannot admit `calls`, so **the cockpit is read-only on the subcollection and every write is an API route** — the "one flag-gated writer at most" the Brief asks for is the answer endpoint. VERIFIED.

**8.10 The mobile budget.** No fixed heights: back bar (44 px button) + `ArenaHeader` (face 40 / score 30 / bar 7 / turn row ≥ 14) + `ThisTurnStrip` + board rows with sticky tier headers + `paddingBottom: 96` for the mark (`AVATAR_CLEARANCE_PX`, `:185`). The board does not scroll under a pinned header. VERIFIED.

**Target 8 — size:** pane tab **S** · pills **S** · mobile Board ⇄ Cockpit (segmented control + slide + pinned header + shell-aware tab list) **M–L** · **fenced: no** · **blockers:** the motion-token ruling (§11); the pinned-header layout change on a shipped screen; the one-tab-list-for-both-shells seam.

## 9. Research surfaces

**9.1 The stock modal and how it opens.** The live research modal is `src/components/draft/AssetResearchModal.jsx` (1,335 lines, NF): a `ReactDOM.createPortal` (`:358`) fixed overlay (`zIndex` 1100/1101, `:364-388`), full-viewport on mobile (`:340-347`). Props `:86-108`: `asset = { symbol, name, price, … }`, `defaultTab` (`:103`), `defaultTimeframe` (`:104`), `wsPrice`, `realtimeExtremes`, and **one** `actionConfig` button slot — **no `context` or `framing` prop**. It loads client-side EODHD data through `useResearchData` (`src/components/Research/useResearchData.js`, no `/api/` call). The Battle View host is `src/screens/AgentBattleScreen.jsx` (2,819 lines; `src/screens/BattleViewScreen.jsx:44-47` only routes to it): state `researchAsset` (`:555`), `handleSymbolClick` (`:1118-1123`), mount `:2759-2768` with `defaultTab="baggerbomb"`, `defaultTimeframe="bomb"`, `showActionButton={false}`; opened from `TacticalRow.jsx:346`, `TradeTickerCard.jsx:68` and the two `AgentChat` mounts (`:1924`, `:2616`). Twenty-two mounts repo-wide. `src/components/BaggerBomb/StockDetailModal.jsx` is a legacy mock-data modal (one caller, `PortfolioBuilderBaggerBomb.jsx:1909-1910`). VERIFIED.

**9.2 The screener / Search & Discover.** `src/components/Search/SearchDiscover.jsx` (229) is a top-level screen (`src/App.jsx:9756` `screen === 'search'`), tabs explore · rankings · institutional · screen (`:16-25`), default `'explore'` (`:29`), props `({ user, isMobile, isDesktop, setScreen, stocksData })` (`:27`); `ScreenerView.jsx` (925) is prompt-driven — `POST /api/screener/chat` (`:106`), props `({ onOpenResearch, isMobile })` (`:58`), a result tap opens `AssetResearchModal` (`SearchDiscover.jsx:33`, `:217-218`), "save as watchlist" posts to `/api/forge/watchlists` and `/commit` (`ScreenerView.jsx:280`, `:296`). **No `initialSymbol` / `initialQuery` / `prefill` prop exists anywhere under `src/components/Search/` or `DiscoverPanel.jsx` — NOT FOUND.** VERIFIED.

**9.3 The research modal.** No `ResearchModal` or `ResearchSheet` component exists. What exists is **Show It**: `api/agent/research.js` (428, NF) — `POST { agentId, battleId, symbol }` (no free-text question), auth **then** the flag 404 (`:226`, deliberately after auth, `:217-221` — the house "404-after-auth" shape other routes cite), universe check `canonicalUniverseSymbol` (`:269`), `RESEARCH_CAP = 3` (`src/data/researchCap.js:36`; `:276`), a **code-composed** card (`api/_utils/researchCard.js`, no model call), one transaction `:330-351` appending a `research` exchange (`:348`); rendered by `src/components/Agent/ResearchCard.jsx` (145, `cssVar` only). `TermResearchModal.jsx` covers term definitions. VERIFIED.

**9.4 `SHOW_IT_ENABLED` and its gate — CORRECTED.** `false` (`src/config/featureFlags.js:2420`; DARK_BY_DESIGN `flagPinGuard.test.js:123-124`). V3 §6 / V4 §5 say Show It "appears inside the sheet once grounding is on"; at HEAD the route and the doors are gated by **`isShowItOn() = isBattleViewControllerOn() && SHOW_IT_ENABLED`** (`:2435-2437`) — **not by grounding**. Grounding gates only the narrator's ability to see the card: "NOT a VOICE_GROUNDING_MODE gate" (`:2395-2396`); the flip order is grounding `'on'` first, then Show It, because the `PLATFORM RESEARCH` block rides the grounded prompt (`:2401-2410`; `chat.js:920-923`; `voiceLayerPrompt.js:3250`, `:3287`). Doors: `WhyPanel.jsx:613-625`, `PaneBench.jsx:88`; host `AgentBattleScreen.jsx:539`, `:1297-1330`, `:1872`, `:2011`. VERIFIED.

**9.5 The three outcomes.** **(a) Answer** → the endpoint in 4.10. **(b) Flag for next deploy → the watchlist path.** The agent's watchlist is a Forge document `watchlists/{watchlistId}` — owner-readable, **client writes denied** (`firestore.rules:1078-1082`) — linked by `agents/{agentId}.equippedWatchlistId`, written only by `api/agent/equip-watchlist.js:118-119` (the rules check it at create, `:299-302`, and the agent-doc update allowlist excludes it). **No client Firestore write path exists.** API only: `PATCH /api/forge/watchlists/{id}` replaces the **whole** `tickers` array (`api/forge/watchlists/[id].js:161-162`) and 409s on a committed list (`:224-226`); `/commit` and `/uncommit` flip status; equipping a *different* list mid-battle is refused — 409 `battle_active` (`equip-watchlist.js:89`). The deploy reads the list fresh in the **fenced** `api/agent/decide.js:356-364` (read-only here), ignores drafts (`api/_utils/watchlistEquip.js:53-55`), folds tickers into the shortlist (`:77-90`) and freezes a snapshot on the battle (`api/_utils/agentBattleService.js:196-200`, fenced). **So "flag for next deploy" at HEAD is uncommit → PATCH the whole array → commit on the equipped list; no single-ticker add endpoint exists; the Show-It card's "Equip" door is unbuilt (`src/components/Agent/ResearchCard.jsx:60-64`).** The contract's `hypothesisRef.watchlistId`: `hypothesisRef` is NOT FOUND (§0.2); `watchlistId` exists as the Forge doc id and as the frozen snapshot key `agentContext.equippedWatchlist.watchlistId`. Out-of-scope finding: the Dashboard watchlist widget is **localStorage only** (`src/services/watchlistService.js:2-4`, `:20-25`) and never reaches the agent. **(c) Ask in chat** — the prefill exists: `composerPrefill { text, nonce }` (`AgentBattleScreen.jsx:571`), `handleAskFollowUp(symbol)` (`:1232-1239`) → `followUpPrefill: (symbol) => \`About ${symbol} — \`` (`src/screens/battleView/battleViewCopy.js:305`), consumed by `AgentChat.jsx:583-584` (effect `:918-946`; a typed draft wins). Settable only from inside `AgentBattleScreen` — a research sheet mounted there reuses it verbatim. VERIFIED.

**9.6 What a research sheet can mount with a symbol pre-loaded.** `AssetResearchModal` takes the symbol (`asset`, `defaultTab`, `defaultTimeframe`, `wsPrice`) but is a self-contained portal with fixed geometry, styled in `HOLO_COLORS` + 39 raw hex (off the `--ft-*` system); there is no generic bottom-sheet primitive (`src/components/shared/CenteredModal.jsx` is the only modal shell); the Battle View's one non-modal sheet is `ChatSheet.jsx` (detents peek/half/full, `useChatSheet.js:23-39`). **Verdict:** the research sheet is a **new host** — re-mount the modal as the Battle View does today (full-viewport on mobile) with a new three-button footer, or build a `--ft-*` sheet around its tabs. The three outcomes are wiring to existing paths: answer (4.10), flag (the three-call sequence, or a new add-ticker endpoint — and a founder call on whether editing the equipped list mid-battle is wanted, since the running battle keeps its frozen snapshot), ask in chat (`handleAskFollowUp`). "Show It" inside the sheet is the existing card, gated by `isShowItOn()`, not grounding. **Size:** S (ask in chat) + M (sheet host) + M (flag path). **Fenced: no** (the deploy read is fenced but unchanged). **Blockers:** the Show It flip order; the mid-battle watchlist semantics; the pre-loaded "agent's framing" has no prop to land in.

## 10. Tokens

**10.1 `CMD` — every mockup name exists.** `src/components/Dashboard/commandUI.jsx:16-33` (100 lines, NF): `bg :17`, `surface :18`, `raised :19`, `hair :20`, `hair2 :21`, `ink :22`, `ink2 :23`, `ink3 :24`, `teal :25`, `gold :26`, `copper :27`, `technical :29`, `fundamental :30`, `risk :31`, `allocation :32` — all fifteen. Other exports: `MONO :35`, `alpha :38` (returns teal on a non-hex input — the BUILD_RULES §10 hazard), `readableOn :48`, `Eyebrow :58`, `SectionLabel :71`, `ErrorBanner :87`. VERIFIED.

**10.2 But `CMD` is legacy; the canonical source is `--ft-*` — CORRECTED vs contract §9.** `src/theme/tokens.css:6` "THIS IS THE CANONICAL COLOR SOURCE (ruling R-S5)"; `:7-9` names `CMD` as one of four legacy JS systems. 39 tokens (`tokenBaseline.json` holds 39). Mapping: `bg` → `--ft-bg-dashboard` (`:57`, same value) · `surface` → `--ft-bg-card` (`:61`) · `raised` → `--ft-bg-agent` (`:75`) · `teal`/`technical` → `--ft-teal` (`:120`) · `gold` → `--ft-gold` (`:147`) · `copper` → `--ft-copper` (`:155`) · `allocation` (purple) → `--ft-purple` (`:135`, not `--ft-purple-deep` `:139`) · `fundamental` → `--ft-amber` (`:126`) · `risk` → `--ft-red` (`:129`) · `hair`/`hair2` → compose `rgba(var(--ft-scrim-rgb), .07 / .12)` (`:176`). **`ink`, `ink2`, `ink3` have no `--ft-*` token** (`#F4F5F8` / `#9A9DAB` / `#5E6170`; nearest `--ft-text-primary #e2e8f0 :93`, `--ft-text-secondary-holo #8b949e :105`, `--ft-text-muted #6e7681 :109` — different values). `src/components/League/leagueTokens.js:22` `LTOKENS = CMD` is the League re-export. VERIFIED.

**10.3 What the Battle View actually uses.** Every non-test file in `src/screens/battleView/` consumes `cssVar()` / `var(--ft-…)` **only** — 114 `cssVar(` + 25 `var(--ft-`, zero `CMD.`, zero `LTOKENS.`, zero raw hex (one hex in a comment, `TapeCards.jsx:74-75`). `src/theme/tokens.guard.test.js:52-100` guards **all 35** non-test files in that directory (`:57-59` "a zero-literal ratchet on the whole directory") → **a new cockpit file under `src/screens/battleView/` must be added to `GUARDED_FILES` in the same commit** and may not carry a hex literal. The mockup's `CMD` names therefore map per 10.2 and are consumed through `src/theme/cssTokens.js` (`cssVar` `:168`, `readToken` `:183` for Framer Motion, `readTokenRgb` `:212`). VERIFIED.

**10.4 The V4 §4 state colours — every one needs a derived token; two collide with shipped conventions.** The semantic tier at HEAD is `accent · warp-tint · success · danger · warning · game-baggerbomb · game-draft` (`tokens.css:202-212`); no alias exists for watching / your call / acted / held off / asking / expired / dropped. Base tokens exist for grey (`--ft-text-muted`), purple (`--ft-purple`), green (`--ft-emerald` / `--ft-success`), gold (`--ft-gold`), copper (`--ft-copper`); **"dim" has none**; "your call" and "held off" are both purple and indistinguishable without two aliases. Seven `--ft-call-*` aliases go into `tokens.css` and `tokenBaseline.json` in one commit. **CORRECTED — "the player's purple":** the shipped Battle View player colour is **teal** and the CPU's is **copper** (`src/screens/battleView/ArenaHeader.jsx:13-14`, `:82-83`); the unread dot is teal (`ChatSheet.jsx:232`). A purple "player" accent diverges from teal-as-player everywhere on the screen, and copper-as-dropped collides with copper-as-CPU (`tokens.css:149-154` reserves it for the arena's CPU side). **The fuse:** no fuse exists in `src/screens/battleView/`; the glowing fuse dots live in legacy BaggerBomb `ChamberFuse.jsx` (`HOLO_COLORS`) and League `FuseHero.jsx` (`LTOKENS` + raw hex) — neither on `--ft-*`; V4 §4's "the same glowing dot the fuse uses" is a new `--ft-*` dot, not a reuse. VERIFIED.

**10.5 `docs/DESIGN_TOKENS.md` — CORRECTED vs Brief §7.** The file exists and is self-marked deprecated (`:3` "⛔ DEPRECATED — DO NOT USE AS MIGRATION INPUT"; BUILD_RULES §10 `:116`). The design-system input for Claude Design is `src/theme/tokens.css` + `tokenBaseline.json` via `cssTokens.js`. The Controller brief's §10 hex list (`docs/design/COMMAND_CENTER_CONTROLLER_DESIGN_BRIEF_V1_2.md:165`) carries three values in neither system (`#2E6FE8`, `#3DDC97`, `#FF4D5E`). Stale pointers to the deprecated file remain in `docs/CLAUDE_CODE_RESEARCH_MODE_PROMPT_V2.md:11`, `:878`, `:926` and `docs/MARKETCLASH_DASHBOARD_FINAL_SPEC.md:670` (separate tasking). VERIFIED.

**Target 10 — size S · fenced: no · blockers:** the founder's ruling on purple-vs-teal for "your call", and copper for "dropped".

## 11. Flags at HEAD (`src/config/featureFlags.js`)

The Sep 22 lines hold up to `:2496`; every flag defined after `:2530` sits **3 lines lower** (the `BACKING_BETA_ENABLED` docstring grew, `:2496-2530`). All VERIFIED at HEAD.

| Flag | Value | Line | Pin | Dark-by-design (`src/config/flagPinGuard.test.js`) | Note |
|---|---|---|---|---|---|
| `TICK_CAPTURE_ENABLED` | **`true`** | `:2695` (was `:2692`) | `src/config/tickCaptureFlags.test.js:34` | no — flipped Sep 21 | lit; mints `cronState.tickSeq` (`agent-evaluate.js:679-682`) |
| `DIRECTIVE_FIT_CHECK_ENABLED` | `false` | `:887` | `directiveFitCheckFlags.test.js:23` | **yes** `:172-173` | flip halted Sep 18 (4.7) |
| `ANTICIPATION_THRESHOLD_LINT_MODE` | `'off'` | `:2566` (was `:2563`); modes `:2569` | `anticipationThresholdLintFlags.test.js:39` | no (string) | contract §8 retires the flip |
| anticipation **dispatch** | — | no flag | — | — | gated only by cron budget `> 12 s` (`agent-evaluate.js:4122-4124`) |
| `INTRADAY_DIAGNOSTIC_ENABLED` | `false` | `:2608` (was `:2605`) | `intradayFlags.test.js:39` | no | gates `intradayViews` write + the eight pointer fields |
| `INTRADAY_COLLECT_ENABLED` | `true` | `:2590` | `intradayFlags.test.js:36` | no | flipped Sep 21 |
| `INTRADAY_AGENT_USE_ENABLED` / `INTRADAY_PRICE_SOURCE` / `INTRADAY_RISK_ACTIVATION_ENABLED` | `false` / `'legacy'` / `false` | `:2619` / `:2631` / `:2645` | `intradayFlags.test.js:42`, `intradayPriceSourceFlags.test.js:20`, `intradayFlags.test.js:45` | yes `:79-80` / no (string) / yes `:81-82` | stages 2–4 |
| `VOICE_GROUNDING_MODE` | `'shadow'` | `:2271`; walk `:2274`; `getVoiceGroundingMode(uid)` `:2322` | `voiceGroundingFlags.test.js:54` | no (string) | the cockpit's biggest gate |
| `SHOW_IT_ENABLED` | `false` | `:2420` | `showItFlags.test.js:28` | **yes** `:123-124` | Block 5.4 precedent |
| `SHADOW_ASSEMBLY_ENABLED` | `false` | `:1482` | `api/_utils/shadowAssemblyCapture.test.js:119` | **yes** `:176-177` | deploy-prompt corpus; unrelated to chat `'shadow'` |
| `TICK_STAMPS_ENABLED` | `true` | `:2372` | `tickStampsFlags.test.js:34` | no | `heard` / `evidence` / `vintages` / `candidates` |
| `BACKING_BETA_ENABLED` (context) | `false` | `:2529` | `backingBetaFlags.test.js:118` | **yes** `:174-175` | its PRs caused the +3 drift |

**`cronState.evalSeq` — present.** Mint `agent-evaluate.js:2740-2741` (`evalSeq = (cronState.evalSeq ?? evaluations.length ?? 0) + 1`, `evalId = eval_NNN`), written as an explicit value on the same update that appends the entry (`:3986`); the entry array is `slice(-150)` (`:3938`). `cronState.tickSeq` is minted inside the admission transaction (`:679-682`). VERIFIED.

## 12. Unmerged branches bearing on any target

**Method and one caveat.** 611 `origin/*` branches beside `main`. **The clone is shallow** — `.git/shallow` lists 20 boundary commits; `origin/main` resolves 280 commits, oldest `8866071a` (2026-09-08) — so `git merge-base --is-ancestor` is decidable for 85 branches (66 merged, 19 unmerged with a connected history) and undecidable for 526 whose tips predate the boundary; those were classified by a `Merge pull request #N from fashraf94/<branch>` subject in the fetched history (467) or, for the 59 without one, by tip date (48 ≤ 2026-08-14 — audits and recon from before any target existed) and by whether the tip's files are byte-identical on `main` (the 11 Sep 1–8 tips). `git fetch --unshallow` is §3-permitted and was **not** run; exact ancestry for the 526 needs it. Terms grepped over `+`/`−` diff lines and branch names: `evalSeq`, `deferred`, `tape`, `dayClose`, `directive`, `capture`, plus `anticipation`, `calls`, `callId`, `declarations`, `cockpit`, `intradayViews`, `pick`. VERIFIED (`git for-each-ref`, `merge-base`, `rev-list`, `diff origin/main...<b>`, `merge-tree --write-tree` for conflict probes — object writes only, no ref or tree change).

**12.1 The one source branch that matters — `origin/claude/b2-directive-transaction`.** Tip `5ce73eb9` (Sep 13), **13 commits ahead, 21 files, +5090/−305**, base `c35f9a59`; **still unmerged**. Adds `api/_utils/directiveTransaction.js` (462 lines: `runDirectiveTransaction`, `DIRECTIVE_CONFLICT_POLICY`, `DIRECTIVE_BUDGET_POLICY`, `attestThrown`) — absent from `main`; rewrites `api/agent/chat.js` (+352/−106) to file through the shared transaction with status and budget re-read inside the write and `persisted`/`charged` on every response; rewrites `api/agent/file-directive.js` (+136/−138) onto the same transaction; adds `FLAGGED_DISPLAY_CAP = 5` applied last in `selectBench` (`selectFlagged` stays uncapped); touches `agentChatBudget.js`, `decisionRecord.js`, `AgentChat.jsx`, `FilmRoomChat.jsx` (+ an attestation test), `useArenaEngine.js`, `battleViewCopy.js`, `compositionProtectedStoresAllowlist.json`. **What merging would change for the cockpit:** the answer endpoint would have a shared transaction to call instead of copying `file-directive.js`'s, and the attestation the contract §6 implies ("nothing written on rejection") would exist on the wire. **CORRECTED vs the Sep 22 §F4 row:** `git merge-tree` against HEAD yields **one** content conflict, in `api/_utils/compositionProtectedStoresAllowlist.json` — not `chat.js`, which auto-merges textually (though a 16-hunk change onto a `chat.js` rewritten since Sep 16 is not semantic safety; B2 never touches `directiveGate.js`); and its build report's §7 STOP item (a landed commit whose reply was lost) is ruled and built in-branch as §7-A (`3574e756`, "the ambiguous commit is a no-op"). Whether the founder accepted that ruling is not in the tree.

**12.2 The docs branches that are still unmerged and citable only from the branch** (each one commit, merge-tree clean):

| Branch | Tip · date | Adds | Bears on |
|---|---|---|---|
| `claude/epic-bell-stwcxu` | `d75c35bd` · Sep 15 | `docs/audits/20260915_PHASE0B_FILM_ROOM_DISCOVERY.md` (886 l.) | targets 4, 6 — the Film Room's own record (gameplan approval UI unreachable; result never on the battle doc) |
| `claude/review-intraday-build-1` | `89968119` · Sep 19 | `docs/audits/20260919_BUILD1_INTRADAY_REVIEW.md` (480 l.) | targets 3, 11 — the `evalId`-from-length finding that became `cronState.evalSeq` (`929624ca`) |
| `claude/elegant-mccarthy-vwxcog` | `2722dfa0` · Sep 21 | `docs/audits/20260921_INTRADAY_FOLLOWUPS_REVIEW.md` (413 l.) | targets 3, 11 — mutation rows on `evalSeq` |
| `claude/phase0-b2-showit` | `99c89c6f` · Sep 13 | `docs/audits/20260913_PHASE0_B2_SHOWIT_PREREQS.md` (498 l.) | targets 4, 9 |
| `claude/nice-thompson-2q1o4a` | `47217ca4` · Sep 14 | four FT environment / Firestore-surface audit docs (2,288 l.) | target 3 (Firestore surface) |
| `claude/phase0a-archetype-identity` | `4f1efeb3` · Sep 15 | `docs/audits/20260915_PHASE0A_ARCHETYPE_IDENTITY_INVENTORY.md` (701 l.) | target 4 (the menu) |
| `claude/stoic-cray-wwhf0n` · `claude/vwap-exit-dating-census-2sdebc` | `5d90d260` · `d9b57917` · Sep 17 | the two read-only census scripts | the exit-dials arc (C3), not a cockpit target |
| `claude/tender-lovelace-x6axjz` | `986a3f61` · Sep 11 | `api/_utils/gcsCredentials.js` + shadow-logger plumbing (7 source files) | none — GCS credential loading |

**12.3 The yes/no the contract depends on.** (i) A deferred beat or persisted `summary.skipped` in `agent-evaluate.js`: **NO branch** — the deferral is still the in-memory count (`:393-394`, `:693`); the instrumentation PR that `docs/audits/20260918_PHASE0_EVAL_CRON_SCALING.md:271`, `:298` recommends exists on no branch. (ii) `evalSeq`: source only on `main` (`929624ca`); two review docs mention it. (iii) `dayClose` / `scoreState.result` writer: **none**. (iv) `agentBattles/{id}/calls`, `declarations`, `callId`, `playerResponse`: **none anywhere**. (v) a `pick` directive kind or any edit to `src/data/archetypeAdjustments.js`: **none**. (vi) `api/_utils/tickCapture/*`, `firestore.rules`, `firestore.indexes.json`, `vercel.json`: touched by **no** unmerged branch. (vii) `docs/phase0-tick-capture` is **merged** (PRs #879/#881) — cite `main`. **Merged since the Sep 22 doc:** `cc-arc-answers-film-room-tz3wch` (`47802e3d`, PR #888) — **CORRECTED** from "unmerged"; `docs/framework-review-gate-fix` (`54c69793`, landed the Forge Record Phase 0 doc); `docs/framework-closeout` (`e010ef0b`). **Missed by the Sep 22 scan:** the five branches in 12.2 dated Sep 11–19; none changes its conclusions. One flag branch worth knowing: `claude/cc-sync-flip` (Sep 1) sets `COMMAND_CENTER_SYNC_ENABLED = true`; HEAD still has **`false`** (`src/config/featureFlags.js:2026`) — the Pass 1 Command Center sync surfaces are dark. VERIFIED.

**Target 12 — size n/a · fenced: n/a · blockers:** none of these branches blocks Build 0; B2 is the one whose merge (or abandonment) Build 1 should know before copying `file-directive.js`'s transaction into a second route.

## 13. Proposed split — Build 0 / 1 / 2

### Build 0 — `declarations` schema · call writer · `CALL_RECORDS_MODE` (shadow) · rules for `calls` · tick `callId` reference

**Prerequisites.** None of the Brief §8 gates (shadow renders nothing). `TICK_CAPTURE_ENABLED` is already `true` and the record is independent of it by construction. Two founder rulings before the first line: **(a) the route** — schema-description-only (no fence contact) or a system-prompt section (two fenced splices via DR-13); **(b) the capture schema-version bump** for the new `calls` paths. One design decision: the call id — `${tickId}:call:${n}` (deterministic, idempotent under a re-run transaction) over `randomUUID()`.

**Fenced files touched.** None on route (a). On route (b): `api/_utils/agentEvalPromptAssembly.js` at `:485-511` and `:688-714` — one import, one call each, behind a dark flag so flag-off bytes hold — with `voiceLayer…`-style registration in `PROMPT_CONTRIBUTING_MODULES` (`promptHonestyRegistry.js:46-54`) in the same commit (BUILD_RULES §1 `:30`). §7-class review either way if (b).

**Files (all NF unless noted).** `api/_utils/agentEvalToolSchema.js` (+`declarations`, typed `['object','null']`, beside `:157-192`; **keep `required` untouched**, §1.2); a new `api/_utils/callRecords/` module (validate the four sub-blocks strictly — the validator will not — mint ids, compose the typed record with `kind ∈ {called_shot, confirmation, pick}`, `state`, `horizon`, `evidence: { tickId, priceAsOf: promptBuiltAt }`, `hypothesisRef: { watchlistId: agentContext.equippedWatchlist.watchlistId, equippedConfigHash, battleId } | null` (§9.5; `equippedConfigHash` exists at `api/_utils/resolvedAgentManifest.js:165`), `origin`, `said`; mint `invalidated` when the typed level or symbol is absent from the tick's present signals — reuse `buildPresentSignals` (`agent-evaluate.js:2773-2779`) and write a numeric presence check the prose lint does not have; write with a bounded timeout like `writeIntradayView`; flip `open → hit | expired_unresolved` on encounter at the next check); `api/cron/agent-evaluate.js` (the mint call after the lint at `:2836`, before the final update `:4058`; the encounter flip where the check reads its held/bench state; `tickCapture.call(...)`); `api/_utils/tickCapture/captureContext.js` (+`calls: []`, +`call` mutator, `:150`, `:246-261` pattern), `captureSerializer.js` (+`'calls': 'array'`, `'calls.*': 'object'`, `'calls.*.callId': 'id'`, `'calls.*.n': 'number'`, a `kind` enum), `captureWriter.js` (+`calls: state.calls` in `:207-337`), `captureConfig.js` (version bump if ruled); `firestore.rules` (+`match /calls/{callId}` after `:452`) + `test/rules/callsDenials.rules.mjs` (copy of `intradayViewsDenials.rules.mjs`); `firestore.indexes.json` (+ a `calls` composite only if the client query filters by `state`); `src/config/featureFlags.js` (+`CALL_RECORDS_MODE = 'off'`, modes `['off','shadow','on']`, a string pin like `ANTICIPATION_THRESHOLD_LINT_MODE`'s at `src/config/anticipationThresholdLintFlags.test.js:39`); `api/_utils/compositionProtectedStoresAllowlist.json` (+ rows for the writer's `set`).

**Harness and fixture updates.** `api/_utils/__fixtures__/tickStampsHarness.js:346-363` (`makeHoldResult` gains `declarations`) and a `makeDeclarations()` factory; `api/_utils/intradayPromptExclusions.test.js:26-33` (+ a row proving the four prompt allowlists never admit `declarations`); `api/_utils/composition.m7e2eBudget.test.js:198` (measure the margin); `api/_utils/thresholdLintCopy.test.js:100-102` (no `20-day` in the new descriptions); `api/_utils/tickCapture/captureSerializer.test.js:66-73` (the new enum names a real list); a `callRecords` unit suite (the four-state machine; `invalidated` at mint; idempotent re-mint; the flag-off byte-identical battle update). **Untouched by design:** the frozen entry golden (`tickStamps.flagOff.test.js:196-205`), the flag-on shape (`flagOn.test.js:386`), `captureWriter.test.js:191-194` (no new batch path).

**Review path.** ≥ 10 files → BUILD_RULES §2's mandatory multi-lens adversarial review with a `vite build`, written to `docs/audits/`; reviewers on snapshot trees. Dark-merge guarantee to assert: flag `off` → the battle document, the tick record and every prompt byte are identical to HEAD.

**Size: M.** **The one thing most likely to be wrong in the contract as written:** §2 "born structured or not at all" assumes the decider fills a typed `condition.level` and that a `declarations` property is free while shadow. At HEAD `threshold` is prose **by ruling** (D-103 / D-112: "persisted, never rendered in the narrator's voice"; `tickStamps.js:302`), the validator checks nothing nested (`agentEvalToolResultValidation.js:39-43`), and the mint-time `invalidated` check needs a numeric presence test that does not exist. And **the tool schema is what Haiku sees on every tick regardless of `CALL_RECORDS_MODE`** — `TRADE_DECISION_TOOL` is one frozen constant (`agentEvalToolSchema.js:4`) stringified into the request (`m7e2eBudget.test.js:152`); a `declarations` property is model-visible at `off` unless the schema is built flag-conditionally. "Shadow" is therefore not silent to the model; the contract's §8 should say so, or the schema must be conditional.

### Build 1 — close sweep · answer endpoint · `pick` and divergent-answer kinds at the gate (+ four-layer fixture) · chat calls block · chat rendering from the record with the chip

**Prerequisites.** Build 0 with a week of shadow records; **the deferred-beat PR** (on no branch — the sweep's dependency, §6.4); **the R6 translation table for BaggerBomb** (`horizonPhrase` → `expiresAt`; NOT FOUND, §6.6); founder rulings on **the latest-wins collision** (§4.8 — latest wins, refuse a second divergent answer while the slot is unheard, or STOP-class multi-directive), on **whether an `ack` writes an exchange and whether it charges** (§4.10), and on **the exchange id** (§2.3). Grounding `'on'` is **not** required for the endpoint or the chip; it is required for the calls block to be visible (grounded-only, §7.5), which is Brief §8's own gate.

**The §1 intersection.** None, provided `battle.directive` stays a single object and no new top-level battle key is written. Multi-directive = STOP.

**Files.** `api/agent/call-response.js` (new — mirrors `file-directive.js` `:142-330`: security middleware, 405, `requireAuth`, its own `CALL_RECORDS_MODE === 'on'` gate before any read, body validation with a nullable-and-required belief, **one `runTransaction`** carrying owner / active / binding / belief / archetype / membership / budget, the call's `open`|`hit` check and per-`kind` answer legality, `randomUUID` thread id, `buildDirectiveSlot`/`buildDirectiveRecord`, `tx.update` on `chatExchanges` + `directive` (+ budget), the `playerResponse` write with `directiveThreadId`, response after commit; `archetypeImportBoundaryBaseline.json` and the protected-stores allowlist in the same commit); `src/data/archetypeAdjustments.js` (+ a per-archetype family `XX-hold-off`, `XX-ask-first`, `XX-pick` with a `canonicalTemplate` and a `getCanonicalTextFor(codeId, id, { symbol })` renderer, the symbol proved by `canonicalUniverseSymbol` (`api/agent/research.js:349`); `ARCHETYPE_IDENTITY_VERSION` bump; the 46-count pins and the prompt goldens move); `api/_utils/directiveUtils.js` (+ a registered expiry value if a horizon-bound directive is wanted — `:71-72` treats unknown values as active); `api/_utils/directiveFiling.js` (additive `symbol`/`kind` on the slot; re-pin `file-directive.test.js:401-405`); `src/data/decisionRecord.js` (+ message types and copy); the sweep: `ended_with_battle` inside `completeBattle`'s transaction (`agent-evaluate.js:5699-5955`) or a follow-on with a repair marker, and a queue flag drained by an existing cron (`process-pending-reflections.js:47-90` shape); `api/_utils/voiceLayerGrounding.js` (+`buildCallsBlock(calls)` — open · hit-awaiting · last N resolved with `playerResponse` and heard status, the `heard` renderer already exists at `:310-336`), `api/_utils/voiceLayerPrompt.js` (+ the block beside `:3250-3252`, pushed near `:3315-3317`, grounded-only and flag-gated; a new named parameter like `capabilitiesManifest` `:2856-2861`), `api/agent/chat.js` (+ the `calls` read inside the `Promise.all` at `:569-586`, degrade to `null`); `api/_utils/voiceLayerAnticipation.js` (+ the join key on the anticipation exchange — `callId` in `anticipationContext` `:363-368` and `:133-138`); `src/components/Agent/AgentChat.jsx` (+ the "→ cockpit" chip beside the eyebrow `:366-388`, a `data-…-id` on `MessageBubble` and a nonce-keyed scroll effect mirroring `:901-914`), `deriveChatMessages.js` (+ the id).

**Tests.** The **four-layer mismatch fixture** — selection · commitment · narration · effect — for the `pick` kind (no precedent exists; the origin is the Reuse Audit's Speculator case, `docs/audits/FantasyTrades_Expanded_Capability_Reuse_Audit_2026-09-23.md:575`, `:597`); a `file-directive.test.js`-shaped suite for the endpoint (409 / 422 / 429 / 200; typed rejections; nothing written on rejection); the menu pins (`archetypeAdjustments.test.js:74-77`, `:182`, `:207`; `archetypeRegistry.test.js:117`); the grounded prompt goldens (`voiceLayerPrompt.grounding.goldens.test.js`) with the calls block flag-off byte-identical; `AgentChat.tapeKinds.render.test.jsx:203-210` (the chip must not reuse `data-tape-kind-eyebrow`).

**Size: L** (endpoint M · gate family M · sweep S–M · calls block S · chip S). **The one thing most likely to be wrong in the contract as written:** §6 "divergent answers file a directive through the canonical gate" presumes a directive can be symbol-scoped and call-scoped. At HEAD every directive is an archetype-wide fixed string (`archetypeAdjustments.js:283-286`) in **one latest-wins slot** (`chat.js:1081`, `file-directive.js:284`); "the canonical gate" as the chip route practises it is `isValidAdjustmentId` + `getCanonicalText` (`file-directive.js:222-228`), not `gateDirective`; and two divergent answers in one interval collide before any check hears the first. The contract's `directiveId` must read `directiveThreadId`.

### Build 2 — the cockpit UI: pane tab · segmented control + slide · pills · tiles / sheets / research sheet · agent-mark door

**Prerequisites.** Brief §8 in full — grounding `'on'` (the twenty-pair read, `docs/design/VOICE_LAYER_GROUNDING_SPEC_V1_3.md:127-131`), the fit-check flip (halted Sep 18 with an open founder decision, `docs/audits/20260918_FLIP_DIRECTIVE_FIT_CHECK.md:192-208`), the capture flip (**done**), held-position parity (unbuilt, fenced); `CALL_RECORDS_MODE = 'on'`; Build 1's endpoint. Four design rulings before the mockups are re-cut: **teal, not purple, for "your call"** and **not copper for "dropped"** (§10.4); **the motion tokens** — `smooth` (0.3 easeOut) and `fade` (0.2) or a founder tune of the LOCKED table (§8.5); **the pinned mobile header** — a layout change on a shipped screen (§8.5); **Show It in the sheet is gated by `isShowItOn()`, not grounding** (§9.4).

**The §1 intersection.** None. The client is read-only on `agentBattles` beyond ten allowlisted keys (`firestore.rules:434-437`); every cockpit action is an API route.

**Files.** `src/screens/battleView/useCharacterPane.js` (+`COCKPIT`, first in order, default; a shell-aware section list so the mobile mark keeps Chat · Bench · Tape), `CharacterPane.jsx` (+panel; re-derive `PANE_HEADER_FIXED_PX`/`ARCHETYPE_MIN_VIEWPORT_PX` for four tabs), new `PaneCockpit.jsx` + `useCalls.js` (the `onSnapshot` over `calls` ordered by `mintedAt`; needs-you ordering per V3 §5; tiles → sheets; `Earlier` fold), `AgentBattleScreen.jsx` (the mobile Board ⇄ Cockpit track: a bounded board column with its own scroller, `ThisTurnStrip` lifted above it, a framer-driven slide or native scroll-snap — never a CSS transition; the segmented control with a `layoutId` thumb on a token; the "Cockpit · n" count from the records), `ThisTurnStrip.jsx` (the two pills at `:49-57`), `CharacterAvatar.jsx` (the calls badge, `:287-311`), a research-sheet host (re-mount `AssetResearchModal` with a three-outcome footer — answer → Build 1's endpoint; flag for next deploy → the uncommit / PATCH / commit sequence or a new add-ticker endpoint, a founder call on mid-battle edits of the equipped list; ask in chat → `handleAskFollowUp(symbol)` verbatim), `src/theme/tokens.css` + `tokenBaseline.json` (+ seven `--ft-call-*` aliases and a glow-dot token), `tokens.guard.test.js` `GUARDED_FILES` (+ every new file), `src/theme/motion.js` LOCKED table if tuned, `battleViewCopy.js` (the honesty strings: the as-of line, "cited evidence the check didn't have", the four armed states — C3's Guarding tile is thin until the exit-dials arc).

**Size: L.** **The one thing most likely to be wrong in the contract/V4 as written:** V4 §1's "keep the Battle View as it is" on mobile. Nothing is pinned on the mobile Battle View today — the page is the scroller (`AgentBattleScreen.jsx:2095`, `:2398`) — so a pinned header plus turn row over a two-screen horizontal track is a real layout change to a shipped screen, and the same `CharacterPane` tab list serves both shells. V4 §4's palette also contradicts the shipped convention (player = teal, CPU = copper, `ArenaHeader.jsx:13-14`) and names a fuse dot the Battle View does not have.

### Sequence and the gates, restated once

Build 0 can start now (shadow, no §8 gate, no fence on route a). Build 1 waits on the deferred-beat PR and the R6 table, and on three founder rulings (collision, ack/charge, exchange id). Build 2 waits on grounding `'on'`, the fit-check flip decision and held-position parity, plus the four design rulings. The exit dials (C3) stay with the exit-dials arc; the Guarding tile ships thin and says so. C5 (Record glance) ships last (Brief R-3) and has no backed-week concept to draw on in BaggerBomb — backing is keyed on tournament groups (`api/_utils/backingWeek.js:176`, `:226`, `:249`, `:359`).

---

## Method (BUILD_RULES §3) and out-of-scope findings

`git fetch origin` was the first command of the session; `origin/main` = `2a1a16b1`; the branch `claude/phase0-cockpit` was cut from it (the harness had designated `claude/nice-lamport-totn3c`; the task named `claude/phase0-cockpit`, which was used and is reported as such — the report is pushed to both so neither pointer dangles); working tree clean at cut. The four inputs were read from the session's upload directory; nothing under `docs/inputs/` exists. Discovery ran as seven parallel read-only research passes (one per target group), each writing its own report to the session scratchpad; **every `file:line` in this document was then re-read by the coordinating session with `sed -n`/`grep -n` at HEAD** — where a pass's line was off by one or two, the corrected line is given here. Fenced files (`agentEvalPromptAssembly.js`, `agentPromptAssembly.js`, `decide.js`, `agentBattleService.js`, `agentScoring.js`) were **read, never edited**. No test was run; no build was run. `git fetch --unshallow` was **not** run (the clone is shallow — §12); `git merge-tree --write-tree` was used for two conflict probes (object writes only; no ref, index or working-tree change). Unmerged-branch claims come from `git diff origin/main...<branch>`, `git show <branch>:<path>` and `git merge-base --is-ancestor`. Production values (the twenty-pair count, census counts) are *production* and were not read. A byte-identical copy of this report exists outside the repo tree at the session scratchpad (`…/scratchpad/20260923_PHASE0_COCKPIT.md`). One file is added by this branch: this document.

**Out of scope — report, do not fix (BUILD_RULES §3):**
1. `docs/BUILD_RULES.md:82` "(19 of 21 are registered)" — 23 of 25 handlers are registered at HEAD (§6.3).
2. `api/cron/agent-daily-scores.js:4` header "ET Mon-Fri 8:45 PM" is the EST reading; the single UTC entry fires 21:45 ET during EDT and the handler has no ET-minute guard (§6.1).
3. `docs/design/COMMAND_CENTER_COCKPIT_DESIGN_BRIEF_V1.md` is the Sep 1 hub brief under the Sep 22 Brief's natural filename (§0.2).
4. `src/services/watchlistService.js:2-4`, `:20-25` — the Dashboard Watchlist widget persists to `localStorage` only and never reaches the agent (§9.5).
5. `src/screens/battleView/TapeCards.jsx:74-75` cites AgentChat's raw teal at `:130`; the literals are at `AgentChat.jsx:46`, `:58`, `:90`, `:93`.
6. `docs/CLAUDE_CODE_RESEARCH_MODE_PROMPT_V2.md:11`, `:878`, `:926` and `docs/MARKETCLASH_DASHBOARD_FINAL_SPEC.md:670` still point readers at the deprecated `docs/DESIGN_TOKENS.md`.
7. `api/agent/file-directive.js:157` cites the chip-mint resolution at `chat.js:409`; at HEAD it is `chat.js:480`.
8. `api/agent/research.js:10-24` header lists the flag gate before auth; the code (deliberately) auths first (`:212-226`).
9. `src/hooks/useAgentBattle.js:13` logs on every render; `src/screens/AgentBattleScreen.jsx:22` says the controller flag is "dark" while `featureFlags.js:2142` is `true`; `AgentBattleScreen.jsx:66-67` imports `StrategyPresetBadge` / `HypothesisTicker` with no JSX mount.
10. `api/_utils/directiveUtils.js:71-72` treats an unknown `expiry` value as **active** — a silent fail-open any new expiry vocabulary must register against.
