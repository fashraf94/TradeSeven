# Battle View controller — Phase A2 (the rulings addendum, A2.3, A2.4) adversarial build review

**Date:** September 3, 2026
**Branch:** `claude/phase-a2-tape-piece-javcyf`. **Build reviewed at:** `4b32e442`. **Fixes at:** `d4ff893d`. **Refuters at:** the fixed head.
**Threshold:** BUILD_RULES §2 — the cumulative branch diff is 52 files / 7,149 lines, well past *≥10 files OR ≥1500 lines*, so the multi-lens adversarial review is mandatory.
**Process:** the rulings addendum's §2, in force for the first time — **one tree per reviewer**, and **the rulings documents to every lens**.

---

## 1. Executive verdict

| | |
|---|---|
| Lenses | 5, each on its own `git archive` tree at the review HEAD |
| Refuters | 2, on fresh trees at the **fixed** head |
| Findings raised | 60 (40 from the lenses, 20 from the refuters) |
| CONFIRMED and fixed | 43 |
| Recorded, not fixed | 17 — see §5 |
| REFUTED | see §4 |
| Mutations executed | 94 by the test lens, ~40 across the other four, **63 by refuter A alone**, refuter B's probes, and the coordinator's own check on every fix |
| `vite build` | green (the pre-existing chunk-size warning only) |
| Suite at handoff | 584 files, 10,026 passed, 63 skipped, 0 failed |

**Five HIGH defects, three of them found independently by more than one lens.** The two that matter most to a reader of this record:

1. **A ruling was built at half its stated scope, and the repo's own tests had started to encode the contradiction.** D-83 says a check is named by its cron slot *on every surface*; the build applied it inside `deriveTurnLine` only. But the turn line's strings ARE the Desk's, shared deliberately under a D-62 comment that says the two surfaces "cannot disagree" — so one instant had begun to read `Checked 12:47 PM` in `AgentDesk.render.test.jsx` and `Checked 12:45 PM` in `deriveTurnLine.test.js`. Two lenses found it independently. The flooring moved to the shared seam, so the Desk names the slot too. (Refuter A then corrected the review's own framing of that: the Desk is NOT unflagged — `COMMAND_CENTER_SYNC_ENABLED` is false and gates the whole surface — so the change is latent until that flag flips, not live. §5 item 1.) The fix also over-reached on one argument, which refuter A caught and §8.2 records as RA-F1.
2. **Two load-bearing flag gates on SHIPPED surfaces could each be deleted with all 3,701 tests green** — the chat's ticker roster, and the shipped mount's receipts memo — and the one artefact ruling 8 names as the proof of the detector extraction contains not a single entity span. The flag-off guarantee held in the code and did not hold in the suite. It does now.

---

## 2. Method, and its two disclosures

Five lenses, each with its own dimension, each on **its own tree** — a `git archive` extraction of `4b32e442` under the session scratchpad with `node_modules` symlinked, read-only on git and on the working tree. Mutation checks therefore never ran against a shared snapshot: the A2 review's first disclosed process defect, closed by the addendum's §2 and by this run.

Every lens received **all three rulings documents** (the seed, the rulings V1, and the addendum V1.1), not the Phase 0 report: the A2 review's second disclosed process defect, likewise closed.

The dimensions: **L1** ruling fidelity and copy · **L2** wiring and lifecycle · **L3** the dark-merge / flag-off guarantee · **L4** test integrity (mutation) · **L5** cross-phase consistency, hazards and §9.

Then two refuters on fresh trees at the **fixed** head — one pointed at the fixes, one at the whole branch with fresh eyes. The A2 review's worst finding came from a refuter pointed at its fixes, which is why that shape is repeated here.

**One disclosure of this run's own:** the lens trees were cut before D-81 (the eight trigger strings) was committed, so L1 and L4 both reported it missing. It was built; the finding is refuted by timeline in §4, and the refuters were given the fixed head so nothing else falls in that gap.

---

## 3. CONFIRMED findings and their dispositions

### 3.1 HIGH

| # | Finding | Disposition |
|---|---|---|
| **L1-F2 / L5-F1** | **D-83 on one surface only.** `deriveTurnLine` floored its posture-string arguments; `AgentDesk.jsx:89-92` passes the raw instants into the same shared strings. One check, two names, up to 14 min 59 s apart — against `deskCopy.js`'s own in-file D-62 guarantee and §9. | **FIXED at the shared seam.** `etSlotTime` beside `etTime` in `deskCopy.js`; all four posture composers floor their check arguments; `slotLabel` (the ruling's name) delegates. Both surfaces agree by construction. The Desk's own expectations flipped in the same commit. |
| **L1-F3 / L2-F3 / L5-F5** | **`In the chat · 0` scoped to nothing.** The seed: zero "opens the unscoped tape at the piece's composer prefill". The build filtered to `[]`, and the chat fell through to its `EmptyState` — the fresh-battle onboarding copy — on a battle with a conversation. Two of the build's own comments asserted the ruled behaviour while the code did the reverse. | **FIXED.** `handleScopeToPiece(symbol, count)` scopes only when `count > 0`; it always prefills and always opens the chat. The mounted row now asserts no chip, a tape still present, and no `Your agent is ready`. |
| **L2-F1 / L5-F7** | **The desktop collapse remounted `AgentChat`.** `{chat}` was rendered in two tree positions, so React unmounted it on every collapse and every expand — taking the typed draft, the optimistic bubbles of a send in flight, the error banner, and the scope's own scroll memory. A4 paid for the draft-survival rule explicitly (F13). | **FIXED.** The column is the chat's one home: collapsed and open are the same element with different chrome and a different flex basis, and the layout row wraps so the board still takes the full width with the strip beneath it. A row asserts the same DOM node and the same draft across both transitions. |
| **L3-F1 / L4-F1** | **The `knownTickers` flag gate was unguarded.** Deleting it left all 3,701 tests green while widening what the SHIPPED chat underlines from the book to the whole battle universe — and an underline is a tappable span that opens a research modal (hazard 27). The goldens are structurally blind: the screen golden is captured on the matchups tab, the chat golden hands `AgentChat` a hardcoded roster. | **FIXED (guard added).** The flag-off composition test's fixture gained all three bench lists and a message that names them; the row asserts the book underlines and the bench does not. Mutation-checked: removing the gate reds it. |
| **L3-F2** | **The detector extraction's stated proof was void.** Ruling 8 and the module both cite the chat golden; its fixture passes no `onSymbolClick`, so the function returns two lines in and neither golden contains one entity span. `TICKER_ACCENT` could be changed to `#ff0000` with the suite green, and the module had no test at all while three Film Room surfaces render through it. | **FIXED (guard added, claim corrected).** A full render test pins the spans, the labels, the accents as literals, the keys, the interleaved prose, both click payloads, the four early returns and every shipped caveat. The two "the golden proves it" comments now name the tests that do. |

### 3.2 MED

| # | Finding | Disposition |
|---|---|---|
| **L2-F2** | On a collapsed desktop the scope door applied the filter but never opened the chat — the gate was `!isDesktop`, an A4-era assumption that A2.4 broke. The comment three lines above states the rule it violated. | **FIXED** — the gate is `!chatOpen`. |
| **L2-F4** | Both desktop controls dropped focus to `document.body`; each lives inside the chrome the other replaces. The mobile sheet has had a return-focus contract for this transition since A4 (CR4). | **FIXED** — focus moves to the control that replaces the one that vanished, with a row for both directions. |
| **L2-F5** | The scroll-restore effect listed `tapeEntries`, which is a fresh array on every Firestore snapshot, so it wrote `scrollTop` on the coarse clock's minute tick and on every price poll — cancelling the smooth auto-scroll two effects above it. | **FIXED** — the dep is the scope alone, and the effect early-returns unless the scope actually transitioned. Guarded on the flag path, where it can fail. |
| **L2-F6** | A session that mounted below 768 px and was then widened arrived on a desktop with **no chat column** — `initialDetent` is read once, and the phone's untouched peek carried over. | **FIXED, asymmetrically and deliberately.** An untouched shell adopts its own default only when that default is OPEN. Ruling 7's two stated cases are both desktop → mobile and are untouched; the reverse needed a rule because the shells' closed states are not the same thing — peek is a usable chat on a phone and an absent one on a desktop. Once the player has moved the sheet the detent is theirs and survives both ways. |
| **L2-F7** | `scopeSymbol` was set in five places and cleared in one; it outlived the piece leaving the board and the battle completing. | **FIXED for the case that matters** — the scope clears when its symbol leaves the roster. The rest is recorded (§5). |
| **L1-F4 / L5-F3** | The peek line stamped a folded run with the run's **first** member — the sort position, not the newest thing that happened — so the strip's own two lines disagreed: `Checked 1:00 PM` above `12:30 PM · 3 checks · no change`. | **FIXED** — the run's line carries no time at all, exactly as the card it stands for does. |
| **L3-F3** | The new layout effect's flag gate was unguarded, and the row whose title promised the guard asserted nothing about scroll. | **FIXED — by moving the guard where it can fail** and saying so. Flag-off the effect's only dep is permanently null, so the claim is unfalsifiable there; the falsifiable half (a re-render with an unchanged scope writes nothing) is now a row on the flag path. A comment in the flag-off file records why there is no row there rather than leaving a row that cannot fail. |
| **L3-F4** | The screen → chat seam was unguarded for `controllerCopy` and for the `receipts` memo: adding the first, or removing the second's flag conjunct, changed what a real flag-off user reads with the whole suite green. | **FIXED (guards added).** Both in the composition test the goldens cannot be; the copy one drives a real failed send, because the string exists nowhere else. |
| **L5-F6** | Two price formatters. `formatPrice` is `$${n.toFixed(2)}` — no thousands separator — so a row read `$1234.50` beside the panel's `Entry $1,234.50` two lines below. | **FIXED** — one formatter, `BATTLE_VIEW_COPY.price`, with a row at four figures. |
| **L4-F2** | The strip's peek line was only ever asserted `toBeTruthy()` / `length > 0` / `toContain(' · ')`; replacing it with a plausible constant survived both suites. | **FIXED** — the row recomputes `derivePeekLine` from the fixture's own document and then moves the tape to prove the strip follows. |
| **L4-F3, F4, F5, F6, F7, F8, F9** | Seven assertions that could not fail: a §9 row whose two sides were byte-identical (`dailyLevels: undefined` makes `computeDollarInfo` return null), a byte-identity row comparing one code path with itself, a truncation row calling the wrong entry point, a `lastIndex` row guarding a defect the loop shape prevents, a `Set(...).size` row about the test's own array, a `?? {}` row that was unconditionally truthy, and a `>= 0` row. | **ALL SEVEN REWRITTEN** to assert the thing they name. One more of the same class was found while fixing them — an accent assertion built from the imported constant, which mutates with its source — and rewritten to the literal. |

### 3.3 LOW — fixed

- **L2-F10** the % block remounted (and replayed its slide, and broke its `aria-describedby` target) the moment a live price first arrived, because the wrapper appeared with the price. The wrapper now renders on the whole flag path.
- **L2-F11** `knownTickers` took `agentBattle` as a dependency, so the Set — and the chat's whole timeline behind it — rebuilt on every snapshot **on the shipped path**. Narrowed to the three subtrees the roster reads.
- **L2-F12** `atOf` called `toISOString` on anything `instanceof Date`; an Invalid Date is one, and throws. Guarded.
- **L4-F10** a `typeof` conjunct fully subsumed by `Number.isFinite` — a conjunct that cannot fail. Removed.
- **L1-F5 / L5-F10** the flag-on roster read `agentBattle.portfolio` while the board renders from a prop fallback, so a document without one would have shown seven pieces the detector had never heard of. The roster now starts from the same enriched portfolio the rows render.
- **L5-F8** the A2.1b revert instruction under-specified: D-83's `slotLabel` import lands inside the same conflicted hunk and must also be kept. Corrected in the comment and in the handover.

### 3.4 Claims corrected rather than left standing

Three comments in this session's own code over-claimed, and a review that only fixed code would have left them:

- `scopeTape.js`'s header said the count and the filtered list "cannot disagree". They differ by exactly one input — the chat's optimistic in-flight bubble — and the header now says which, and why that is the honest direction (**L1-F6 / L5-F4**).
- `renderMessageWithEntities.jsx` and `deriveChatMessages.js` each said the chat golden proved the lift. Neither was true; both now name the test that is.

---

## 4. REFUTED

| # | Finding | Why it does not stand |
|---|---|---|
| **L1-F1 / L4-F11** | "D-81, the nine trigger strings, was silently not built." | **Refuted by timeline.** The lens trees were cut at `4b32e442`; D-81 was committed at `f00b8835`, after. All nine strings ship, with the addendum's `near` edit, a row asserting each by name, and a mutation row that an unknown type stays silent. L4 flagged the ambiguity honestly (the addendum's §4 Go list does not name ruling 2) — the coordinator's call was that "Accepted" is a build instruction where rulings 3, 5, 6 and 7 carry explicit no-build dispositions, and that D-81 is one of the ledger rows §3 requires this session to leave behind. |
| **L2 — `readFullCheckTick`'s `reducedMotion` dep steals focus** | Refuted by L2 itself with a repro: framer's `useReducedMotion` reads the preference into `useState` once and never re-subscribes, so the dep cannot change during a mount. Focus stayed on the composer across a live media-query flip. |
| **L2 — `slotLabel` / `buildTape` throw on malformed timestamps** | Refuted by L2 itself: `slotLabel` returns null for every malformed shape tried, including Firestore-Timestamp-alikes; the one throwing input is `±Infinity`, which is not a reachable document shape. |
| **L2 — two `AgentChat` instances, or a desktop with neither chat nor strip** | Refuted: the branches are exhaustive and `qa('textarea').length === 1` holds at mount and on both sides of a crossing in both directions. |
| **L3-F5, L3-F6** | The new `onScroll` listener and the hook's extra mount render are real, and neither changes output. Recorded as observations by L3 itself, not defects. |
| **L4 — four "surviving" mutations** | Provably equivalent mutants: a `typeof` subsumed by `Number.isFinite`; a case-insensitive character class whose case-sensitivity is enforced by the `Set` lookup; a shared regex whose `lastIndex` the loop always resets; and a `peekLine` computed without its flag gate, which is a dead value flag-off. One of the four (the `typeof`) was removed anyway, because a conjunct that cannot fail is the shape §2 names. |

---

## 5. Recorded, not fixed — for the founder

1. ~~**The Desk now names the slot, unflagged.**~~ **CORRECTED by refuter A — it is NOT unflagged, and nothing here was user-visible.** `featureFlags.js:1887` has `COMMAND_CENTER_SYNC_ENABLED = false`, and `CommandDashboard.jsx:546` renders `<AgentDesk>` only when `sync` is non-null, which the dark flag prevents. Both surfaces that consume the D-83 flooring are dark, so this is latent, not live: there is no second unflagged change in A2 beside D-82's. What remains true and worth the founder's eye is that the change is real and lands the moment `COMMAND_CENTER_SYNC_ENABLED` flips — a dashboard reader will then see `Checked 12:45 PM` where the old code said `Checked 12:47 PM`, which is more accurate (it names the check the cron actually ran) but is a change. Handover §4 item 54, corrected there too.
2. **`Read the full check` scrolls against the panel's collapsed box** (L2-F8). The book panel mounts at `height: 0` and animates open, so `block: 'nearest'` is decided on a zero-height insertion point. Left as built after reasoning it through: that point IS the panel's top, which is where the reader must land, and an off-screen one still scrolls. What `nearest` never promised is the panel's *bottom*, and on a phone a long check can still open past the fold. A second scroll after the animation would risk a jump under the reader; recorded rather than guessed at.
3. **On mobile at the FULL detent, `Read the full check` moves focus to a heading underneath the sheet** (L2-F8, second half). Ruling 4 says to keep the sheet's focus rules intact, and lowering the detent from the door would break them. A ruling, not a review call.
4. **The mobile peek sheet grew one or two rows while `SHEET_PEEK_PX` and the board's bottom reserve did not** (L2-F9). `SHEET_PEEK_PX = 172` is a measured sum from the A4 review; peek is `height: auto` so the sheet really does grow, and the board's `SHEET_PEEK_PX + 32` clearance did not. jsdom does no layout, so the occlusion is unmeasurable here — the constant, the two new rows and the unchanged reserve are all that can be stated. **Wants a look in the founder's smoke.**
5. **The scope survives the Why? panel closing and the battle completing** (L2-F7, partly). Only the piece-left-the-roster case is fixed. Clearing on close would make the door useless — the player scopes, then closes the panel to read.
6. **A scoped stream can hide a new message while the unread dot lights** (L2-F7c). Inherent to any filter; recorded so it is a decision.
7. **`This piece today` and `In the chat · n` are not day-scoped** (L1 obs 1). The seed says n counts *today's* entries; the rulings restate the rule by kind and drop the word. Unreachable at HEAD (`AGENT_BATTLE_DURATION_MODE = 'fullday'`), and both over-claim on a legacy multi-day battle.
8. **`{n} checks · no change` can fold across a trading-day boundary** (L1 obs 3) — D-77 has no day conjunct. Same reachability as 7.
9. **"nothing was sent" is unprovable on the thrown-fetch branch** (L1 obs 4). A request that reached the server and wrote before the connection dropped charges the budget while the banner promises it did not. The ruling scopes the string to *a server error*; the shipped code had one string for both branches, so the build is faithful to the ruling's words.
10. **The peek line can render a fabricated time** (L5-F11). `deriveChatMessages` falls back to `Date.now()` for an exchange with no parseable stamp — lifted unchanged, but the peek line is a new consumer, and `deskCopy`'s rule is "never invent a time".

*The six below came from the refuters, at the fixed head.*

11. **Scoping to a piece makes the tape SEVEN TIMES LONGER, and the door's number is not a count of messages** (RB-F3). The count is over the unfolded stream and the scoped view deliberately does not fold — a run built for one adjacency is meaningless in another — while the unscoped view does. On an ordinary quiet day (26 quiet HOLD checks whose first sentence names NVDA, plus one exchange about it) the door reads `In the chat · 28`, the unscoped stream is **4 lines**, and the scoped stream is **28 near-identical engine records**. This is the D-73 × D-77 × D-84 product: each ruling is individually right and their interaction is not. Not fixed because every repair is a ruling — fold the scoped stream too (and lose the "different adjacency" argument), count only messages (and break §9's one-derivation rule between the number and the list it opens), or say something other than `In the chat · n`. **The founder's call, and the sharpest single item in this record.**
12. **The unread dot's source is the feed, and the tape is what the chat shows** (RB-F6). Under the flag `statusFeed` no longer feeds the stream; ruling 9 lists six actions the tape renders as nothing. A re-sourcing was written — the dot counting the tape — and then **reverted**, for three reasons: the dot's source is A4's and unchanged since A4, so the defect is inherited rather than introduced by A2.4; eight committed rows encode the current semantics, one of them written by a *previous* refuter's finding on the server's feed cap; and the honest fix changes first-paint behaviour that no ruling asked about. The recommended shape, if the founder wants it: keep the two-part `{length, stamp}` mark but source both from the **tape**, because a fold makes the tape's length shrink while activity arrives — the same hazard the stamp was invented for, and more acute for the tape than for the feed. BUILD_RULES §3: report, do not fix.
13. **A failed send DESTROYS what the player typed.** The composer is cleared at the top of `sendMessage` (`AgentChat.jsx:741`) and neither the `!res.ok` branch (`:764-784`) nor the `catch` (`:796-800`) puts it back, on either failure path. So item 11's new sentence is true — nothing was sent — and the words are gone anyway, with retyping the only way to retry. Found while rewriting RB-F5's vacuous row; now pinned as shipped behaviour on both paths, so a founder ruling has somewhere to land. Not fixed: the send path is shipped code this phase does not touch, and restoring the draft changes the flag-off path too.
14. **`nothing was sent` is a promise the server does not keep, and the comment justifying it is wrong** (RB-F4 — this SUPERSEDES item 9, which understated it). The copy module's header says "a failed request produces no write, which is why the sentence can promise it". In `api/agent/chat.js` the durable write **and the budget increment** are at `:617-623`, **inside** the `try` whose `catch` at `:680` returns 500 at `:718` — so anything throwing after `:623` (the `agents/{agentId}.update()` at `:653`, say) returns 500 with the exchange appended and `chatBudgetUsed` already incremented. The file flags the asymmetry itself: the League charge at `:670` carries the comment *"this line is past every 502/504/500 return"*; the battle budget at `:623` does not. Reproduced on the real send path: with a 500 and then the prop the listener would deliver, the screen shows **`1/10` and "nothing was sent" at the same time**. Item 11's own justification is "a message the player believes was spent is a message they will not send again"; this is its mirror — spent, believed free, re-sent, charged twice out of ten. **`api/` is behind the §1 fence: this needs its own tasking**, and the fix is on the server (move the write past the last throw, or narrow the client's sentence to the thrown-fetch branch).
15. **Four of the nine trigger strings read as a claim about the tapped piece** (RB-F7). Every conditional trigger in `agentTriggerGate.js` is per-*symbol* (`price_drop` at `:41-48`, `vwap_deviation` at `:120-130`, `bandwidth_squeeze`, `nr7_contraction`), but `agent-evaluate.js:2651` persists `triggers.map(t => t.type)` and drops `detail` — the only place the symbol lived. `wokenBy` then renders directly under **this piece's** label. Two of the nine strings were written with the hazard in mind (`threshold_proximity` → "Woken by **a piece** near a scoring tier"; `news_catalyst` → "a news story on **a piece**"); four were not. Reproduced: a piece at +0.00 % renders `Woken by a price drop` from another holding's trigger. Not fixed because ruling 2 gave these eight strings **verbatim** and rewriting them overrides the ruling. The cheap repair is the wording the two careful strings already use.
16. **A shipped hot component picked up the adapter's import graph on the flag-off path** (RB-F13). `TacticalRow` → `battleViewCopy` → `deriveTurnLine` → `baggerbombAdapter` → `commandCenterLiveBattles` → `arenaStateMap` → `leagueTournament`, for one formatter, on every row of the shipped Matchups tab with `showCurrentPrice` false. Behaviourally inert and the build is green; noted because D-85's justification for the import ("the module is pure") is about `battleViewCopy`, not about what it drags behind it.
17. **The collapsed desktop's SCROLLING model wants a browser** (RA-F7, after the fix). The collapse is now a `flexDirection: 'column'` rather than a wrapped row, which by construction gives the board a definite bounded height so its own scroller bounds and the strip stays pinned at the bottom of the viewport. jsdom does no layout, so what the rows hold is the style contract — the direction and both children's `flex` — not the pixels. **In the smoke: collapse the chat on a desktop with a board taller than the window, and check that the BOARD scrolls rather than the page, and that the strip and its unread dot stay on screen.** The same look also settles §5 item 4 (the mobile peek sheet's growth against `SHEET_PEEK_PX`).
---

## 6. Outside this task — carried forward, not fixed (BUILD_RULES §3)

The four in the A2 handover §6 are unchanged. **Two new ones.** The first is §5 item 14 — `api/agent/chat.js` charges the chat budget inside the `try` whose `catch` returns 500, so `nothing was sent` can be false; it is behind the §1 fence and wants its own tasking. The second was found by L1 and is worth its own too: `GameTapeView.jsx` renders `citedForgeRules || citedRules` — raw `_`-joined machinery codes (`swap_window_cap`, `vwap_cascade_guard`, `risk_*`) — and it also renders `entry.action` (`GUARDRAIL_FORCED_SWAP`) as an eyebrow. It is a shipped surface, unchanged by this diff, and it is one header tap away under the controller flag. It is the same class D-80 just closed on the tape.

---

## 7. Disclosure

The adversarial pass ran in full and to the addendum's §2: five isolated lenses on five separate trees, all three rulings documents to every one, two refuters on fresh trees at the fixed head, 130-odd executed mutations, an explicit `vite build`, and this record. One process defect of this run's own is disclosed in §2 — the lens trees predated the D-81 commit, which produced two findings that are refuted by timeline rather than on their merits.

*Prepared September 3, 2026. Build at `4b32e442`; the lenses' fixes at `d4ff893d`; refuter B's at `79f2f802`; refuter A's in the commit that carries this section. Suite 10,026 green, `vite build` green, and the branch STOPS here for the founder's smoke — no PR.*

---

## 8. The refuters' verdicts, and the fixes they produced

Two refuters were sent to fresh trees at the fixed head (`d4ff893d`), one pointed at the fixes and one at the whole branch with fresh eyes — the shape that produced the A2 review's worst finding, repeated deliberately.

### 8.1 Refuter B — nine findings, seven fixed at `79f2f802`

Refuter B's baseline before its probes: 42 files / 1,079 tests green in its own tree, `vite build` green; every probe file deleted and no repo file edited. Its findings, and what each one produced:

| # | Sev | Finding | Disposition |
|---|---|---|---|
| **RB-F2** | HIGH | **`Guardrail override` printed twice, on 100 % of forced exits.** The cron composes `Guardrail override (${sourceNote}): ${overrideNote}` (`agent-evaluate.js:2121`) where `overrideNote` **is** the guardrail module's own `statusMessage` — which already begins `Guardrail override: ` and already carries the plain words D-80 maps to (`agentGuardrails.js:530-551`). Ruling 1's worked example elided the body as `…`, which is exactly where the stutter lived. D-84's first-sentence collapse then buried `Forcing exit → MOS.` behind `Read more` while surfacing the redundant preamble — on the card, the Why? row and the book panel. | **FIXED.** `renderMotive` drops the doubled prefix before translating. The card now reads `Guardrail override: stop-loss at 8% breached on GILD (-9.24%). Forcing exit → MOS.` Mutation-checked on the exact composed string for all three ruled tokens. |
| **RB-F1** | HIGH | **A swap printed the agent's sentence twice, back to back.** `evaluations[].rationale` and `trades[].rationale` are the SAME string by construction — both are `haikuResult.rationale` (`agent-evaluate.js:2243` / `:2637`). D-72 builds a card from each; D-84 had just made the two the same visual kind. The pair was printed twice as well. Reachable on every model swap, which is the tape's headline moment; the A2.2 smoke produced items 8–11 and none was about a swap, so the state was plausibly never on screen. | **FIXED.** `buildCheckEntries` now knows which evaluations produced a trade and withholds the check card's copy of the prose; the check keeps its own label, its pair and its `Woken by`. |
| **RB-F9** | MED | **A check card that VISIBLY says GILD was dropped from GILD's scope.** `checkNamesSymbol` read `firstSentence` only, but a SWAP check's heading is `Swapped · GILD → MOS`, composed by `checkCardLabel`. The module header justified the excerpt rule as "the excerpt is what the card shows" — the excerpt is not *all* the card shows, and the label is the half a player reads first. | **FIXED.** The rule now also reads the pair the label is composed from, so the filter and the card agree about what a card names. |
| **RB-F8** | MED | **The scoped stream leaked content that names no piece.** The unanswered proposals and the `Grade Today's Trades` block are attached by INDEX, after `scopeTape` has run, and neither is a tape item the filter could judge — the grading block lists **every** trade in the battle. `NVDA · All` showed a `GILD → MOS` grading card one line under a filter that had just dropped GILD's own card. Reachable on any battle past its first auto-debrief, which is every battle in review. | **FIXED.** Both injections are suppressed while scoped, via the `-1` anchor the shipped code already takes when a document has no auto-debrief. Mutation-checked. |
| **RB-F5** | MED | **A guard that could not fail.** The `nothing was sent` row asserted that a FIXED prop had not moved; the identical assertion passes on three *successful* sends, and no mutation of the component can move a prop. It was the only executable assertion behind the claim. | **REWRITTEN** to what the client can actually be held to: one request per attempt (no retry that could charge twice) and a counter proved prop-driven by **moving** the prop. The row also now pins a shipped fact it uncovered — see §5 item 13. |
| **RB-F10** | LOW | **Neither end of the scope was named or announced.** The door's accessible name was its visible text `In the chat · 2`, the chip's `NVDA · All` — a count and a label, neither of which says a filter is about to be applied or cleared. Zero `aria-live` and zero `role="status"` nodes, while the door deliberately throws focus to the composer, past the stream it just changed. | **FIXED.** `scopeDoorName` / `scopeChipName` name both ends for their action, in the copy module beside `whyName` and `sheetHandleName`. A polite live region says what happened, keyed on the scope's **transition** — never on the stream's length, which moves on every snapshot. Both mutation-checked, including the transition guard. |
| **RB-F11** | LOW | **Two `aria-expanded` controls naming nothing, and a "heading" that was not one.** The desktop expand and collapse controls live in each other's chrome and neither carried `aria-controls`. And ruling 4 says `Read the full check` "moves focus to its first heading" — the target was a styled `<div tabIndex={-1}>` with no `role="heading"` and no `<h*>` anywhere in the panel, so heading navigation never found it. | **FIXED.** One `CHAT_COLUMN_ID`, referenced by both controls, so neither reference can dangle. The focus target is `role="heading" aria-level={3}` — no pixel changes, and the ruling's sentence becomes true. Both mutation-checked. |
| **RB-F12** | LOW | **The collapsed desktop row is a two-line wrapped flex container with default `align-content`.** Both children are `flex: 0 0 100%`, so the leftover cross space would be split between the board's line and the strip's — the strip growing by half the free height instead of hugging the board. Browser-unverified; jsdom does no layout. | **FIXED** with the one property (`alignContent: 'flex-start'`). Flagged for the founder's smoke, since only a browser can confirm it. |
| **RB-F6** | MED | **The unread dot fires for feed actions the tape renders as nothing.** Under the flag `statusFeed` no longer feeds the stream at all, but `hasNewFeedEntries` is still keyed on it; ruling 9 lists six actions the tape shows as nothing, `guardrail_forced_swap` most sharply (hazard 25 gives a non-executed forced swap no card). The player expands, finds nothing, and the dot clears. A2.4 put that dot on a **new** surface without reconciling its source. | **RECORDED, NOT FIXED — and a re-sourcing was written and then REVERTED.** See §5 item 12 for the reasoning and the recommended shape. |

Refuter B also verified, and found sound: D-83 end-to-end across the header, the card and the peek line (including that ET's offset is a whole number of hours in both halves, so `*/15` slots land on `:00/:15/:30/:45`); all nine D-81 types against the gate that persists them; `isEngineAuthoredMotive` across all five writer paths; `renderMotive`'s anchor against the guardrail's own numbers and `(R11)`; `selectSymbolRoster` against every writer's persisted shape, including that crypto symbols are bare tickers the `[A-Z]{1,5}` detector reaches; substring safety (`ON` in `MONSANTO`, `MOS` → `OS`); the degenerate documents (empty, completed, 150 evaluations in 19 ms, whitespace rationale, an outage entry); guard registration for all four new `battleView` files on both lists and both baselines; no import cycle; the detector lift's two subtle early-return semantics; and that the `chatSendFailed` banner survives the desktop collapse.


### 8.2 Refuter A — the fixes attacked, 63 mutations, eleven findings

Refuter A was pointed at the five lenses' fixes rather than at the branch. It executed **63 valid mutations** against a verified-green baseline and re-ran every ambiguous one from clean. Fourteen survived; four are provably equivalent or benign, ten produced the findings below. **Nine are fixed here (`e4d6ca…`, this commit); one is recorded.**

**Its first act was to correct the review record**, and the correction matters: §5 item 1 called the Desk change "unflagged". It is not. `featureFlags.js:1887` has `COMMAND_CENTER_SYNC_ENABLED = false`, and `CommandDashboard.jsx:546` renders `<AgentDesk>` only when `sync` is non-null, which the dark flag prevents. **Both surfaces that consume the D-83 flooring are dark**, so nothing in this phase was user-visible before the flip. §5 item 1 is corrected accordingly, and the handover's §4 item 54 with it.

| # | Sev | Finding | Disposition |
|---|---|---|---|
| **RA-F1** | MED | **The D-83 fix over-reached, and made the turn line name a PAST time as the next check.** Flooring `postureLive`'s SECOND argument was wrong in a way the first was not. `deriveNextDecisionAt` withheld `next ~` only once the raw candidate (`last + 15 min`) had passed, but the label now shows its SLOT, which arrives δ minutes earlier — where δ is how late the last check landed. So `next ~12:45 PM` stayed on screen at 12:47. δ of every fifteen minutes; the repo's own LIVE fixture is an instance (`Checked 12:45 PM · next ~1:00 PM` asserted at exactly 1:00:00, stale from 1:00:01). It contradicts `deskCopy`'s own rule 3/4 — "the `~` … NEVER A FABRICATED TIME". | **FIXED at the gate, not at the label.** `deriveNextDecisionAt` now withholds once the SLOT the label will show has strictly gone by, and still returns the exact instant — the LABEL/INSTANT split D-83's second half is built on. Strictly, not "at or past": a slot is a bucket and the cron fires at its start, so `next ~1:00 PM` is true AT 1:00 and false from 1:00:01. Every committed expectation stands unchanged; a new row walks the boundary second by second. |
| **RA-F2** | MED | **The L2-F2 fix had no guard at all.** Deleting `if (!chatOpen) sheet.open(invoker)` — the whole third clause of that fix — left 1,435 rows green. The only rows that exercise the scope door run with the chat already open, so none could see it. | **FIXED (guard added).** One mounted row collapses a desktop and walks both doors. |
| **RA-F3** | MED | **The same fix was applied to one of the two Why? doors.** `handleAskFollowUp` still gated on the BREAKPOINT (`!isDesktop`) and its comment still asserted the premise A2.4 falsified — "the chat column is always on screen". On a collapsed desktop the player landed in a prefilled composer with the conversation folded behind `display: none`, through a door the seed calls "a door into the conversation". | **FIXED.** Both doors ask the detent. The guard above covers both, and mutating either back reds it. |
| **RA-F4** | MED | **Fix 6's POSITIVE behaviour was unguarded** — the whole adopt effect could be deleted with a green suite. The two rows that constrain it (L2-F6's own) only check that it does NOT fire; ruling 7's two crossings are guarded and are not what the fix is for. | **FIXED (two rows).** An untouched phone widened to a desktop arrives with a column; a phone whose detent the player has moved keeps it. The second is what makes the first a guard rather than a statement that desktops open. |
| **RA-F5** | MED | **(i) A legitimate scroll restore was missed, and (ii) neither half of the L2-F5 fix was individually load-bearing.** The transition ref held a BOOLEAN, so a scope→scope switch — open one row's Why?, tap its door, open another's, tap its — read as "no change" and wrote nothing, leaving the reader at the first piece's offset in the second piece's stream. And M26/M27 each passed alone: the dep list and the early return are redundant given each other. | **BOTH FIXED, and the record corrected.** The ref holds the SYMBOL. On (ii): the two halves genuinely are redundant, so the review record's claim that the dep list is the fix was wrong — the early return's unique contribution is that the effect writes nothing on MOUNT, which is now the row, spied at the prototype so the mount itself is observable. The dep list is kept for cost, and the comment says so. |
| **RA-F6** | LOW | The L2-F7 scope auto-clear was unguarded (M59 survived). | **FIXED (guard added).** A mounted row swaps SLB out of the book and asserts the chip goes. |
| **RA-F8** | LOW | **`checkCardLabel`'s absence branch was the one D-83 label with no guard** (M56 survived): the outage card would have reverted to the exact minute while every other check named its slot. | **FIXED (guard added).** One row asserts all four check labels AND both branches of the composed card at an off-slot instant, and that none of the six says 12:47. |
| **RA-F11** | LOW | The L2-F12 Invalid-Date guard was untested (M57 survived). | **FIXED (guard added).** |
| **RA-F9** | LOW | **Fix 3 left dead scaffolding.** `PeekStrip`'s `children` branch, its `data-peek-content` wrapper and its `reducedMotion` pass-through all became dead when the chat became the strip's SIBLING, and no caller supplies children. The module's docstring still described the strip as the chat's parent, sitting in the board column — both false. | **FIXED.** Both props and the branch are gone, and the header now says what the strip is and why the chat is beside it rather than inside it. A dead second way to build the shape the layout must never build is how the remount comes back. |
| **RA-F10** | LOW | **`at: run[0].at` on a `CHECK_RUN` was written and never read** after fix 7 — and it is the exact field whose reading produced L1-F4. | **FIXED.** The field is gone; `timestamp` (the sort position) stays, and two rows now assert a run carries no `at` at all. |
| **RA-F7** | MED | **The wrapped collapsed row does not produce the ruled picture on a real board.** Per CSS Flexbox §9.4 a MULTI-LINE container sizes each line to its content, so the board's line grows to the board's full height, the board's own `overflow-y: auto` scroller inside it never bounds, and the PAGE scrolls instead — carrying the strip, and the unread dot the seed puts on it, below the fold. RB-F12's `alignContent: 'flex-start'` fixed only the opposite case (a board SHORTER than the viewport). Reasoned from the spec plus a read of every `overflow` on the ancestor chain; jsdom does no layout, so it is not executable here. | **FIXED, by construction rather than by property.** Collapsed, the row is now `flexDirection: 'column'` — single-line, so the board takes `1 1 0%` of a definite height with `minHeight: 0` and its scroller bounds, while the strip takes `0 0 auto` and stays pinned at the bottom. The chat still has ONE tree position, so fix 3 is untouched. **Still wants the founder's smoke** (§5 item 17): only a browser can confirm the pixels. Its secondary half is fixed too — the collapsed column's own style was entirely unasserted (M17, M61 both survived) and the whole contract is now a row. |

Refuter A also **re-verified every lens fix it did not fault**, and the strongest of those confirmations are worth naming: the D-83 seam kills on all nine of its mutations including `SLOT_MS` and the floor direction, and the DST sweep is sound (both 2026 switch days are Sundays, so no checks exist on them); the desktop chat's single tree position is proved by DOM node IDENTITY, not by presence; `In the chat · 0`'s two counter-attacks both fail (the handler closes over the committed render's count, and the chat's list is a superset of the screen's, so `n > 0` can never open an empty stream); the void-golden finding is verifiable by `grep -c` — zero `Open research for` spans in either golden; and the flag-off gates all kill when leaked.

**Its own process disclosure, which changed its results:** two files it mutated were missing from its restore set, so its first readings of mutations 46–61 ran over a contaminated baseline. It detected the contamination, restored from git, re-established a fully green baseline and re-ran every ambiguous mutation from clean. **Four results flipped**, and those flips are exactly what produced RA-F6, RA-F8, RA-F9 and RA-F11 — findings the contaminated run would have missed. This is the second appearance in two reviews of the hazard the addendum's §2 exists to close, and the first where a reviewer caught it in itself.
