# Exit-Behavior Rebalance — Ask 2 (Rescoped) Build + §2 Adversarial Review Record

**The precedence made honest; the absolutist MUSTs qualified — built dark, dual-adversarially reviewed, `/code-review` at high effort, fix-forwarded.**
Branch `claude/ask2-kickoff-v1-rescoped-qo34tm` off `origin/main` @ `de4113fd`. Commits: `152217ea` (the build + the `/code-review` and Reviewer-A fixes), plus the review-B/flip-proofing commit carrying this record. Authoritative set (precedence, later wins): the Ask 2 (Rescoped) Build Kickoff V1 (2026-09-01) → the five founder decisions of 2026-09-02 (§2 — transcribed verbatim, the Addendum V1.1 relay precedent: they arrived in-session) → Rulings V1 / Addendum V1.1 → the Ask 1 build record. Reads with `20260902_EXIT_BEHAVIOR_ASK2_PHASE0_ANCHOR_CONFIRM.md` (this arc's Phase 0, same directory).

**Fence statement:** the only fenced files edited are the two the kickoff sanctioned — `api/_utils/agentEvalPromptAssembly.js` (the ruled MUSTs, rendered from per-MUST helpers) and `api/_utils/evalIdentityBlocks.js` (the clause only). Fenced exports *called*, never edited: `calculateAssetScoreServer` (the shared Ask 1 fixture), `sanitizeRuleText` (pre-existing import). `agentArchetypeConfig.js` untouched (R5); `agentGuardrails.js` / `agentRiskManager.js` / `agentSwapExecution.js` / `decide.js` untouched; Gate-7 locked call forms NO-EDIT; no voice-layer file (`voiceLayerPrompt.js`, `archetypeAdjustments.js`) touched. No new `./` import entered a fenced assembler (the C-20 honesty tripwire surface is unchanged; the `src/config` flag import line was extended). §2.3 import-boundary ratchet: `declaredRuleConflicts.js` reads trait definitions through the registry adapter `getTraitById`, never a direct legacy-table import (CR-1 / B-F1 — the build's first cut tripped the ratchet and was fixed before push).

---

## 1. Executive verdict

| Surface | Status |
|---|---|
| DECISION FRAMEWORK MUSTs (both variants) | **BUILT dark** — seven ruled MUSTs qualified from ONE helper each into BOTH prompt variants (`agentEvalPromptAssembly.js:199-338`); the platform floors absolute; **§8 CONVICTION and the THRESHOLD PROXIMITY bonus bullet kept absolute as engine floors** — a correction to Phase 0 and to decision 5 as briefed (§4) |
| R8 yield clause (every identity block) | **BUILT dark** — the founder-approved clause byte-locked (`evalIdentityBlocks.js:73-74`); flag-off constant, six renders, doc-parity lock and size cap untouched |
| SX-04 × mb-08 declaration (equip-time compiler) | **BUILT doubly dark** — pure detector (`declaredRuleConflicts.js`); `compileBuild` stays pure (opt-in input, `:167`); call-time read inside the enabled path (`compileOnSettingsChange.js:352`); trait hosts by trait definition; one declaration per pairing; only rules that will behave are declared |
| Dark contract | **Byte-identical flag-off** — goldens captured from `git archive de4113fd` under the live flags; independently regenerated byte-exact by BOTH reviewers (A: 115 surfaces; B: both JSONs) |
| Flag placement | All reads CALL-TIME; 14 hermetic whitelist mocks swept by both reviewers (none reaches a read; B's M7 mutation proves the six compile-path suites would red on a pre-gate read) |
| Flip map | Written into the flag docstring; proven by a simulated flip: exactly 14 mechanical snapshot regens + the deliberate pin row redden, nothing else (§6) |
| Review | Reviewer A + Reviewer B (both instructed to refute) + `/code-review` high: **9 CONFIRMED fixed on-branch, 4 CONFIRMED dispositioned (recorded / registered), 3 informational, every dark-contract / fence / engine attack REFUTED** (§5) |
| Tests | Red-first 36 red / 47 green before the edit → green after; full suite `Test Files 533 passed | 1 skipped (534)`, `Tests 8909 passed | 62 skipped (8971)`, exit 0 — the exit code asserted and the `Test Files` line read from the log directly; `vite build` ✓ (exit 0, 23.06s; the four `css-syntax-error` esbuild warnings are pre-existing repo CSS, not this diff) |

---

## 2. The five founder decisions (2026-09-02, verbatim)

> All five decisions ruled — build.
>
> 1. Yield clause — approved with "exit" dropped: "Platform limits and enforced values override this identity. Equipped rules outrank my instinct: your equipped rules decide WHETHER a trade happens; this identity shapes only HOW." The falsehood at `:58` is about all equipped rules and the reconciler ranks any conflict `user_equipped > archetype_default` — an exit-only clause would leave the general falsehood standing with an exception carved out of it. Byte-lock this text.
> 2. `EQUIPPED_RULE_PRECEDENCE_ENABLED` — approved. Call-time reads; per-file explicit `false` in hermetic mocks, never blanket `importOriginal`.
> 3. Option A, doubly dark, with trait-hosted mb-08 coverage via `traitLibrary.js` ids labelled "by trait definition." Register that the `COMPILER_ENABLED` flip PR carries this declaration.
> 4. Distressed, LOCKED, ANTI-THRASH, SURVIVAL stay absolute; EV prohibition no-edit. Confirmed on your reasoning.
> 5. Rule IN MARKET POSTURE `selective >80%` and CLOCK mid-battle `>80%` — same mechanism as §8; unqualified siblings would partially defeat the §8 qualifier in the selective regime. Both variants. Leave MARKET POSTURE defensive, S5, FORGE RULES out.
>
> Goldens from `git archive de4113fd` under live flags, red-first flag-on suite before the edit, dual adversarial review + `/code-review` high effort, full suite with exit code asserted and the `Test Files` line read. Push and STOP.

Decision 3's registration: the flag docstring (`featureFlags.js`), the `COMPILER_ENABLED` note in `flagPinGuard.test.js` `DARK_BY_DESIGN` (line 60 — "REGISTERED CARRIAGE"), and §6 below. Decision 2's hermetic-mock rule needed no per-file edit: no whitelist mock reaches a read (§5, B §4) — the rule stands for the next module-scope read.

## 3. Build map (primary anchors, HEAD of this branch)

- `api/_utils/agentEvalPromptAssembly.js` — `:22` flag import; `:199-338` the Ask 2 section: `EQUIPPED_RULE_QUALIFIER` (`:230`, the ONE phrase) and the helpers `renderDefaultToHold` `:234`, `renderRelativeStrength` `:247`, `renderClockManagement` `:264` (mid-battle `>80%` + late-battle DEFENSIVE ONLY), `renderSectorAwareness` `:288`, `renderNr7Line` `:301`, `renderSelectivePostureLine` `:314`, `renderInstitutionalLagNote` `:324`; spliced into the tiered template (`:340-`) and the flat6 template (`:549-`) and the identity trailer (`:821`). §8 CONVICTION stays the literal in both templates (`:387`, `:591`).
- `api/_utils/evalIdentityBlocks.js` — `:37` flag import; `EVAL_IDENTITY_YIELD_CLAUSE` `:73-74`; `renderSubordinationClause()` `:78-80` (call-time); used at `:198`. `EVAL_IDENTITY_SUBORDINATION_CLAUSE` (`:57-58`) unchanged as the flag-off text of record.
- `api/_utils/declaredRuleConflicts.js` (new, pure) — `resolveDeclaredProfitTargetPct` (`:59`, engine mirror: keep-LAST, `Math.abs`, non-positive skip), `detectDeclaredRuleConflicts` (`:99`; trait pass via `getTraitById` `:117`; projection de-dup `:121-122`).
- `api/_utils/compileBuild.js` — `:55` detector import; `declaredConflictDetection = false` input (`:167`); behaving-rule filter + detection (`:504-530`); additive key (`:552`).
- `api/_utils/compileOnSettingsChange.js` — `:36-41` flag import; `:352` the ONE call-time read after the enabled gate; `:411-414` `equippedTraits` threading (`nextState` first); `:430` input pass-through; `:452` preview key.
- `api/agent/change-archetype.js:266-280` — threads the freshly seeded trait layer into the compile's `nextState` (review A-4).
- `src/config/featureFlags.js` — `EQUIPPED_RULE_PRECEDENCE_ENABLED = false` with docstring + flip map + `// Pinned by:`; `src/config/equippedRulePrecedenceFlags.test.js` (dark pin); `src/config/flagPinGuard.test.js:58-62` (`DARK_BY_DESIGN` entry + the `COMPILER_ENABLED` carriage note).
- Tests + fixtures: `agentEvalPromptAssembly.ask2.test.js`, `evalIdentityBlocks.ask2.test.js`, `declaredRuleConflicts.test.js`, `compileBuild.ask2.test.js`; `__fixtures__/ask2PromptGoldens.json` (both variants × six keys, the six identity renders, the trailer with and without an institutional rule), `__fixtures__/ask2CompilerGoldens.json` (three compiled builds), `__fixtures__/ask2CompilerFixtures.js`, `__fixtures__/ask2PromptFixtures.js`. Flip-proofed: `evalIdentityBlocks.flagOn.test.js`, `evalIdentityBlocks.test.js`, `agentEvalPromptAssembly.ask1.test.js`.

## 4. The two Phase 0 corrections (deviations from the ruling as briefed — RATIFIED by the founder post-push, §11)

Both are the founder's own principle from decision 4, applied where the build found the engine fact the Phase 0 report missed: a qualifier must never promise what the engine refuses (the maxPosition label-lie class).

1. **THRESHOLD PROXIMITY bonus bullet — kept ABSOLUTE.** Phase 0 called it "prompt-layer only". It is not: its "within 0.2x ATR of a bonus" band is exactly the risk manager's deterministic LOCK band (`agentRiskManager.js:7-8`, `:149-156` — `LOCK_PROXIMITY = 0.2` over `BONUS_THRESHOLDS`), a Haiku SWAP of a locked symbol is blocked at `agent-evaluate.js:2142`, and even the deterministic target defers on LOCK (R6). A qualifier there would tell the model an equipped rule can exit inside a band the engine refuses to release. Reviewer A verified the reasoning.
2. **§8 CONVICTION — kept ABSOLUTE** (found by `/code-review` CR-2, verified by both reviewers). Phase 0 reported "no deterministic conviction gate" after grepping the cron only; the gate lives in the fenced swap validator — `agentSwapExecution.js:77-78` rejects every SWAP with conviction < 70, and `agent-evaluate.js:2160-2164` downgrades it to HOLD. The qualified §8 would have told the model an equipped rule lets a sub-70 swap through when the engine never will. The forced guardrail path is unaffected (the cron floors conviction to 70 on a forced exit, `:2123`), so the Phase 0 finding that the executor is model-independent still stands.
   - Decision 5 ruled the `selective >80%` and mid-battle `>80%` lines in "as the same mechanism as §8". They ARE qualified: 80 is prompt-only, so an equipped rule lowers the bar to the engine's 70 — never below it. With §8 absolute at the engine's own number, the three lines are coherent and honest.

**The seven flag-on qualifier sentences, verbatim (review A-6 asked that the founder see them before flip; both variants render the same set):**

1. `Absent an equipped user rule that calls for the trade, you need a compelling, data-backed reason to trade.` (DEFAULT TO HOLD)
2. `Absent an equipped user rule that calls for the exit, do not panic-sell outperformers.` (RELATIVE STRENGTH)
3. `Absent an equipped user rule that calls for the swap, only swap on strong conviction (>80%).` (CLOCK, mid battle)
4. `Absent an equipped user rule that calls for the swap, swaps are DEFENSIVE ONLY — cut a position approaching Bust/Crash to protect banked points. Do NOT chase momentum late on your own initiative.` (CLOCK, late battle — "on your own initiative" names model discretion as the thing still forbidden)
5. `Absent an equipped user rule that calls for it, do not swap a bleeding stock for a bench stock in the same sector — if the sector is weak, the replacement will bleed too.` (SECTOR AWARENESS)
6. `Absent an equipped user rule that calls for the exit, do NOT swap out NR7 stocks unless they're bleeding.` (NR7)
7. `Absent an equipped user rule that calls for the swap, only swap on >80% conviction.` (MARKET POSTURE selective)

Plus the C_INST note (identity trailer, only when an institutional rule is equipped): `Absent an equipped user rule that says otherwise, never hold a position based solely on strong institutional accumulation if VWAP (held positions) or RSI-14 shows a breakdown; live technicals override stale institutional signals by default.` — the 135-day lag fact survives both states.

**The yield clause, verbatim (byte-locked):** `Platform limits and enforced values override this identity. Equipped rules outrank my instinct: your equipped rules decide WHETHER a trade happens; this identity shapes only HOW.`

**The A9 tension, stated as ruled:** the DR-13 harness compared `decision` and `symbols` only (`scripts/paired-eval-harness.js:200-201`, `:386-387`; `conviction` extracted at `:98`, never compared; `rationale` never extracted). The clause is an honesty fix for `evalIdentityBlocks.js:58` versus the engine (`ruleConflictReconciler.js:46-49`, `:444-446`; live at `decide.js:262-265`). **No claim is made that it changes a decision.** If `profit_take` stays at zero post-flip, look at the measured-effective surface: the MUSTs of §3 (this build), no winner crossing a plausible threshold, or no equipped target.

## 5. Review — findings and dispositions

Process: two adversarial reviewers ran as independent subagents on the working tree, each instructed to refute every finding with a concrete repro; `/code-review` ran at high effort (single-pass, inline — it disclosed that it did not itself fan out; the §2 adversarial requirement is met by the two reviewers). Both reviewers independently regenerated the goldens from `git archive de4113fd`.

### `/code-review` (high) — 4 findings

| # | Finding | Disposition |
|---|---|---|
| CR-1 | `declaredRuleConflicts.js` imported `src/data/traitLibrary.js` directly → §2.3 ratchet red (`archetypeRegistry.test.js`) | **CONFIRMED — fixed**: trait definitions read through `archetypeRegistry.getTraitById` (`:40`, `:117`); ratchet green; the record cites the red run (§9) |
| CR-2 | §8 CONVICTION qualified while `agentSwapExecution.js:77` rejects every sub-70 SWAP | **CONFIRMED — fixed**: §8 restored absolute in both variants (§4.2); the ask2 suite pins it as an absolute and counts exactly seven qualifiers |
| CR-3 | In unified-projection mode a trait-hosted mb-08 declared twice (projection doc + trait definition) | **CONFIRMED — fixed**: the trait pass skips a trait already declared through its projection-hosted doc (`declaredRuleConflicts.js:121-122`); tests pin one-per-pairing and the two-host bundle+trait case |
| CR-4 | A third hand-copy of the engine's target resolution (assembler, guardrails, detector) | **CONFIRMED — mitigated + registered**: a parity tripwire holds the detector's number equal to the fenced assembler's SX-04 render across five guardrail shapes (`declaredRuleConflicts.test.js:147-`); delegating `resolveEnforcedProfitTargetPct` to the pure helper is fenced work outside this sanction (§8) |

### Reviewer A (rulings fidelity · dark contract · fence + blast radius · engine truth)

| # | Finding | Disposition |
|---|---|---|
| A-1 | Mid-review the tree was inconsistent (§8 helper vs test/header) — the CR-2 edits were in flight and then overwritten (§10) | **CONFIRMED — resolved**: helper deleted, literal restored in both variants, header rewritten; verified by checksum and the suite |
| A-2 | `flagPinGuard` red: the CR-4 parity test pinned `PROFIT_TARGET_EXECUTOR_ENABLED` without a "Pinned by:" pointer | **CONFIRMED — fixed**: behavior branch on the live value, no pin (the `leagueBattleviewFlags` lesson) |
| A-3 | The ratchet (= CR-1) | fixed |
| A-4 | `change-archetype.js` compiled with `nextState: { archetype }` while the same tx re-seeds `equippedTraits` → stale traits on the lit path | **CONFIRMED — fixed**: the seeded layer is threaded into the compile's `nextState` (`change-archetype.js:270-280`; the PR 3.5 review-F2 discipline) |
| A-5 | Duplicate declaration (= CR-3) | fixed |
| A-6 | Two sentences go beyond a bare qualifier insertion ("on your own initiative"; C_INST "by default") | INFO — shown verbatim in §4 for founder reading before flip |
| A-7 | `host` conflated channel with host kind in projection mode | **CONFIRMED — fixed**: host is the KIND (`'projection'` iff trait-hosted, else `'bundle'`; `compileBuild.js:517-522`) |
| REFUTED | Dark contract (115 surfaces byte-identical across both trees, both JSON goldens = independent capture, `writeCompiledBuildsInTx` tx docs identical in legacy / candidate / disabled modes); yield clause byte-exact; one qualifier source in both variants; no hunk touches the ruled absolutes or the EV section; only the sanctioned fenced file among the §1 list changed; `./` import set identical to `de4113fd`; every flag read inside a function body; 14 hermetic suites never reach a reader; the §8/executor engine chain; the harness compared decision + symbols only; the detector mirrors the engine's resolution; the THRESHOLD PROXIMITY = LOCK band reasoning | held |

### Reviewer B (golden provenance · mutation checks · hermetic-mock sweep · test integrity · purity)

| # | Finding | Disposition |
|---|---|---|
| B-F1 | The ratchet (= CR-1); the pre-fix full suite was red (`Test Files 1 failed`) — the record must cite it | **fixed**; cited in §9 |
| B-F2 | The transient pin-guard red (= A-2) | fixed |
| B-F3 | The flag-off C_INST text was guarded only by a two-line substring — a one-byte drift in its flag-off branch passed 7 suites | **CONFIRMED — fixed**: the institutional trailer is golden-pinned byte-exact from the pre-edit tree (1,838 bytes; shared builder `ask2PromptFixtures.js`) |
| B-F4 | The flip reddens 5 existing suites / 23 tests beyond the pin row (the Ask 1 §3 A8+B4 class) | **CONFIRMED — fixed + mapped**: three suites flip-proofed (§6); the remaining 14 are mechanical snapshot regens, named in the flag docstring and verified by a simulated flip |
| B-F5 | A two-entry profitTarget doc makes one CompiledBuild cite two numbers: the strictest-wins preview uses keep-FIRST (`compileBuild.js:445`), the engine and the declaration keep-LAST | **CONFIRMED, pre-existing** (Ask 3's merge) — registered (§8); the declaration is the engine-true one |
| B-F6 | A BLOCKED or metadata-missing mb-08 was still declared, with a message saying it "governs discretionary swaps" | **CONFIRMED — fixed**: only rules carrying an unblocked verdict are declared (`compileBuild.js:504-512`); tests pin core_conflict and metadata-missing cases. Consequence stated honestly in the caller test: through the live corpus (no §5.6 metadata yet) a bundle-hosted mb-08 is refused by the compile and therefore not declared; the trait host is declared by definition |
| B-F7 | "returns null before any flag read" could not detect a pre-gate read | **CONFIRMED — fixed**: the mock getter is armed to THROW; the disabled path must return null without throwing, and the enabled path must throw |
| B-F8 | Removing a `DARK_BY_DESIGN` entry is uncaught by the guard (validates listed entries only) | Guard design, pre-existing — noted (§8) |
| REFUTED | Golden provenance (both JSONs byte-identical to B's own regeneration; contentHashes `095c…`/`78f1…`/`03eb…` match; the pre-edit compiler ignores `equippedTraits` on the delta); no module-scope reads; engine mirror; qualifier count (7 post-CR-2) in both variants × six keys; purity (deep-frozen inputs, deterministic, unmutated); no residual absolutist literals outside the helpers; `validateCompiledBuild` tolerates the additive key; expected strings are literals; compile golden equality is `toEqual` on the full build including `contentHash` | held |
| Mutation checks (19) | Every defect class caught by at least one row after the fixes: qualifier dropped (one variant), flag-off byte altered, tiered-only helper (one-wording drift), module-scope reads (assembler, clause), yield clause −1 char / old clause left flag-on, key when OFF / missing when ON-empty / trait path removed / host mislabel, FIRST-not-LAST / no `Math.abs` / declare-on-0, "Pinned by:" removed; the two initially uncaught (institutional byte → B-F3; pre-gate read → B-F7) now caught | — |
| Hermetic sweep | 14 explicit-factory `featureFlags` mocks enumerated; six reach `compileOnSettingsChange` but pin `COMPILER_ENABLED:false` (return before the read — proven by the M7 mutation, which reds exactly those six); eight have no flag-reading module in graph; all 14 green | — |

## 6. THE FLIP MAP (the Ask 1 §4 precedent)

The flip PR (`EQUIPPED_RULE_PRECEDENCE_ENABLED` false → true, its own one-line PR after founder smoke, after Asks 1+3 per R10) reconciles:

1. `src/config/equippedRulePrecedenceFlags.test.js` — the `toBe(false)` pin → `true`, and drop the `DARK_BY_DESIGN` entry in `flagPinGuard.test.js` (the guard's own message walks you through it).
2. **Snapshot regens, mechanical:** `npx vitest run api/_utils/p4Equivalence.battery.test.js api/_utils/evalIdentityBlock.injection.test.js -u` — 2 P4 + 12 DR-13 flag-on prompt snapshots re-capture the seven qualified MUSTs, the C_INST note (where rendered) and the yield clause. **Review that diff: it must be exactly the ruled prose delta of §4** — anything else in it is a finding.
3. Nothing else — proven by a simulated flip this session (flag set true, 12 suites run, reverted): exactly 15 red = 14 snapshots + the pin row. `evalIdentityBlocks.test.js` branches on the live clause; `evalIdentityBlocks.flagOn.test.js` and `agentEvalPromptAssembly.ask1.test.js` pin the Ask-2-dark branch (they lock DR-13 / Ask 1, not this arc); the ask2 suites, the honesty sweep and the M7 budgets are flip-proof.
4. **Registered carriage (decision 3):** the compiler's `declaredConflicts` declaration reaches a user only when `COMPILER_ENABLED` flips — that flip PR carries it (its `DARK_BY_DESIGN` note says so).

## 7. Founder attention (pre-flip) — items 1–2 closed by the §11 rulings

- **Ratify the two absolutes of §4** (THRESHOLD PROXIMITY bonus bullet; §8 CONVICTION) — both deviate from the ruling as briefed, on the ruling's own principle, and both are one-line changes if you disagree.
- **Read the seven flag-on sentences in §4** (review A-6): two carry words beyond the bare qualifier — "on your own initiative" (late battle) and "by default" (C_INST). Both yield only to an equipped user rule.
- **Doubly dark** (decision 3, accepted): the compiler declaration is produced nowhere in production until `COMPILER_ENABLED` flips; and under today's live corpus (no §5.6 metadata) a bundle-hosted mb-08 would be refused by the compile and not declared — only trait-hosted mb-08 declares by definition until Phase 3 authors metadata (B-F6's honest consequence).
- **B-F5 is pre-existing** and §9-class: the compiled preview's `effective` profit target keeps FIRST while the executor keeps LAST — a two-entry guardrail doc makes the preview and the executor disagree. Not this build's fence; registered.
- **The A9 tension** stands as the kickoff stated it; this record makes no behavioral claim for the clause.

## 8. Separate-tasking register (§3 — found, not fixed)

1. **B-F5** — `compileBuild.js:445` strictest-wins merge reads the user guardrail keep-FIRST; the engine (`agentGuardrails` byType) and both resolvers keep LAST. Mirror the engine (Ask 3's compiler surface).
2. **CR-4** — delegate the fenced `resolveEnforcedProfitTargetPct` to the pure `resolveDeclaredProfitTargetPct` (one import, one line) so one resolver mirrors the engine; needs its own fence sanction. The parity tripwire holds them equal meanwhile.
3. **B-F8** — the flag-pin guard validates listed `DARK_BY_DESIGN` entries but cannot notice a removed one; a count or a flag→entry cross-check would close it.
4. **A-4's endpoint-level lit test** — `change-archetype` now threads the seeded traits, guarded by the compile-side threading test; an endpoint-level assertion needs a `COMPILER_ENABLED`-on harness the compat suites don't have.
5. Reviewer process (§10): reviewers must mutate in a snapshot tree, never the shared working tree — the Ask 1 review instruction "restore byte-exactly" is unsafe under concurrent fix-forwarding.
6. Carried from the Ask 1 register, unchanged: the C-2 flat6 tier-multiplier stamp; the Tier-3 `profitTargetOverridePct` coupling; the explicit-object `featureFlags` mock migration.

## 9. Verification

- **Red-first:** the five new suites run against the untouched tree: `Test Files 5 failed (5)`, `Tests 36 failed | 47 passed (83)` — the flag-on rows and the missing module red, the dark goldens already green (the dark contract holds by construction).
- **Goldens:** `ask2PromptGoldens.json` and `ask2CompilerGoldens.json` captured from `git archive de4113fd` with `node_modules` symlinked, under that tree's live flags (`PROFIT_TARGET_EXECUTOR_ENABLED=true`, `EVAL_IDENTITY_BLOCK_ENABLED=true`); regenerated byte-exact by Reviewer A (115 surfaces) and Reviewer B (both files).
- **Full suite, run 1 (post-build, pre-review):** `Test Files 1 failed | 532 passed | 1 skipped (534)`, `Tests 1 failed | 8900 passed | 62 skipped (8963)`, exit 1 — the one red was the §2.3 ratchet (CR-1 / B-F1), fixed.
- **Touched-surface battery (post-fix):** 25 files / 451 tests green, exit 0 (both new prompt suites, Ask 1, honesty, the DR-13 injection snapshots, both identity suites, the P4 battery, the pin + guard, the ratchet, the detector, the compiler suites, the seven hermetic `api/agent` suites, the M7 budgets).
- **Simulated flip:** 15 red / 247 green across 12 suites = 14 snapshot regens + the pin row; flag reverted; pin + guard green after revert.
- **Full suite, FINAL (this tree):** `Test Files 533 passed | 1 skipped (534)`, `Tests 8909 passed | 62 skipped (8971)`, exit 0 — the exit code asserted and the `Test Files` line read from the log directly
- **`vite build`:** ✓ exit 0 (`built in 23.06s`; the four `css-syntax-error` esbuild warnings are pre-existing repo CSS, not this diff) (§2 — no test imports `App.jsx`).
- **Lint:** the touched set is clean except three pre-existing baseline lines proven present at `de4113fd` (`agentEvalPromptAssembly.js` `lastDay`; `compileBuild.js` `const { contentHash, compiledAt, ...hashable }`).
- **Never piped through `tail`:** the exit code and the `Test Files` line were read from the log directly (the Ask 3 §8 rule).

## 10. Process notes

- **The reviewer restore incident.** Reviewer B's mutation harness restored `agentEvalPromptAssembly.js` from its 01:33 backup five times (last ≈ 01:50:18), overwriting the CR-2 fixes in flight; Reviewer A caught the resulting inconsistency (A-1). The three edits were re-applied after the tree settled and the file's checksum (`29ca1342…`) was held constant through the rest of the session. B also notes the prescribed `git checkout -- <file>` revert would have restored the base tree and erased the unstaged build — it did not use it. Lesson registered (§8.5).
- **Commit `152217ea`** was found on the branch when the final verification began (author `Claude`, 02:00:58 UTC, correct trailer, content = the build state at that time, not pushed). It was not issued by an explicit step in this session's transcript; its content was verified (no stale §8 helper, registry adapter present, two §8 literals) and kept as commit 1. Everything after it rides the second commit with this record.
- `/code-review` ran single-pass and said so; the §2 multi-lens, independently-verified requirement is met by the two subagent reviewers whose refute attempts are recorded above.

## 11. Post-push founder rulings (2026-09-02, verbatim)

> Both absolutes ratified; seven sentences approved as written. Process rule going forward: subagent reviewers work on a snapshot tree and are read-only on git. Merging dark; flip PR after the current ranked week closes.

- §4's two absolutes (§8 CONVICTION; the THRESHOLD PROXIMITY bonus bullet) are ratified — the build stands as pushed; no code change.
- The seven flag-on sentences of §4 are approved as written (review A-6 closed).
- The reviewer-isolation rule is codified in `docs/BUILD_RULES.md` §2 (founder-cited, this commit) and closes §8.5.
- Merge is dark; the flip PR (§6 map) follows the close of the current ranked week — not this branch, not this session.

**Push and STOP (BUILD_RULES §2):** pushed, not deployed; the founder reads CI, smokes the preview, and merges. The flag flip is a separate one-line PR.
