# Exit-Behavior Rebalance — Ask 1 Build + §2 Adversarial Review Record

**The prompt made honest — contradiction resolved, cost priced, copy enforcement-true. Built dark, dual-adversarially reviewed per phase.**
Branch `claude/exit-behavior-ask1-prompt-honest` off `origin/main` @ `7c70ae6b` (Ask 3 merged). Build commits: `6a779188` (Phase A — framework rewrite), `cae89763` (Phase B — data-adds + renders + copy), plus the audit/fix commit carrying this record. Authoritative set (precedence): Addendum V1.1 → Rulings V1 → Fable Review V1 → Brief V2 → Phase-0 Verification → the Ask 3 build record.

**Fence statement:** the only fenced file edited is `agentEvalPromptAssembly.js` (sanctioned in the kickoff). Fenced exports *called*, never edited: `calculateAssetScoreServer` (fixtures), `sanitizeRuleText` (pre-existing import). `agentArchetypeConfig.js` untouched (R5); `agentGuardrails.js`/`agentRiskManager.js` untouched (Ask 3 complete); Gate-7 locked call forms untouched. No new `./` import entered the fenced assemblers (the honesty tripwire's surface is unchanged); the two non-local imports added (`featureFlags` extension, `baggerBombScoring` — BUILD_RULES §4 canonical constants) carry the dependency-surface guard note in the ask1 test.

---

## 1. Executive verdict

| Surface | Status |
|---|---|
| DECISION FRAMEWORK rewrite (both variants) | **BUILT dark** — prohibition + P&L-framing removed flag-on; four-layer precedence from ONE helper at all three sites; pricing doctrine + motive sanctioning; bust-override machinery byte-preserved in both states |
| Data-add #1 (LockNow decomposition + Δ-to-next-bonus) | **BUILT dark** — rendered off the one fenced scorer's output (§9 cross-surface test); Δ computed fresh, never the banded helpers |
| Data-add #3 (held-position Levels) | **BUILT dark** — conditionally populated, `-` when absent (R12 honest-null) |
| SX-04 render | **BUILT dark** — enforcement-true wording with resolved X; fail-honest fallback |
| Promise copy | **ONE source** (`profitTargetPromise.js`) with the full R2 physics + the A4 protective-precedence disclosure + trigger-gating honesty |
| Dark contract | **Byte-identical flag-off**, proven against goldens captured from the untouched base — independently regenerated byte-exact by BOTH Phase A reviewers from a reconstructed `7c70ae6b` tree |
| Flag placement | All reads CALL-TIME (the Ask 3 §8 module-scope lesson applied); mock blast radius swept in both flag states |
| Flip surface | Shrunk + fully documented (§4) — the "zero test reconciliation" claim of the Ask 3 record is **superseded by this §4** |

---

## 2. What was built (primary anchors)

- `agentEvalPromptAssembly.js` — the Ask 1 helper section (call-time flag reads; three prose helpers + `renderPrecedenceBlock` used at all three precedence sites; `renderEquippedRuleText`/`resolveSx04TargetPct`; `lockNowCell`/`nextBonusCell`/`levelsCell`); §2 rewrite splices in both variants; SURVIVAL MODE de-framing (machinery verbatim); forge-rules trailer; `buildPortfolioCSV` exported + flag-on cells + `rankingsMap` threading at the one call site.
- `src/constants/profitTargetPromise.js` — the one promise source; `StrategyDimensions.jsx` consumes it (no literal copy remains there).
- Tests: `agentEvalPromptAssembly.ask1.test.js` (goldens + red-first flag-on suite + Phase B §9/render/promise assertions), `__fixtures__/ask1PromptFixtures.js` (real-scorer fixtures), `__fixtures__/ask1PromptGoldens.json` (pre-edit capture), flip-proofing mocks in `ruleCompatInvariantR.test.js` + `hardSoftOverride.parity.test.js`.

**Red-first, verified process:** the flag-on suite was written before the rewrite and failed 6-red against the untouched prose; the goldens were captured from the untouched code and later **independently regenerated byte-identical by both reviewers from `git archive 7c70ae6b` scratch trees** — the strongest available proof the dark contract is real.

## 3. STOP-1 dual adversarial review (Phase A) — findings & dispositions

**Reviewer A** (rulings fidelity + prompt-semantics blast radius; verdict APPROVE-dark-merge) and **Reviewer B** (goldens/test integrity + blast radius; verdict no-confirmed-defect). Both independently rebuilt the pre-edit tree and byte-verified the goldens.

| # | Finding | Disposition |
|---|---|---|
| A8+B4 **CONFIRMED (process)** | The Ask 3 record's flip map ("zero test reconciliation") became false: ~16 live-flag golden surfaces redden at flip | **Fixed**: two non-snapshot suites pinned to the dark branch (they test compat/parity, not exit copy) and verified green under a simulated flip; the remaining surface is snapshot regen only, documented in §4 below which **supersedes the Ask 3 §4 claim** |
| A11c **CONFIRMED (scope)** | Brief V2's Ask-1 bullet — sanction profit-taking/momentum-rotation in the `swap_type` vocabulary — was absent from Phase A | **Fixed in Phase B**: the flag-on §2 sanctions both motives in the schema's own vocabulary (R5 untouched — prose only, no gate) |
| A9 DESIGN-CONCERN | `evalIdentityBlocks.js:58` ("equipped rules refine … but never reverse") is the mirror-image of the new inversion — live in every prompt during the Ask 1+3 → Ask 2 window | Founder-visible (§6): the sentence is Ask 2's sanctioned surface (R8 yield clause); the window is R10's own sequencing. Not touched here |
| A10 DESIGN-CONCERN | Absolutist layer-4 MUSTs (§8 conviction, late-battle DEFENSIVE ONLY, NR7) fight the inversion with no tiebreak | Acceptable-layering per the precedence block; a one-line "absent an equipped user rule" qualifier would be NEW fence contact needing its own sanction — founder's call (§6) |
| A11a/b | SX-04 render + cost decomposition absent (Phase A alone not flip-ready) | **Fixed in Phase B** (both built); the R10 gate note stays in §6 |
| B3a-d, A7 minor | Mutation gaps: loser bullet / survival sentence / motive line unpinned flag-on; extract-regex tail | **Fixed**: positive pins added for all three; regex tail accepted (three-way equality constrains same-line drift; documented) |
| B2a note | Fixture `swapPrice` == `startingPrices` leaves the CSV entry-baseline precedence untested by goldens | Accepted (the precedence is the cron's concern, tested elsewhere); recorded |
| All other attacks | Rulings fidelity (four layers, inversion, HOW/WHETHER, pricing sentence, survival machinery), golden provenance, call-time flag placement, hermetic-mock sweep (both flag states), honesty-sweep mechanics, no scope creep | **REFUTED-OK** — attacks failed; details in the reviewer reports (session artifacts) |

## 4. THE FLIP MAP (supersedes the Ask 3 record §4 "zero test reconciliation" line)

The joint Ask 1+3 flip PR (one constant + Ask 1's copy going live) reconciles:

1. `agentGuardrails.pairing.test.js` — the `toBe(false)` dark pin (Ask 3's documented manual item) + drop the `DARK_BY_DESIGN` entry (the flagPinGuard message walks you through it).
2. **Snapshot regens, mechanical:** `npx vitest run api/_utils/p4Equivalence.battery.test.js api/_utils/evalIdentityBlock.injection.test.js -u` — 2 p4 snapshots + 12 DR-13 snapshots re-capture the flag-on prose. **Review the snapshot diff: it must be exactly the ruled prose delta** (prohibition→pricing doctrine + motive bullet, precedence block, survival de-framing, SX-04 render, the three CSV columns) — anything else in that diff is a finding.
3. Nothing else: the ask1 suite, `ruleCompatInvariantR`, and `hardSoftOverride.parity` are flip-proof (empirically verified under a simulated flip this session), and the display pins were behavior-branched in Ask 3.
4. State in the flip PR: from the flip forward, deterministic-reason swaps stamp `swapMotive: null` (Ask 3 A1 — the pre/post boundary for the Tier-1 motive baseline read), and the Ask 1 prose delta is live in every eval prompt.

## 5. STOP-2 review (Phase B) — Reviewer C + `/code-review` (high), findings & dispositions

**Reviewer C** (Phase B fenced diff: §9 numeric honesty, rulings fidelity, dark contract; verdict BLOCK-as-was, two CONFIRMED — both dispositioned below). `/code-review` at high effort: 5 findings, all dispositioned.

| # | Finding | Disposition |
|---|---|---|
| **C-5/C-6 CONFIRMED (critical, in-fence)** | The SX-04 render was DEAD against production data: keyed on `id` (live writers emit `ruleId`), read param `profitTargetPct` (stores key `pct`), and cited the rule store instead of the value the ENGINE enforces; its test passed on an invented fixture shape | **Fixed**: render keys `ruleId ?? id`; X resolves ONLY from `deployedGuardrails` (the store `applyGuardrails` fires on), engine-mirrored (keep-last dedup + `Math.abs` + the non-positive skip — CR-1 folded in); fixtures reshaped to the production item shape; drifted-bundle test asserts the render cites the engine's 20, never the store's 15. Deviation from C's suggestion argued: NO rule-store fallback — the enforcement sentence must never cite a number the engine does not hold |
| **C-2 CONFIRMED (pre-existing engine; ESCALATION — see §6 item 1)** | Flat6 live scoring drops the D2 `tierMultiplier: 1.0` stamp (the cron rebuilds assets without it at its four scorer call sites; the executor's outgoing-asset rebuild likewise), so slot labels score 2.0/1.5x in production flat6 — and flag-on, the LockNow cell makes that contradiction with the same prompt's "flat — NO tier multipliers" text model-checkable. CSV==ledger HOLDS (the cell honestly previews the actual lock); the flat6 scoring TEXT is the falsified party | **Escalated, not patched** (§3 report-don't-fix + the guardrails escalate-never-patch doctrine; the engine sites are outside this task's sanction). Founder ruling required BEFORE flip: fix the stamp pass-through (engine change) or accept/hold the flat6 cell. Fixture models the STAMPED design shape (CR-3), stated in-file |
| CR-1 | Enforced-X selection didn't mirror the engine (find-first + `>0` vs keep-last + `Math.abs`) | **Fixed** (folded into the C-6 fix; two-entry and negative-value docs now resolve identically at both sites) |
| CR-2 | The live `targetFor` per-position override hook can contradict "never negotiable" if a doc carries `profitTargetOverridePct` | **Accepted + recorded**: no writer exists (verified); the Tier-3 lever arc MUST update the render when the writer ships — carried in §6 and the register |
| CR-3 | Fixture computed flat6 scores with tier multipliers (an off-design shape) | **Fixed**: fixture stamps `tierMultiplier: 1.0` (the D2 doc shape) with the C-2 caveat stated in-file |
| CR-4 | `sx-04`↔`profitTarget` pairing duplicated from `compile-dimensions.js` | Accepted + registered (a structural tripwire is future hygiene) |
| CR-5 | Hand-listed bonus levels | **Fixed**: derived from the canonical ordered `BAGGER_TIERS` (§4 one-source) |
| C-1, C-3, C-4, dark contract, flip sims, promise module, motive bullet | LockNow parity with the executor's lock math (baseline precedences identical both sides), NextBonus correctness, Levels staleness = bench precedent, goldens, flip map exactness | **REFUTED-OK** — attacks failed; C's flip simulation confirmed §4's map exactly (Phase B removed 3 flip-reds, added zero) |

## 6. Founder attention (pre-flip)

- **① C-2 — FLIP-BLOCKING ESCALATION (and a standalone production finding):** flat6 live scoring applies 2.0/1.5x by slot label because the D2 `tierMultiplier: 1.0` stamp is dropped when the cron (and the executor's outgoing-asset rebuild) reconstruct assets for the scorer — production tournament `activeScore`/`lockedPoints` are off the flat-1x design TODAY, masked because nothing prints the decomposition. This predates Ask 1 entirely and deserves its own tasking regardless of this arc. For THIS arc: flag-on, the LockNow cell would let the model see the ×2 against the same prompt's flat-1x text — rule before flip: (a) fix the stamp pass-through (engine change, its own PR), or (b) accept the current math and fix the flat6 scoring TEXT instead, or (c) hold the flat6 LockNow cell. The build models the stamped design (a).
- **R10 gate:** flip only with this branch merged — Phase A prose alone was ruled not flip-ready (A11); Phase B closes that, but the flip PR should confirm both commits are in.
- **The inter-flip window (A9):** until Ask 2 flips, every prompt carries both the new inversion and the DR-13 "never reverse them" postscript. R8's yield clause is the Ask 2 fix; decide whether the window is acceptable (it is founder-sequenced today).
- **A10 MUSTs:** optional one-line qualifiers on §8/late-battle text = new fence contact; rule if wanted, else the precedence block governs.
- **R9:** N set at flip; the Tier-1 motive baseline is pre-treatment evidence on both sides of the boundary named in §4.4.

## 7. Separate-tasking register (§3 — found, not fixed)

1. **The C-2 engine defect** (§6 ①): flat6 tier-multiplier stamp dropped at the cron's four scorer call sites + the executor's outgoing rebuild; no test pins the pass-through (the P4 battery proves the mechanism honors an explicit stamp, never that the callers supply it).
2. Tier-3 coupling (CR-2): when the `profitTargetOverridePct` writer ships, the SX-04 render must account for per-position overrides.
3. `sx-04`↔`profitTarget` pairing tripwire (CR-4); `sanitizeRuleText` 200-char clip on fractional X (cosmetic); NextBonus baseline-denomination header hint (precedented, optional).
4. The explicit-object `featureFlags` mock migration (carried from the Ask 3 register; six files remain).

## 8. Verification (STOP-2 gate)

- Golden-provenance note: the fixture was reshaped post-capture (production `ruleId`/`pct` shape + the D2 stamp + `deployedGuardrails`); the flag-off goldens still bind byte-exactly because every reshaped field is invisible to the dark render (text/hardness drive it; points never print dark) — asserted by the passing golden suite, and the reshape is why the SX-04 dead-render class can never pass on an invented shape again.
- Full repository suite at the final commit: exit code asserted and the `Test Files` line read directly (never piped through tail — the Ask 3 §8 lesson); numbers in the closing line below.
- `vite build` ✓ (§2). Touched files lint clean against the repo's pre-existing baseline.
- Flip simulations (three independent runs this arc — reviewers B, C, and the in-session pin check): the §4 map is exact.
