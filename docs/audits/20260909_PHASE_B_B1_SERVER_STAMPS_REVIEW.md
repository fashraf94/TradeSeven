# Phase B — B1 (server half): the §2 adversarial review record

**What was reviewed:** the tick-stamps build on `claude/phase-b-server-tick-stamps-lo6jwb` — `0bc604f1` (the build) + `b952f783` (the fail-safe) over `origin/main` @ `4a8ae54a`; 14 files / 2,709 lines at review time (BUILD_RULES §2 threshold: ≥10 files or ≥1,500 lines — both crossed). Companion: `20260909_PHASE_B_B1_SERVER_STAMPS_BUILD_REPORT.md` (same directory).
**Method (BUILD_RULES §2, reviewer isolation per the Sep 2 ruling):** four reviewers, one lens each — **A** domain correctness against the spec and the discovery · **B** wiring, lifecycle and persistence at the splice · **C** the dark-merge contract, flag discipline and rules compliance · **D** test integrity and harness realism ("a row that cannot fail under the defect it names is not a guard"). Each worked READ-ONLY on a `git archive HEAD` snapshot under the session scratchpad (node_modules symlinked), with no git writes and no access to the live tree; experiments ran in their own copies. Every finding was then handed to a **verifier instructed to refute it** with a concrete reproduction — **V1** (A-1…A-12), **V2** (B-1…B-6, C-1…C-7), **V3** (D-1…D-10). Findings that survived are CONFIRMED; the rest REFUTED, with the reasoning. The coordinator applied the fixes to the live tree only after the verdicts.
**Outcome in one line:** 35 findings across four lenses; the three refuters CONFIRMED 30, PARTIALLY confirmed 4 (A-5 magnitude, A-9 framing, C-1's lock-release clause, D-5's reachability), REFUTED 1 (C-7 — the shadow envelope never carries the entry); 27 fixed or documented on the branch in `e1d19a42`, 8 recorded for the founder or separate tasking; the dark-merge contract drew no finding; the post-fix mutation run is 20 / 20 CAUGHT.

---

## 1. Executive verdict

| Lens | Findings | Highest severity | Verifier | CONFIRMED | PARTIAL | REFUTED |
|---|---|---|---|---|---|---|
| A — domain correctness | 12 (10 in scope + 2 outside-task) | **high ×2** (A-1 `chg`, A-2 `rsPct`) | V1 | 10 | 2 (A-5 magnitude, A-9 framing) | 0 |
| B — wiring & persistence | 6 | medium (B-1 = A-4) | V2 | 6 | 0 | 0 |
| C — dark contract & rules | 7 | medium (C-3 = A-1) | V2 | 5 | 1 (C-1: the lock IS released) | 1 (C-7) |
| D — test integrity | 10 | medium (D-1) | V3 | 9 | 1 (D-5: `'unknown'` IS reachable) | 0 |

**Fixed on the branch (commit `e1d19a42`):** A-1/C-3, A-2, A-3, A-4/B-1, A-5/C-2, A-6, A-7/A-8/A-10 (documentation), B-3, B-4/D-4, C-1/B-2, C-5, D-1, D-2, D-3, D-5, D-6, D-7, D-8, D-9, D-10. **Recorded, not code (the founder's or separate tasking):** A-9, A-11, A-12, B-5, B-6, C-4, C-6, C-7. The dark-merge contract itself drew **no finding**: flag off, the write is byte-identical and zero work runs (C §2; the golden re-derived from the pre-stamp cron by two independent parties).

---

## 2. Findings, verdicts and dispositions

Severity is the reviewer's; the verdict is the verifier's; the disposition is what the branch now carries.

| ID | Lens | Sev | Claim (one sentence) | Verdict | Disposition |
|---|---|---|---|---|---|
| A-1 / C-3 | A, C | **high** / medium | `chg` was stamped from the quote's session `changePercent`, a number the prompt renders for BENCH rows only; the ACTIVE POSITIONS row the decider saw carries Gain% from entry (`score.priceChange`). | **CONFIRMED** (V1 rendered the whole prompt: the stamped value appears on no line naming a held symbol; V2: CONFIRMED — the captured prompt carries the stamped `chg` on no held-symbol line) | **FIXED** — `chg` ← `assetScores[i].priceChange` at the row's `formatPct` 2dp (the rendered Gain%); the module header and field comment corrected; unit + e2e rows moved (NVDA 2.41 / 2.57, TSLA −2.08, BTC 0.67). Deviation from the discovery B8's named source, not from the spec's label — founder ruling requested (build report §5.2). |
| A-2 | A | **high** | `rsPct` was stamped per held position, but `rsPercentile` is rendered only in BENCH TECHNICAL CONTEXT; a held name's technical read reaches the decider as the `regime` word. | **CONFIRMED** (V1: exactly two `rsPercentile=` occurrences in the whole prompt, both bench) | **FIXED** — `rsPct` dropped; the evidence is eight fields (`regime` stays); a row pins `rsPct` out of `EVIDENCE_FIELDS`. Deviation from the ruled nine names — founder ruling requested (§5.3). Separate tasking: the fenced system prompt tells the model it "can see rsPercentile" for holdings it cannot see (`agentEvalPromptAssembly.js:493`, `:696`). |
| A-3 | A | medium | `vintages.tech: 'daily'` is a cadence word the stamp cannot support: `stockTechnicalScores` is rewritten hourly during RTH by `compute-index-intelligence?mode=intraday`, in the same batch as `stockRankings.computedAt`. | **CONFIRMED** (V1: one `batch.commit()` carries both with `serverTimestamp`; `vercel.json` `0 14-20 * * 1-5`) | **FIXED** — `tech` replaced by `techAt`, the newest held technical doc's `updatedAt` as an instant (no new I/O: the cron already holds `doc.data()`); rows forbid "daily" beside "weekly". |
| A-4 / B-1 | A, B | medium | `haikuAttempted` is set BEFORE the prompt is built; a throw inside a prompt builder (caught by the same try as a Haiku failure) left it true, so Heard + evidence were stamped on a tick whose prompt never existed. | **CONFIRMED** (V1 reproduced it twice — with the builder mocked and with a NATURAL fault, a `trades[]` record with `lockedPoints: null` → `buildClosedTradesCSV` throws; V2: CONFIRMED, twice — a live-block throw and an identity-block throw both stamped; hence all three builders are hoisted) | **FIXED** — a new local `promptBuilt` flips after the three prompt parts are built and immediately before `messages.create` (the parts hoisted into consts in the request's order; same builders, same argument lists); the stamp block and the composer gate on it; `haikuAttempted` untouched for its existing consumers. New e2e row (gates suite): a live-context throw → no stamp. Spec §1.2 / D-110 name `haikuAttempted` — a one-word amendment for the founder (§6). |
| A-5 / C-2 | A, C | medium → low | `Math.round(v*100)/100` disagrees with the renderer's `toFixed(2)` on `.xx5` boundaries (0.615 → 0.62 vs "0.61"; −9.955 → −9.95 vs "−9.96"), so the §9 "the stamp IS the rendered number" claim was false for that class. | **PARTIALLY CONFIRMED** (V1: the mechanism and the grid figure are exact; on 70,007 realistically computed multipliers the two never diverged — severity low; V2: CONFIRMED, low — zero divergences on 2-dp inputs; the sources are not pre-rounded) | **FIXED** — `round2 = Number(v.toFixed(2)) \|\| 0` (the renderer's primitive; a negative zero folds to 0); a unit row on 0.615 / 2.675 / −9.955 / −0.001. |
| A-6 | A | medium/low | `fundAsOf` took the newest fundamentals vintage across HELD only; the FUNDAMENTALS block's header ("Fundamentals data as of") is the newest across HELD + BENCH, so on a mixed-vintage tick the stamp disagreed with the rendered header. | **CONFIRMED** (V1 rendered the block with bench AMD fresher: header `09-08`, stamp `2026-09-04`) | **FIXED** — the cron passes `flattenBenchServer(battle.portfolio?.bench)`; the composer applies the block's exact symbol rule (held, then non-crypto bench, deduped); a unit row calls the REAL `buildFundamentalsBlock` and asserts the stamp equals its header date by construction. |
| A-7 | A | low | For a LOCK the prompt renders the verdict's `detail` sentence while the stamp keeps the `reason` code; the reason→detail fallback is dead in production. | **CONFIRMED** | **RECORDED in the module header** (same verdict, the compact code); the fallback kept as defence. |
| A-8 | A | info | `risk: { action: 'HOLD' }` is stamped on all-HOLD ticks when no RISK STATUS block rendered at all (true by the block's absence). | **CONFIRMED** | **RECORDED in the module header.** |
| A-9 | A | low/info | The "≤ 1.1 KB per entry" row is fixture-shaped and excludes `candidates`; worst case 1,236 B; two candidates add ≈ 440 B. | **PARTIALLY CONFIRMED** (numbers exact; the report already split them) | **RECORDED** — the build report states the row precisely (post-fix numbers: 949 / 922 B; worst case 1,166 B; candidates 189–240 B each, ~1 KB/day; an entry carrying three candidates ≈ 1.6 KB). |
| A-10 | A | info | `suppressed: 'malformed'` is reachable from the cron only via a type-corrupt directive (non-string truthy `text`); the shared pre-gate turns an id-less or text-less directive into NO directive (key absent). | **CONFIRMED** (V1 drove both shapes through the real cron) | **RECORDED in the module header**; a flag-on row now drives `text: 42` → `malformed` end to end, and the unit row is annotated. |
| A-11 | A | info (outside task) | On a risk-swap tick `assetScores` (a `const`, never recomputed) still drives the ACTIVE POSITIONS CSV, so the decider sees a phantom row for the swapped-out symbol and none for the swapped-in one; the stamp mirrors the render. Pre-existing fenced behaviour. | **CONFIRMED by a live-tick repro** (V1: EMERGENCY_SWAP TSLA → AMD; the CSV rendered `support,TSLA,Unknown,…,-0.91x`) | **SEPARATE TASKING** (§7). The stamp's "stamped set = rendered set" is the honest label for this build. |
| A-12 | A | info (outside task) | `chg: 0` cannot distinguish a flat session from a missing quote field (`change_p \|\| 0` at the cache). | **CONFIRMED** | **SEPARATE TASKING** (moot for `chg` after A-1's fix; the cache's `\|\| 0` stands). |
| B-1 | B | medium | = A-4. | **CONFIRMED** (V2) | FIXED with A-4 (all three builders hoisted, per V2's refinement). |
| B-2 / C-1 | B, C | low | The flag docstring and a pin row claimed a bare-factory `featureFlags` mock "resolves undefined → off, never a throw"; under vitest 4 a missing export on a factory mock THROWS on access, and the flag read sat outside the fail-safe. No suite both bare-mocks the flags and drives the cron today. | **CONFIRMED** (V2 reproduced the throw on the real cron: no finalUpdate; the outer catch still released the lock) | **FIXED** — docstring rewritten to the true rule (the v2dispatch.bareMock precedent); the flag read moved inside the fail-safe `try`; the vacuous pin row replaced by a real one (every cron suite that imports `agent-evaluate.js` and mocks the flags module must spread `importOriginal` — scanned). |
| B-3 | B | low | With the flag on, a `budget_skipped` tick still ran `resolveControls` and a rankings `data()` decode before the composer returned `{}`. | **CONFIRMED** (V2 counted +1 resolveControls call and +1 rankings decode on a starved tick) | **FIXED** — the gate is `if (TICK_STAMPS_ENABLED && promptBuilt)`; no stamp work on a skipped tick. |
| B-4 / D-4 | B, D | low | The e2e suites drove HOLD ticks only; the SWAP path (the spec's "after the Haiku swap") rested on source reading. | **CONFIRMED** (V2: a post-swap re-read mutant passed 29/29 shipped rows while Heard named the newer thread; V3: the mock db supports the real executor — `conviction ≥ 70` needed) | **FIXED** — a flag-on row drives a real autopilot SWAP (KO → AMD) through the fenced executor: the evidence is the PRE-swap book (KO in, AMD out), the doc carries the swap, Heard unchanged; the no-re-read window also bans `Object.assign(battle` and pins the file-wide count. |
| B-5 | B | info | `candidates` is uncapped in count and string length (no `maxItems` in the tool schema); the size row excludes it. | **CONFIRMED** (V2: 20 verbose items → 17,250 B) | **RECORDED for the founder** (§6) — the dispatch queue is equally uncapped; the fenced instruction expects 1–3 per day. |
| B-6 | B | info | Brief-premise correction: on the Haiku SWAP path neither the fenced executor nor the cron writes the swap onto the in-memory `battle`, so `assetScores`, `riskStatus`, the CSV rows and the in-memory book are all the pre-swap state — the label is coherent by construction. | **CONFIRMED** (V2: doc support `[AMD,PG,BTC]` vs in-memory `[KO,PG,BTC]`) | **RECORDED**; now also proved by the SWAP row. |
| C-1 | C | low | = B-2. | **PARTIALLY CONFIRMED** (V2: mechanism, docstring and row claims hold; the "no lock release" clause is refuted — the outer catch writes `evaluatingAt: null`) | FIXED with B-2; the wording here corrected. |
| C-2 | C | low | = A-5. | **CONFIRMED** (V2) | FIXED with A-5. |
| C-3 | C | medium | = A-1. | **CONFIRMED** (V2) | FIXED with A-1. |
| C-4 | C | low | The committed spec still shows `fund:'weekly'` (and "67 bytes", "beside hypothesis") while the build ships the ruled shape; D-111 is silent on the change. | **CONFIRMED** (V2) | **RECORDED for the founder** — a spec V1.1 amendment line beside the D-110→D-115 ledger append (build report §5.6, §6.2). The spec was committed byte-exact by instruction. |
| C-5 | C | info | Pin blind spots: dot-only `ev.` reads, `battle.evaluations` counted in one form, `battle.directive` assignment in one form. | **CONFIRMED** (V2: destructuring / optional-chain readers and `??=` / bracket / `Object.assign` writes all passed the shipped pins) | **FIXED** — the regexes now cover bracket access, destructuring, spread/whole-object passes, `??=`/`\|\|=`, `delete`, and `Object.assign(battle`; the behavioral twin remains the load-bearing guarantee. |
| C-6 | C | info | The golden's provenance was corroborated at the content level only (reviewer isolation). | **CONFIRMED — and closed at SHA level by V2** (read-only `git show 4a8ae54a:…` byte-identical to the reverse-patched cron) | **RECORDED** — the coordinator regenerated the golden from `git show 4a8ae54a:api/cron/agent-evaluate.js` in a scratch tree (zero "Phase B" mentions) and verified it there before copying it in; the generating run fails on purpose (D-10). |
| C-7 | C | info | Under flag-on the stamped entry rides `runShadowTickCapture` (hazard 20). | **REFUTED** (V2 ran a stamped flag-on tick with a manifest and a shadowDiffs-accepting db: the capture wrote its envelope, diff and aggregates — none of the twelve stamp markers appears in any persisted shadow record) | **RECORDED** — Reviewer B read the envelope: it never carries the entry (`resolveTerminalGate` reads two fields); hazard 20 is moot. |
| D-1 | D | medium | The mock db recorded `update()` payloads BY REFERENCE, so every e2e entry assertion could not tell "stamped before the write" from "stamped after"; a stamp block moved after the write passed 18 of 18 behavioral rows. | **CONFIRMED** (V3 re-ran N7 both ways; the deep-copy fix reddens 10 rows with no collateral) | **FIXED** — the mock records `deepClone(payload)` at both write sites; a flag-on row also asserts the persisted store's entry; Reviewer D's N7 mutant now reddens ten rows. |
| D-2 | D | low | The fail-safe's extent was unpinned: a `try` hoisted to wrap only `Object.assign` passed 29 of 29 rows while a `resolveControls` throw at the stamp site would have lost the tick's write. | **CONFIRMED** with a wording correction (V3: the outer catch still releases the lock; the finalUpdate — scores, entry, counters — is what is lost) | **FIXED** — the gates suite throws from `resolveControls` at the stamp site only (the telemetry's and the assembler's calls stay real) and proves the unstamped write; the pins suite requires `try { if (…) { … } } catch` containment by brace matching. |
| D-3 | D | low | The pins row claimed "inside the gate" but asserted index order, not containment; a hollowed gate passed. | **CONFIRMED** (V3: pins 10/10 green on N23; a brace walk from the gate's `{` is safe — only balanced `${}` inside) | **FIXED** — brace-matched containment (`gate < call < assign < gateEnd`, `try` directly before the gate, `catch` directly after it). |
| D-5 | D | info | The `'unknown'` half of the malformed unit row is unreachable via the cron; `mode_not_enforce` and `malformed` had no e2e row. | **PARTIALLY CONFIRMED** (V3: the `'unknown'` id IS reachable through the cron via a non-string thread id — `{ text: 'ok', directiveThreadId: 7 }` — so the unit row keeps that half, re-shaped to the reachable case) | **FIXED** — annotated; a flag-on `text: 42` row and a one-file `ARCHETYPE_INTEGRITY_MODE: 'observe'` suite prove both words end to end (the prompt carries no directive block; the stamp names the withheld thread). |
| D-6 | D | info | A print-only row; ~70 unsilenced cron log lines per run. | **CONFIRMED** (V3 counted ≈ 70) | **FIXED** — the table prints inside the ≤ 1,100 row; the e2e suites silence `console.log` / `console.warn` (the gates suite spies `console.error` for its assertions). |
| D-7 | D | info | The flag-off golden pinned the entry bytes and the finalUpdate key set, not the other values. | **CONFIRMED** (V3: a statusFeed-line leak mutant passed 19/19; two pristine ticks serialize identically at 4,200 B — no normalisation needed) | **FIXED** — the golden now pins the WHOLE finalUpdate byte-for-byte (33 keys; deterministic under the frozen clock). |
| D-8 | D | info | `battleDocGets === 1` was fixture-specific (no marketContext, a meeting pre-set for today). | **CONFIRMED** (V3: with a production-shaped `marketContext` the count is 2 — the regimeAtStart transaction — before and after the model call) | **FIXED** — the row captures the read count AT the model call and asserts zero reads after it. |
| D-9 | D | info | The mock `update()` accepted `undefined`; the SDK rejects it. | **CONFIRMED** (V3 traced the SDK 7.11.6 serializer throw; the throwing mock has no collateral on the pristine tree) | **FIXED** — the mock write throws on any `undefined` (sentinels are leaves), so every e2e row guards the whole write. |
| D-10 | D | info | Under `GENERATE_TICK_STAMPS_GOLDEN=1` the row compared against the file it had just written. | **CONFIRMED** (V3: the row went green against a golden that had absorbed `heard`/`evidence`/`vintages`) | **FIXED** — the generating run writes and then fails on purpose; it refuses to run under `CI`. |

---

## 3. The refutation pass

- **V1 (A-1…A-12): 10 CONFIRMED · 2 PARTIALLY CONFIRMED · 0 REFUTED.** Attacked A-1 where it should have cracked (the macro BTC line, the held-symbol trigger details) — neither did; the macro line reads `prices['BTC-USD.CC']`, a parallel key. A-5's headline "~0.4 %" was cut to "exact `.xx5` literals only; zero divergences on 70,007 computed multipliers" (severity low, fix still free). A-4 was re-proved WITHOUT any mock through a natural production fault. A-11 was re-proved with a live EMERGENCY_SWAP tick. Repros: `scratchpad/verifier-1/tree/api/**/v1.*.test.js`, `round2Sweep.mjs`.
- **V2 (B-1…B-6, C-1…C-7): 11 CONFIRMED · 1 PARTIALLY CONFIRMED (C-1 — the eval lock IS released by the outer catch; what a bare-factory throw loses is the finalUpdate) · 1 REFUTED (C-7 — Reviewer B was right: the shadow envelope never carries the entry; discovery hazard 20 is moot).** V2 disclosed and corrected an instrument error of its own (a first B-4 mutant was "caught" only because its comment carried the pin's grep strings). Notes carried into the fix: hoist all three builders, not just the live block (done); move the two `Math.round` test rows and the two gate-text pins with the code (done). Repros: `scratchpad/verifier-2/tree/api/cron/v2.*.test.js`, `bare/`, `mut-*`, `golden-sim`.
- **V3 (D-1…D-10): 9 CONFIRMED · 1 PARTIALLY CONFIRMED (D-5 — `'unknown'` is reachable via a non-string id, so "drop the 'unknown' half" would have removed a live guard; the row was re-shaped instead) · 0 REFUTED.** V3 confirmed the mock db supports a real autopilot SWAP (the fenced executor's transaction runs; `conviction ≥ 70` clears the validator) and that the pristine tree passes 66/66 under the `undefined`-rejecting mock. Trees: `scratchpad/verifier-3/t-*`.

Two pre-existing items surfaced by V1 for separate tasking: `cronState.totalHaikuCalls` counts a prompt-build throw as an attempt (`agent-evaluate.js`, the `haikuAttempted ? 1 : 0` term); the system prompt's "WARNING risk status" names a verdict the risk manager never produces (`agentEvalPromptAssembly.js:493`, `:696`).

---

## 4. Mutation checks

**The builder's 13 (pre-review, on a `git archive` snapshot; all CAUGHT):** M1 flag gate removed → golden + keys + pins · M2 doc re-read before the resolution → mid-tick fixture + window · M3 argument-list drift → both pin rows · M4 `haikuAttempted` gate dropped → unit + e2e budget_skipped · M5 `rationale` leaks → 7 rows · M6 `weekly` stamped → 5 rows · M7 `risk.reason` on HOLD → 2 rows · M8 `undefined` `signalSource` → 6 rows · M9 suppressed stamped as Heard → 6 rows · M10 (fenced twin, snapshot copy only) `formatRecentEvals` reads `ev.heard` → whitelist + single-reader pins · M11 one byte of the golden → byte row · M12 `fundAsOf` as ms → 5 rows · M13 fail-safe rethrows → fail-safe row.

**Reviewer D's 18 new mutations (on the reviewed snapshot):** 16 CAUGHT (N1 bench leak via `Object.keys(prices)` 10 rows · N2 rounding removed 3 · N3 `nr7` false-not-null 2 · N4 `rankingsAt` from the clock 1 · N5 Heard from the echo 1 · N6 admission on `direction` 7 · N9 `chg` from `priceChange` 2 — now the intended source · N11 `fundAsOf` oldest 4 · N13 `heard` `{null,null}` 5 · N18 lean descriptor 7 · N19 gate `=== false` 1 · N20 extra argument appended 3 · N20b pre-gate drift 2 · N29 detail fallback dropped 1 · N33 new top-level key 1 · N23 hollowed gate — flagOff only) and **2 NOT CAUGHT → D-1 (N7: stamp after the write) and D-2 (N8: fail-safe around `Object.assign` only)** — both fixed and re-checked below.

**Post-fix re-run on the final tree (`git archive e1d19a42`): 20 / 20 CAUGHT** — the 13 originals re-anchored (M1–M13; M11 split into M11a entry-byte and M11b finalUpdate-byte now that the golden pins the whole write), three regression mutants for the review's own fixes (M14 `chg` back to the session change → 3 rows; M15 `Math.round` back → the `toFixed` row; M16 `promptBuilt` set before the build → the pins ordering row and the gates prompt-build row), and Reviewer D's three (N7 → 14 rows now that recorded writes are deep copies; N8 → the pins containment row and the gates `resolveControls` row; N23 → containment + golden + keys). Table with the reddened rows: build report §4.

---

## 5. Probed and found correct (aggregated)

- **Flag off:** the only per-tick code sits inside the gate inside the fail-safe; no object touched, no log; the golden regenerated byte-identical from the pre-stamp cron by Reviewer C (reverse-patched diff) and by the coordinator (`git show 4a8ae54a:…`); deterministic under `TZ=America/Los_Angeles`, `Asia/Kolkata`, `UTC` (explicit `en-US` + zone; the U+202F hazard checked on Node 22 / ICU 78 and CI's Node 20).
- **Flip simulation (C):** `TICK_STAMPS_ENABLED = true` reddens exactly three rows — the pin guard's live-value row (its message names `tickStampsFlags.test.js`), its DARK_BY_DESIGN integrity row, and the pin — while the flag-off, flag-on, gates, pins and unit suites stay green: the docstring's flip map is verified.
- **Rules:** no fenced file in the diff; the fenced assembler's import list unchanged (honesty registry N/A; `agentEvalPromptAssembly.honesty.test.js` green); the composer has zero imports; the cron→flags import pre-existed and its unmocked guard is `agent-evaluate.test.js`; the §2.3 import ratchet baseline unchanged; the stamps ride the existing `battleRef.update(finalUpdate)` (protected-stores allowlist count unchanged, scan green); the pins suite's read + call of the fenced module is §1-permitted.
- **Heard:** effective / descriptor exclusivity; lean-only descriptors ignored; the three words are the resolver's; never the echo (the e2e row feeds a bogus `d1`); `isDirectiveActive` fails OPEN identically on both calls; the kill set equals the assembler's (the lock refreshes `controlEpochLog`, the telemetry syncs in memory before the prompt, nothing reassigns the three inputs before the stamp).
- **Persistence (B):** all stamp-site identifiers are try-body `const`/`let` initialised before the stamp; `rankingsResult` always defined (`allSettled`); downstream readers of `evaluation` read named fields only; the real SDK serializer accepts a dotted symbol key as one literal nested field and the `candidates` array-in-map-in-array; the fail-safe wraps only additive work; key order is append-only and no consumer depends on it; the six early returns are untouched (the cron diff is the import lines and the block).
- **Harness (D):** `filterToLatestSession`'s pass-through stub hides nothing (E1: the real filter gives identical results); the `{ toMillis }` Timestamp stand-in matches the cron's read; no leaked timers; fixtures frozen or factory-fresh; the mid-tick fixture reddens three assertions under any `Object.assign(battle, doc)` after the model call; the argument-list pin catches an appended property, a pre-gate drift, a reorder and a fenced edit (its text-only limit — a rebinding of an identifier — is covered by the flag-on Heard rows).

---

## 6. For the founder — where the build now differs from the spec's letter (each one line to reverse)

1. **`chg` = the row's Gain% from entry** (A-1), not the quote's session change the discovery B8 named. The label ("what the decider saw") decided it. Reverse: one source line + four expectation values.
2. **`rsPct` dropped** (A-2): eight evidence fields, not nine. Reverse: one line + the field list.
3. **`techAt` (an instant) replaces `tech: 'daily'`** (A-3) — the same principle as the ruled "never the word weekly". `rankingsAt` kept. Reverse: two lines.
4. **`fundAsOf` = the FUNDAMENTALS block's header date (held + non-crypto bench)** (A-6), proved equal to the rendered header. Reverse: drop the bench argument.
5. **The gate is `promptBuilt`, not `haikuAttempted`** (A-4 / B-1): D-110 / spec §1.2 should say "gated on the prompt having been built and handed to the transport". One-word ledger amendment.
6. **The evidence and vintages share Heard's gate** (build decision; Reviewer A endorsed: the label is the claim; readers are presence-gated; the alternative is a different product claim needing its own label).
7. **`candidates` uncapped** (B-5): the queue is equally uncapped and the fenced instruction expects 1–3 per day; a cap would be a spec-level rule (e.g. first five). No code.
8. **Spec V1.1 errata** (C-4): `fund:'weekly'` → the ruled shape; "67 bytes" → 47–74 by thread-id length; "beside `hypothesis`" → trailing keys; §1.2's `haikuAttempted` → `promptBuilt`; §1.3's nine → eight (`rsPct`), `tech` → `techAt`. Plus the D-110 → D-115 ledger append (build report §5.6).

## 7. Separate tasking (BUILD_RULES §3 — found outside the task, not fixed)

1. **A-11** — on a risk-swap tick the decider's ACTIVE POSITIONS CSV renders a phantom row for the swapped-out symbol and none for the swapped-in holding (`assetScores` is a `const` computed before the risk layer and never recomputed; `flat.find` falls back to `Unknown`/`support`). Reproduced live by V1. Fenced render, pre-existing.
2. **A-2's prose gap** — the fenced system prompt tells the model it "can see … rsPercentile" for ACTIVE HOLDINGS (`agentEvalPromptAssembly.js:493`, `:696`); the live block renders it for bench names only. Fenced.
3. **V1's two** — `cronState.totalHaikuCalls` counts a prompt-build throw as an attempt; the system prompt's "WARNING risk status" names a verdict the risk manager never produces.
4. **A-12** — `marketDataCache.js:598` materialises `change_p || 0`, so a missing daily change reads as a flat session downstream.
5. **The cron's five pre-existing ESLint errors** (identical on `origin/main`) and the browser-only ESLint globals for `api/` — see the build report §6.1.

## 8. Verification of record (post-fix)

| Check | Result |
|---|---|
| The seven new suites (76 rows) | green |
| Full suite (final tree `e1d19a42`) | `Test Files 629 passed \| 3 skipped (632)` · `Tests 11224 passed \| 64 skipped (11288)` · exit 0 (181.4 s) |
| `vite build` | ✓ built in 41.41 s · exit 0 |
| ESLint | clean on all 12 new / changed files; the cron's five pre-existing errors unchanged |
| Mutation checks | 20 / 20 CAUGHT (§4) |
| Golden provenance | regenerated by the coordinator from `git show 4a8ae54a:api/cron/agent-evaluate.js` in a scratch tree (zero "Phase B" mentions), verified there 4/4, then copied in; independently re-derived by Reviewer C (reverse patch) and V2 (SHA-level) |

*Reviewed adversarially: four lenses, three refuters, thirty-five findings, twenty-two fixed on the branch, none of the dark-merge contract's claims disturbed.*
