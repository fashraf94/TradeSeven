# Backing pre-flip cleanup — multi-lens adversarial review record

**Branch:** `claude/kind-goodall-3f72qi` (the harness-assigned branch; the task prompt named `backing/preflip-cleanup` — see the PR body) · **Reviewed commit:** `6115b63e` (the build: `ca3b08e2` D-af, `8bedfb49` D-ag, `0e6e5fcc` WIRE-R-1, `6115b63e` Amendment C) · **Fix commits:** `997a0fe0` (the coordinator's mutation pass — two rows made able to fail), `48cc1256` (MONEY-1…8), `0eb8de98` (RAWID-1…6 + RAWID-R-2, WIRING-1…14 + WIRING-R-1) · **Base:** `main` (`2a1a16b1`).
**Rule:** BUILD_RULES §2 — review is mandatory at ≥10 files or ≥1,500 lines (this branch at `6115b63e`: 47 files, +3,439 / −357; with the fixes: 57 files, +4,241 / −407). Multi-lens, adversarial, independently refuted, `vite build` run, mutation-checked, written down.
**Reviewer isolation (founder ruling, Sep 2, 2026):** every lens and every refuter worked on its own `git archive 6115b63e` extraction under the session scratchpad (`review/lens-{money,rawid,wiring}`, `review/refute-{money,rawid,wiring}`), `node_modules` symlinked, read-only on git and on the shared working tree; probes and mutants ran only in those trees, and every mutated file was restored byte-identical (sha256 recorded in each report). A read-only `git archive main` extraction (`review/base-main`) served comparisons. The coordinator's mutation checks ran on `git archive` / `tar` copies under `scratchpad/mut/`. No reviewer touched `/home/user/TradeSeven`.

## 0. Verdict

| | |
| --- | --- |
| **Points lost, minted or double-counted through a top-up** (the founder's MONEY brief) | **None found.** 21 lens probe sequences + 9 refuter probes through the real stake route and the real close / settlement / refund primitives: Σ ledger entries = the cached balance and net BP per §2 after every one. |
| **A raw account id on any screen** (the founder's RAW-ID brief) | **None found today.** One MAJOR defect in the name chain (RAWID-1: the player-name rung read a document shape production never writes — the belt held, so teams read "Unnamed team" instead of the player's name) — **fixed**. One path to an id-shaped agent name (RAWID-2 — an agent named `cpu-3` is creatable) — **fixed**. |
| **Findings** | 30 lens items — 1 MAJOR · 7 MINOR · 21 NOTE after refutation, 1 REFUTED (WIRING-2(b)); the refuters raised 6 more (2 in-branch defects, fixed — RAWID-R-2, WIRING-R-1; 3 pre-existing or outside the branch — MONEY-R-1, MONEY-R-2, RAWID-R-1; 1 on a proposed fix — WIRING-R-2). |
| **Fixed in this branch** | Every in-branch CONFIRMED item is fixed or documented, except two cost notes routed to separate tasking (WIRING-6's server half, WIRING-11). Every behavioural fix has a guard row, and every guard row was mutation-checked red (§6). |
| **For separate tasking** | RAWID-R-1 (League surfaces may print raw uids **today** — possibly live), WIRING-15 + WIRING-1's results-reader twin, MONEY-R-1 (the close has no write ceiling), MONEY-R-2 (spec §4 "as of last deploy"), WIRING-10's board-production alignment, WIRING-6 server half, WIRING-11. |
| **For the founder** | Amendment C §C2's "unreachable by construction" holds only below 24 distinct backers on one pod (MONEY-1) — a per-pool cap would be a new product rule, yours to make. New visible copy: "Added {n} BP to your stake.", "Your stake on this team can no longer be added to.", and "…" while a pod's names are on their way (§9). |

## 1. Scope

The prescribed "Backing Pre-Flip Cleanup" build, flags `false` throughout: Amendment C (`docs/FANTASYTRADES_BACKING_BETA_SPEC_V1_3_AMENDMENT_C.md`) — §C1 D-af (a team named by its primary agent; one server-side resolver, `api/_utils/backingTeamLabels.js`; the new `GET /api/backing/team-labels`; the raw-id guard), §C2 D-ag (one stake per team per backer; top-ups; per-request ledger keying), §C4 (the Diversifier line, confirmed) — plus the PR 5 review record's WIRE-R-1 carry-in (the pod list re-reads the viewer's stakes after its own lazy close).

Not in scope and not reviewed as this PR's: desktop layouts, the activation PR, the gate-3 honesty fixes, tournament code (lobby formation untouched), any fenced file (none touched).

## 2. Method

| Lens | Brief | Tree | Agent |
| --- | --- | --- | --- |
| MONEY | The founder's: "lose or double-count points through a top-up" — the stake transaction, the wallet, close / settlement / refund of a topped-up stake, totals and public counters, stats, results, the Sybil split, replays, races, rollovers, out-of-band states | `lens-money` (6115b63e) | 1 read-only reviewer |
| RAW-ID | The founder's: "find a raw id on any screen" — every backing surface in every state, every endpoint response, the resolver's chain and belt, the client helpers and the hook's loading/failure states, and the guard's own coverage | `lens-rawid` (6115b63e) | 1 read-only reviewer |
| WIRING | End-to-end wiring and lifecycle: WIRE-R-1 against the results reader's WIRE-1, resolver batching and board-production parity, the new route's rungs / seal / dark coverage, the client hook's lifecycle, removed fields, BUILD_RULES §4 | `lens-wiring` (6115b63e) | 1 read-only reviewer |
| MUTATION | The prompt's four required checks (§4), then a per-fix guard check for every review fix (§6) | `mut/*` (`git archive` / `tar` copies) | the coordinator |
| REFUTATION | Every finding of each lens handed to a refuter, on its own tree, instructed to refute it with a concrete repro and to assess each proposed fix against the build's constraints | `refute-{money,rawid,wiring}` (6115b63e) | 3 read-only refuters |

Six agents in total (a separate tests lens was not run: the three lenses ran their own mutants in their own trees — 12 RAW-ID, 10 WIRING, 1 MONEY — and the coordinator's pass is the mutating lens, the PR 5 precedent). The full reports are under `scratchpad/review/reports/`; each is reproduced in substance below.

## 3. Findings and dispositions

Severities as the refuter left them. **Disposition** is the coordinator's; the CONFIRMED / REFUTED split is §5.

### MONEY lens — 8 findings (0 blocker / 0 major / 1 minor / 7 notes); all 8 CONFIRMED by the refuter; refuter added 2 (pre-existing)

Founder's brief: "lose or double-count points through a top-up". **No BP lost, minted or double-counted**: the lens drove 21 probe sequences through the real stake route and the real close / settlement / refund primitives on the versioned harness — after every one, Σ ledger entries equalled the wallet's cached `allowanceRemaining`, `careerNet` and `seasons.{m}.net`, and net BP per §2 held; no stake counted twice in the pot, the per-team totals, a payout, a refund, the stats, the results card or the Sybil clusters. The refuter re-ran the probe (21/21, P-lines identical to the lens's transcript by `diff`) and its own (9/9).

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| MONEY-1 | MINOR | The header said the settlement ceiling (120) and the refund stuck state (PR 5 MONEY-4, 96 stakes) are "unreachable by construction". One stake per team caps a backer at 4 documents per pod, but nothing bounds distinct backers: 24 backers × 4 seats = 96 documents holds the refund `stake_ceiling` (and the admin `fromHold` refund); 31 × 4 = 124 holds settlement. Two older sites stated a different wrong threshold ("five backers"). | CONFIRMED (R1a–R1d at the exact boundaries: 23→refunds, 24→holds; 30→settles, 31→holds). Rejected the lens's option (a), a per-pool cap — a new product rule, the founder's call (PR 5 record). Noted the amendment's own "unreachable" rests on "a few dozen at beta scale". | **FIXED (prose)** (`48cc1256`) — `backing-stake.js` header, `backing-settle.js`, `backingSettlement.js` ×2 state the real bound; **for the founder:** Amendment C §C2's "unreachable by construction" holds only below 24 distinct backers on one pod. |
| MONEY-2 | NOTE | `debitsOf`'s docstring claimed it reads a pre-D-ag stake; those live at the old (uid, requestId) id, which nothing reads, so a pre-deploy request replayed after the deploy is a new request (P14: two docs, careerNet −200). Unreachable: the flag has been false on main throughout. | CONFIRMED | **FIXED** (`48cc1256`) — the docstring says what the fallback covers. |
| MONEY-3 | NOTE | A requestId spent on a production pod and reused on a DEV pod is accepted (separate dev ledger) and its `stake_confirmed` overwrote the production record (the event id carried no namespace). Unreachable at HEAD (no `allowDev` caller). | CONFIRMED; the smallest fix namespaces only the dev event id (production ids and their pins unchanged). | **FIXED** (`48cc1256`) — `stake_confirmed:dev:{debitKey}` for a dev pod; row added. |
| MONEY-4 | NOTE (test) | No row caught the RETURNED ledger pre-check moving below `ensureAllowance`: the mutant passed all 105 stake/top-up rows, and on a week rollover commits a refused request's expiry + grant + re-keyed wallet. | CONFIRMED; harm corrected — no spendable BP is lost (the prior week's remainder is already dead under §2), but "a refused request writes nothing" breaks. | **FIXED** (`48cc1256`) — the refuter's row R4 (409, empty `tx.set` log, wallet untouched); reds under the mutant (§6). |
| MONEY-5 | NOTE | The cap on a top-up counted the (userId, weekKey) query, not the document being topped up; a document outside the query (out-of-band `weekKey` drift) let a top-up reach 800 (P13). | CONFIRMED, unreachable; belt verified green. | **FIXED** (`48cc1256`) — `capBase = max(query sum, the document's amount)`; row added. |
| MONEY-6 | NOTE | Stale descriptions of the idempotency key: the write-scanner allowlist note, `backingService.js`, the "normally absent" meta comment. | CONFIRMED + one more (`event.test.js` title). | **FIXED** (`48cc1256`) — all four; the allowlist's pinned count (4) is unchanged. |
| MONEY-7 | NOTE | The Sybil runner scopes `--since` / the newest-N window on `placedAt`, the FIRST placement; a late top-up's address falls outside a `--since` run. | CONFIRMED (R6: a 2-account cluster vanishes from `--since=Fri`). | **FIXED (documentation)** (`48cc1256`) — the runner and runbook say so and point at `--week`; a `lastPlacedAt` field is a larger stake-shape change, not taken. |
| MONEY-8 | NOTE | A topped-up stake's loadout marker used only the first placement's hash. | CONFIRMED and **sharper**: settled back on the first hash, the card said "Loadout unchanged" — false for BP added while the seat showed another loadout (R7). | **FIXED** (`48cc1256`) — `loadoutChangedFor` never answers `false` for a stake with >1 debit (a difference is still "changed"; a match is "not known"); no copy or stake-shape change; row added. |
| MONEY-R-1 | NOTE (pre-existing) | `closePool` has no write ceiling: 25 backers × 4 seats on a deleted pod → 502 writes in one commit (R3); above the platform's 500 the close would throw on every attempt. | Raised by the refuter; **not independently refuted**. Untouched by this branch (D-ag raised the threshold from 5 backers to 25). | **REPORTED for separate tasking** (BUILD_RULES §3); MONEY-1's prose does not claim the belts cover the close. |
| MONEY-R-2 | NOTE (pre-existing) | Spec §4 requires "as of last deploy" whenever the fallback hash is used; the route always uses it and no copy carries the label. | Raised by the refuter; **not independently refuted**. | **REPORTED for separate tasking.** |

Clean (lens, re-verified by the refuter): every read precedes the first buffered write; every refusal after `ensureAllowance` throws; exactly four `tx.set` sites (allowlist pin 4, scanner 9/9); the replay probe (ownership mismatch refuses, same body replays, different amount refuses), racing top-ups and racing replays; same-namespace reuse across pods refused with the store byte-identical; the cap on the total; Σ debits = amount with the first placement's fields riding through; `excluded` survives and `topUps[]` appends; one fold for the totals — a top-up never moves `uniqueBackers`, `teamsBacked` or a team's `backerCount`, and the public document stays byte-identical; the running `private/totals` equals `totalsFromStakes` at close; a top-up racing the close conserves the book in both orders; refunds at close (`insufficient`, `seat_left`) and WIN / LOSS / refund pay or return the total once; week rollover expires the prior remainder once; my-stats, trainer-stats and the results card count one stake at its total; the Sybil split never double-counts.

### RAW-ID lens — 6 findings (1 major / 3 minor / 2 notes); refuter: 5 CONFIRMED, 1 PARTIALLY REFUTED, severities 1 major / 1 minor / 4 notes; refuter added 2

No raw account id reaches a backing screen today: `looksLikeAccountId` refuses blanks, non-strings, known ids, `cpu-N` and 28-character uids; `cpuDisplayName` never returns the id (`cpu-7`+ cycle the six names, malformed ids read "CPU"); every door names teams through the resolver in one batch; the client never composes a name from an id; refusal copy maps codes only; stats name no teams; the preview page walked whole in all 30 states found 0 hits.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| RAWID-1 | **MAJOR** | The player-name rung read `users/{uid}.username / .displayName` at the TOP level; the one writer of that document (`src/firebase/authService.js`, email sign-up and Google sign-in) nests them under `profile`, and the app reads them there (`UserContext.jsx`). On lobby pods (no `seatNames`) the rung never resolved: `secondary` always null, the team card's human half "Unnamed team" ("Back Unnamed team & Shadow"), the reveal filing the player's picks under the agent's name. Every backing fixture seeded the top-level shape, so no row could see it. | CONFIRMED (MAJOR — the main decision surface, every lobby pod; the whole-label "Unnamed team" leg is narrower than the lens said: onboarding forces an agent and agents cannot be deleted). Traced the wrong reading to the League helpers it copied (RAWID-R-1). Fix trial green: 295/295. | **FIXED** (`0eb8de98`) — `profile.displayName`, then `profile.username`, the top level as a legacy fallback; every backing fixture re-seeded with the exact production document; rows pin the nested read (resolver ×2, team card, the guard's "Back ada & Shadow"). |
| RAWID-2 | MINOR | Agent names bypassed the resolver's belt on the team card's agent row, the CTA, the stake title, last week's tape and Your Backing's reveal. | CONFIRMED, trigger UPGRADED: an agent named `cpu-3` can be created through the ordinary onboarding UI (1–20 characters, no id check). The lens's "better still" variant (return the label when `secondary != null`) is WRONG — it would erase every lobby-pod agent name. | **FIXED** (`0eb8de98`) — the card's agent name is the label's own belted name (`layersFor`, one derivation — also WIRING-12); last week's agent name passes the same belt; the reveal's agent half is the server's (RAWID-R-2). Rows added. |
| RAWID-3 | NOTE (lens: MINOR) | Nothing guarded group, stake or agent ids: a pod showing its group id (M3) and a strip row showing a stake id (M15) passed every suite. | CONFIRMED as a coverage gap; NOTE — those ids are outside §C1's "account id" and the prompt's patterns, and none is printed today. | **FIXED** (`0eb8de98`) — the guard also fails on ANY id the world knows (membership over every document-id segment after every door has run, the stake route's minted ids included), with a self-test row; M3 now reds it (§6). |
| RAWID-4 | NOTE (lens: MINOR) | The guard covers fewer states than its header claims; the preview suite's attribute walk skipped each surface root's own attributes (M9 green there). | PARTIALLY REFUTED — the guard meets the prompt's letter and no unbuilt-state mutant escapes the whole suite; two residues remain (the root-attribute hole; non-production-shaped data). | **FIXED (the residues)** (`0eb8de98`) — the preview walk reads the root's own attributes (M9 now red there, §6); production-shaped profiles and a player-name assertion in the guard. More world states: not taken (other suites already catch M5–M8). |
| RAWID-5 | NOTE | The screenshot harness still sent the removed `seatNames` and no labels — re-run, every strip row read "Unnamed team"; the committed PNGs showed pre-D-af names. | CONFIRMED | **FIXED** (`0eb8de98`) — the harness carries the server's shapes; `strip-staked.png` and `strip-week.png` re-rendered (Playwright Chromium, fonts served locally); the other four re-rendered byte-identical. Same as WIRING-7. |
| RAWID-6 | NOTE | Before settlement the label is the owner's CURRENT primary agent, while the reveal names the battle's agent (§9). | CONFIRMED (code shape); the rename trigger REFUTED — `firestore.rules` denies `name` updates; a mid-week second agent needs a crafted client create. | **FIXED (documentation)** (`0eb8de98`) — the resolver's header states the in-play source and the one way the two can differ. |
| RAWID-R-1 | out of branch, **possibly live** | The League's own name helpers (`fetchDisplayNames` in `src/services/tournamentGroupService.js`, `resolveDisplayNames` in `api/_utils/tournamentLeaderboard.js`) read the same top-level fields and answer the raw uid for a production-shaped profile; `leagueAdapter.js` `buildSeat` names a seat `names[id] || id`, and live League components print it — the backing "Open the tape" link leads into one. | Raised by the refuter, with a repro against a stubbed Firestore; not independently refuted. | **REPORTED for separate tasking** (league code — BUILD_RULES §3). The founder can settle it at a glance: a real name beside the "You" tag in the League pod list means production docs carry top-level names; a 28-character id means both are live. |
| RAWID-R-2 | NOTE (in branch) | `{ label, secondary }` cannot say whether a lone label is the agent or the player; Your Backing's reveal assumed the player ("What Shadow and Shadow hold"), and part of it survived the RAWID-1 fix. | Raised by the refuter; its repro ran before and under the RAWID-1 trial. | **FIXED** (`0eb8de98`) — the resolver's `layersFor` names the two layers apart; the team-labels route answers `player` and `agent` beside the label; the reveal reads them (a player the server cannot name reads "Unnamed team" beside the agent). Rows added. |

Outside this lens (classified by the refuter, all out of branch): the Your Backing / strip day-count disagreement (PR 4 logic, byte-identical on `main`); the CPU card CTA naming the CPU twice (PR 4 copy); slot `seatNames` are uncapped client strings ranked above the profile (tournament code — an impersonation/length question, not a raw id; the belt refuses id-shaped values).

### WIRING lens — 15 findings (7 minor / 8 notes); refuter: 14 CONFIRMED + WIRING-2(a) CONFIRMED, WIRING-2(b) REFUTED; two severities lowered (WIRING-1, WIRING-3 → NOTE); refuter added 2

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| WIRING-1 | NOTE (lens: MINOR) | The viewer's stakes are read BEFORE any pool read; a close ANOTHER request commits in between pairs an `insufficient` pool with a `live` stake (`before === after`, no re-read). | CONFIRMED and reachable (a viewer's own Backing screen fires the pod list and the results reader together; 188/300 interleavings stale at a two-viewer close); outside the literal ask. | **FIXED** (`0eb8de98`) — F2's third trigger: a `live` copy the answered pool contradicts (`liveStakeContradicts` — `insufficient` / `refunded` / `resolved`, or a seat missing from the frozen teams) is re-read, whoever moved the pool; zero reads in the steady state (a row pins a closed pool's live stakes at zero reads). The results reader's same ordering is reported with WIRING-15. |
| WIRING-2(a) | MINOR | The list took the pool the close returned only on `closed === true`: a close found already committed (a racing request, or this request's OWN close re-run by the SDK after a lost acknowledgement) left the stale OPEN copy — seats backable, stake `live`. | CONFIRMED — Firestore 7.11.6 re-runs the callback on ABORTED / DEADLINE_EXCEEDED / UNAVAILABLE; R1 reproduced "open + live, every seat backable" against a store holding `insufficient` + `voided`. | **FIXED** (`0eb8de98`) — `if (closed.pool) pool = closed.pool` and `pool = settled.pool ?? pool` (the results reader's rule); row added. |
| WIRING-2(b) | — | Settle-on-read routing to the refund left the reply `closed` + `live`. | **REFUTED** — needs a `complete`, banked pod on a list that only holds the next battle Monday's week; the lens reached it only by faking the group query. | The mirror fix covers it harmlessly. |
| WIRING-3 | NOTE (lens: MINOR) | No row pinned the settle-on-read half of WIRE-R-1 (a mutant hiding it passed 47/47). | CONFIRMED; the proposed row is a real guard. | **FIXED** (`0eb8de98`) — the viewer as a backer in the settle-on-read world, answered `won` with its payout. Green under a settle-trigger-only mutant because F2's contradiction clause re-reads independently; red with the behaviour removed from all three triggers (§6). |
| WIRING-4 | MINOR | `useMyBacking` never re-asked for names when a backed pod's SEATS changed (a seat joining a slot pod, CPUs added at fire). | CONFIRMED, with a sharper path (the viewer's own row reads "Unnamed team · 50 BP" and stays). | **FIXED** (`0eb8de98`) — the names' key is each backed pod's team set (stake teams + live seats) and its settlement. Row added. |
| WIRING-5 | MINOR | While loading, and after any failure, every team read "Unnamed team" (CPU seats included — a regression: `main` named them synchronously); a failure was never retried. | CONFIRMED (the regression for CPU seats; for lobby-pod humans `main` printed a raw id). Recommended the placeholder + retry; rejected naming CPUs client-side (a second source, §9). | **FIXED** (`0eb8de98`) — a pod whose names have not arrived reads the pending placeholder `TEAM_NAME_PENDING` ("…"), never "Unnamed team"; one retry after 3 s; a final failure answers the pod empty (the neutral name). Rows added. |
| WIRING-6 | MINOR (cost) | Mount-time fan-out (6 requests for 5 resolved pods; the pod list landing re-keyed and refetched); each request re-reads the viewer's whole stake history. | CONFIRMED. | **FIXED (client)** (`0eb8de98`) — one request once every backed pod's pool and group snapshot has landed; week keys pruned, not reset. Row added. **The server half** (read by week keys rather than all history) **reported for separate tasking.** |
| WIRING-7 | MINOR | The screenshot harness was not migrated. | CONFIRMED; not in any test run or CI; the PR 4 record re-renders at each visible copy change. | **FIXED** (`0eb8de98`) — see RAWID-5. |
| WIRING-8 | NOTE | The stale-reply guard was correct but unpinned (dropping `if (active)` passed 5/5). | CONFIRMED | **FIXED** (`0eb8de98`) — row added. |
| WIRING-9 | NOTE | The dark suite's service mock had no `fetchTeamLabels` and always answered zero stakes, so it could not see a label request. | CONFIRMED (darkness itself holds — the hook runs only inside flag-gated inner components). | **FIXED** (`0eb8de98`) — the request is counted; a dark row with a backed pod asks for no names, a lit row asks once (the dark row is not vacuous). |
| WIRING-10 | NOTE | The owner → primary agent selection differs from board production in one owner shape: a flagless `casual-agent-{uid}` document sorting first (a pre-rules squat; `firestore.rules` now forbids new ones). | CONFIRMED | **FIXED (documentation)** (`0eb8de98`) — the resolver's header says "board production's selection plus an id-prefix belt" and names the shape; aligning board production is tournament code, **reported for separate tasking**. |
| WIRING-11 | NOTE (cost) | The owner lookup reads every agent document of every owner in full, training clones included. | CONFIRMED; the lens's proposed `.select(…)` mask is UNSAFE (it would null the team card's archetype, approach and counts — no test would notice) and would not reduce the billed read count (WIRING-R-2). | **RECORDED; reported for separate tasking** — no mask added. |
| WIRING-12 | NOTE (§9) | `team.label` and `team.agent.name` were derived differently in one card response. | CONFIRMED, narrower trigger (the lens's 28-character name cannot be entered; `cpu-3` can). | **FIXED** (`0eb8de98`) — see RAWID-2. |
| WIRING-13 | NOTE | Two stale pointers (`src/constants/backing.js`, the team-card test's §4 guard comment). | CONFIRMED | **FIXED** (`0eb8de98`). |
| WIRING-14 | NOTE | `ownerAgentFor` in `team-card.js` had no caller. | CONFIRMED | **FIXED** (`0eb8de98`) — deleted. |
| WIRING-15 | NOTE (pre-existing) | The results reader's catch path never re-reads: a close followed by a settlement failure answers `open` + `live`. | CONFIRMED; OUT OF BRANCH (`loadPod` is byte-identical to `main` but for its return line). | **REPORTED for separate tasking**, with WIRING-1's results-reader twin. |
| WIRING-R-1 | MINOR (in branch) | A failed re-fetch WIPED names already on screen (at the moment a pool settles). | Raised by the refuter (L5 before/after). | **FIXED** (`0eb8de98`) — a failure keeps what is on screen. Row added. |
| WIRING-R-2 | NOTE | WIRING-11's proposed mask would blank the team card. | Raised by the refuter. | **RECORDED** — the mask was not applied. |

Clean (lens, re-verified by the refuter): one `resolveTeamLabels` call per response on all five doors, `in` chunked at 30, profiles by `getAll`; the new route's rungs (security + rate limit → method → auth → the flag's 404 AFTER auth → validation), and it reveals only `{label, secondary}` (now with the two layers) for pods the viewer holds a stake on — mutations T1–T4 all red the dark suite or the ratchet; the ratchet's exact importer lists; no product consumer of the removed `seatNames` / `seatDisplayName`; BUILD_RULES §4 (every new api → src import Node-clean, guarded by a never-mocked test import); the stake reply's `topUp` / `added` / `teamLabel` and the pod list's labels leak nothing sealed (Amendment B's best-placed observer).

## 4. The prompt's four mutation checks

Run by the coordinator on isolated trees (`scratchpad/mut/`), vitest JSON reporter. Two rows were first made able to fail under the defect they name (`997a0fe0`), which is itself a finding of this pass.

| # | Mutant | Named row | Result |
| --- | --- | --- | --- |
| 1 | A second document instead of a top-up — the stake id per REQUEST again (`stakeIdFor(uid, groupId, \`${team}\|${requestId}\`)`) | top-up suite — "ONE-PER-TEAM: backing the same team again TOPS UP that document …" | **RED** — `expected { replay: false, topUp: false, … } to match object { … topUp: true … }`; 31 rows red in all (the max-count row, the LEDGER rows, the racing and replay rows among them). |
| 2 | One ledger entry id reused for two top-ups (`entryKey` and `debits[].entryId` shared by every top-up on the stake) | top-up suite — LEDGER WIN / LOSS / REFUND | **RED** — with the replay belt: `the top-up x-3 must land: expected 409 to be 200`; **with the belt removed, on the ledger-sum assertion itself**: `backer-x: Σ stake entries ≠ −Σ stake documents: expected -200 to be -250`. First run found the LEDGER book topped up only ONCE (a shared id invisible) — **row fixed** (`997a0fe0`): the book now tops up twice, same totals, same payouts. |
| 3 | The cap checked against the top-up amount only (`amount > PER_TEAM_CAP_BP`) | top-up suite — "CAP: a top-up that would take the team past the cap is REFUSED, and nothing is written" | **RED** — `expected 200 to be 409`; 6 rows red. |
| 4 | A uid back in the winner line — the projection (`winnerLabels = winners`) and, separately, the card (`winnerNames = winners`) | the raw-id guard — ROW 1 and ROW 2 | **RED** — offenders `GET /api/backing/results (one pod) $.pod.winnerLabels[0] = "CyHarrison00000000000000000Z"` and `BackingResultsCard (one pod): "CyHarrison00000000000000000Z" in …Winner: CyHarrison00000000000000000Z ‖ Pot 400 BP…`. First run found ROW 2 red on its name check, which ran BEFORE the scan — **row fixed** (`997a0fe0`): the scan asserts first, so the uid is reported by the scan itself. |
| + | WIRE-R-1 without the re-read | pod-list suite — the two WIRE-R-1 rows | **RED** — the stale `live` copy, no `voidReason`. |

## 5. CONFIRMED / REFUTED

| Lens | Items | CONFIRMED | REFUTED | Refuter-raised |
| --- | --- | --- | --- | --- |
| MONEY | 8 | 8 (MONEY-4's harm statement, MONEY-1's fix (a), MONEY-3's fix size and MONEY-8's "skip the query" refuted in part) | 0 | 2 — MONEY-R-1, MONEY-R-2 (pre-existing; not independently refuted) |
| RAW-ID | 6 | 5 + RAWID-4 PARTIALLY REFUTED (RAWID-6's rename trigger refuted, its code shape confirmed) | 0 whole | 2 — RAWID-R-1 (out of branch), RAWID-R-2 (in branch, fixed) |
| WIRING | 16 (WIRING-2 split (a)/(b)) | 15 | 1 — WIRING-2(b) | 2 — WIRING-R-1 (fixed), WIRING-R-2 |
| **Total** | **30** | **29** (1 in part) | **1** | **6** |

## 6. Per-fix guard checks

Every review fix with a row was reverted on an isolated tree (`scratchpad/mut/perfix.py` on `scratchpad/mut/perfix-tree`, a `tar` copy at `0eb8de98`), the guarding rows run, every touched file restored byte-identical (sha256 asserted). A fix counts as guarded only when its NAMED row goes red.

| Fix reverted | Result |
| --- | --- |
| MONEY-3 dev confirmation not namespaced · MONEY-4 returned pre-check below `ensureAllowance` · MONEY-5 cap base from the week query only · MONEY-8 topped-up stake answered "unchanged" | each named row **RED** |
| RAWID-1 profile read at the top level only | **RED** — 16 rows (the two RAWID-1 resolver rows, the team card's lobby row, the `layersFor` row …) |
| RAWID-2 card agent name unbelted · RAWID-2 last-week agent name unbelted · RAWID-R-2 reveal guessing the layers | each named row **RED** |
| RAWID-3 a pod shows its group id (the lens's M3 — green before) · RAWID-4 `cpu-9` in the strip root's aria-label (the lens's M9 — green in the preview suite before) | **RED** (guard ROW 2; the preview D-af row) |
| WIRING-1 no re-read on a contradicted copy · WIRING-2 returned pool ignored | each named row **RED** |
| WIRING-3 settle-on-read hidden from the settle trigger only | **GREEN** — by design: F2's contradiction clause re-reads independently (a resolved pool contradicts a live copy). Hidden from all three triggers: **RED** (the WIRING-3 row and the predicate table). |
| WIRING-4 seats out of the key · WIRING-5 no retry · WIRING-5 names on their way read "Unnamed team" · WIRING-6 no snapshot gate · WIRING-8 superseded reply lands · WIRING-R-1 failure clears the names · WIRING-9 a label request while dark | each named row **RED** |

## 7. Verification

- `npm run test:run` at `0eb8de98` (every fix in) — `Test Files  773 passed | 3 skipped (776)` · `Tests  15094 passed | 64 skipped (15158)`, exit 0 (verbatim from the full log, never through `tail`; the 3 skipped files are pre-existing — the emulator rules suite and two golden captures). At the reviewed commit `6115b63e`: `Test Files  773 passed | 3 skipped (776)` · `Tests  15065 passed | 64 skipped (15129)`. The final HEAD's line is in the PR body.
- `npm run test:rules` (Firestore emulator) — `Test Files  11 passed (11)` · `Tests  242 passed (242)` at `6115b63e`; this branch changes no rules.
- `npx vite build` — `✓ built`, exit 0.
- `npm run lint:gate` — exit 0, zero warnings.
- Fenced files (BUILD_RULES §1): none touched; `.env`, `vercel.json`, `firestore.rules`, `firestore.indexes.json`: none touched. Flags: unchanged (`BACKING_BETA_ENABLED = false`; `featureFlags.js` changes a docstring only).

## 8. Reported for separate tasking (BUILD_RULES §3 — found, not fixed here)

1. **RAWID-R-1 — possibly LIVE:** the League's own name helpers (`src/services/tournamentGroupService.js` `fetchDisplayNames`, `api/_utils/tournamentLeaderboard.js` `resolveDisplayNames`) read top-level profile fields that the one writer of `users/{uid}` never writes, and `leagueAdapter.js` `buildSeat` falls back to the id — League surfaces may name human seats by raw uid today.
2. **WIRING-15 + WIRING-1's twin:** `api/backing/results.js` — the catch path never re-reads the viewer's stakes, and the stakes are read before the pool (the pod list now handles both).
3. **MONEY-R-1:** `closePool` has no write ceiling — 25 backers × 4 seats on a deleted pod is 502 writes in one commit (over the platform's 500, ASSUMED); the close would throw on every attempt.
4. **MONEY-R-2:** spec §4 requires "as of last deploy" whenever the fallback hash is used; the route always uses it and no copy carries the label.
5. **WIRING-10:** align board production's owner → agent selection with the resolver's id-prefix belt (tournament code).
6. **WIRING-6 (server half) and WIRING-11:** `GET /api/backing/team-labels` reads the viewer's whole stake history; the owner lookup reads every agent document in full (a field mask as proposed is unsafe — WIRING-R-2).

## 9. For the founder

- **Amendment C §C2's "unreachable by construction" holds only at beta scale** (MONEY-1): one stake per team caps a backer at four documents per pod, but nothing bounds distinct backers — 24 backers each backing all four seats reach the refund's stuck state, 31 the settlement ceiling. The code now says so; a per-pool cap would be a new rule, yours to make.
- **New visible copy** from this build and its review: the top-up line "Adds to your {n} BP on {label}." (Amendment C's own); "Added {n} BP to your stake." (Backed state of a top-up); "Your stake on this team can no longer be added to." (refusal); "…" while a pod's names are on their way.
- **RAWID-R-1** can be checked at a glance in production: a real name beside the "You" tag in the League pod list, or a 28-character id.
- **Screenshots:** `docs/design/backing/screenshots/strip-staked.png` and `strip-week.png` re-rendered with agent-named teams.
