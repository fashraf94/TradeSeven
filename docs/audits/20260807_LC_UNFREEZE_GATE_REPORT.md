# League Lifecycle L-C — Advancement-Unfreeze Gate Report

**Date:** 2026-08-07 · **Branch:** `claude/league-lc-unfreeze-gate` (fresh off `origin/main` @ `01e11501` — the L-B merge, PR #721) · clean tree.
**Preamble (BUILD_RULES §3):** `git fetch origin` run first (recorded: `origin/main` `0b400974 → 01e11501`). **READ-ONLY** — no code edited, `TOURNAMENT_ADVANCEMENT_FROZEN` **not touched** (the flip is a separate one-line PR after founder confirmation). Every code claim was read at this HEAD this session and is **VERIFIED**; anything requiring production data is marked **NEEDS-LIVE-DATA** and is **not fabricated**. Findings cross-checked by a 3-lane adversarial read-only workflow (one lane tasked to *refute* the precondition-1 STOP).

---

## Executive verdict

| # | Precondition | Verdict |
|---|---|---|
| **1** | **The §7 scoring fix landed.** | ❌ **NOT MET — the blocker.** No §7 scoring-model fix exists in main (or on any branch). The anomaly was never even adjudicated: the decisive live decomposition was BLOCKED for lack of credentials, leaving "real defect vs broken-window" undecided. **Do not flip on this precondition as written.** |
| **2** | **No poisoned cohort remains in BATTLE.** | ⚠️ **NEEDS-LIVE-DATA (founder-run).** Cannot be enumerated from the repo (no Firestore creds here — same block as the anomaly's Phase 0.5). Exact read-only method provided. The known cohort `lds_wed-1900_2026-07-22` is VOIDED and is provably excluded from a `status=='battle'` query, so L-A removed it. Whether *other* pre-fix BATTLE groups exist is unverifiable here. |
| **3** | **Day-5-carried status at flip.** | ⚠️ **NEEDS-LIVE-DATA (founder-run).** The predicate is `isFinalSnapshotDegraded(group)` per BATTLE group; runs in the same read-only pass as #2. Not a blocker (the pause is designed post-B-F3), but the operator must know going in. |
| **4** | **Flip inert on a zero-BATTLE state.** | ✅ **VERIFIED (conditional).** With zero `status=='battle'` groups AND zero active brackets, flipping the flag is a **write no-op** — every withheld consumer iterates over an empty set. Test-locked. First real effect is deferred to the next cohort's day-5 Friday close. *Conditional on #2's live count being zero.* |

**Bottom line.** Precondition **4 holds** (the flip is mechanically safe on an empty board), and precondition **2's known cohort is handled** (VOIDED). But precondition **1 is not met and cannot be met from code**: the freeze was a tourniquet explicitly awaiting "a separate §7 pass," that pass **never happened**, and the diagnosis that would tell us whether a fix is even *needed* (FORK-1 broken-window vs FORK-2 real-defect) was **blocked on credentials** and remains open. **Recommend HOLD the flip** until (a) the founder runs the live read-only pass (preconditions 2/3 + the anomaly's A1 decomposition) and (b) the FORK is adjudicated — because the unfreeze's safety depends on whether any surviving or future BATTLE group carries a genuine scoring defect, which is exactly what is unknown.

---

## Precondition 1 — The §7 scoring fix (the blocker), in detail

**Locating the audit.** The freeze rationale (`src/config/featureFlags.js:931-954`) points at `docs/audits/LEAGUE_SCORING_ANOMALY_*`. **Those files are not in main.** They exist only on the unmerged branch `origin/claude/league-scoring-anomaly-v6b19j`, and that branch is **docs-only** (three reports, 336 insertions, **zero code**):
- `LEAGUE_SCORING_ANOMALY_DISCOVERY_PHASE0_REPORT.md` (2026-07-18) — read-only diagnosis.
- `LEAGUE_SCORING_ANOMALY_PHASE0_5_LIVE_REPORT.md` (2026-07-18) — the live adjudication, **⛔ BLOCKED — no creds**.
- `LEAGUE_ADVANCEMENT_FREEZE_PHASE0_DISCOVERY.md` (2026-07-19) — the freeze-design discovery.

**The exact fix it required — never determined.** Phase 0 refuted all six suspected corruption mechanisms (H1–H6) on the code and localized the likely driver to **legitimate-but-unbounded accumulation on the agent half** of the composite — chiefly the agent `bankedBadgePoints` daily re-bank (`api/cron/agent-daily-scores.js:176`, a nightly `FieldValue.increment`, uncapped by the 5-day length) over a **calendar-decoupled** day index. But it explicitly deferred the fix: *"Whether that is 'working as designed' or the bug is a founder scoring-model call, gated behind A1"* — the A1 agent/user decomposition — which **requires live Firestore and was never run** (Phase 0.5: `FIREBASE_*` / `EODHD_API_KEY` all UNSET, no service-account JSON, `firebase-admin` not installed). The fork is stated verbatim as **UNADJUDICATED**: FORK-1 (honest arithmetic over a broken accumulation window) vs FORK-2 (a real defect). No fix could be, or was, specified.

**Verifying "merged and deployed" — nothing to verify.** An adversarial history search (`4975af54` = the freeze-introducing commit, 2026-07-19 … `01e11501`) genuinely tried to *find* a scoring-model fix and could not:
- `git diff 4975af54 origin/main --` over the three Phase-0 driver files (`api/cron/agent-daily-scores.js`, `api/_utils/agentScoring.js`, `src/utils/baggerBombUtils.js`) is **EMPTY** — byte-identical. The uncapped nightly accumulator at `agent-daily-scores.js:176` is unchanged.
- `computeComposite` (`src/constants/leagueTournament.js:802-804`) is still `agentPoints + USER_LAYER_K × userPoints`; `USER_LAYER_K` unchanged.
- `-S`/`-G` over the range for `bankedBadgePoints`/`penalty`/`composite`/`elapsedDay` returns only L-A (void), L-B (recurrence guards), EODHD-containment *tests*, docs, and merges — **no scoring-model change**.

**L-B is not the scoring fix (and does not bound the driver).** L-B Guard 1 (`tournamentBanking.js:136-137`) skips `computeBankingUpdate` once the **group's** `dayN ≥ WEEK_DAYS_REQUIRED` — it caps the group `dailyScores` doc from growing to day-6+ (the zombie *recurrence*). But the agent score it consumes, `fetchGroupAgentScores` (`tournamentBanking.js:61-89`), sums `scoreState.currentScore` across **all** the group's tournament agentBattles "regardless of status" — the live, uncapped agent sum, keyed off nothing about the group's `dayN`. L-B's own commit (`639f0bde`) states "`TOURNAMENT_ADVANCEMENT_FROZEN` untouched … no `scoreState`/`createAgentBattle`/`closeScores` shape change." And the L-B B-F3 closeout (`3a8f82ab`, whose L-C gate note governs this report) lists "confirm the §7 scoring fix landed" as a **still-open** precondition. So L-A (disposition) + L-B (recurrence prevention) are the two remediations that landed; **neither is the scoring-model fix**, and the freeze rationale keeps that fix future-tense.

**Can precondition 1 be satisfied without the A1 run? No.** Current code *does* cap the agent day index (`getCurrentTradingDayServer`, `agentEvalPromptAssembly.js:1126-1140`, returns `min(position, tradingDays.length)`; per-`dayKey` idempotency at `agent-daily-scores.js:55`; non-trading-day skip at `:219`), so within a well-formed 5-entry window `bankedBadgePoints` takes ≤5 increments — which makes **FORK-1 plausible**. But plausible ≠ confirmed: whether the poisoned composites came from an over-long/decoupled window (FORK-1, now closed by the cron/window repair + L-B's clamp) or a logic defect in the accumulate/reset (FORK-2, still live) is **undecidable from code** — it needs the live per-`dayKey` `breakdown` of the poisoned docs, i.e. the blocked A1 decomposition.

**The flag sub-question (explicitly asked).** No part of the scoring fix hides behind `LEAGUE_LIVE_ORB_ENABLED` — that flag (`featureFlags.js:280-282`) is **display-only, client-only, effectively OFF in production** (`window` + `?leagueLiveOrb=1` gated), controlling a your-seat live-orb *estimate* that "byte-identical when off" and never persists to `closeScores` (Phase 0, `buildArenaModel.js`). More to the point: there is **no scoring-fix code at all** — flagged or unflagged — so the unfreeze is **not** blocked on another flip; it is blocked on **work that has not been done** (the §7 pass) and **a diagnosis that was never completed** (A1). `LEAGUE_CANONICAL_OPEN_CAPTURE = true` (`featureFlags.js:333`) is the baseline-capture policy, also unrelated to the anomaly.

## Precondition 2 — No poisoned cohort in BATTLE (live-run method)

**Cannot be run here** (no Admin-SDK creds — verified: `firebaseAdmin.js:15-21` needs `FIREBASE_PROJECT_ID`/`CLIENT_EMAIL`/`PRIVATE_KEY`, none set; same block Phase 0.5 recorded). The **exact read-only method**:
1. **Enumerate:** `fetchEligibleGroupsByStatus(db, GROUP_STATUS.BATTLE, { excludeTraining: true })` (`api/_utils/tournamentGroupService.js:301-315`; body `db.collection('tournamentGroups').where('status','==','battle').get()`). Pass `excludeTraining:true` — the frozen consumers are the **ranked, non-training** population (`featureFlags.js:934-940`). Collection/value: `TOURNAMENT_GROUPS_COLLECTION='tournamentGroups'` (`leagueTournament.js:18`), `GROUP_STATUS.BATTLE='battle'` (`:93`). Precedent read-only runners that already stand up `db`: `scripts/canonical-open-baseline-census.js`, `scripts/lifecycle-void-precheck.js`.
2. **"Predates the scoring fix" — a founder judgment, not a coded predicate.** There is **no scoring-fix stamp field** anywhere in the repo (grep for `fixLandedAt`/`scoringFix`/`2026-07-15` returns only unrelated hits), *because there is no fix*. `baselinePolicy` is **not** the discriminator (`BASELINE_POLICY.CANONICAL_OPEN` tracks the unrelated 2026-07-18 canonical-open flip). The only per-group temporal signals that exist are `createdAt` (ISO, `lifecycle-void-precheck.js:63`) and the `dailyScores.dayN.recordedDate` range (`:74-76`). So per BATTLE group, report `createdAt` + min/max `recordedDate`, and the founder decides whether that window predates whatever fix eventually deploys.
3. **The known cohort is already excluded.** `voidGroup` writes `status:'voided'` (`tournamentGroupService.js:258`), a distinct terminal status (transition map `:57,:60`), and the enumeration is exact-equality `where('status','==','battle')` — so a voided doc is **structurally unreturnable**. `lds_wed-1900_2026-07-22` (voided in L-A) cannot appear; no special-casing needed. **Expected result:** if L-A voided the only poisoned cohort and no new cohort has formed, this list is **empty**. If any BATTLE group predates the fix window → **STOP; disposition it before the flip** (per the freeze rationale, an un-voided pre-fix group would lock at its unfreeze-day standing, not day 5).

## Precondition 3 — Day-5-carried at flip (live-run method)

For each BATTLE group from #2, evaluate **`isFinalSnapshotDegraded(group)`** (`src/constants/leagueTournament.js:1301-1308`): true iff `getLatestBankedDayEntry(group)?.entry?.agentScoresCarried === true`. `getLatestBankedDayEntry` (`:1228-1242`) is the L-B week clamp (`n ≤ WEEK_DAYS_REQUIRED=5`, `:1274`), so it reads the **week's day-5** snapshot even if a stalled finalizer banked a contaminated day-6+. `agentScoresCarried` is the §7.2 degrade marker (agent layer carried/zeroed, not freshly read). Per the B-F3 ruling, any such group **pauses under §7.2 with no self-heal** after unfreeze — **not a blocker, but list every such group by id** so the flip is made knowingly. Pure predicate; runs in the same read-only pass as #2.

## Precondition 4 — What the flip does on current data (VERIFIED)

**With zero `status=='battle'` groups + zero active brackets, the flip is a write no-op.** Trace of `runFridayAdvancement` (`api/_utils/tournamentAdvancement.js:236`) at current HEAD:
- Fetch (`:241`) → `fetchEligibleGroupsByStatus(BATTLE)` returns `[]` on an empty snapshot (`tournamentGroupService.js:313`) → `summary.groups=0`.
- Frozen block (`:282`, flag ON today): `for (const group of groups)` over `[]` → returns at `:300` before the bracket sweep — flag-on issues exactly one read.
- **Non-frozen path (flag OFF, the flip):** base-layer loop (`:307`, `baseGroups=[]`), cohort/advance loop (`:363-386`, empty Map → `advanceCohort` never called → no bracket lock/finalScores/composition/rank), active-bracket sweep (`:392`, `status==ACTIVE` query → `[]` with zero brackets → no `finalizeRound`/champion/recap). Return (`:431`) with every write-counter zero. The lazy rankings read (`:304`) is never invoked.
- **Consumers the flip turns from withheld → live** (`featureFlags.js:936-947`; guard `tournamentAdvancement.js:264-270`): bracket locks + `finalScores`; the career-rank ratchet (`appliedGroups`, in `runWeekSideEffects`); the seasonal-leaderboard final upsert (`tournamentLeaderboard.js:242-252`, covering the nightly aggregate `:345` **and** the manual bank endpoint); next-round composition; the champion write; and the second defense-in-depth belt (`tournamentAdvancement.js:462-465`). **Each is reached only through the base-layer / cohort / sweep loops — all empty on a zero-BATTLE + zero-bracket state.**
- **Test-locked:** `tournamentAdvancement.test.js:281` ('zero groups — production state at merge', flag-OFF) → `summary.groups===0`, zero writes; `tournamentAdvancementFreeze.test.js:152` (flag-ON) → day-5 battle groups withheld, `writeLog===[]`, groups stay BATTLE; second belt asserted `:257-268`.

**One honest nuance:** the flip is a **write** no-op, not a byte-identical *execution* — flag-off issues one extra READ (the `status==ACTIVE` brackets query the frozen early-return skips). Zero active brackets → zero writes. **First real effect** of the flip: the next newly-formed cohort's **day-5 Friday close** (`isWeekBanked` on a Friday `FRIDAY_ADVANCEMENT` tick) — the first lock/rank/leaderboard-final/compose/crown. The real verification, as the founder noted, is that Friday: finalizer seals at day 5, `RoundBoundaryView` lights, THE FIELD populates.

**Caveat (the conditional):** this inertness is proven *conditional on* zero battle groups + zero active brackets — the **actual live count is precondition #2**, unverifiable here. If #2 returns a live BATTLE group, re-read this section: a day-5-banked ranked group present at flip **would** lock/rank/upsert on the next Friday tick, at its latest-banked (L-B-clamped day-5) composite.

---

## Recommendation (founder decision — nothing executed)

1. **HOLD the flip.** Precondition 1 is unmet in the strong sense: no scoring fix exists, and whether one is *needed* is unadjudicated. Flipping now bets the whole freeze on FORK-1 being true without having run the test that decides it.
2. **Run the live read-only pass** (a small runner beside `canonical-open-baseline-census.js`, off-tree per the Phase 0.5 precedent, `.get()`-only): for every `status=='battle'` non-training group print `{ id, createdAt, min/max dailyScores.recordedDate, isFinalSnapshotDegraded(group) }` — this closes preconditions **2 and 3** in one shot. Expected: empty (L-A voided the only known cohort).
3. **Adjudicate the FORK** (the anomaly's A1 decomposition) on the poisoned docs' live `bankedBadgePoints.breakdown` — the Phase 0.5 script `phase0_5_live_queries.mjs` is the ready tool. If FORK-1 (broken window, now closed by the cron/window repair + L-B's day-5 clamp) → the §7 pass may reduce to "confirm no model change needed" and the unfreeze can proceed once #2 is empty. If FORK-2 (real defect) → the §7 scoring fix must land before unfreeze.
4. **Then** flip in the separate one-line PR — safe on an empty board (precondition 4), and re-checking #2/#3 immediately before, since a cohort could form in the interim.

**HARD STOP.** No flip, no code, no fence contact, `TOURNAMENT_ADVANCEMENT_FROZEN` untouched. Awaiting founder direction.
