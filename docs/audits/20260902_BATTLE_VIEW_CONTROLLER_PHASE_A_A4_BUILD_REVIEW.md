# Battle View controller — Phase A (A4, the layout) adversarial build review

**Date:** September 2, 2026
**Status:** BUILD_RULES §2 review record (the cumulative Phase A diff is 52+ files; the A4 diff alone 21 files / +1716 / −126). Findings, dispositions and the CONFIRMED / REFUTED split — three refuters complete (A: the code and lifecycle findings; B: the honesty and cross-phase findings; C: the byte-identity notes and the mutation-lens gaps).
**Branch:** `claude/battle-view-phase-a-layout-425nnm` (the A4 session's harness branch — `…-v5gog5` rebased onto `def0fcbe` + A4; D-63 carries the note).
**Diff reviewed:** `ce574d46..e1db2e08` — A4.0 `59c67a6c` · A4.1 `89b07b37` · A4.2 `4a639fd6` · A4.3 `e1db2e08`. **Fixes:** `958c5f17` (the findings), `23717042` (the L4 guards), `7f480fb9` (refuter A's sharpenings); the docs `8a789ffe` and this record.
**Prepared by:** Claude Code (coordinator), under `docs/BUILD_RULES.md` §2.

## 0. Method

- **Isolation (BUILD_RULES §2, Sep 2 ruling):** every reviewer and refuter worked on `git archive e1db2e08` extracted under the session scratchpad with `node_modules` symlinked, read-only on git and on the working tree; every mutation and probe ran in a private copy of that snapshot (each verified restored or left in the private copy only). A git worktree of the pre-build commit `eaf2a0e2` served the byte-identity lens and the golden's provenance checks. The refuters judged the ORIGINAL snapshot; the fixes below landed in the working tree afterwards and were re-run.
- **Five independent lenses**, each a separate subagent with its own brief: **L1** domain correctness and honesty (copy rules, hazards 1–23, D-63 → D-68, hostile renders); **L2** wiring and lifecycle (the sheet's detent machine and focus moves, the unread effect, the door, Game Tape, StrictMode, the breakpoint crossing, listeners, SSR); **L3** flag-off byte-identity (the golden's provenance; renderToString matrices in BOTH trees; flag-off listeners); **L4** test integrity (62 mutations in a private extraction, each recorded by failing-test name); **L5** cross-phase consistency, the fence, the seed checklist item by item, tokens and motion, the strings inventory, the DO-NOTs. Plus the environment's `/code-review` skill at effort `high` — **a single-pass inline review without the subagent fan-out or its own verify pass (it says so itself)** — recorded as a sixth input, not as the §2 review.
- **Refutation:** every finding was handed to a refuter with the reviewer's repro (or a sharper one) against the untouched snapshot. Refuter A additionally laid out the real DOM in **Chromium 141 with the production stylesheet** (a `vite build` of the snapshot — the same asset hash as the founder's build) for the two painting claims jsdom cannot show. Verdicts below.
- **Build and suite:** `vite build` green at `e1db2e08` and after every fix commit; full unit suite 566 files / 9,524 tests green at `e1db2e08`, **568 files / 9,605 tests green at `7f480fb9`** (2 files and 63 tests skipped by design — the env-gated capture harness among them). `flagPinGuard`, `deskHonesty`, `tokens.guard`, `motion.guard` green throughout; the flag-off golden green at every step.

## 1. Executive verdict

| Lens | Raised | CONFIRMED | REFUTED | Fixed / guarded | Recorded (copy / ruling / deferral) |
|---|---|---|---|---|---|
| `/code-review` | 6 | 6 (refuter A) | 0 | 6 (CR1–CR6) | — |
| L1 domain / honesty | 9 | 9 (refuter B) | 0 | 4 (F1 code; F3, F7 rows; F9) | F2 (copy request), F4, F5, F6, F8 |
| L2 wiring / lifecycle | 12 | 12 (refuter A) | 0 | 9 (F1, F2, F3, F5, F6 = CR; F7, F8a, F9 harness, F10, F11) | F4 (the crossing), F8b, F12 |
| L3 byte-identity | 0 + 6 notes | pending (refuter C in progress) | pending | — | N1–N6 (no action) |
| L4 test integrity | 62 mutations → 15 MINOR gaps + 5 notes | pending (refuter C in progress) | pending | 14 gaps closed (rows / guards / the harness) | M9a, M10b, M14, M24, M50 (recorded limits) |
| L5 fence / cross-phase | 0 + 6 notes | 6 (refuter B) | 0 | 4 (N2, N3, N5, N6) + N1 (the docs commit) | N4 |

**Totals:** 33 of the 59 items raised have a refuter verdict so far — 33 CONFIRMED, 0 REFUTED (refuters A and B); the 26 L3 / L4 items are with refuter C and this table is updated when it reports. Every finding that named a defect was fixed with a row that fails under it; every note is recorded here or in the handover's CONSTRAINED list (items 19–36).

**Bottom line:** no finding touched the fence, a battle-doc write, a model call, or flag-off byte-identity (L3: 122,820 rendered cases per tree, the only differences the sanctioned D-62 Desk cells; the golden re-captured identically by two reviewers and by the committed harness). The confirmed defects were all inside the flag and all in the new layout: the mobile sheet trapped inside the layout's stacking context (the header painted over its handle at full — measured in Chromium), a fixed peek height that clipped the composer's send button (28 px even with an empty draft under the production cascade), `document.body` accepted as a return-focus target, a focus steal on mount from the Game Tape effect, a header button whose short name hid the scores from assistive tech, a length-keyed unread mark that goes silent at the server's 100-entry feed cap, and — the one copy defect — an outage label that said "timed out" for five failure classes that are not timeouts. Every one is fixed with a guard that fails under the defect.

## 2. Findings — CONFIRMED, fixed

| # | Sev | Where (at `e1db2e08`) | Claim | Refuter evidence | Fix (commit) | Guard |
|---|---|---|---|---|---|---|
| CR1 = L2-F1 | MAJOR | `AgentBattleScreen.jsx:1430-1441, :1475`; `ChatSheet.jsx:118-134` | The `position: fixed; z-index: 20` sheet is a descendant of the layout container (`position: relative; z-index: 2`), so the root-level header (`z-index: 3`) paints over the sheet's handle at the full detent; a tap there opens the book panel. | Refuter A, Chromium + production CSS at 390×800, sheet at full, scroll 0: handle row 56–97 px, top section 0–142 px; `elementFromPoint` at the cycle, the ▾ and the handle centre all return the header's `Why? · the whole book` button; clearing the layout's z-index flips all three hits to the handle. | The sheet renders at the ROOT, a sibling of the layout container, and hides with the page under the tape (`958c5f17`). | `layout.jsdom` mobile: the sheet is not inside `[data-layout]`; `visibility: hidden` under the tape |
| CR3 = L2-F6 | MINOR | `useChatSheet.js:32` (`SHEET_PEEK_PX = 148`); `ChatSheet.jsx` (`overflow: hidden`) | A fixed peek budget clips the composer's send button once the textarea auto-grows, an error row or the counter appears. | Refuter A: the reviewers' border-box / preflight arithmetic was wrong — the production CSS has NO Tailwind preflight and forces every button / textarea to 16 px — and the finding UNDERSTATES it: measured 39 + 20 + 24 + 105 = 188 px of content in a 148 px section, the send button clipped 28 of 42 px with an EMPTY one-line draft; hidden entirely with an error row or three lines. | Peek is `height: auto` around the handle and the composer; the chat collapses its message list at peek (`listCollapsed`); `SHEET_PEEK_PX = 172` (the measured base) floors the other detents and the board's clearance (`958c5f17`, `7f480fb9`). | `layout.jsdom`: `data-sheet-height="auto"` at peek, the list `display: none`; px at half; `ChatSheet.motion` |
| CR4 = L2-F3 | MINOR | `ChatSheet.jsx:88-93` | On collapse `backIsOutside` accepts `document.body` (Safari / touch leave it focused after a tap), so `body.focus()` no-ops and focus stays in the composer instead of the handle. | Refuter A: `body.focus()` leaves the textarea focused; the predicate is true for body; the reviewer's scenario reproduces (door with nothing focused → half → ▾ → activeElement still the textarea). | `document.body` is never a return target — the handle is (`958c5f17`). | `layout.jsdom`: "a pointer that left nothing focused" |
| CR6 = L2-F2 | MINOR (latent) | `AgentBattleScreen.jsx:983-996` | The Game Tape focus effect's closed branch runs on mount and focuses the header link on any first commit that renders the header (jsdom mounts do; production's first commit is the spinner today). | Refuter A: `activeElement === [data-game-tape-link]` immediately after mount at both breakpoints; no steal when the first commit is the loading spinner. | The effect acts only after an OPEN (`gameTapeWasOpenRef`) (`958c5f17`). | `layout.jsdom`: "mounting steals no focus" |
| CR2 = L2-F5 | MINOR (a11y) | `AgentBattleScreen.jsx:229-239` | `aria-label` on the `role=button` header replaces its name and, button children being presentational, the names / scores / day label leave the accessibility tree; no description. | Refuter A: `aria-describedby` null; `Aurora`, `CPU`, `1 trade` inside the button with no other accessible home on the page. | The header button is DESCRIBED by the names / day / CPU blocks it contains (ids under the flag) (`958c5f17`). | `layout.jsdom`: "the score header button is named for the book and DESCRIBED…" |
| CR5 | simplification | `TacticalRow.jsx:419-447` | The 10-prop `<ProximityLabel>` element duplicated in both arms of the wrapper conditional. | Refuter A: the two blocks byte-identical after whitespace normalisation. | One element, wrapped under the flag (`958c5f17`). | the golden (flag-off) |
| L1-F1 | MAJOR | `battleViewCopy.js:68`; `selectWhyState.js:81-83` | `… · the evaluation timed out` rendered for EVERY `haikuError`, but the cron persists `failureClass ∈ {timeout, budget_skipped, truncated_response, <http status>, <error name>, unknown}` — one is a timeout; the seed's premise "the Pulse already states it" is false (the Pulse renders `Evaluation engine degraded this tick ({failureClass})`). | Refuter B: all three refutation routes fail — `agentEvalTransport.js:48-65` returns `timeout` only for timeout / abort shapes; `agent-evaluate.js:1968-1975` (`budget_skipped` = the call never started), `:2020-2025` (`truncated_response` = a response arrived); `:2667` persists every class; `StatusFeedTimeline.jsx:168-176` renders the class verbatim; no "timed out" anywhere evaluation-side. | Only `failureClass === 'timeout'` earns the timeout words; every other outage keeps the plain absence label; a class-neutral line is a copy request (handover item 20; ledger D-65 amended) (`958c5f17`). | `selectWhyState.test.js` (nine classes), `WhyPanel.render.test.jsx` |
| L1-F9 | NOTE | `useChatSheet.js:71-83`; `ChatSheet.jsx:116-117` | At half / full the sheet's height tweens (0.3 s) on every `resize` — mobile toolbars fire it while the player scrolls. | Refuter B: the `resize` listener feeds `viewportHeight`, the height is a function of it, framer re-animates a changed `animate` value through `smooth`; nothing gates or debounces. | The height tweens on a DETENT change only; a viewport change re-sizes through `instant` (`958c5f17`). | `ChatSheet.motion.jsdom.test.jsx` |
| L2-F7 | NOTE | `ChatSheet.jsx:70, :159` | `transition={gesture}` on the drag grabber is inert — framer builds the release from `dragTransition`. | Refuter A: framer 12 `VisualElementDragControls.mjs:274-305` reads only `dragTransition`; a 10-second linear tween on the pill and none behave identically. | The `gesture` spring rides `dragTransition` (`{ bounceStiffness, bounceDamping }`); nothing under reduced motion (`958c5f17`). | `ChatSheet.motion.jsdom.test.jsx` |
| L2-F8 (a) | NOTE → defect | `AgentBattleScreen.jsx:878-885` | A feed that shrinks then regrows below the seen count shows no dot — "append-only, so a note". | Refuter A SHARPENED it: the server caps `statusFeed` at 100 (`agent-evaluate.js:672`, `.slice(-STATUS_FEED_CAP)` at five sites) while `decide.js`, `ensure-opener.js` and the trade narration `arrayUnion` past the cap, so the feed plateaus and shrinks in production; once at the cap a length-keyed dot never lights again (probe: a rolled feed → `data-unread="false"`). | The seen mark records the length AND the newest entry's stamp; the effect's deps are the seed's plus that stamp (`7f480fb9`; handover item 28). | `layout.jsdom`: "at the server's feed cap…"; the source row pins the deps |
| L2-F10 | NOTE | `AgentBattleScreen.jsx:1162-1221` | No body-scroll lock under the tape overlay — the document (the board) scrolls beneath it. | Refuter A, Chromium: a wheel of 300 over the overlay → `window.scrollY` 300. | The document scroll is locked while the tape is up, restored on close / unmount (`958c5f17`). | `layout.jsdom`: `document.body.style.overflow` under the tape and after |
| L2-F11 | NOTE | `AgentBattleScreen.jsx:1230`, `:123-134`; `useChatSheet.js:71-83` | (a) the desktop branch is chosen by width alone — landscape phones get `height: 100vh`, the large viewport on iOS; (b) `innerHeight` does not track iOS's keyboard (`visualViewport` does). | Refuter A: code anchors verified; the device behaviour accepted from documented viewport-unit semantics (pages blocked by the proxy; marked from memory). | The visible viewport (`visualViewport.height` where present) sizes the sheet AND the desktop page (px, not `100vh`) (`958c5f17`). | `layout.jsdom`: a resize re-sizes an open sheet; `useChatSheet.test.js` |
| L1-F3 | MINOR (test gap) | `AgentBattleScreen.jsx:878` | Deleting `!gameTapeOpen` from `chatVisible` leaves every suite green. | Refuter B: 37 files / 561 tests green with the term deleted; the observable consequence corrected — the term shows only in the hidden DOM while the tape is up (at half: `true` vs the mutant's `false`) and on a desktop→mobile crossing under the tape. | A row asserting the hidden-DOM state while the tape is open at half (`958c5f17`). | `layout.jsdom`: "entries that land while the Game Tape covers the chat stay unseen" |
| L1-F7 | NOTE (test gap) | `AgentChat.jsx:102-134` | Re-keying the eyebrow on the receipt OBJECT stays green (a flag-on card with no thread id would read `DIRECTIVE LOCKED IN`). | Refuter B: 30 files / 270 tests green; the mutant observable on the legacy card. | The legacy-card row asserts `>Directive<` and no `DIRECTIVE LOCKED IN` (`958c5f17`). | `AgentChat.receipts.render.test.jsx` |
| L2-F9 | NOTE (harness) | `AgentBattleScreen.layout.jsdom.test.jsx:110-114` | The suite's `matchMedia` stub answers `matches: width >= 768` to every query, so framer's reduced-motion singleton latches TRUE for the whole file and the animated paths never run. | Refuter A: the real screen mounted under the shipped stub reads `useReducedMotion() === true` at 1280 and still at 390. | A query-aware stub with a listener registry (`958c5f17`, `23717042`). | the landing row (seven washes) and the crossing rows now run with motion |
| L5-N2 / N3 / N5 / N6 | NOTE | the screen tests; `selectWhyState.js:35`; `ChatSheet.jsx:105-106`; the theme guards | No screen-level wash count under the new layout; no tripwire on the cron's `Swap execution failed` prefix; the handle name composed in the component; neither theme guard covers an A4 file. | Refuter B: all four CONFIRMED (N3 stronger — no api-side test pins the string either). | `data-wash` + `data-wash-index` and a seven-wash row in render order; a source-reading tripwire in `selectWhyState.test.js`; `BATTLE_VIEW_COPY.sheetHandleName`; the whole `src/screens/battleView/` directory under both guards at zero literals (`958c5f17`). | the rows named; `tokens.guard` / `motion.guard` |
| L4 gaps | MINOR ×14 | see §4 | Fourteen mutations left the suites green. | Refuter C: pending — the mutation runs themselves are the evidence (each GREEN recorded by suite and count in L4.md) | Rows / guards / the committed capture harness (`23717042`, `958c5f17`). | §4 |

## 3. Findings — CONFIRMED, recorded (copy requests, notes, deferrals)

| # | Where | Claim | Disposition |
|---|---|---|---|
| L1-F2 | `selectWhyState.js:91-107` ← `agent-evaluate.js:2102-2121` | A guardrail-FORCED swap (Haiku said HOLD; `applyGuardrails` forced a SWAP and overwrote `rationale` with `Guardrail override (…): …`) that is then blocked (distressed / validation) or fails at execution renders under `Argued for a swap …` / `The agent's own words …` — the agent argued for nothing. | **CONFIRMED** (refuter B; the LOCKED sub-site alone is unreachable — `agentGuardrails.js:451-465` defers a locked breach). Reachable, rare. **Copy request** + a branch on the persisted `guardrailOverrides` / `guardrailSourceNote` (handover item 21). |
| L1-F4 | `AgentBattleScreen.jsx:886`; `ChatSheet.jsx` | `new activity` also rides a pending proposal, which keeps the dot while the sheet is open. | **CONFIRMED** (refuter B; the input is dormant under the launch guard — `pendingProposal` has no live writer). Shipped amber semantics; recorded (item 28); guarded as shipped by a row. |
| L1-F5 | `AgentBattleScreen.jsx:506` | A fresh mount treats the whole feed as unseen; at the cap a new entry does not change the length. | **CONFIRMED** (refuter B). The first half is the shipped semantics (recorded, item 28); the second half is FIXED by the stamp (§2, L2-F8a). |
| L1-F6 = L2-F8 (b) | `AgentBattleScreen.jsx:879-882` | An entry landing while the sheet is open can paint for one frame before the passive effect clears the dot. | **CONFIRMED** (refuters A and B: React defers the passive flush to a Scheduler task for a non-sync update; the dot is committed first — a paint in between is possible, not certain; "one frame" is the upper bound). The seed's effect-not-render rule kept; recorded (item 28). |
| L1-F8 | `AgentChat.jsx:759, :776` | `Agent is thinking too hard. Try again.` — a forbidden verb in the shipped chat error copy, now on the controller page permanently; `AgentChat.jsx` is not under the copy guard. | **CONFIRMED** (refuter B: rendered under the controller layout on an HTTP 500 and a network error). Pre-existing; for bug 2's "shipped promise copy" PR (handover item 35). |
| L2-F4 | `AgentBattleScreen.jsx:1460-1488` | Crossing 768 px re-mounts `AgentChat` (a different parent): an unsent draft, an in-flight bubble, `isSending` and `error` are lost; focus drops to body; one `AgentChat` at every moment; `ensure-opener` does not re-fire. | **CONFIRMED** (refuter A: the textarea's DOM identity changes; value `''`). Recorded (item 26); the crossing is now guarded (one textarea before, during and after; the sheet reset). |
| L2-F12 | `ChatSheet.jsx:152-169` | The only pointer DRAG surface is the 36 × 4 px pill (the buttons are the other pointer paths). | **CONFIRMED** (refuter A; the wording "only pointer path" loose, the substance stands). A comfort note; the pull is now guarded by a real-pointer row. |
| L5-N1 | the ledger | D-63 → D-68 absent at `e1db2e08`; D-63 names `…-v5gog5` while A4 shipped on `…-layout-425nnm`. | **CONFIRMED** (refuter B). The rows landed in `8a789ffe` with D-63's status carrying the real branch; D-65 amended here for the timeout-only scope. |
| L5-N4 | `AgentBattleScreen.jsx:1141` | `'Your Agent'` typed a third time (the controller mount). | **CONFIRMED** (refuter B: 2 → 3). A pre-existing literal duplicated; the screen is not under the copy guard. Recorded (item 34). |
| L3-N1 … N6 | see §5 | Six notes, no action: the reduced-motion init re-ordered (same net listeners); the disabled sheet's same-value `setState`; `useViewportHeight(false)` reads `innerHeight` once; the `MAX` label branch unreachable in both trees; a classifier note; a fiber-shape change with no markup or DOM effect. | Recorded; refuter C verdicts pending. |
| L4-M9a / M10b | `AgentBattleScreen.jsx` | Render-time vs effect, and the door's tab change, are observable only through the source-scan row (jsdom cannot tell them apart). | Recorded: by design — the source row is the guard of both. |
| L4-M14 / M50 | `WhyPanel.render.test.jsx`; `deskHonesty.test.js` | The panel's "no This turn" row cannot fail for the slot's return (the screen row is the guard); the inline-copy regex needs 7+ characters (the inherited T9 shape). | Recorded (test-name precision; the guard's known floor). |
| L4-M24 | the backdrop gate | `isMatchupsBackdropOn` is mocked false in every suite (the canvas cannot render in jsdom). | Recorded (pre-existing pattern). |

## 4. Test-integrity findings (L4) — verdicts and guards

L4 ran 62 mutations in private extractions: 43 RED, each recorded by failing-row name (the seed's "removing the FAILED branch fails EXACTLY the new row" confirmed: 1 failed / 538 passed; the outage label → exactly the selector row + the panel row; the eyebrow → the receipts row and, for the flag-off variant, the GOLDEN; `postureClosed` → the two Desk rows; the TabBar under the flag, the missing controller early return, a SECOND `AgentChat` (8 rows at both breakpoints), the door's tab change, the cycle broken, the focus effect removed, the collapse control removed, the region / its name / its `tabIndex` removed, `open()` dropping full to half, the Game Tape link / focus / hiding removed, the This turn slot restored, the row's name / description / ids removed, the wrapper applied flag-off (→ the golden), a flag-off style key reordered or a literal changed (→ the golden), the unread dot's rendering and its name, the FAILED colour slot, the failure prefix without `downgraded`, a stale outage entry, an inline string in the new file (→ `deskHonesty`), a forbidden verb in a `title`). Refuter C: pending (in progress at the time of this commit; the line is replaced when it reports).

| # | Gap (the mutation left the suites green) | Sev | Closed by (row / guard) |
|---|---|---|---|
| M12b | `atLeastHalf` was a dead export | NOTE | removed (`23717042`) |
| M12c | raise ↔ lower swapped — the grabber's pull untested | MINOR | `useChatSheet.test.js` (the pull, saturation, inverses); `layout.jsdom` "a pull on the grabber moves ONE detent" (real PointerEvents: −45 / −45 / saturate / +45 / −39 under the threshold / +45 / saturate) |
| M12d | `detentHeightPx` returning peek for every detent | MINOR | `useChatSheet.test.js` (148 → 172 floor, 400, 744, the tiny-viewport clamp, the SSR default); `layout.jsdom` `data-sheet-height` at peek / half / full |
| M17 | nothing executable protects the golden's provenance (regenerating from a mutated tree makes the golden suite green) | MINOR | the capture harness committed beside the goldens, skipped unless `GOLDEN_OUT_DIR` is set, with the regeneration procedure; run from this tree it reproduces the committed goldens byte for byte; the goldens' sha256 recorded below (`23717042`) |
| M19 | `!gameTapeOpen` dropped from `chatVisible` | MINOR | the "unseen behind the tape" row (hidden-DOM state while the tape is up) |
| M20 | the `enabled` reset removed | MINOR | `layout.jsdom` "a breakpoint crossing keeps exactly ONE AgentChat … brings the sheet back at peek" |
| M21 | no `resize` listener | MINOR | `layout.jsdom` "a viewport change re-sizes an open sheet" (400 → 300 → 544) |
| M22 | the pending-proposal amber never applied | MINOR | `layout.jsdom` "a pending proposal colours the handle dot amber" |
| M23 | the Game Tape link's bookmark dot never rendered | MINOR | `layout.jsdom` "the Game Tape link carries the bookmark dot" |
| M31 | the region re-focused on half → full | MINOR | `layout.jsdom` "half → full does not move focus again" |
| M37 | `chatVisible` without `isDesktop \|\|` — the desktop unread row was vacuous | MINOR | the desktop row now crosses to mobile and asserts no dot for entries seen on desktop, then a dot for a new one |
| M42 | `overscrollBehavior: contain` dropped | NOTE | `layout.jsdom` "at half / full the message list is the scroll container and contains its overscroll" |
| M47 | no mobile Game Tape row | MINOR | the "unseen behind the tape" row opens the tape on mobile (`data-game-tape="open"`, the sheet hidden) |
| M54 | reduced motion not honoured by the sheet | MINOR | `ChatSheet.motion.jsdom.test.jsx` (`instant` under reduced motion; no drag spring) |
| M55 / M56 | a colour or transition literal in the new files unguarded | MINOR / NOTE | `src/screens/battleView/` (13 files) under `tokens.guard` and `motion.guard` at zero literals |
| M66 | the landing index restarting per tier | MINOR | `data-wash-index` 0 → 6 asserted in render order |

The goldens (captured from a worktree of `eaf2a0e2`; re-captured identically by L3, L5 and the committed harness): `agentBattleScreen.tabbed.html` sha256 `1a8d7353bb32a46d7e796be49d5320b96fe0b0037ec04398f8922b77214f664f` · `agentChat.tabbed.html` sha256 `e750b785dc5eaae71c4671df9d2f4e8506706c43aab47dfe82a688db23663311`.

## 5. Checked and clean (by lens, condensed)

- **L1:** every new string in the guarded modules and consumed from there; 0 forbidden terms and 0 machinery fields (`haiku_decision`, `guardrail_stopLoss`, `triggeredBy`, `source`, `exitReason`, the promise copy, `Agent is active.`, `Live Activity`) in hostile renders at 1280 and 390 px with a row panel and the book panel open; the fourth state's `[0]` prefix rule verified against `agent-evaluate.js:2464` (the array is empty when that push runs, so `[0]` is exact); "it did not go through" proven for the execution catch (one transaction, the pre-read and throws before any write); `Directive` flag-only (the flag-off eyebrow byte for byte, golden-pinned); `postureClosed` one function, two callers; the peek handle renders the header's own `turn.text` (one source); no second derivation of a check time; hazards 1–23 walked; C2 intact.
- **L2:** the detent machine, the collapse control, the drag (real PointerEvents, the 40 px threshold, saturation), focus into the region / left on the composer / back to the invoker / the handle when the invoker is gone; StrictMode idempotent; the `enabled` reset both ways; exactly one `<textarea>` after every commit across two crossings; `ensure-opener` once per battle; the unread dot in every visibility case; the flag-off hook count grew by exactly seventeen unconditional calls before the early return, none altering markup; every listener under the flag removed on unmount and absent flag-off; no timer starts a landing; seven `position: relative` wash hosts in tier order; the Why? panels' exits under real motion; SSR clean; the layout mechanics from the DOM (sticky tier headers in the scrolling column; z-order); `GameTapeView` with the same six props as the flag-off tab.
- **L3:** 40,940 cases × three environments per tree — ProximityLabel 22,416 (ATR and dollar branches, every tier and achievement text), AssetSide 3,000, TacticalRow 3,000, AgentChat 600 (both shipped layouts, the shipped `ExecutionCard`), AgentBattleScreen 400 (the tabbed paint, the loading screen, Flat6, cash / null / crypto slots, live-price points and badges, the chip, the day label, the banner, the tab dots), AgentDesk 11,524 — **0 differences outside the D-62 set** (769 per environment, every one `LIVE_CLOSED` with a parseable `lastCheckedAt`; the no-last-check cell equals `eaf2a0e2` by bytes); identical throw sets (`ChamberFuse.jsx:227`, pre-existing); listeners, intervals and subscriptions flag-off identical in both trees, net balance identical after unmount; `ChatSheet` and its `useId` never mounted flag-off; the hoisted rows' keys unchanged; every conditional style spread empty or last flag-off; the client DOM across the shipped tab switches identical modulo running-animation numerics.
- **L4:** tests import what they guard; every guard the seed's A4.2 map names is executable; the "exactly the new row" count holds.
- **L5:** the seed checklist SATISFIED item by item (four in a CONSTRAINED form, recorded); the fence (21 files, all under `src/`; `api/`, `src/config/`, `docs/`, `vercel.json` untouched in the range; no §1 file; `archetypeImportBoundaryBaseline.json` byte-identical; 0 `fetch` / Firestore / model-call / `directiveUtils` / archetype-table hits in the added lines); 0 raw hex, 0 numeric `rgba(`, 0 `transition={{` in the added source lines (seven `var()`-based rgba, nine token names all declared); the hoisted `boardRows` block a byte-identical move; every new copy key consumed; the A1–A3 guards all present and green; flag + pin + `DARK_BY_DESIGN` + the override untouched; the rulings §3 directives 4, 7, 9, 10, 11, 12 and §2.5 satisfied.

## 6. Build, suite, mutation checks

| Check | At `e1db2e08` | At `7f480fb9` (after the fixes) |
|---|---|---|
| `vite build` | green | green (31 s) |
| full unit suite (`--maxWorkers=2`) | 566 files / 9,524 tests green (1 file, 62 tests skipped by design) | **568 files / 9,605 tests green** (2 files, 63 tests skipped by design — the env-gated capture harness among them) |
| `flagPinGuard`, `deskHonesty`, `tokens.guard`, `motion.guard`, the flag-off golden | green | green (the theme guards now cover 13 more files) |
| mutation checks | 62 (L4) + the A4.0 four + L1's seven + L3's three: every seed-named guard RED under its defect | the 14 gaps closed; the seed's "exactly the new row" count still 1 |

## 7. Out of scope — for separate tasking (BUILD_RULES §3)

Phase 0 §6 bugs 1–5 and the A1–A3 review's N11 unchanged; `Agent is thinking too hard. Try again.` (L1-F8, for bug 2's PR); the shipped composer's own geometry under the production cascade (an 84 px textarea with an empty draft — `AgentChat.jsx:689-694` plus content-box `minHeight: 42`); the `index.css` rule forcing every button and textarea to 16 px (the shipped tabs and Back button render at 16 px too); the feed cap's silence flag-off (the shipped ref semantics); iOS's keyboard vs `innerHeight` outside the sheet (the composer's scroll-into-view is Safari's); the `MAX` label branch unreachable in both trees (L3-N4).

*Prepared September 2, 2026 against `e1db2e08`; fixes in `958c5f17`, `23717042`, `7f480fb9`. Reviewer and refuter briefs, probes, matrices and the snapshot trees stayed in the session scratchpad; every claim above carries the reviewer's or the refuter's evidence.*
