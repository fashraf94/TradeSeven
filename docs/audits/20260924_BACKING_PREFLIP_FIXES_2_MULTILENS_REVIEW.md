# Backing pre-flip fixes 2 — multi-lens adversarial review record

**Branch:** `claude/backing-preflip-fixes-2-3mlii4` (the prompt names `backing/preflip-fixes-2`; this session's harness binds its work to that designated branch — the PR says so) · **Reviewed commit:** `863a3b12` (the build: `602ebc46` A · SEAL-1, `18967172` E · WIRING-15 + WIRING-1's twin, `19338a1c` D · WIRE-R-2, `91f70346` B · N3, `0c0e15ed` C · PRE-1 / N4, `863a3b12` C's rows strengthened) · **Fix commits:** `977b6db3`, `4c54ab87`, `467aa58e`, `71d84df3`, `441395ad`, `b1c5180a`, `183e6c08`, `f3d2f993`, `f74638de`, `340fc9d6`, `a6d95173`, `be6fef31`, `42e7819c` · **Base:** `main` (`40acd199`).
**Rule:** BUILD_RULES §2 — review is mandatory at ≥10 files or ≥1,500 lines (this branch at `863a3b12`: 27 files, +1,442 / −193; with the fixes: 34 files, +2,456 / −251 at `42e7819c`, before this record). Multi-lens, adversarial, independently refuted, `vite build` run, mutation-checked, written down. The prompt briefed one lens: **"show a sealed number to anyone before close."**
**Reviewer isolation (founder ruling, Sep 2, 2026):** every lens and every refuter worked on its own `git archive 863a3b12` extraction under the session scratchpad (`review/lens-{seal,dark,placement,wiring}`, `review/refute-{seal,dark,placement,wiring}`), `node_modules` symlinked, read-only on git and on the shared working tree; probes and mutants ran only in those trees, and every mutated file was restored byte-identical (sha256 before/after in each report; each reviewer re-verified its whole tree — 3,386 files — against the commit's blobs at the end). Read-only `git archive main` extractions (`review/base-main`, and the DARK reviewers' own) served comparisons. The refuters cross-checked the coordinator's in-flight fix commits read-only and reported on them (PLACE-R-1, DARK-R-3 — both acted on). The coordinator's mutation pass — the mutating lens — ran LAST, after every lens and refuter had reported, each mutant on its own `git archive 42e7819c` extraction under `scratchpad/mut/` (`run_mutants.py`). No reviewer wrote to `/home/user/TradeSeven`. Disclosed by the reviewers themselves: vitest's gitignored cache under the shared `node_modules` symlink was refreshed by some runs (the placement lens moved its cache into its own tree); three refuters each left one empty helper directory beside their trees (`refute-seal-meta`, `refute-wiring-work`, `refute-placement-meta`), which the no-deleting-directories rule kept; one refuter ran a plain `git status` on the shared repository (an index stat refresh at most — no content change).

## 0. Verdict

| | |
| --- | --- |
| **"Show a sealed number to anyone before close"** (the founder's brief for one lens) | **The seal holds in the steady state on every surface this build touches** — the trainer-stats reply walked by key, the stats home, the results reader, the pod list the strip re-reads, the stake signal, Your Backing's cancelled card: byte-identical whatever an open pool's book holds, across 7 pod shapes and 3 books. **Two in-branch gaps at the instant of close, both fixed:** a close landing between the trainer route's stakes query and its pool read showed the trainer a book the reveal never publishes (SEAL-A1 — reproduced end to end through the real slot path: "5 backers · 1,500 BP"; now 0), and the route's reads tracked the sealed book, a timing channel for "someone has backed you" (SEAL-A2 — AUC 0.78 under jitter; now the SET of reads is identical across books, AUC ≈ 0.45). One test-integrity gap fixed (SEAL-R-1). **One residual, stated:** the stakes query's own response still grows with the book (one round trip either way; leaving the sealed stakes out needs a composite index — separate tasking). |
| **Dark and mobile byte-identity** | **Hold** — the lens's CLEAN verdict survived its refuter (100 flag-off scenarios per lobby variant, every timer/listener/observer/fetch/service call recorded; the dark routes against a database that traps every access: 0 accesses; both goldens regenerated independently). Fixed: the dark pin could not see the two non-read side effects this build adds — the stake listener and the close timer (DARK-M1), nor one registered on import (DARK-R-3), nor a wrapper that appears only while dark in the plain `Lobby` (DARK-R-1). |
| **Placement (N3)** | **Holds** — on every one of 100 mounted mobile landing states the strip sits directly under the Auto-draft card (unseated) or the hero's honesty line (seated), never below "Watch a live game" or the bracket line, the desktop's logic mirrored. **Fixed:** its spacing (the slot's own margin stacked with the column's gap — 14 px above / 32 below; now the column's gap, as on desktop: 14 / 14 — PLACE-B1), and the words around the sealed row (PLACE-A1 the first-week line was false for most returning trainers; A2 "This week" named next week's pool; A3 the seal outlasted the close; A4 the line at 2.91:1; A5 the row's wrap). |
| **Wiring** | **Fixed:** the results reader dropped a pod — and its week, for good — when a re-read failed (WIRE-E1), and answered stale copies beside a contradicting pool (E3); the strip's close re-read disarmed every later close after one failed read (WIRE-R-1) and never followed up a pool still open (C1); a failed or cache-only group read could print "This pod was cancelled — your stake will be returned." over a pod in battle (WIRE-D1, WIRE-R-3), and a later failure could un-know a pod known to be voided (PLACE-R-1); two refund sentences for one pool (WIRE-D2, §9); the seal had no week bound (WIRE-A2) and a dev pod sealed the production record (WIRE-A3). |
| **Findings** | **38 findings** across the four lenses (the SEAL lens 4 — one out of lens; DARK 6; PLACEMENT 15; WIRING 13). The refuters **CONFIRMED 37** and **PARTIALLY REFUTED 1** (WIRE-A2 — its stalled-week sub-claim: that seal is true), refuted four proposed fixes or sub-claims (SEAL-A1's re-read variant, SEAL-A2's padding, DARK-N2's reassurance, DARK-N4's read half), re-rated two (PLACE-A1 MAJOR → MINOR, PLACE-D5 NOTE → MINOR), and **added 17** (§3). Five findings surfaced in two lenses and are disposed once: WIRE-A1 = PLACE-A1, WIRE-D1 = PLACE-D1, WIRE-D2 = PLACE-D5, WIRE-D3(c) = PLACE-D2, DARK-N5 = PLACE-B1. |
| **Fixed in this branch** | Every in-branch CONFIRMED finding rated MINOR or above, and every refuter-added in-branch MINOR, is fixed; every behavioural fix carries a row, and each fix's rows were mutation-checked — 54 mutants, 53 red, 1 partial by design (§6). |
| **For separate tasking** | The lobby's pre-existing overlap at ≤1024px with the flag off — **reported again, as the prompt requires** (it is on `main` today); and 15 more items (§9). |
| **For the founder** | The pre-existing exclusion-flag exposure (SEAL-A3 / SEAL-R-3); the copy of the first-week and sealed lines (the reply cannot tell a first week from an unbacked one); the one extra mobile render per "no seat" answer (DARK-N1, the desktop's accepted WIRE-7 cost); the spacing ruling (PLACE-B1 — the goldens' 20 moved rows also drop the slot's margin). (§10) |

## 1. Scope

The prescribed "CC — Backing Pre-Flip Fixes 2", flags `false` throughout: the five fixes the earlier reviews required before the flip — A (SEAL-1 + SEAL-R-2: trainer stats count no open pool and say `thisWeek: { sealed: true }` from the pool, with the design's sealed row and no numbers), B (N3: the mobile strip directly under the ranked-entry position, the desktop's placement mirrored; the mobile goldens change only by the move), C (PRE-1 / N4: the strip re-reads after a confirmed stake and at the earliest close — one timer, cleared on unmount, no polling loop), D (WIRE-R-2: a cancelled pod never reads "plays Monday"; Your Backing's card says it was cancelled, then shows the refund; derived from the group status the surface already reads), E (WIRING-15 + WIRING-1's twin: the results reader answers the viewer's stakes as they stand after the pool).

Not in scope and not reviewed as this PR's: the activation PR; the gate-3 honesty fixes; any new feature; any fenced file (none touched, none called); the lobby overlap at ≤1024px with the flag off (reported again, §9).

## 2. Method

| Lens | Brief | Tree | Agents |
| --- | --- | --- | --- |
| SEAL | The founder's: "show a sealed number to anyone before close" — every surface and reply the build touches, hidden channels (read schedules, timing, orders), races around the close | `lens-seal` | 1 reviewer |
| DARK | The flag-off guarantee (nothing renders, reads, subscribes, listens or arms a timer while dark) and mobile byte-identity: are the 22 regenerated golden rows exactly the move, and can the rows fail | `lens-dark` | 1 reviewer |
| PLACEMENT | N3's placement against the design and the desktop, the sealed row and the first-week line, the cancelled card's words and where they appear, accessibility of the new elements | `lens-placement` | 1 reviewer |
| WIRING | Lifecycle, data paths, races and failure paths of every fix; BUILD_RULES §1, §4, §9, §10, §11 | `lens-wiring` | 1 reviewer |
| REFUTATION | Every finding of each lens handed to a refuter on its own tree, instructed to refute it with a concrete repro and to judge each proposed fix — and the coordinator's own designs (the trainer route's three read rounds; the lazy close; option (b) for the spacing; the follow-up re-read) — against the build's constraints | `refute-{seal,dark,placement,wiring}` | 4 refuters |
| MUTATION | The prompt's two named checks, then a guard check for every fix and review fix (§4, §6) — last, on its own trees | `mut/trees/*` | the coordinator |

Eight agents in total. The full reports stay in the session scratchpad (`review/reports/`, outside the repository); each is reproduced in substance below.

## 3. Findings and dispositions

Severities as the refuter left them. **Disposition** is the coordinator's; the CONFIRMED / REFUTED split is §5.

### SEAL lens — "show a sealed number to anyone before close" — 3 findings + 1 out-of-lens note; refuter: 4 CONFIRMED (A2 broader than reported), 0 REFUTED; refuter added 3 + 1 out-of-lens note

Clean (lens, re-verified by the refuter): the trainer-stats reply, walked by key, byte-identical across open books in 7 pod shapes (seated/open, past `closesAt` still open, two pods open, seat left, left + seated in a never-materialized pod, group gone / pool open, an open dev pod beside) with history present in every case; the marker keyed on the seated pods' pools, never on stakes; the render of `TrainerStats` and the shared stats home identical across books (seated with history, seated first week, seat left); the build's seal rows real guards (the seal line removed: 7 red; the seal moved below the exclusion counter: 3 red); the results reader's one-pod answer on an OPEN pool byte-identical across others' books for a backer, a spectator and the seated trainer; the WIRING-15 catch path on a close that throws answers no figure; the pod list the strip re-reads identical across others' books above the floor; the stake reply the signal carries is the viewer's own stake and the capped pair, ignored by the strip's listener; Your Backing's cancelled card byte-equal with pot, counts, per-team totals and pays × injected into the pool; no new public write, rules, index or stream.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| SEAL-A1 | MINOR (in-branch — a gap left by SEAL-1, not a regression: `main` showed the whole open book) | A close committing between the route's stakes query and its pool read left the fold `live` copies of stakes the close had just voided and never reveals — a seat that left (`seat_left`), a deleted pod's tombstone (`refunded`, `teams: []`, pot 0) — so the trainer read "3 backers · 400 BP" of a book no reveal publishes. Any user can run the lazy close (`results?groupId=`); the tombstone branch has no clock check, so a trainer can manufacture it days before `closesAt`. | **CONFIRMED** end to end through the real slot path (a real claim, open and release; the close run by a stranger's `results?groupId=`): the raced reply counted **5 backers · 1,500 BP**. The *skip* fix CONFIRMED (B1, B2, B3 → 0; B0 = the reveal; every existing row green); the *re-read* variant REFUTED (it adds a round trip that depends on the book on exactly the paths A2 closes). Neither fixes B0's "sealed plus figures" reply unless the marker comes from the same pool read. | **FIXED** (`977b6db3`) — the fold skips a live copy the pool as read contradicts (`liveStakeContradicts`, `api/_utils/backingStats.js:579`); the marker (`trainerSealed`) is read off the same round-2 pools the fold reads. Rows: the race through the real `closePool` for a seat that left, a deleted pod, below the floor, and the kept seat (`api/backing/trainer-stats.test.js`), and the pure fold (`api/_utils/backingStats.test.js`). |
| SEAL-A2 | MINOR (in-branch) | A timing side channel: step 5b read `private/meta` and the pool for every counted stake, open pools included, so for a first-week trainer one stake on their open pool added one sequential round trip — the "someone has backed you" bit SEAL-R-2's pool-keyed marker was built to withhold. | **CONFIRMED, broader:** under jittered latency the channel survives (first week AUC 0.78) and a thick open book still slows the reply through extra PARALLEL reads even with history (AUC 0.79) — the fix must make the SET of reads book-independent, not only the number of rounds. Padding judged fragile. For lone-claimant slot pods the bit is already public (D-ac). The coordinator's redesign (three rounds, live stakes of a departed seat skipped unread) **closes A1 and A2**: across all 7 shapes the same documents are read in the same number of rounds whatever the book; jitter AUC 0.48 / 0.45 / 0.44; bytes equal the build's. Re-checked read-only at `977b6db3`: race 0, AUC ~0.45, read set identical in 6 of 7 shapes — the seventh (`pastClose`) differs by design, because the route now closes an overdue seated pool itself and reveals it in the same reply (PLACE-A3); "the week bound needs the lazy close". | **FIXED** (`977b6db3`) — the route reads in three rounds whose document sets never depend on a sealed book (`api/_utils/backingStats.js`, "THE TRAINER'S READS"): stakes ‖ the seated pods; one pool read per seal pod and per pod a counted stake names (`countedStakes`: a live stake on a pod the viewer left is never read); `private/meta` only for stakes on pools that are not open (`flaggedStakeIds`). Rows: the read log, in order, identical across books for three trainers (closed weeks behind them, a first week, a departed seat); the meta reads never an open pool's. **Residual, stated:** the stakes query's own payload still grows with the book (one round trip either way); removing it needs a composite index (out of scope). |
| SEAL-A3 | NOTE (pre-existing) | The admin `excluded` flag reaches the backer through `my-stats` (`computeMyStats` drops and counts excluded stakes). | **CONFIRMED**; not specific to open pools; introduced deliberately by the PR 5 review's HON-5 fix; two comments now contradict the behaviour (`api/_utils/backingStats.js:27`, `firestore.rules:716`). | **REPORTED — a founder decision** (§9, §10). |
| (out of lens) | NOTE (pre-existing) | `computeMyStats`' season net reads +100 where it should read 0 for an admin-excluded LIVE stake (the excluded effect subtracted twice). | **CONFIRMED** (walked through; only live excluded stakes are wrong). | **REPORTED for separate tasking** (§9). |
| SEAL-R-1 | MINOR (in-branch, test integrity) | The fixture ignores `array-contains`: a misspelled field or an unscoped scan of the whole `tournamentGroups` collection passed every row — in production, no seal at all, or every request reading every pod. | raised by the refuter | **FIXED** (`b1c5180a`) — a row records the clause the route sends and asserts exactly `groupMembers array-contains <uid>` (`api/_utils/backingStats.test.js`); the typo and the unscoped mutants both red (§6). |
| SEAL-R-2 | NOTE (pre-existing) | `closePool` reads each refund wallet with its own sequential `await`, so the request that triggers a close waits once per distinct voided backer: a stranger's `results?groupId=` on a deleted pod learns its backer count, which is never revealed (194 / 232 / 298 ms for 1 / 3 / 6 backers, identical bytes). | raised by the refuter | **REPORTED for separate tasking** (§9): read the wallets in parallel inside the transaction. |
| SEAL-R-3 | NOTE (pre-existing; refines A3) | The exclusion flag also reaches the trainer by subtraction: the public reveal counts excluded stakes (`totalsFromStakes` ignores the flag), trainer stats drop them — 4 backers / 900 BP revealed vs 3 / 400 in trainer stats. | raised by the refuter | **REPORTED with SEAL-A3** — the founder's decision (§10). |
| (refuter, out of lens) | — (pre-existing) | Refunds never restore allowance; the spec's reason ("because the week has closed") is false for slot pods, whose pool closes at fire mid-week, and for the tombstone close. | raised by the refuter (verified by reading; consequence not probed) | **REPORTED for separate tasking** (§9). |

### DARK lens — the flag-off guarantee and mobile byte-identity — 6 findings; refuter: 6 CONFIRMED (one reassurance and one proposed fix partly refuted); the CLEAN verdict SURVIVED; refuter added 3

Clean (lens; re-verified independently by the refuter): mounted flag-off with effects running, on `main` and on this branch, the mobile landing (LobbyTabbed and the plain `Lobby`) and the desktop lobby render the same DOM at every commit and make the same sequence of service calls, subscriptions, timers and listeners — the refuter's own probe, 100 scenarios per lobby variant (bracket / none × unseated / seated / forming × seat answer sync / late / never, signed out, uid switches, tab round trips, StrictMode, server renders), found 86 of 100 identical in every respect and the other 14 differing only in commit count (DARK-N1); 0 backing calls, 0 fetches, 0 `data-backing` elements dark; every effect in League, hooks and contexts declares dependencies (an AST scan of 184), so the extra render re-runs none. The two server routes answer 404 before any read, now also against a database that throws on ANY property access (0 accesses). **The golden claim holds exactly:** both reviewers regenerated the mobile golden on `main`'s code and on the branch's (under the old mock and the new) — the mock change moves no row on `main`; 22 of 96 rows moved, precisely the named ones: in 20 the strip's markup is byte-identical and so is everything else with it excised, in the 2 server renders the strip is absent. The dark golden is byte-identical in both commits and regenerates so. Full suite at `863a3b12` in the refuter's tree: 781 files, 15,412 tests, 0 failed.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| DARK-M1 | MINOR (in-branch) | Neither side effect this build adds — the strip's stake listener and its close timer — had a dark guard: both are dark-safe only because the outer component runs no hook above its flag read, which no row enforced; the dark rows count reads and requests, not listeners or timers. A listener above the flag read (M7) and a timer armed only while dark (M8b) each passed every suite. | **CONFIRMED, wider:** also a realistic refactor (RM7c — the gate moved inside the live strip below its hooks, the reads disabled while dark) and a listener registered at module level on import (RM7d). The template row works against all four with three refinements: assert only the bare strip's timer (the one timer a dark host shows is jsdom's `requestAnimationFrame` shim, not the strip's), take the listener count once after the imports, and assert no stake is announced while dark. | **FIXED** (`71d84df3`, refined `be6fef31`) — a dark row mounts the bare strip, the mobile landing, the desktop lobby and the Backing screen under fake timers with a pass-through on the signal: dark, 0 listeners, 0 strip timers, 0 announcements, and 0 listeners registered on import; lit, 1 timer and 3+ listeners (not vacuous). |
| DARK-N1 | NOTE (in-branch) | One extra re-render of the mobile lobby while dark for a signed-in unseated viewer: `setSeatFor(uid)` changes state even where `setActiveGroup(null)` bails out. DOM, events and effect order identical. | **CONFIRMED, corrected:** it is the desktop's WIRE-7 cost (accepted in the desktop record), mirrored; it happens once per "no seat" ANSWER, not per signed-in viewer (the desktop record's own wording overstates it). The proposed flag read in the setter works (200 of 200 scenarios identical to `main`) but must go on BOTH lobbies or neither — a mobile-only fix breaks "mirror the desktop". | **RECORDED**, restated precisely: one extra render per "no seat" answer, on both viewports, no markup or behavioural change (§10). |
| DARK-N2 | NOTE (test integrity) | The source-text count that replaced the host rule can be defeated by renaming the lobby's parameter; the lens judged the goldens a backstop. | **CONFIRMED; the reassurance partly refuted:** a renamed wrapper that appears only while dark, in the plain `Lobby`, passes all 1,322 tests (DARK-R-1) — no golden renders that lobby dark. | **FIXED** with DARK-R-1 (`be6fef31`) — a behavioural row. |
| DARK-N3 | NOTE (pre-existing) | No committed test renders the flag-off plain `Lobby` (`LEAGUE_NEXT_ARC_ENABLED` false); N3 rewired it. Identical to `main` in the lens's probe. | CONFIRMED; unreachable at HEAD's flags (and on the lit dev preview only). | The dark half is now guarded by DARK-R-1's row. A lit, unseated plain-`Lobby` pin row generated on `main` is **REPORTED for separate tasking** (§9) — adding it here would add golden rows. |
| DARK-N4 | NOTE (in-branch) | Nothing checked that the strip's stake listener is removed on unmount (M12 passed every suite). | **CONFIRMED; the proposed fix partly refuted:** its "announce after unmount, expect no read" half cannot fail under M12 — `refresh` is a state setter, which React 19 drops on an unmounted component; only the listener balance has teeth. | **FIXED** (`71d84df3`) — the refresh suite's pass-through counts live listeners: one while mounted, none after; M12 reds it twice (§6). |
| DARK-N5 | NOTE (in-branch) | = PLACE-B1 (the mobile strip's spacing, 14 / 32 and 12 / 30). | CONFIRMED, measured in Chromium with the build's CSS; judged the lens's fix (dropping the margin everywhere) to break the golden constraint — 27 rows, 5 of them outside N3's set — and left it to the founder. | See PLACE-B1: fixed with option (b), which the PLACEMENT refuter verified moves only the 20 rows N3 already moved. |
| DARK-R-1 | NOTE (test integrity) | A renamed wrapper that renders only while dark, in the plain `Lobby`, escapes every committed test (RM4f: 0 failed / 1,322). | raised by the refuter | **FIXED** (`be6fef31`) — while dark, both mobile lobbies, seated and unseated, with and without a bracket (8 comparisons), must render byte-identically with the real strip in the slot and with no slot at all. RM4f and a plain renamed wrapper both red (§6). |
| DARK-R-2 | NOTE (lit only; the desktop has the same) | The seat gate's "for this uid" clause (`seatFor === uid`) is untested (RM13: `seatFor != null` passes all). The likely consequence (ASSUMED): after an in-place uid switch the strip briefly shows in the previous viewer's centre. | raised by the refuter | **REPORTED for separate tasking** (§9): a uid-switch variant of the WIRE-7 row, both lobbies. |
| DARK-R-3 | MINOR (on the in-flight `71d84df3`) | The new dark row reset its listener count before every test, after the file's imports — a listener registered on import (RM7d) passed. | raised by the refuter | **FIXED** (`be6fef31`) — the count is taken once every module has loaded, and must be 0. RM7d reds it (§6). |

### PLACEMENT lens — N3, the sealed row, the cancelled card — 15 findings; refuter: 15 CONFIRMED (two re-rated); refuter added 4

Clean (lens; re-verified by the refuter): **N3's placement** — a 101-row matrix of the real mobile `LeagueHome` (LobbyTabbed and `Lobby` × four landings × unseated with the live-draft flag on and off × seated as forming / drafting / awaiting-open / battle / before the bell, with and without a practice pod, the Training tab, signed out, a voided-group notice, a failed training start): exactly one strip on every Ranked state; directly under the Auto-draft card (unseated) or the hero's honesty line (seated), directly above "Watch a live game" or the bracket line; never below either; the Training tab shows none; removing the strip from the lit page gives the dark page in every state; the logic is the desktop's (the same SlotCenter / WhileYouWait slot and the same seat-known gate); four placement mutants each red. **The sealed row** matches the design (padding, radius, dashed rule, lock, SEALED's type and tracking, no figure; its background a token equal to the design's). **"Plays Monday" after the close** — never on the strip, the screen header, the section line or the card tag, mobile or desktop, for pods voided, expired, gone, held or refunded after the close. **Accessibility** — the cancelled line 6.64:1, its tags 5.52:1 / 8.62:1, the sealed label 6.86:1; the cancelled state carried by text, not colour. No horizontal overflow at 320–448 px.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| PLACE-A1 | MAJOR → **MINOR** | The sealed first-week line — "No closed weeks yet. Once a pool with your team in it closes, …" — showed whenever nothing was counted from a closed week, not only in a team's first week: a team whose closed weeks nobody backed got the same reply and read a falsehood; the design shows the line only for a first week. (= WIRE-A1.) | CONFIRMED; re-rated MINOR (one false sentence on a private panel — SEAL-R-2's class). The reply cannot tell the two cases apart; a server count of closed weeks would bring back unbounded reads and still need a second string. The WIRING refuter added a third case: backers voided at a below-floor close make "No backers in a closed week yet" false too. | **FIXED** (`441395ad`, `a6d95173`) — "Nothing counted from a closed week yet. Once your team’s pool closes, this shows how many backed you, how much, and how they did." — true for a first week, for closed weeks nobody backed, and for voided backers (`src/components/League/backing/backingCopy.js:664`). The founder owns the copy (§10). |
| PLACE-A2 | MINOR | "This week · your team's pool" names the wrong week: an open pool is always a pod's whose battle week has not begun — "Next week's groups of four" on the pod list (§9). Reachable: a next-week seat while this week plays put "This week · SEALED" directly above this week's "In play on you". | CONFIRMED; the fix is correct provided A3 lands (else wrong Monday 00:00–09:30 ET). | **FIXED** (`441395ad`) — "Next week · your team’s pool" (`backingCopy.js:655`); A3 landed (`977b6db3`). |
| PLACE-A3 | MINOR | "SEALED · until it closes" could outlast the close: the trainer route never ran the lazy close every other pool reader runs, and the view reads once. | CONFIRMED; **the coordinator's lazy close tested:** a due pool closes on the trainer's own read (exactly at `closesAt`; 1 ms before, it stays open); an insufficient close by that read counts none of the pre-close copies; for a pool not yet due the reply bytes and the read log are identical whatever the book holds, with no writes; a seat the trainer left is not closed; no seal leak and no new race (the stake route re-checks status and clock inside its transaction). | **FIXED** (`977b6db3`) — round 2 closes a seated pod's pool that is open past its close (`closeIfDue`, `api/_utils/backingStats.js:247`), as the pod list and the results reader do; a failed close is logged and the pool re-read; a failed re-read is the request's 500. A panel left mounted still says SEALED until it re-reads (recorded, §9). |
| PLACE-A4 | MINOR | The first-week line sat in the empty box's `ink3` at 12 px: 2.91:1. The design sets it in `ink2` 12.5 px, no box. | CONFIRMED (in-branch part). | **FIXED** (`441395ad`) — the design's type: 6.64:1 (`src/components/League/backing/TrainerStats.jsx:67`). |
| PLACE-A5 | NOTE | The sealed row wraps in both homes, dropping SEALED under the label, left-aligned. | CONFIRMED with the real font: it wraps at every row width up to 332 px. | **FIXED** (`441395ad`) — `marginLeft: 'auto'` keeps SEALED on the right (`TrainerStats.jsx:50`). |
| PLACE-B1 | MINOR (in-branch) | The mobile slot kept its `marginBottom: 18` inside the new flex column (SlotCenter gap 14, WhileYouWait gap 12): 14 px above / 32 below unseated, 12 / 30 seated, where the desktop gets 14 / 14 and 12 / 12 and `main` gave 18 / 18; the bracket footnote came loose from its entry. The code comments contradicted each other. (= DARK-N5.) | CONFIRMED in Chromium with the real `LobbyTabbed` and the real strip at 320–448 px. **Option (b) verified by hash:** with the pin test's inline slot mirrored, exactly 20 rows move, all inside N3's 22-row set, each only by the slot's `margin-bottom:18px`; the 2 server renders stay; spacing becomes 14 / 14 and 12 / 12; a null render leaves no gap. The preview's slot and its 4 equivalence rows must mirror it. Option (a) is worse (25 rows, 5 outside N3's set, the committed strip screenshots stale). "Only this move" is defensible if disclosed: the column's gap doing the spacing IS the desktop's rule. | **FIXED** (`42e7819c`) — LeagueHome's mount says `inColumn`, so the slot brings no margin inside the gapped column (`src/components/League/backing/BackingLandingStrip.jsx:93`; `src/components/League/LeagueHome.jsx:147`); the bare mount keeps its margin (the screenshots and `landing-strip/*` rows do not move); the pin suite's inline slot and the preview mirror the mount; the comments now agree. The golden: §8. |
| PLACE-B2 | NOTE | Every seat change while the landing is open remounts the strip (another pod-list read; the strip absent until it answers). | CONFIRMED; mirrors the desktop. | **RECORDED** (accepted, as the desktop's). |
| PLACE-B3 | NOTE | None of the new visual states (the unseated N3 position, the sealed row, the cancelled card) can be seen in the dev preview or the screenshots; the preview's mobile landing is seated-only and flag-off `Lobby`. | CONFIRMED | **REPORTED for separate tasking** (§9); disclosed in the PR. |
| PLACE-D1 | MINOR | A failed group read showed a false "Cancelled · This pod was cancelled — your stake will be returned." for a live pod: `subscribeGroup` answers an error with `null`, the same as a missing document. (= WIRE-D1.) | CONFIRMED, wider: also a late listener error after a good snapshot, and an offline cache miss (WIRE-R-3). | **FIXED** (`467aa58e`, hardened `f74638de`, `340fc9d6`) — see WIRE-D1. |
| PLACE-D2 | MINOR (pre-existing) | A pod deleted while its pool is open still reads as a live stake — on Friday the strip still says "Closes Wed 7:00 PM ET"; Your Backing shows nothing. (= WIRE-D3(c).) | CONFIRMED | **REPORTED for separate tasking** (§9). |
| PLACE-D3 | NOTE (pre-existing wording) | After a cancelled pod's refund lands, the strip's between-weeks state says "Last week banked · Last week's result … standings" — words that fit no voided pod. | CONFIRMED | **REPORTED for separate tasking** (§9). |
| PLACE-D4 | NOTE (pre-existing, §9) | The card's tag says "Settled" for an admin-refunded pool and for an insufficient one, where the results card says "Refunded · stakes void" / "Did not qualify · stakes void". | CONFIRMED | **REPORTED for separate tasking** (§9). |
| PLACE-D5 | NOTE → **MINOR** | One deleted pod could show two refund sentences: the card fell back to the stake's `voidReason`, the results card reads the pool's (none, on the tombstone). (= WIRE-D2.) | CONFIRMED; re-rated MINOR — two sentences on one mobile screen (§9). | **FIXED** (`467aa58e`) — see WIRE-D2. |
| PLACE-D6 | NOTE | Three words for one state ("cancelled … will be returned", then "voided … refunded"; the League's own "voided"); a pool held at the refund's stake ceiling also reads "will be returned". | CONFIRMED | **For the founder** (§10) — the pre-refund sentence is the founder's own wording. |
| PLACE-D7 | NOTE (pre-existing branch) → **escalated** | While a cancelled pod's group read is pending, the card falls through to "Locked · plays Monday" and the section to "Day N of 5". | CONFIRMED; escalated by PLACE-R-1 (with D1 as first committed, the fall-through could persist after an error). Recording it is defensible only for the pending case alone; recommended keep-last-answer PLUS a neutral state. | **FIXED** (`340fc9d6`) — until a closed pod's group read answers, the card carries no status tag and the section no line (`src/components/League/backing/YourBacking.jsx:169`, `:372`); "plays Monday" is said only of a pod known to play. No golden row moves. |
| PLACE-R-1 | MINOR (a regression in `467aa58e`) | A listener error after a good snapshot overwrote it with `undefined`: a pod already known to be voided then read "Locked · plays Monday · Day 4 of 5" — the prompt's "never", broken. | raised by the refuter | **FIXED** (`340fc9d6`) — on an error `useMyBacking` keeps the last answer (`src/hooks/useMyBacking.js:112`); only a pod never answered records `undefined`. |
| PLACE-R-2 | NOTE (pre-existing) | For a voided pod whose refund is deferred (the freeze, a failed refund), the results card says "This pod is complete; its pool settles shortly." while Your Backing on the same screen says "cancelled". | raised by the refuter | **REPORTED for separate tasking** (§9). |
| PLACE-R-3 | NOTE (pre-existing) | Trainer stats count a cancelled pod's stakes, while its refund is pending, as "Backers on you" / "In play on you"; the lazy close shuts pools without refunding them. | raised by the refuter | **REPORTED for separate tasking** (§9). The trainer route's lazy close reaches only seated pods of this week or later that are due; a voided pod's pool closed before its battle began. |
| PLACE-R-4 | NOTE (for the SEAL record) | "No round's set of documents depends on a sealed book" holds for what the route reads, not for round 1's response size. | raised by the refuter | **FIXED** in the words (`a6d95173`) — the claim now covers the queries issued and the documents fetched, and states the stakes query's response as the one residual (`api/_utils/backingStats.js` "THE TRAINER'S READS"; `api/backing/trainer-stats.js` header). |

### WIRING lens — lifecycle, data paths, races, failure paths — 13 findings; refuter: 12 CONFIRMED, 1 PARTIALLY REFUTED; refuter added 6

Clean (lens; re-verified by the refuter): **E** — 13 probes: another request's close or settlement on either side of this request's reads (WIRING-1's twin, both orderings), lost acknowledgements after the close, the settlement and the refund, dev pools, deleted-group tombstones, the freeze, resolving holds, steady-state cost — seven of them fail against `main`'s reader, so the fix is load-bearing; the pod list re-exports the same function object and its 52 rows pass. **C** — StrictMode leaves one listener and one timer; five mount/unmount cycles leave none; a reply inside the grace re-arms nothing; a close beyond `setTimeout`'s ceiling arms nothing; only one strip is ever mounted. **D** — a group read that has not landed is never taken for a missing one; an admin refund of a pod that plays and an insufficient pool are not cancellations; the section line speaks only for pods that play. **A** — one member-scoped `array-contains` clause (no index needed); reads only after auth and the flag; the fold's reorder even closes a `main` leak (an excluded open stake moved `excludedStakes`). **B** — the seat gate's two state updates batch, so a seated strip mounts once. **BUILD_RULES** — no fenced file; no new api→src module edge; no hex literal or `transition={{` added; the token, motion and flag-pin guards green.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| WIRE-E1 | MINOR (in-branch) | When the settle-on-read pass throws AND the pool cannot be re-read, `loadPod` returned null: the weeks path dropped the pod, and its week — become `lastScanned` — was stepped past by the cursor for good; the one-pod path answered `pod: null` (the desktop's first page is `limit: 1`, so an older week would show as "the latest"). The file's own header routes plain-read failures to a 500. | CONFIRMED (two failures in one request; harm limited to a session's pagination); the planned 500 closes it — verified on a prototype, the hook keeps its cursor on an error, the retry answers the week. | **FIXED** (`4c54ab87`) — the re-read is a plain read and its failure the request's 500 (`api/backing/results.js:216`). Rows: both paths answer 500, the cursor never steps past the week, the retry answers it as the close left it. |
| WIRE-E2 | NOTE (pre-existing) | The stakes are re-read after the pool is answered; a refund landing between the two answers `closed` / "settling" beside a voided stake. The pod list has the same window. | CONFIRMED (passes on `main` too); a read-only transaction is feasible (`{ readOnly: true }` in the installed Admin SDK). | **REPORTED for separate tasking** (§9). |
| WIRE-E3 | NOTE (pre-existing posture) | A failed stake re-read kept the pre-read copies, answering a terminal pool beside a `live` stake ("settling" under "Did not qualify · stakes void"). | CONFIRMED (`main` swallowed the same failure); the planned 500 closes it. | **FIXED** (`4c54ab87`) — the request's 500, never the stale copies (`results.js:225`); the header says the pod list keeps its own rule. |
| WIRE-E4 | NOTE (test gap) | No row checked that the catch path re-reads a dev pod's pool at its own id: `.doc(located.poolId)` → `.doc(groupId)` passed all 31 rows. | CONFIRMED | **FIXED** (`4c54ab87`) — a dev-pod row (close commits, settlement throws, the answer is `dev-…` as it now stands); that exact mutant reds it (§6). |
| WIRE-C1 | MINOR (in-branch) | The close re-read fired once, off the client's clock: a client more than 5 s ahead of the server, or one failed read, left the strip saying the window was open after it closed. | **CONFIRMED, wider** (6–120 s ahead: open for good; 10 min behind: 605 s late). Recommended the bounded follow-up, re-armed on each read's COMPLETION, not on the error value (a repeating error code recomputes nothing). | **FIXED** (`f3d2f993`) — a pool a re-read still finds open gets ONE follow-up a minute after its close (`CLOSE_REREAD_FOLLOW_UP_MS`, `src/components/League/backing/backingStripState.js:109`); at most two reads per close, one timer at a time, never a poll. Residuals recorded: a clock more than a minute ahead; a clock behind re-reads late by its skew (WIRE-R-2). |
| WIRE-C2 | NOTE (in-branch, scale) | Every mounted strip fires at the same `closesAt + 5 s`; each pod-list read runs `ensureClosed` for up to 40 pods. Idempotent; contention ASSUMED. | CONFIRMED (10 reads → 10 close transactions per pool; the fixture has no locks). | **RECORDED**; if a jitter is added later, derive it deterministically (a uid hash), under 60 s (§9). |
| WIRE-C3 | NOTE (test coverage) | On mobile the strip is unmounted while the Backing screen is open (LeagueHome swaps the lobby out), so the "mobile" PRE-1 row pictured a composition the app never renders. | CONFIRMED; re-scoping adequate. | **FIXED** (`71d84df3`) — the beside-row is desktop-only; a new row drives the real `LeagueHome`: open Backing from the strip, stake, go back — nothing listens while the screen is open, and the returning strip reads the stake fresh. |
| WIRE-D1 | MINOR (in-branch) | "Gone" and "errored" were not told apart: on a listener error `subscribeGroup` answers `null`, `useMyBacking` stored it, and a pod in battle read "Cancelled · This pod was cancelled — your stake will be returned." (`main` said "Locked · plays Monday" — also wrong, but no promise about money.) (= PLACE-D1.) | CONFIRMED, less likely than implied (an auth or quota failure breaks the pool listener too, and then no card renders); both halves of the fix are required; a cache miss is a second trigger (WIRE-R-3). | **FIXED** (`467aa58e`, `f74638de`, `340fc9d6`) — `subscribeGroup(groupId, callback, onError)` reports an error apart (`src/services/tournamentGroupService.js:82`, every other caller unchanged); `useMyBacking` records an error before any answer as `undefined` and keeps any answer it has (`src/hooks/useMyBacking.js:112`); Your Backing counts only a document or `null` as answered (`YourBacking.jsx:358`). Rows: the service (new suite), the hook, the card on both layouts. |
| WIRE-D2 | MINOR (§9) | Your Backing took its refund sentence from the pool's `refundReason` and fell back to the viewer's stake `voidReason`; the results card reads the pool only; the tombstone close writes no reason — two sentences for one fact. (= PLACE-D5.) | CONFIRMED | **FIXED** (`467aa58e`) — the pool's reason alone (`YourBacking.jsx:146`); both surfaces now say "Every stake in this pool was refunded." Giving the tombstone its reason (`group_deleted`, which `settlePool` already reports) is **REPORTED** (§9). |
| WIRE-D3 | NOTE (mixed) | (a) "will be returned" is also said of a refund held at the refund's stake ceiling (true only after a Console repair); (b) with only a cancelled pod the strip is quiet and the header reads "No pods to back yet"; (c) pre-existing: a live stake on a pod deleted while its pool is open reads STAKED (= PLACE-D2). | CONFIRMED: (a) theoretical at beta scale (≥96 live stakes in one pool); (b) accurate; (c) pre-existing. | (a) **For the founder** (§10, with PLACE-D6); (b) RECORDED; (c) **REPORTED** (§9). |
| WIRE-A1 | MINOR | = PLACE-A1: "No closed weeks yet" shown to a returning trainer whose closed weeks nobody backed. | **CONFIRMED, stronger** (a realistic insufficient week reproduces it); the planned words ("No backers in a closed week yet") are themselves false when the week's backers were voided at a below-floor close. | **FIXED** (`a6d95173`) — see PLACE-A1. |
| WIRE-A2 | MINOR | The seal had no week bound: (a) a pool no path ever closes (a pod voided before any post-close read, with no stakes) sealed "This week" for ever; (b) every request read every group the viewer ever joined and one pool per seated pod. | **PARTIALLY REFUTED:** (a) real, but only a manual void or a failed duty settlement reaches it; the "stalled week" sub-claim is refuted — a stalled pod's pool is genuinely open, so its seal is true. (b) confirmed (65 group docs, 40 pool reads per request). **The briefed bound regresses SEAL-R-2 in a stalled week** unless due seated pools are closed in the route, and "sits in" must stay unbounded for live stakes (the Labor Day week's pod still banks day 5 the following Monday). | **FIXED** (`977b6db3`) — the seal watches seated pods of this week or later (`sealPodsOf`, `api/_utils/backingStats.js:206`), and round 2 closes a due seated pool (PLACE-A3) — so a stalled pool is revealed rather than hidden; `countedStakes` uses every seated pod (`:221`). The group query's own cost still grows with history (an index — separate tasking, §9). |
| WIRE-A3 | NOTE | (a) One failed seal read fails the whole route (500); (b) a seated DEV pod with an open dev pool seals the production record the fold keeps (dev pools skipped). | CONFIRMED: (a) defensible — keep; (b) skip dev pods. | (a) KEPT; (b) **FIXED** (`977b6db3`) — a dev pod never seals (`trainerSealed`, `:290`) and is never closed by this route. |
| WIRE-R-1 | MINOR (in-branch) | A failed close re-read disarmed every LATER close too: the instant was recomputed only on a new reply, so after a failed read Sunday's close was never armed — "Window open · Closes Sun 11:59 PM ET" indefinitely. | raised by the refuter | **FIXED** (`f3d2f993`) — the instant is recomputed as EACH read completes (`loading` falls), success or failure, and no timer runs while a read is in flight (`BackingLandingStrip.jsx:59`). |
| WIRE-R-2 | NOTE | A client clock behind the server re-reads late by its skew (and timers sleep with the machine). | raised by the refuter | **RECORDED** (§9): a server-time anchor or a `visibilitychange` re-check, still not a poll. |
| WIRE-R-3 | NOTE (in-branch) | Offline, the SDK raises a listener's first event from its cache even with nothing cached — for Your Backing that read as "answered: missing", i.e. cancelled. | raised by the refuter | **FIXED** (`f74638de`) — a caller that passes `onError` is never told "gone" by a cache-sourced snapshot; only the server's answer says a pod is missing (`tournamentGroupService.js:85`). |
| WIRE-R-4 | NOTE (pre-existing) | N4 covers the strip only: an open Backing screen keeps its window "open" past the close, and a stake refused `pool_closed` re-reads nothing. | raised by the refuter | **REPORTED for separate tasking** (§9). |
| WIRE-R-5 | NOTE (pre-existing) | The unsealed empty line, "Nobody has backed your team yet.", is false after a below-floor close that voided real backers' stakes. | raised by the refuter | **REPORTED** — honesty copy, the gate-3 fixes' territory (§9). |
| WIRE-R-6 | NOTE (test integrity) | Removing `called ||` from the results reader's stale test survives every row — probably an equivalent mutant (every settlement path that moves stakes also moves the pool's status). | raised by the refuter | **RECORDED.** |

## 4. The prompt's two named mutation checks

Run by the coordinator's pass (§6) — each mutant on its own `git archive 42e7819c` extraction, the edit an exact string replacement asserted to match once, the named suites run with vitest's JSON reporter; a check passes only when its NAMED row goes red.

| Check | Mutation | Result |
| --- | --- | --- |
| **A — "count an open pool → the walk row reds"** | the seal check removed from `computeTrainerStats` (`if (located?.pool?.status === POOL_STATUS.OPEN) continue;`) | **RED — 9 rows**, among them the walk row ("the whole reply, walked by key, is IDENTICAL whatever the open pool's book holds …", `api/backing/trainer-stats.test.js`), the real-close reveal row, the read-log row, and the pure fold's SEAL-1 row (`api/_utils/backingStats.test.js`). |
| **B — "mount it back below 'Watch a live game' → the placement row reds"** | both mobile lobbies mount `{backingSlot}` themselves again, after BracketFunnelSection (the strip below "Watch a live game" and the bracket line) | **RED — 17 rows**: the mobile placement rows ("mobile — seated: directly under the waiting room", "mobile — unseated: directly under the draft-slot picker", the bracket row — `backingLanding.test.jsx`), the host rule (`backingDark.test.jsx`), and 12 lit pin rows plus the row-set check (`backingMobilePin.test.jsx`). |

## 5. CONFIRMED / REFUTED

- **CONFIRMED (37 of 38):** SEAL-A1, SEAL-A2 (broader), SEAL-A3, the season-net note; DARK-M1 (wider), DARK-N1 (corrected), DARK-N2, DARK-N3, DARK-N4, DARK-N5; all 15 PLACEMENT findings (PLACE-A1 re-rated MAJOR → MINOR, PLACE-D5 NOTE → MINOR, PLACE-D7 escalated); WIRE-E1, E2, E3, E4, C1 (wider), C2, C3, D1, D2, D3, A1 (stronger), A3.
- **PARTIALLY REFUTED (1):** WIRE-A2 — its permanent seal is real but reached only by a manual void or a failed duty settlement; its "a stalled week holds it too" sub-claim is refuted (a stalled pod's pool is genuinely open — that seal is true); its cost half confirmed.
- **Proposed fixes or sub-claims refuted:** SEAL-A1's re-read variant (a book-dependent round trip on exactly the paths A2 closes — the skip was adopted); SEAL-A2's padding (a fixed delay cannot mask a variable round; the unread skip removes the need); DARK-N2's "the goldens still catch it" (RM4f escaped all 1,322 tests); DARK-N4's "no read after unmount" half (it cannot fail under the named defect); the first-week line's proposed words — "No backers in a closed week yet" (the WIRING lens's, and the coordinator's first two drafts) — false after a below-floor close that voided real backers, and "Stakes on your team’s sealed pool count here once it closes" (the first draft's second sentence), which presupposes stakes exist; the final wording holds in all three cases.
- **Refuter-added (17):** SEAL-R-1 (MINOR), SEAL-R-2, SEAL-R-3, the allowance note; DARK-R-1, DARK-R-2, DARK-R-3 (MINOR); PLACE-R-1 (MINOR), PLACE-R-2, PLACE-R-3, PLACE-R-4; WIRE-R-1 (MINOR), WIRE-R-2, WIRE-R-3, WIRE-R-4, WIRE-R-5, WIRE-R-6.
- **Every in-branch finding rated MINOR or above is fixed**, and the coordinator's own designs were refuted before landing: the trainer route's three read rounds (the SEAL refuter built it as a probe and measured it: the same reads in every shape whatever the book, jitter AUC 0.48 / 0.45 / 0.44), the lazy close (the PLACEMENT refuter: no leak, no new race), option (b) for the spacing (verified by hash), the follow-up re-read (the WIRING refuter's prototype, re-armed on completion).

## 6. Per-fix guard checks — the coordinator's mutation pass (LAST, on `42e7819c`)

54 mutants — the prompt's two, every one of the build's own fixes, and every review fix — each on its own `git archive 42e7819c` extraction under `scratchpad/mut/trees/`, discarded after; a mutant is caught only when every NAMED row goes red. **53 RED, 1 PARTIAL (by design), 0 GREEN.**

| Fix | Mutant | Red | Named rows that went red |
| --- | --- | --- | --- |
| A · SEAL-1 | A1 an open pool counted (the prompt's) | 9 | the walk row; the pure fold's SEAL-1 row |
| | A2 the seal never said (`trainerSealed` false) | 7 | "carries the seal instead"; "the seal is the POOL's fact"; "the SEAL is the pool's fact" |
| | A3 the seal keyed on stakes, not the pool (SEAL-R-2) | 3 | "carries the seal instead"; "the seal is the POOL's fact" |
| | A4 the old tally order (an excluded stake counted before the seal) | 1 — **PARTIAL, by design** | the pure fold's SEAL-1 row red; the route's walk row can no longer see it — round 3 never reads an open pool's stake flag (SEAL-A2), so the route cannot hand the fold an open pool's exclusion in any order: the reply is guarded twice |
| | A5 the client ignores the seal | 4 | "the row renders ABOVE the closed weeks"; "no closed week yet and this week sealed" |
| | A6 the sealed first week says "Nobody has backed your team yet" | 2 | "no closed week yet and this week sealed" |
| | A7 a figure in the sealed row | 1 | "the row renders ABOVE the closed weeks" |
| | A8 SEAL-A1 back (a contradicted live copy counted) | 2 | "SEAL-A1 — ANOTHER request's close commits …"; "SEAL-A1 — a `live` copy …" |
| | A9 SEAL-A2 back (the flag read for every counted stake) | 3 | "SEAL-A2 — what the reply COSTS …"; "reads the sealed meta of every COUNTABLE stake"; "SEAL-A2 — the stakes round 2 …" |
| | A10 SEAL-A2 back (a left seat's live stake read) | 2 | "SEAL-A2 — what the reply COSTS …"; "SEAL-A2 — the stakes round 2 …" |
| | A11 WIRE-A2 back (no week bound) | 2 | both WIRE-A2 rows |
| | A12 WIRE-A3 back (a dev pod seals) | 3 | "WIRE-A3 — a seated DEV pod …"; "the SEAL is the pool's fact …" |
| | A13 PLACE-A3 back (no lazy close) | 3 | the three PLACE-A3 rows |
| | A14 SEAL-R-1 (the pods query unscoped) | 1 | "SEAL-R-1" |
| | A15 PLACE-A1 back ("No closed weeks yet") | 1 | "PLACE-A1 / WIRE-A1 …" |
| | A16 PLACE-A4 back (the line in the box, ink3) | 1 | "PLACE-A1 / WIRE-A1 …" |
| B · N3 | B1 the strip mounted back below "Watch a live game" (the prompt's) | 17 | see §4 |
| | B2 the waiting room's slot below "Watch a live game" | 4 | "mobile — seated: directly under the waiting room"; "desktop — seated: … directly under the waiting room" |
| | B3 the mobile seat gate removed | 2 | "mobile: a SEATED viewer's strip mounts ONCE"; "a server render of the mobile landing carries no strip" |
| | B4 DARK-R-1 (a renamed wrapper only while dark, plain `Lobby`) | 1 | "DARK-N2 / DARK-R-1" |
| | B5 PLACE-B1 back (the slot's own margin inside the column) | 4 | the four LeagueHome mounted pin rows |
| C · PRE-1 / N4 | C1 the strip deaf to a confirmed stake | 3 | "desktop: the strip left mounted behind the Backing host moves to STAKED"; "DARK-N4" |
| | C2 the Backing screen never announces | 1 | the desktop STAKED row |
| | C3 a refused stake announced | 1 | "a REFUSED stake re-reads nothing" |
| | C4 no close timer | 5 | "one re-read just after the earliest close"; "NO POLLING" |
| | C5 the close timer never cleared | 1 | "one re-read just after the earliest close" (timer count after unmount) |
| | C6 a poll instead of one timer | 5 | "one re-read just after the earliest close"; "NO POLLING" |
| | C7 the re-read AT the close, no grace | 2 | "one re-read just after the earliest close"; `nextCloseRereadAt` |
| | C8 DARK-M1 (a listener above the flag read) | 1 | "DARK-M1" |
| | C9 DARK-M1 (a timer armed only while dark) | 1 | "DARK-M1" |
| | C10 DARK-N4 (the listener never removed) | 2 | "DARK-N4" (and the mobile LeagueHome row's listener count) |
| | C11 WIRE-C1 back (no follow-up) | 5 | both WIRE-C1 rows; "NO POLLING" |
| | C12 WIRE-R-1 back (recomputed on a new reply only) | 2 | "WIRE-R-1"; "WIRE-C1 — a re-read that FAILS" |
| | C13 a poll after the close (a follow-up every minute) | 2 | "NO POLLING"; `nextCloseRereadAt` |
| | C14 DARK-R-3 (a listener registered on import) | 1 | "DARK-M1" |
| D · WIRE-R-2 | D1 the strip files a cancelled pod's stake in the window | 1 | "is not the window" |
| | D2 Your Backing never sees a cancellation | 8 | VOIDED / EXPIRED / GONE; "then SHOWS THE REFUND"; `podCancellation` |
| | D3 the section's line over cancelled pods | 4 | "beside a pod that PLAYS"; "EXPIRED: the card reads" |
| | D4 a loading group read taken for a missing pod | 4 | "NOT landed"; `podCancellation` |
| | D5 the refund never shown | 1 | "then SHOWS THE REFUND" |
| | D6 WIRE-D1 back (card: a failed read taken for an answer) | 2 | "WIRE-D1 — nor is a pod whose group read FAILED" |
| | D7 WIRE-D1 back (hook: a failed read recorded as `null`) | 1 | "BEFORE any answer, the error is recorded as `undefined`" |
| | D8 WIRE-D1 back (service: `onError` ignored) | 1 | "with `onError` …" |
| | D9 WIRE-D2 back (the stake's `voidReason` fallback) | 1 | "then SHOWS THE REFUND" |
| | D10 WIRE-R-3 back (a cache "missing" answered as gone) | 1 | "WIRE-R-3" |
| | D11 PLACE-R-1 back (a later failure overwrites a known answer) | 1 | "PLACE-R-1" |
| | D12 D7 back (the tag guessed while unanswered) | 1 | "D7 / PLACE-R-1" |
| | D13 D7 back (the section line guessed while unanswered) | 1 | "D7 / PLACE-R-1" |
| E · WIRING-15 + twin | E1 WIRING-1's twin back (no re-read on a contradiction) | 2 | both "ANOTHER request's close / SETTLEMENT commits" rows |
| | E2 WIRING-15 back (the catch path answers the pre-pass pool) | 3 | "WIRING-15 — the lazy close COMMITS, then the settlement THROWS" |
| | E3 the stakes re-read on every read | 1 | "the steady state costs NO stake read" |
| | E4 WIRE-E1 back (a pod dropped on a failed re-read) | 1 | "WIRE-E1" |
| | E5 WIRE-E3 back (stale copies kept) | 1 | "WIRE-E3" |
| | E6 WIRE-E4 (the dev pod re-read at the production id) | 1 | "WIRE-E4" |

## 7. Measurements (the reviewers' own probes)

| What | Before | After | Where |
| --- | --- | --- | --- |
| The mobile strip's spacing (above / below), Chromium, real `LobbyTabbed`, 320–448 px | unseated 14 / 32, seated 12 / 30 (`main`: 18 / 18) | 14 / 14 and 12 / 12 — the desktop's | PLACE-B1: the lens, both refuters |
| The first-week line's contrast | 2.91 : 1 | 6.64 : 1 | PLACE-A4 |
| "Someone has backed you" by timing (jittered latency, AUC; 0.5 = indistinguishable) | first week 0.78; with history, thick book 0.79 | 0.48 / 0.45 / 0.44 | SEAL-A2: the refuter's R2 on the redesign, re-checked at `977b6db3` |
| The raced trainer reply at a voiding close | "5 backers · 1,500 BP" (never revealed) | 0 | SEAL-A1: the refuter's R1 through the real slot path |
| Trainer reads per request (40 old pods, 25 training) | 40 pool reads | the seal's pods only (one or two) | WIRE-A2 |

## 8. Verification

At `42e7819c`, the last code commit (this record's commit adds only this file):

- `npm run test:run` (the whole suite): `Test Files  779 passed | 3 skipped (782)` · `Tests  15374 passed | 64 skipped (15438)` · exit 0. (Before the review, at `0c0e15ed`: 778 files / 15,348 tests; the fixes added one suite and 26 rows.)
- `npm run test:rules` (the Firestore emulator): `Test Files  12 passed (12)` · `Tests  254 passed (254)` · exit 0. `firestore.rules`, `firestore.indexes.json`, `vercel.json` and every `.env` file: no diff against `main`.
- `vite build`: exit 0 — 3,924 modules, built in 33.4 s (the pre-existing chunk-size notices only).
- `npm run lint:gate`: exit 0.
- `git diff --stat 40acd199..42e7819c`: 34 files changed, 2,456 insertions(+), 251 deletions(-).
- **The fence (BUILD_RULES §1):** no fenced file changed; no changed file imports a fenced module; no fenced function is called.
- **The flags:** `BACKING_BETA_ENABLED` stays `false`; no flag changed.
- **The mobile golden** (`backingMobilePin.golden.json`) — regenerated for N3 (`91f70346`, 22 rows) and again for PLACE-B1 (`42e7819c`, the same 20 moved rows, each only by the slot's `margin-bottom:18px` — 20 of 20 checked row by row). Against `main`'s markup, row by row: 22 of the 94 markup rows changed — `landing/{bracket,no-bracket}/on/{unseated-mounted,seated-mounted}` and `{lobby,lobby-tabbed}/{bracket,no-bracket}/seated/{open,staked,week,between}` (20: the strip moved to directly under the Auto-draft card / the hero, into the centre's gapped column; its markup otherwise byte-identical, and everything else identical with it excised) and `landing/{bracket,no-bracket}/on/unseated-ssr` (2: the strip absent — it mounts once the seat subscription answers, and a server render runs no effect); the other 72 markup rows and both funnel rows are `main`'s, byte for byte. **The dark golden** is unchanged (byte-identical in both commits; the DARK reviewers regenerated it so).

## 9. Reported for separate tasking (BUILD_RULES §3 — found, not fixed here)

1. **The lobby's pre-existing overlap at ≤1024px with the flag off** (`LD_STYLE`'s `min-height: 0` on the reflowed grid items) — on `main` today (the desktop record §9), **reported again, as this build's prompt requires.**
2. SEAL-A3 / SEAL-R-3 — the admin exclusion flag reaches the backer (`my-stats`) and, by subtraction, the trainer — **a founder decision** (§10); two comments contradict the behaviour (`api/_utils/backingStats.js:27`, `firestore.rules:716`).
3. `computeMyStats`' season net reads +100 for an admin-excluded LIVE stake (the excluded effect subtracted twice) — SEAL out-of-lens note.
4. SEAL-R-2 — `closePool` reads each refund wallet with a sequential `await`: a triggering request learns a deleted pod's distinct-backer count by duration; read them in parallel inside the transaction.
5. Refunds never restore allowance; the spec's reason ("because the week has closed") is false for slot pods and the tombstone close (SEAL refuter, out of lens).
6. The stakes query's response on the trainer route grows with the sealed book (one round trip either way) — leaving the sealed stakes out needs a composite index (PLACE-R-4's residual).
7. The trainer route's member-scoped pods query returns every pod the viewer ever sat in (WIRE-A2(b)'s group half) — an index or a field.
8. The in-memory Firestore fixture ignores `array-contains` (test infrastructure; SEAL-R-1's row records the clause instead).
9. WIRE-E2 — the results reader's pool/stake read window (a read-only transaction is feasible).
10. WIRE-C2 — every mounted strip fires at the same close; if a jitter is added, derive it deterministically, under 60 s. WIRE-R-2 — a clock behind re-reads late by its skew (a server-time anchor, or a `visibilitychange` re-check).
11. WIRE-R-4 — N4 covers the strip only: an open Backing screen stays "open" past the close; a `pool_closed` refusal re-reads nothing.
12. WIRE-D2's other half — the tombstone close should write `refundReason: 'group_deleted'` (settlement already reports it), so both surfaces say the specific sentence.
13. PLACE-D2 / WIRE-D3(c) — a pod deleted while its pool is open still reads STAKED with a passed close ("Closes Wed" on Friday).
14. PLACE-D3 (the between-weeks wording after a refund-only settlement), PLACE-D4 ("Settled" for admin-refunded and insufficient pools), PLACE-R-2 (the results card's "settles shortly" beside Your Backing's "cancelled" for a deferred refund), PLACE-R-3 (trainer stats count a cancelled pod's stakes while its refund is pending), WIRE-R-5 ("Nobody has backed your team yet." after a below-floor close — honesty copy, the gate-3 fixes' territory).
15. PLACE-B3 — no preview state for the new visuals (the unseated N3 position, the sealed row, the cancelled card); DARK-N3's lit plain-`Lobby` pin row (generated on `main`); DARK-R-2 — a uid-switch variant of the WIRE-7 row, both lobbies.
16. PLACE-A3's residual — a trainer-stats panel left mounted still says SEALED until it re-reads.

## 10. For the founder

- **The exclusion flag's reach** (SEAL-A3, SEAL-R-3): whether any count of admin exclusions belongs in either stats reply — today the backer's `my-stats` drops and counts excluded stakes (by the PR 5 review's HON-5), and the trainer can infer them by subtracting the reveal's figures from theirs.
- **The first-week line and the sealed row's copy** (PLACE-A1 / A2, WIRE-A1): "Nothing counted from a closed week yet. Once your team’s pool closes, this shows how many backed you, how much, and how they did." under "Next week · your team’s pool · SEALED · until it closes". The reply cannot tell a first week from an unbacked or voided one, so the design's "No closed weeks yet" could not be kept true.
- **One extra mobile lobby render per "no seat" answer** (DARK-N1) — the desktop's accepted WIRE-7 cost, mirrored; no markup or behavioural change. Removing it means a flag read in both lobbies' seat setters.
- **The spacing** (PLACE-B1): the 20 golden rows N3 moved also drop the mobile slot's own 18px margin — the column's gap spaces the strip, as on desktop (14 / 14 and 12 / 12 instead of 14 / 32 and 12 / 30). The alternative was to ship the uneven spacing and move these rows again later.
- **The cancelled pod's words** (PLACE-D6, WIRE-D3(a)): "This pod was cancelled — your stake will be returned." (the founder's own sentence) is also said of a refund held at the refund's stake ceiling, true only after a Console repair — theoretical at beta scale.
