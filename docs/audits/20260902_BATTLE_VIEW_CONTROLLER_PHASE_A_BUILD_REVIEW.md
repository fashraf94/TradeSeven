# Battle View controller — Phase A (A1 → A3) adversarial build review

**Date:** September 2, 2026
**Status:** BUILD_RULES §2 review record (the diff exceeds 10 files / 1500 lines). Findings, dispositions and the CONFIRMED / REFUTED split — both refuters complete (Refuter A: the code findings; Refuter B: the test-integrity findings and the notes). First committed with Refuter B's rows pending; completed in the follow-up docs commit.
**Branch:** `claude/battle-view-controller-phase-a-v5gog5` (harness-assigned to the build session; the rulings §1.2 name `…-i51j5l`, the Phase 0 session's branch — same task, new session, new harness name).
**Diff reviewed:** `eaf2a0e2..d817f16` — A1 `540fdc9`, A2 `c570d61`, A3 `d817f16`; 37 files, +3514 / −257. **Fixes:** `a41b311` (this review), then the handover `69e18ef`.
**Prepared by:** Claude Code (coordinator), under `docs/BUILD_RULES.md` §2.

## 0. Method

- **Isolation (BUILD_RULES §2, Sep 2 ruling):** every reviewer worked on a `git archive d817f16` extraction under the session scratchpad with `node_modules` symlinked, read-only on git and on the working tree; mutation checks ran in the snapshot only. A second extraction of the pre-build commit `eaf2a0e2` served the byte-identity lens.
- **Five independent lenses**, each a separate subagent with its own brief: **L1** domain correctness and honesty (copy rules, hazards 1–23, D-51/59/60/61/62); **L2** wiring and lifecycle (jsdom mounts, effects, tab switches, StrictMode); **L3** flag-off byte-identity (the same `renderToString` script run in both trees over prop matrices; a randomized oracle sweep of the adapter); **L4** test integrity (27 mutations in a private extraction, each recorded by failing-test name); **L5** cross-phase consistency, the fence, the string inventory, the seed §3 test map, the rulings §3 directives, the DO-NOTs, A4 / Phase B readiness. Plus the environment's `/code-review` skill at effort `high` (single-pass, no fan-out — it says so itself; recorded as a sixth input, not as the §2 review).
- **Refutation:** every finding was handed to a refuter with the reviewer's own repro (or a sharper one), against the untouched snapshot. Verdicts below. Refuter A: the code findings; Refuter B: the test-integrity findings and the notes.
- **Build and suite:** `vite build` green after the fixes; full unit suite 562 files / 9456 tests green after the fixes (557 / 9406 at `d817f16`). `flagPinGuard`, `deskHonesty`, `tokens.guard`, `motion.guard` green throughout.

## 1. Executive verdict

| Lens | Raised | CONFIRMED | REFUTED | Fixed in `a41b311` | Recorded (copy / ruling / deferral) |
|---|---|---|---|---|---|
| `/code-review` | 8 | 8 | 0 | 8 | — |
| L1 domain / honesty | 3 + 3 notes | 3 + 3 | 0 | 2 (F10, F12) + 1 note (N9 docstring) | N5, N6, N7 |
| L2 wiring / lifecycle | 5 + 4 notes | 5 + 3 | 1 (F15) | 5 (F1, F14, F5, F3, F13) | N2, F16 |
| L3 byte-identity | 0 + 2 notes | — | — | N9 (docstring) | N10, N11 (pre-existing) |
| L4 test integrity | 5 MAJOR + 2 MINOR gaps + 4 notes | 6 gaps + 4 notes | 1 (T7, as a gap) | T1–T8 (guards added; T7's fixture sharpened anyway) | T9, T10 |
| L5 fence / cross-phase | 5 + 5 notes | 5 + 5 | 0 | F1, F7, F9, F10, F11 | N1, N2, N3, N4 |

**Totals:** 34 items raised across the lenses (13 code findings, 3 lifecycle notes, 10 test-integrity items, 8 notes); 32 CONFIRMED, 2 REFUTED (F15, T7). Twenty-three fixed or guarded in `a41b311`; the rest recorded below as copy requests, rulings or A4 deferrals.

**Bottom line:** no finding touched the fence, a battle-doc write, a model call, or flag-off byte-identity (L3: 0 differences over ~76,000 rendered cases and an 84,312-point adapter sweep, outside the sanctioned D-62 Desk string). The confirmed defects were all inside the flag: the landing played once per mount, the Safari-incompatible regex, the per-snapshot price-poll restart, the stale Why? state after a swap, the receipts' completion flattening, the engine-outage `Held`, and the draft-erasing door — every one fixed with a guard that fails under the defect. The test-integrity lens found the guards the first pass lacked; all are now in place.

## 2. Findings — CONFIRMED, fixed in `a41b311`

| # | Sev | Where (at `d817f16`) | Claim | Refuter evidence | Fix | Guard |
|---|---|---|---|---|---|---|
| F1 | MAJOR | `src/screens/battleView/LandingWash.jsx:18-27` | `done` is never reset when `landingKey` changes; the wash plays once per mount, not once per check (CR, L1, L2, L5 — four independent repros). | jsdom, `motion.div` stubbed to fire `onAnimationComplete`: key k1 → mounts 1; key k2 → mounts still 1. | Played-state keyed on the landing (`doneFor === landingKey`). | `LandingWash.test.jsx` |
| F14 | MINOR | same | A re-entered Matchups tab remounts every wash with `done=false` and a non-null key → a stale landing replays (L2). | jdom at the screen level, flag on: open → 0 washes; a new stamp → 7; Command → Matchups with no new snapshot → 14. `useLandingKey` never cleared the key. | `useLandingKey` clears the key `LANDING_CLEAR_MS` (1 s) after the landing starts — a timer that only ENDS a landing the snapshot started. | `useLandingKey.test.jsx` "the key clears itself" |
| F2 | MAJOR | `src/screens/battleView/selectWhyState.js:103` | `(?<![A-Za-z0-9])` throws at `RegExp` construction on Safari < 16.4 — inside Vite 7's default `safari16` target; any row panel with a rationale unmounts the screen to the App ErrorBoundary (CR, L2 note). | Vite 7.1.12 `ESBUILD_BASELINE_WIDELY_AVAILABLE_TARGET = […, "safari16"]`; caniuse-lite `js-regexp-lookbehind` safari 16.0–16.3 = n; esbuild leaves a string-built RegExp untouched; hosted under `<ErrorBoundary name="Battle View">`. Narrowing: the book panel never builds the regex. | A captured prefix character instead of the lookbehind. | emphasis rows in `selectWhyState.test.js` |
| F3 | MAJOR | `src/screens/AgentBattleScreen.jsx:536-552, :608-614` | Under the flag `startingPrices` / `allSymbols` take identity from the per-snapshot doc → `fetchPrices` recreated → an EODHD fetch + poll-timer restart on every doc write (CR, L2). | jsdom store with deep-cloned snapshots: fetches 1 → 2 → 3, `setInterval` 2 → 3 → 4 across two `statusFeed`-only writes; flag-off stays at 1. | `useContentStable`: the live sources keep their identity until a value changes. Flag-off untouched. | `useContentStable.test.jsx` |
| F4 | MINOR | `src/screens/battleView/landing.js:51-57` | Seeding on the first STAMP means a battle opened before its first check never lands that check (CR; L2 called it a design call). | `enabled, lastScoredAt: undefined` (doc present) → first stamp → key null. | Seed on the doc's first snapshot (`docPresent`), even with a null stamp. | `useLandingKey.test.jsx` "opened BEFORE its first check" |
| F5 | MINOR | `src/screens/AgentBattleScreen.jsx:906-909` | `whyOpen` keeps the old symbol after a swap; the render guard hides the panel but the next tap on the new piece closes the hidden state instead of opening (CR, L2). | jsdom screen: swap star-0 AAPL → XOM; 1st tap `aria-expanded=false`, panels 0; 2nd tap opens. | Toggle on row AND symbol. | `AgentBattleScreen.controller.jsdom.test.jsx` |
| F6 | MINOR | `src/screens/battleView/WhyPanel.jsx:92` | `aria-labelledby` names an id rendered only when `state.header` exists → an unlabelled region with no check time (CR). | `renderToString` with `selectWhyState(null, 'AAPL', null)`: the attribute present, the id absent. | `aria-label={state.label}` fallback. | `WhyPanel.render.test.jsx` "accessible name" |
| F7 | MINOR | `src/components/Agent/AgentChat.jsx:155-162` | New colour literals in the receipt line contra BUILD_RULES §10 / rulings §3.11 (CR, L5). | `DIFF.patch` lines 211/212/221 are `+` lines; `#5eead4` is the `--ft-teal` value. | `cssVar('teal')` / `cssVar('text-secondary')`. | (flag-off card untouched — L3) |
| F8 | MINOR | `src/screens/battleView/deriveTurnLine.js:58` | `toMillis` duplicates the adapter's private one verbatim (CR). | Bodies byte-identical modulo `export`. | Exported from the adapter, re-exported by `deriveTurnLine.js`. | existing suites |
| F9 | MINOR | `battleViewCopy.js:35`; `TacticalRow.jsx:201-238` | `Why?` defined, consumed nowhere; the row's tap surface has no visible or accessible "Why?" (L5). | `rg "COPY\.why\b"` → none; the rendered button carries no text/aria-label/title. | A visible `Why?` (from the copy module, passed as `whyLabel`) under the left gauge; part of the button's accessible name. | controller test "visible Why? affordance"; flag-off "no data-why-label" |
| F10 | MINOR | `WhyPanel.jsx:119-121`; `selectWhyState.js:131` | Raw `exitReason` codes (`haiku_decision`, `guardrail_stopLoss`, …) reach the player verbatim — the attribution family hazard 12 names for `source` / `triggeredBy`; one names the model tier (L1, L5). | Both codes render in the html; the engine writes them top-level on `trades[]`; `agentRiskManager.js:552-554`: "source (WHICH system decided) is orthogonal to exitReason (WHY)". The "hazard 12 forbids" part is by analogy — a judgement, recorded as such. | `exitReason` is not surfaced anywhere; the trade line renders the receipt's `rationale` (the agent's words). Recorded as CONSTRAINED for the ledger (the seed's "engine text (`exitReason` / …)" is a machine code). | `selectWhyState.test.js`, `WhyPanel.render.test.jsx` |
| F11 | MINOR | `src/screens/battleView/deriveReceipts.js:84-87` | Completion flattens a proven `Replaced {t}` into `Expired` (`at: null`) — the less specific state (L5). | `'active'` → `t-1 = replaced@16:58`; `'completed'` → `t-1 = expired@null`. D-61's text says only "Expired = battle complete"; "every directive, current included" was the author's reading. | Only the directive current at the close expires; Replaced stays Replaced. | `deriveReceipts.test.js`, `AgentChat.receipts.render.test.jsx` |
| F12 | MINOR | `src/screens/battleView/selectWhyState.js:54-91` | An engine-outage tick (`haikuError` stamped; rationale `Haiku call failed — defaulting to HOLD` / the budget-skipped text) renders as `Held` + that text — an outage dressed as a decision, leaking the vendor model and the cron budget (L1). | Fixtures match `agent-evaluate.js:2628-2667` exactly; `selectWhyState` → `{ held, 'Haiku call failed — defaulting to HOLD' }`; `haikuError` never read. | `haikuError` → the absence state (`No decision recorded at this check`); C1. | `selectWhyState.test.js`, `WhyPanel.render.test.jsx` |
| F13 | MINOR | `AgentChat.jsx:645`; `AgentBattleScreen.jsx:913` | The door's prefill overwrites a typed draft; the book door hands `''` and wipes it (L1 note, L2). | Draft → prefill → `'About SLB — '`; draft → book door → `''`. Narrowing: in the tabbed layout only the book door reaches a live draft (the row door's tab has no composer); the row case becomes reachable at A4. | An empty prefill only focuses; a piece prefill fills the composer only when it is empty or still holds the previous, untouched prefill. | `AgentChat.prefill.test.jsx` "a typed draft is never erased" |
| N9 | NOTE | `featureFlags.js` (the flag's docstring) | "When FALSE every surface is byte-identical" contradicted by the sanctioned D-62 Desk string (gated by `COMMAND_CENTER_SYNC_ENABLED`, dark) (L3). | L3's Desk matrix: exactly the 80 `LIVE_CLOSED` rows differ, as D-62 prescribes. | Docstring names the one flag-independent change. | `flagPinGuard` docstring check |

## 3. Findings — CONFIRMED, recorded (copy requests, rulings, deferrals)

| # | Where | Claim | Disposition |
|---|---|---|---|
| N5 | `selectWhyState.js:65` ← `agent-evaluate.js:2462-2466` | `Argued for a swap · held by a guardrail` also fires when `downgraded = true` came from a thrown `executeSwapServer` (`validationErrors: ['Swap execution failed: …']`) — no guardrail held anything (L1). Refuter: fixture through `selectWhyState` → the guardrail label. | The seed mandates the string for `downgraded === true`; the footer `the system held it` is the accurate half. **Copy request** to the design chat; `validationErrors[]` distinguishes the cases. In the handover §5. |
| N6 | `AgentChat.jsx:131` | `DIRECTIVE LOCKED IN` (shipped label, kept per the seed) sits above `Replaced {t}` / `Expired` (L1). Refuter: both in the same card html. | **Copy request** — a superseded card's label. In the handover §5. |
| N7 | `deriveTurnLine.js:132-134` | A sixth turn-line state, `First check coming up`, for LIVE with no stamp (L1). | Honest, guarded, Desk-shipped; recorded for the ledger. |
| N10 | `deskCopy.js:140-146` | With no last check the closed string reads `Market closed · next Tue 9:30 AM ET` — the word "check" dropped vs the shipped `next check` (L3). Refuter: confirmed against `eaf2a0e2`'s string; D-62's template does not spell out that cell. | **Copy note** for the design chat. |
| N1 | `battleViewCopy.js:7` | `Game Tape` claimed by the header, absent from the module (L5). | Deferred to A4 with the link (handover §3). |
| N2 | `AgentBattleScreen.jsx` (`:1108`, `:1163` at `d817f16`) | This turn renders above the board AND inside the open book panel in the tabbed layout (L2, L5). | The seed's letter for both places; A4 picks one (handover §6). |
| N3 | — | Branch name mismatch with rulings §1.2 (L5). | Recorded here and in the handover. |
| N4 | `AgentChat.jsx:1155` | `LiveActivityPanel` still mounts under the flag until A4 — `Agent is active.` + pulse beside the turn line (L1, L5). | Told to the founder in the handover §1 ("expect until A4"). |
| N8 | `AgentBattleScreen.jsx:858` | `getMarketState()` reads its own wall clock while the grace test uses the coarse `now` → the due→late flip can lag ≤60 s (L1). | What "coarse" means; not a lie. Recorded. |
| F15 | `AgentBattleScreen.jsx:457` | Flag-off gains a `useReducedMotion` matchMedia subscription on every mount (L2). | **REFUTED** (Refuter B): framer-motion keeps a module-level singleton (`hasReducedMotionListener` / `initPrefersReducedMotion`); every motion element already runs the same one-time init on mount, and the `eaf2a0e2` screen mounts motion elements on its first paint, so the one `(prefers-reduced-motion)` listener existed before Phase A. Measured: 1 call / 1 listener after the first flag-off mount, still 1 / 1 after four mounts. Flag-off gains one `useState` slot; markup identical. The handover's item 17 is corrected accordingly. |
| F16 | `TacticalRow.jsx:221-226`; the ScoreHeader row | a11y: the left row button contains the (pre-existing, mouse-only) symbol/points click targets; the header button is named by its whole content (L2). | A4's a11y pass (handover §6). |
| N11 | `ChamberFuse.jsx:227` (not in the diff) | `history: null` throws — identical in both trees; unreachable from this screen (L3). | Out of scope; reported (handover §8). |

## 4. Test-integrity findings (L4) — verdicts and guards

L4 ran 27 mutations in a private `git archive d817f16` extraction (backup → edit → 22 suites → restore, md5-verified). Mutations 1–9 and 10a failed exactly the rows the brief named (the downgraded branch → the three downgraded rows; `>=`→`>` → the equality rows; the grace boundary; the close clamp → 8 rows across three suites; Replaced's time; the enum family; the dollar branch → 12 dollar rows, ATR rows green; the promise beside the receipt; the flag flip → exactly the pin + the two guard rows, full suite; the forbidden word and the inline literal; the player half of D-59). The gaps it found:

| # | Sev | Claim (mutation left the suites green) | Refuter B verdict | Guard added in `a41b311` |
|---|---|---|---|---|
| T1 | MAJOR | The CPU half of D-59 cannot fail: prop and doc opponents were both MSFT (`liveOpponentPortfolio = null` → 10/10 green). | CONFIRMED — 22 files / 563 tests green under the mutation (clean re-run). | Prop MSFT vs doc AMD; both directions asserted under both flag states (`AgentBattleScreen.controller.test.jsx`). |
| T2 | MAJOR | No executable guard on the flag-off label text now that every row renders the row-computed proximity (`baseATR` default 99 → green). | CONFIRMED — 563 green under the mutation; no test rendered a row whose asset lacks `baseATR`. | `💣 2.5% to Bagger` pinned flag-off (`restFallback`, controller) and flag-on. |
| T3 | MAJOR | `useCoarseNow`'s cadence and flag-off inertness untested (interval 1 s → green; enabled guard removed → green). | CONFIRMED — both mutations 563 green. | `useCoarseNow.test.jsx`: 59 one-second steps → no render, ten minute steps → ten; visibilitychange; inert flag-off; cleanup. |
| T4 | MAJOR | The `?battleViewController=1` override — the founder's smoke door — untested (`=== '2'` → green). | CONFIRMED — 563 green; the pin test covers only the window-less path. | `battleViewControllerAccessor.test.jsx` (jsdom; exactly `1`; other params; call-time read). |
| T5 | MAJOR | The screen→WhyPanel and screen→AgentChat wiring unguarded (`entryPrice={null}` → green; `receipts={null}` → green — a flag-on card would show the D-60 promise with the suite green). | CONFIRMED — both green; also `setActiveTab('command')` removed from the door → green. | `AgentBattleScreen.controller.jsdom.test.jsx`: tap → facts (`Entry $34.10 · Held since 11:02 AM`), downgraded label, no lock, no code; one row at a time; CPU never; door → chat tab → `About SLB — ` focused → `data-receipt="filed"`, no promise; header → book with This turn between decision and door. |
| T6 | MINOR | prefers-reduced-motion → no sequence has zero coverage. | CONFIRMED — `reducedMotion ||` removed from the wash guard → green; the delay fold forced → green. | `LandingWash.test.jsx`: no wash under reduced motion; `useLandingTransition` → the `instant` token, no delay. |
| T7 | MINOR | `createdAt` sat in the same ET minute as the exchange, so "Filed from the EXCHANGE timestamp" passed when the strip read `createdAt`. | **REFUTED as a gap** — the two NAMED rows do pass under a `createdAt`-reading strip, but `ThisTurnStrip.render.test.jsx` "…without inventing a time" (receipts `{}`, `createdAt` 11:31) goes red under both the direct read and a fallback read. The fixture was sharpened anyway (different minute, `11:33` asserted absent). | `createdAt` planted at 11:33 vs the exchange 11:31; `11:33` asserted absent (`ThisTurnStrip.render.test.jsx`, controller test). |
| T8 | NOTE | The oracle imports the three config tables from the module under test (verbatim to `eaf2a0e2` by mechanical diff). | CONFIRMED — 124 identical lines; a `doubleBagger` 1.5 → 1.6 mutation left 563 green before the pinned tables. | `THRESHOLDS`, the `ACHIEVEMENT_CONFIG` texts and spot `calculateNextThreshold` values pinned literally beside the oracle. |
| T9 | NOTE | The inline-copy regex only matches `>text<` (`{'…'}`, `title=` escape it) — the inherited Desk rule shape. | CONFIRMED — `{'…'}` + `title` in WhyPanel → 165/165 green; plain `>…<` text → red. Regex identical to the base Desk rule. | Recorded; the verb scan still covers every string literal. |
| T10 | NOTE | The `3_games` row guards mid-battle expiry, not the enum family (`:93` does). | CONFIRMED — the enum mutation leaves `:86` green and reds `:93` + three render rows; a createdAt-age mutation reds only `:86`. | Recorded (test-name precision). |

## 5. Checked and clean (by lens, condensed)

- **L1:** no forbidden term in any rendered html over hostile fixtures; the `>=` join is the one join (and the server writes the stamp and the entry in ONE update on the full path — no transient "no decision" window; the early returns write the stamp alone, the designed absence); `downgraded` first, mutation-checked; one `next` (`deriveDueAt`), one proximity (`computeProximity`, once per side); `expiry` never read as a time; `directiveUtils` never imported; Replaced = text + time; Acted untouched; Expired on `completed` only; D-59 sources and the CPU field name (`opponent`, `agentBattleService.js:167`, `set-opponent.js:96`); no lock, no `source` / `triggeredBy`; the §4 strings match by code point; the landing keys on `!==` of the ISO stamp; no per-second clock.
- **L2:** Phase A adds one `useMemo` to `AssetSide`, placed before both early returns (the pre-existing after-return hooks are unchanged and unreachable in practice: swaps replace a slot in place, the server never writes `isCash`); `useLandingKey` seeds/plays identically under StrictMode; `useCoarseNow` adds one interval + one listener flag-on, none flag-off, both removed on unmount; the coarse tick alone moves live → `Checked {t}` → late; the Why? panel exits, one row at a time, cash/null slots not whyable, keyboard toggles; the prefill survives `AnimatePresence mode="wait"`'s deferred chat mount; `ExecutionCard` undefined / null / object; no stale closures; SSR clean; flag-off perf: `computeProximity` exactly once per populated side.
- **L3:** ProximityLabel 26,208 + 48,000 cases, TacticalRow 1,615 markup cases, AgentChat 768 + 8, AgentBattleScreen flag-off 40, AgentDesk 288 (LIVE / PRE_OPEN / POST_CLOSE) — all BYTE-IDENTICAL to `eaf2a0e2`; the adapter oracle sweep 84,312 points, 0 mismatches; `ManageStation` golden 24/24 in both trees; `openPrice` is added to the enriched asset AFTER the scorer receives the raw asset, and no consumer spreads or serialises the enriched object.
- **L4:** tests import what they guard (the only re-implementation is the documented oracle); the flag flip's blast radius is exactly the pin + the two guard rows.
- **L5:** the fence (37 files under `src/`; no §1 file; `archetypeImportBoundaryBaseline.json` byte-identical; the §2.3 ratchet green); no write / fetch / model call in any added line; all 19 §4 strings present and exact except `Game Tape` (A4); the rulings §3 directives 1–12 satisfied or correctly deferred (7, 9, 10 → A4); DO-NOTs 1–23 clean (12 by the letter; F10 closed the same class); flag + pin + `DARK_BY_DESIGN` in the first build commit; ruling 2's "same commit" honoured.

## 6. Build, suite, mutation checks

| Check | At `d817f16` | After `a41b311` |
|---|---|---|
| `vite build` | green (27.5 s) | green (22.1 s) |
| full unit suite (`--maxWorkers=2`) | 557 files / 9406 tests green | 562 files / 9456 tests green |
| `flagPinGuard`, `deskHonesty`, `tokens.guard`, `motion.guard` | green | green |
| mutation checks (L4, 27 mutations) | 10 gaps (§4) | all closed; the downgraded-branch mutation still fails exactly the three downgraded rows |

## 7. Out of scope — for separate tasking (BUILD_RULES §3)

Phase 0 §6 bugs 1–5 unchanged; `ChamberFuse.jsx:227` `history: null` (N11); D-14 latency: `This piece today` filters all of `trades[]` and `Held since` / `Checked` carry no date — truthful under fullday, revisit with the 3-day flip (L1).

*Prepared September 2, 2026 against `d817f16`; fixes in `a41b311`. Reviewer briefs, the ledger and the snapshot trees stayed in the session scratchpad; every claim above carries the reviewer's or the refuter's evidence.*
