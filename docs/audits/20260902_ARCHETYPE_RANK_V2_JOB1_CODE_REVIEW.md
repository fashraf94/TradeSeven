# Archetype Rank Interface V2 — Job 1 (dark build) — BUILD_RULES §2 adversarial review

**Date:** Sep 2, 2026 · **Branch:** `claude/archetype-rank-v2-job1-62tk4h` (base `origin/main` `de4113fd`) · **Diff at review:** 29 files, +3412/−26 (HEAD `2ad873c1`: seed `8e151ff6`, Phase A `613da8d8`, Phase B `2ad873c1`) — above the §2 threshold (≥ 10 files OR ≥ 1,500 lines), so this review is mandatory. **Spec:** `docs/specs/ARCHETYPE_RANK_INTERFACE_V2_BUILD_SPEC_V1_3.md`. **Fixes:** applied on-branch in the commit after `2ad873c1` (`9c36757b`).

## Executive verdict

**The dark build holds its two load-bearing claims — the fenced entry is exactly the three sanctioned lines, and flag-off is byte-identical to V1 — and every lens confirmed them independently.** The review found **no blocker**. Of 23 filed items: **12 defects CONFIRMED or PARTIALLY CONFIRMED by the independent refuter and FIXED on the branch** (3 major: SPEC-1, CT-1, CR-1-class; the rest minor/nit), **1 REFUTED** (CR-1 — hardening kept), **3 uncontested cleanups applied**, **1 process item** (this record), and **6 items REPORTED** for founder ruling or the flip checklist (spec ambiguities and preconditions, not code defects). Two review lenses (producer/Firestore lifecycle; test integrity) were cut off by an API session limit before reporting and were **re-run inline by the building session** with the checks listed in §4 — disclosed here per §2's "say so explicitly" clause. The refutation pass ran as an **independent agent** over every finding (§3). `vite build` ✓ (28.96 s). Final suite: **539 files / 8929 tests green**, 1 file / 62 tests skipped (pre-existing skips).

## 1. How the review ran

| Lens | Scope | Ran by | Findings |
|---|---|---|---|
| `/code-review` (skill, level high) | whole branch diff, 8 angles, single pass | harness skill (forked, no subagents — disclosed by the skill itself) | CR-1 … CR-8 |
| L1 — spec fidelity | `axisDerivation.js` + `archetypeScoringV2.js` vs spec §0/§2/§3/§4/§5, numeric probes | independent agent | SPEC-1 … SPEC-8 |
| L2 — fence, flag-off guarantee, import graph | fenced diff exactness, golden re-derived from `origin/main`, bare-mock semantics (vitest 4.0.17), ratchet/hash locks, client bundle sourcemap walk | independent agent | FENCE-1, GRAPH-1, RATCHET-1 |
| L3 — producer + Firestore lifecycle | cron diff, snapshot module, rules, size, expiry, timing | **agent lost to the session limit → re-run inline** (§4) | (none beyond CR-1/CR-7) |
| L4 — caller threading & data flow | draft core, live draft, client hook, tournament utils; mutation runs on a scratch copy | independent agent | CT-1 … CT-6 |
| L5 — test integrity (mutation) | test 16, golden validity, vacuity, the sixteen-test map | **agent lost to the session limit → re-run inline** (§4) | (none) |
| Refutation pass | every finding above, with concrete repros | independent agent | verdicts in §3 |

## 2. Findings, dispositions

Severity as filed by the lens; **Verdict** is the independent refuter's; **Disposition** is what the branch does.

| ID | Sev | Claim (one line) | Verdict | Disposition |
|---|---|---|---|---|
| CR-1 | major | `techRaw.rsi` mirrored `factors.rsi`, which `computeTechnicalScore` imputes to 50 when RSI is null — contradicting "never a neutral default". | **REFUTED** as a reachable defect — `calculateRSI` is null only under 15 closes and retention needs 50 (`rsi_reach.mjs`: 15/50-length arrays incl. undefined/null closes all finite). Hardening kept. | **HARDENED** (kept although refuted): a `rawRsiMap` from `calculateRSI` makes the contract true by construction rather than by a reachability argument; the contract test pins the source and forbids `factors.rsi`. |
| CR-2 | minor | Empty input + `universeSize` threw `axes_subset_unavailable` for the Contrarian only (week floor resolved before the empty check). | **CONFIRMED** — `[]` + `universeSize` → throws for the Contrarian, `[]` for the others. | **FIXED** — the floor is never resolved on an empty input; `[]` like every archetype. |
| CR-3 | minor | The doc-field parser (`axes_universe_size` / `universe_median_return1W`, undefined-vs-null) was hand-copied in the server draft core and the client hook. | not contested (cleanup) | **FIXED** — one `readUniverseAxisContext` in `axisDerivation.js`, imported by both. |
| CR-4 | nit | `readStockUniverse` became a dead wrapper. | not contested (cleanup) | **FIXED** — removed; `readStockUniverseContext` is the one reader. |
| CR-5 | nit | `isFiniteNumber` / `clamp100` duplicated across the two pipeline halves. | not contested (cleanup) | **FIXED** — exported from `axisDerivation.js`. |
| CR-6 | process | No §2 review record on the branch; the skill's own pass was not adversarial. | n/a | **ADDRESSED** — this record; the adversarial lenses + refutation above. |
| CR-7 | minor | Snapshot run label + ET date resolved from the run's FINISH time (a late/long 20:00 UTC run could skip its `intraday-last` snapshot). | **PARTIALLY CONFIRMED** — real, bounded: with `maxDuration: 300` the 20:00 run must START ≥ 20:55 UTC to miss; the ET date cannot roll. | **FIXED** — bound to `startTime`; contract test pins it. |
| CR-8 | minor | With the flag on, the client overlay would `console.warn` a coverage event on every late-draft recompute. | **PARTIALLY CONFIRMED** — mechanism real (default sink is `console.warn`, memo recomputes per pick); reaching < 5 needs V2 filter attrition the §6 ≥ 35 gate precludes. | **FIXED** — silent client sink (the console sink is the server's; snapshots carry the events). |
| SPEC-1 | major | Dislocation's inner percentiles used the complete-set pool; spec §2 + P-9 read as "each term over every name carrying that input". Reviewer fixture: code P 50 / Q 100 / R 0 vs spec-literal P 100 / Q 50 / R 0. | **PARTIALLY CONFIRMED** — both numbers reproduce (code P 50 / Q 100 / R 0; literal P 100 / Q 50 / R 0); the spec does not pin the inner pool — a ruling item on an under-specified formula. | **CHANGED** to the P-9-literal per-input pools (the reading that follows P-9's definition and keeps every name's returns in play) and pinned with the reviewer's fixture; **REPORTED** for ruling (report §9) — a one-line change either way. |
| SPEC-2 | minor | `score < anchor − gap` in floats gap-blocked a candidate exactly 10 below for 84 of 901 1-dp anchors (64.4 − 10 = 54.400000000000006). | **CONFIRMED** — `64.4 − 10 = 54.400000000000006`; T1 blocked at exactly anchor − 10; 84 of 901 1-dp anchors misclassify. | **FIXED** — the gap is compared at 1-dp resolution; 64.4/54.4 pinned. |
| SPEC-3 | minor | With < 5 distinct sectors the breadth phase stopped silently; spec step 2 says emit when none qualifies. | **PARTIALLY CONFIRMED** — behaviour as claimed; "none qualifies" has two readings and the event payload presupposes a candidate — a ruling item, improbable on the 11-sector universe. | **FIXED** — every breadth stop emits, with `reason` ∈ gap / no_unrepresented_sector / no_eligible. |
| SPEC-4 | minor | `techRaw` ABSENT ⇒ volatility from the persisted `atrPercentile` is an unsanctioned third state (spec names only the present-with-null case). | not contested (ruling item) | **REPORTED** for founder ruling (report §8/§9) — kept: the literal alternative nulls volatility for every name on a pre-Phase-A doc and empties 4 of 6 archetypes in the R-16 fallback; the behaviour is pinned by a test either way. |
| SPEC-5 / CT-2 | minor | A subset caller on a pre-Phase-A doc (both context fields undefined) computes the Contrarian week floor over the subset, silently. | **CONFIRMED** — the floor is computed over `available` with no event; reachable only on a pre-Phase-A doc with the flag on (the four tournament callers pass the full doc). | **PARTIALLY FIXED** — `week_floor_computed` is now emitted whenever the floor comes from the input; the transitional case itself is the R-16 acceptance (axes derive over the input too) and is REPORTED. |
| SPEC-6 / FENCE-1 | minor | The V2 module imported `VALID_ARCHETYPES` from the §1-fenced `agentArchetypeConfig.js` (spec: "imports nothing from the fenced tables"); the import was behaviourally redundant. | **PARTIALLY CONFIRMED** — the import is real and deviates from the literal spec text; the "redundant" sub-claim **REFUTED**: `ARCHETYPE_WEIGHTS_V2['constructor']` is truthy via the prototype, so a plain lookup would not throw. | **FIXED** — no fenced import; the unknown-archetype check is a `ARCHETYPE_KEYS_V2.includes()` on the frozen key list (prototype keys `constructor` / `__proto__` / `toString` / `hasOwnProperty` pinned to throw); `ARCHETYPE_KEYS_V2` is pinned equal to `VALID_ARCHETYPES` in the test; the ratchet baseline delta shrinks to `archetypeScoring.js` alone. |
| SPEC-7 | nit | The Diversifier's tie rule (quality desc) was applied to every archetype — a Speculator tie decided by FUND. | **CONFIRMED** — three degen names with equal axes and quality 90 / 10 / null order by FUND. | **FIXED** — quality tiebreak Diversifier-only; others by symbol. Pinned. |
| SPEC-8 | nit | Under the code's (only content-bearing) reading of "skipped candidates are never reconsidered", the anchor slides past skipped names, so the 10-point gap bounds each step, not the cumulative sacrifice. | not contested (ruling item) | **REPORTED** — a spec ambiguity for the founder (report §9); the code's reading is pinned by test. |
| GRAPH-1 | nit | The prompt-honesty tripwire only sees `from './x.js'` inside the two fenced assemblers, so the V2 module's registration at flip depends on discipline. | not contested (flip checklist) | **REPORTED** — flip-PR checklist item (report §8). |
| RATCHET-1 | nit | The baseline's two note strings were re-escaped (`§` → `§`) with no semantic change, tripling the diff on a ratchet file. | **CONFIRMED** — JSON-equal notes, raw text re-escaped; importers the only semantic change. | **FIXED** — original escaping; the diff is the one importer line. |
| CT-1 | major | No executable guard on the universe threading: nulling the universe at any of the three server call sites passed every suite (monotone one-sector fixtures made the fit equal the pool head). | **CONFIRMED** — mutation (universe nulled at all three call sites): 10 files / 188 tests pass. | **FIXED** — `trainingLifecycle.v2threading.test.js` + `liveDraftLifecycle.v2threading.test.js`: fit ≠ pool head, `fallback:false` asserted at both live sites and the training site, plus the no-doc `fallback:true` counterpart. |
| CT-3 | nit | Readers coerce a present-but-non-finite doc value to null (floor 0) while `resolveWeekFloor` treated the same value passed directly as "not supplied". Not producible by the writer. | **PARTIALLY CONFIRMED** — real split in code; not producible by the writer (integer size, `numOrNull`-filtered median). | **FIXED** — a supplied non-finite value takes the null reading. |
| CT-4 | minor | The client hook's V2 throw path was fully silent (`catch { ranked = null }`) against §4's "every throw path is loud". | not contested | **FIXED** — the R3 degrade stays; the cause is `console.warn`ed. |
| CT-5 | nit | Spec §4 / Phase 0 V-5 name `lobby-quickplay-training` as an entry to path 7; it calls only `formTrainingDraft` and never reaches the scorer. | not contested (doc accuracy) | **REPORTED** — doc accuracy (report §8). |
| CT-6 | minor | Flip precondition: P-14 turns V1's silent unknown-archetype → analyst fallback into a throw, and five non-fenced callers pass stored archetype strings unvalidated — one legacy key on an agent doc wedges that pod after the flip (loud, but a wedge until flag-off). | not contested (flip checklist) | **REPORTED** — flip-PR checklist: audit `agents.archetype` values against `VALID_ARCHETYPES` before flipping (report §8/§9). |

## 3. Refutation pass (independent agent)

The refuter ran read-only at HEAD `2ad873c1` with its own probes (`refute1.mjs`, `rsi_reach.mjs`, a tar copy `ct1/` with the three call sites mutated) and re-ran the two lens probes. Thirteen findings were attacked; the rest are uncontested cleanups or reported items.

| ID | Verdict | The refuter's evidence (condensed) |
|---|---|---|
| CR-1 | REFUTED | `calculateRSI` returns null only for < 15 closes; retention requires ≥ 50 bars; every 15/50-length probe (flat, random, undefined/null closes) is finite. `factors.rsi` is always the real RSI on the producer path. |
| CR-2 | CONFIRMED | `computeArchetypeRankingsV2([], 'contrarian', { gameMode: 'tournament', universeSize: 239 })` → throws; analyst / momentum_chaser / guardian → `[]`. |
| CR-7 | PARTIALLY CONFIRMED | Finish-time binding is real; with `maxDuration: 300` the 20:00 UTC run must start ≥ 20:55 to miss its label; the ET date cannot roll (runs are 06:30–16:00 ET). |
| SPEC-1 | PARTIALLY CONFIRMED | Both readings reproduce exactly (code P 50/Q 100/R 0; literal P 100/Q 50/R 0); the spec leaves the inner pool unpinned. |
| SPEC-2 | CONFIRMED | `54.4 < 64.4 − 10` is true in IEEE-754; the fixture emits `gap_blocked` at a reported gap of exactly 10.0. |
| SPEC-3 | PARTIALLY CONFIRMED | Silent stop confirmed; "none qualifies" admits two readings and the specified payload presupposes a candidate. |
| SPEC-5 / CT-2 | CONFIRMED | With both context fields undefined the floor is computed over `available`, no event; reachable on a pre-Phase-A doc only (the tournament callers pass the full doc). |
| SPEC-6 / FENCE-1 | PARTIALLY CONFIRMED | The fenced import is real; the "redundant" sub-claim REFUTED — `ARCHETYPE_WEIGHTS_V2['constructor']` is truthy through `Object.prototype`, so only an explicit key check throws `archetype_unknown` for it. |
| SPEC-7 | CONFIRMED | Equal-score degen names order by quality 90 / 10 / null. |
| CT-1 | CONFIRMED | Universe nulled at `trainingLifecycle.js` + both `liveDraftLifecycle.js` sites → 10 files / 188 tests still pass. |
| CT-3 | PARTIALLY CONFIRMED | The split exists; the writer emits only an integer size and a `numOrNull`-filtered median, so it is not producible without a hand-edited doc. |
| RATCHET-1 | CONFIRMED | Notes JSON-equal, raw text re-escaped; only `importers` changed semantically. |
| CR-8 | PARTIALLY CONFIRMED | Mechanism real; the < 5-name precondition needs V2 filter attrition that the §6 gate precludes in a normal draft. |

Uncontested (no refutation attempted, applied or reported as filed): CR-3, CR-4, CR-5 (cleanups), CR-6 (this record), SPEC-4, SPEC-8, GRAPH-1, CT-4, CT-5, CT-6.

## 4. The two inline lenses (disclosed)

**L3 — producer + Firestore lifecycle (inline checks, VERIFIED at HEAD):** `axes` is derived after the `stockEntry` objects are built (atrPercentile already rounded to 2 dp, returns 2 dp, `fundamentalScore` `|| null`) and those same objects are persisted (`rankingStocks` → `rankingsPayload.stocks`); the four doc-level fields are written inside the same block as the payload; the v1 `arch_scores` loop runs before `attachArchScoresV2`, which writes only `arch_scores_v2` (test 9's control proves v1 moves with `baggerBombFit` while v2 does not); the ops read is one Firestore read per run, before Step 2, and a read error ⇒ off + logged; the snapshot write is after `await batch.commit()`, inside try/catch → `errors[]`, and the handler still returns 200; expiry only under `!intraday`; the `?snapshotLabel=` override is regex-validated; the intraday schedule `0 14-20 * * 1-5` is UTC-only so 20:00 UTC is always the last run (no DST shift); the expiry query is a single-field range (no composite index); `firestore.rules` has no other `match /ops` and the two blocks sit inside the documents block after `stockTechnicalScores`; the stockRankings doc grows ≈ 290 B/stock (axes + techRaw + arch_scores_v2) ≈ +69 KB → ≈ 431 KB ≈ 41 % of 1 MiB, under the 60 % warn line (Phase 0's estimate: 442 KB); a snapshot ≈ 80 KB. Stage marks: `intradayQuotes` exists only on intraday runs (documented); no mark is skipped inside a taken branch. No strict consumer of the cron's JSON response exists (`firestoreWrites` / `stocksScored` have no readers outside the crons). CR-7 (finish-time binding) was the one real defect here and is fixed.

**L5 — test integrity (inline checks, VERIFIED at HEAD):** `git diff origin/main..HEAD --stat -- '*.test.*'` shows every pre-existing test file untouched except the two spec-mandated additions (`archetypeRegistry.test.js` +4: the `'archetypeScoringV2'` basename; `flagPinGuard.test.js` +2: the `DARK_BY_DESIGN` entry) — no assertion changed; `docs/registry-snapshots/` and `compositionProtectedStoresAllowlist.json` unchanged (test 16 ✓). The test-6 golden was independently re-derived by L2 from the `origin/main` engine (`6776c931…`, and the pre-entry file sha256 `26d4aa1b…`). Mutation evidence: the interleave's 13-name fixture was hand-traced through §3.3(a) steps 1–4 in the build session and again by L1 (which also produced the exact-gap and no-sector counter-fixtures now pinned); CT-1's mutation (universe nulled at the call sites) was run by L4 on a scratch copy and is now caught by the two threading suites; test 9's control row (v1 moves, v2 fixed) fails if the dual-write reads `archetypeScore` instead of `archetypeBaseScore`. The sixteen-test map is in the Job 1 report §4.

## 5. What was NOT changed by the review

The fenced file (`archetypeScoring.js`) — still exactly the three P-2 lines (re-verified after the fixes: `git diff origin/main -- api/_utils/archetypeScoring.js` = 3 +/1 −). `decide.js`, the assemblers, `agentArchetypeConfig.js`, the version constants, the hashed tables, `vercel.json`, the constraint strings, the CSV columns. No pre-existing test was edited to pass.
