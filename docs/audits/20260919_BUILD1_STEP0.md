# Build 1 — Intraday Data — Step 0 (read-only anchor re-verification)

**Date:** 2026-09-19 · **Branch:** `claude/intraday-build-1-utkb37` · **Base:** `origin/main` @ `ceffdc78` (PR #870 merge; clean tree at session open)
**Contract:** `docs/specs/INTRADAY_DATA_BUILD_1_CONTRACT_V1_1.md` (V1.1). **Scope of this step:** read-only. No source file changed; nothing measured; one new file: this report.

**Preamble (BUILD_RULES §3):**
- `git fetch origin` was the first step of the session (recorded). `origin/main` = `ceffdc78`; the working branch is at the same SHA with zero commits beyond it. The harness nominated `claude/intraday-build-1-utkb37`; the task prompt names `claude/intraday-build-1`. The nominated branch is the one on `origin` and the one the founder checked out, so the build lands there; the prompt's short name is read as its stem.
- The container is a shallow clone; it was not unshallowed (no commit older than the clone horizon is cited here).
- Every anchor below was **re-read at this HEAD** in this session. Markers: **VERIFIED** = read at that line here; **MEASURED** = computed here from files in the tree; **ASSUMED** = vendor documentation or founder report not reproducible from this session.
- **Fence:** `agentEvalPromptAssembly.js` (`formatRecentEvals`) was READ for §9.2. Nothing was edited. Fence contact in this build: none (contract §14).
- **External calls:** two attempts to reach `eodhd.com` for the §4 shape check (curl via the agent proxy; the WebFetch tool). Both were refused by the egress policy (`CONNECT 403`; `EGRESS_BLOCKED`). No EODHD API call was made; 0 units.

---

## Executive verdict

| # | Check | Verdict | Marker |
|---|---|---|---|
| 1 | Preconditions 1–3, 5 | All hold on `origin/main` at HEAD. `INTRADAY_*` absent from `featureFlags.js` (grep: zero hits). | VERIFIED |
| 2 | Precondition 4 — the 1-minute fixture | **DRIFT (path + encoding), substance holds.** The contract names `docs/audits/fixtures/AAPL_2026-09-17_1m.json`; that directory does not exist at HEAD. The founder's upload landed at **`docs/audits/AAPL_2026-09-17_1m.json.md`** (commit `6b643eca`, "Add files via upload"), wrapped by GitHub's editor: `\[` … `\]` with a trailing `&nbsp;`. De-escaped, it parses as a JSON array of **391 rows, `2026-09-17 13:30:00` → `20:00:00` UTC, contiguous at 60 s, no null volumes, 16:00 row `volume: 19,122,063`, Σ volume 44,574,829**. Keys: `timestamp, gmtoffset, datetime, open, high, low, close, volume`. | MEASURED |
| 3 | Live v2 response shape vs vendor page (§4) | **Unverifiable from this session** — `eodhd.com` is egress-blocked on both available paths. The §4 / G7 shape (`data` object keyed by symbol; fields `symbol, lastTradePrice, lastTradeTime, size, open, high, low, volume, averageVolume, previousClosePrice, change, changePercent, timestamp`; omitted symbols silently absent) is carried into the build **ASSUMED**, exactly as the contract states it. It does not "differ materially" — it could not be compared. Not a STOP condition by the contract's own wording; recorded as the build's first open vendor item alongside §15. | ASSUMED |
| 4 | `marketSchedule.js` exports what §5.1 needs | **DRIFT.** `getSessionForDate` does **not** exist (full export list below). The canonical copy is `api/_utils/marketSchedule.js` (its header: "the SINGLE holiday source … new holiday years land here first"; 2026–2027 holidays + 13:00 ET early closes; `MAINTAINED_HOLIDAY_YEARS = [2026, 2027]`). The build adds `getSessionForDate(etDate)` and `getPreviousSessionDate(etDate)` **to that file** (non-fenced), built only from its existing tables, returning `null` outside the maintained years (→ `calendar_missing`). | VERIFIED |
| 5 | Every other `file:line` the contract cites | All present; three minor line drifts noted in §2; one test pin that §11 must move in the same commit. | VERIFIED |
| 6 | Cron slot count | `vercel.json` has **39** entries (MEASURED). The build adds two (§5.1, §10.1) → 41. BUILD_RULES §6 states an *assumed* ceiling of 40; contract G10 (V1.1) records the vendor limit as 100 and §16 files the §6 ceiling as hygiene. The contract governs; the count is stated here so the founder sees it before merge. | VERIFIED / ASSUMED (ceiling) |

**Conclusion: no cited anchor is missing and the vendor shape could not be shown to differ. The build proceeds** under the two stated dispositions (rows 2 and 4) and the one stated assumption (row 3).

---

## 1. Preconditions

| # | Requirement | State at HEAD | Marker |
|---|---|---|---|
| 1 | `docs/specs/INTRADAY_DATA_BUILD_1_CONTRACT_V1_1.md` | present, 260 lines | VERIFIED |
| 2 | `docs/audits/20260918_PHASE0_INTRADAY_DATA.md` | present, 641 lines | VERIFIED |
| 3 | `docs/audits/20260918_PHASE0_INTRADAY_ADDENDA.md` | present, 414 lines | VERIFIED |
| 4 | `docs/audits/fixtures/AAPL_2026-09-17_1m.json` — 391 rows, 13:30–20:00 UTC | **path absent**; content present at `docs/audits/AAPL_2026-09-17_1m.json.md` with markdown escaping (see verdict row 2) | MEASURED |
| 5 | `INTRADAY_*` flags absent from `src/config/featureFlags.js` | absent (2,566-line file; zero matches) | VERIFIED |

**Disposition for row 4 (stated, not silent):** the build commits the de-escaped array, byte-for-byte in its row values, at the contract's canonical path `docs/audits/fixtures/AAPL_2026-09-17_1m.json`, and the validator tests read that path. The uploaded `.md` is left untouched (it is the founder's upload of record). A row-count / first-last / Σ-volume assertion in the test suite ties the two together. This is a docs-tree file, not source, and adds no runtime import.

---

## 2. Anchor re-verification (every `file:line` the contract cites, plus the report anchors the build relies on)

### 2.1 Cited directly in the contract

| Contract clause | Anchor | What is there at HEAD | Marker |
|---|---|---|---|
| G2 | `agent-evaluate.js:303-306` | `// ---- 3. Market hours guard …` / `if (!isMarketOpen()) { … return res.status(200).json({ skipped: true, reason: 'market_closed', … }) }` — exact | VERIFIED |
| §6.5 | `technicalCalculations.js:76-78` | `:76-77` gains/losses split; the **SMA seed** of Wilder RSI is at `:80-81` (`avgGain = gains.slice(0, period)… / period`) and the Wilder loop at `:83-86`. **Minor drift:** the seed phase the step function must copy spans `:76-81`, not `:76-78`. | VERIFIED |
| §6.5 | `technicalCalculations.js:169-173` | `calculateEMASeries` SMA seed loop `:169-173`, seed written at `:174` (`ema[period - 1] = sum / period`), multiplier `2/(period+1)` at `:165`, recursion `:177-179`. Exact. | VERIFIED |
| §6.5 | `technicalCalculations.js:214-219` | `validMacdValues = macdLine.filter(v => v !== null)` `:215`; `if (validMacdValues.length < signal) return null` `:216`; `signalSeries = calculateEMASeries(validMacdValues, signal)` `:219` — signal EMA seeded over the valid-MACD subsequence. Exact. Minimum length `slow + signal = 35` at `:195`. | VERIFIED |
| §16 | `featureFlags.js:2339` | `evidence — nine fields per held position + one vintages block per entry` (code has eight: `tickStamps.js:89-91`). Hygiene only. | VERIFIED |
| §16 | `compute-rankings.js:1498` | `// A4: Fetch historical prices for SPY + sector ETFs (13 calls)` (12 symbols at `:1500`). Hygiene only. | VERIFIED |
| §5.1 | `marketSchedule.js` — `getSessionForDate` | **absent** (see §3 below) | VERIFIED |
| §3 | `flagPinGuard.test.js` | scans `FLAG_SOURCE_MODULES` for `export const X_ENABLED = true|false` (`:184`) and every `*.test.(js|jsx)` under `src/` and `api/` for `expect(X_ENABLED).toBe(bool)` (`:214`); requires a `// Pinned by:` pointer naming each pinning file in the flag's docstring (`:345-360`). It holds no pins itself → the four booleans will be pinned in a new `src/config/intradayFlags.test.js` that the guard scans; the enum in `src/config/intradayPriceSourceFlags.test.js` on the `anticipationThresholdLintFlags.test.js` precedent (string values are outside the guard's scan; pinned directly with the `Pinned by:` pointer). | VERIFIED |

### 2.2 Report anchors the build writes against (Phase 0 §1, §2.3, §8; Addenda §6, §9)

| Purpose | Anchor | At HEAD | Marker |
|---|---|---|---|
| §11 legacy gate | `api/_utils/agentVwapFloor.js:36-38` | `isVwapSessionUsable({ sessionDate, todayET, sessionCandleCount })` → `sessionDate === todayET && sessionCandleCount >= MIN_SESSION_CANDLES` (`MIN_SESSION_CANDLES = 3` at `:13`). Date-only, no freshness term. | VERIFIED |
| §11 call site | `api/cron/agent-evaluate.js:982-992` | `filterToLatestSession(candles)` `:982`; `calculateVWAP(sessionCandles)` `:983`; the gate `:989`; `momentumData.vwap[symbol] = { ...vwapResult, sma20_5m, sessionDate }` `:991`. `todayET` at `:766` (not re-read here; the identifier is in scope at `:989`). | VERIFIED |
| §11 second caller | `agentVwapFloor.js:102-108` | `isReplacementQualified` composes the same gate — it must receive the freshness inputs too, or the cascade-guard re-qualification keeps the age-blind hole. | VERIFIED |
| **§11 test pin that MUST move** | `api/cron/agent-evaluate.test.js:868` | pins the call-site source text verbatim: `if (vwapResult && isVwapSessionUsable({ sessionDate, todayET, sessionCandleCount: sessionCandles.length })) {` … The §11 commit changes that line, so this pin moves in the same commit (BUILD_RULES §2 flip-reconciliation spirit). | VERIFIED |
| §11 existing tests | `api/_utils/agentVwapFloor.test.js:21-40`; `api/cron/agentVwapFloor.replay.test.js:48, :157` | call the gate with the three legacy args; they keep passing only if the freshness clause is **mandatory in semantics but supplied by the caller** — the build adds `asOfMs/nowMs/maxAgeMs` and makes a missing `asOfMs` **refuse** (fail-closed), so these rows will be updated to pass fresh instants (they assert the date/count legs, which survive). | VERIFIED |
| §8.4 vintages writer | `api/_utils/tickStamps.js:244-265` | `composeVintages` returns `{ quote: 'tick', vwap: 'tick', techAt, fundAsOf, rankingsAt }`; `VINTAGE_FIELDS` `:93`. **Pinned** by `tickStamps.test.js:91, :274, :313, :392, :414` and `agent-evaluate.tickStamps.flagOn.test.js:316-319` → the §8.4 keys are added **only under `INTRADAY_DIAGNOSTIC_ENABLED`**, so the flag-off block stays byte-identical. | VERIFIED |
| §8.1 hook site | `api/cron/agent-evaluate.js:2845-2875` (entry object, `evalId` at `:2846`), `:2912-2953` (tick-stamps splice, `TICK_STAMPS_ENABLED && promptBuilt`), `:3016` (`evaluations … .slice(-150)`), `:3025` (`finalUpdate`) | the view computation + subcollection write goes between the stamps splice and the write, wrapped and bounded (§8.1); the snapshot read goes in the handler after the market guard (`:304-307`) once per invocation. | VERIFIED |
| §8.1 auth pattern | `api/cron/agent-evaluate.js:184-190` | `x-vercel-cron === '1'` or `Bearer ${CRON_SECRET}` — the pattern both new crons copy. `config.maxDuration = 300` `:146`, `TIME_BUDGET_MS = 290_000` `:150`. | VERIFIED |
| §9.2 S1 (fenced) | `api/_utils/agentEvalPromptAssembly.js:1389-1402` | `formatRecentEvals` reads `evalId, timestamp, decision, symbolOut, symbolIn, tier, rationale, hypothesis` only — key-explicit. READ ONLY. | VERIFIED |
| §9.2 S4 | `api/_utils/voiceLayerGrounding.js:213-229`, `:348-379` | `renderEvidenceLines` → `evidenceFactLines(evidence[symbol])` + `provenanceLine(evaluation.vintages, …)`; `buildYourRecordBlock` slices the last `RECORD_WINDOW`. Key-explicit through `decisionRecord.js:1003-1037` (`px, chg, atrX, vwapDev, bbPct, nr7, regime, risk`) and `:1086-1097` (`fundAsOf, techAt, rankingsAt`). | VERIFIED |
| §9.2 S5 | `api/_utils/voiceLayerAnticipation.js:97-100` | reads the entry's `timestamp` only. | VERIFIED |
| §9.2 S6 | `api/_utils/agentReflectionUtils.js:154-223`, `:311-319` | `truncateBattleHistory` passes **whole entries** through (`truncatedEvals = evaluations` when ≤ 8); the prompt lines render `timestamp, decision, conviction, scores.total, hypothesis` (`:311-315`). The build adds an explicit allowlist pick in `truncateBattleHistory` so pointer fields never travel. | VERIFIED |
| §9.3 | `api/_utils/anticipationThresholdLint.js:240-297` (`buildPresentSignals`), `:73-79` (`NEVER_PRESENT_SIGNALS`: `MACD_5M, RSI_5M, VWAP_5M, SMA_20_LEVEL, MACD_HISTOGRAM`), `:117-131` (vocabulary; intraday-timeframe rows for MACD/RSI/VWAP at `:119-121`, `SMA_20_LEVEL` at `:124`) | the "five 5-minute names" are the five `NEVER_PRESENT_SIGNALS`; they become conditional on the view **and** `INTRADAY_AGENT_USE_ENABLED`. The exact indicator tokens `macd5m`, `rsi5m`, `sma20_5m` are not matched by any row today → rows added. | VERIFIED |
| §8.2 dead band | `api/_utils/agentPresetConfig.js:15-20`, `:36-38`, `:53-56` | `vwapDeadBandPct` aggressive 0.7 / balanced 0.5 / defensive 0.3; `getPresetConfig(battle.strategyPreset \|\| 'balanced')` at `agent-evaluate.js:695`. | VERIFIED |
| §4 universe | `compute-index-intelligence.js:80-86` (5 index), `:738-742` (assembly), `rankingConfig.js:359` (`ALL_TICKERS`), `:362` (`SECTOR_ETFS`) | 5 + 11 + 239 = **255** (lengths imported and counted). | MEASURED |
| §4 quote batching precedent | `compute-index-intelligence.js:298-305` | `/real-time/{first}?…&s={rest}` in groups of 20. Live v2 uses `?s=` for all tickers per §4. | VERIFIED |
| §5.5 crypto adapter | `marketDataCache.js:742-758` | Live v1 fields consumed today: `close, previousClose, change, change_p, high, low, volume, timestamp`. `isCryptoSymbol` `:76-80`, `formatEODHDSymbol` `:86-90`. | VERIFIED |
| §10.2 bar fetch | `marketDataCache.js:792-870` | `fetchIntradayCandles` **strips** partial candles (`:828-835`) and the null-volume flat 16:00 row (`:851-858`) — the validator therefore uses its own raw 1-minute fetch with an absolute `from/to` window and never this filter. | VERIFIED |
| ET conversion | `marketDataCache.js:1000-1016` | `toEtParts(utcDate)` — `Intl.DateTimeFormat('en-CA', { timeZone: 'America/New_York' })` — exported; the BUILD_RULES §6 pattern. `parseEodhdDatetime` `:980-989` is module-private. | VERIFIED |
| §8.1 rules | `firestore.rules:429-440` | `match /agentBattles/{battleId}`: `allow read: if request.auth != null && resource.data.ownerId == request.auth.uid`; update limited to an execution-controls allowlist; `create, delete: if false`. No subcollection match exists (G13). Rules suites live in `test/rules/*.rules.mjs` under `npm run test:rules` (emulator; Java 21 present in this container). | VERIFIED |
| §9.1 client | `src/screens/battleView/WhyPanel.jsx:1-6` ("A PURE READ … No fetch"), `:219-230`; mounts at `src/screens/AgentBattleScreen.jsx:1835-1850` (row) and `:2277-2286` (book); open state `whyOpen` `:568`; client Firestore `src/hooks/useAgentBattle.js:2-3` (`db` from `../firebase/config`). | the one `get` on open lives in a hook in the screen, not in the panel; the panel stays a pure read of a prop. | VERIFIED |
| Test doubles | `api/_utils/__fixtures__/inMemoryFirestore.js:145-158` (`runTransaction` with `tx.get/set/update/create`, no retry), `wireFirestoreFake.js:186` (optimistic retry) | the lease/publish transaction tests use these. | VERIFIED |
| Cron table | `vercel.json` | 39 entries (parsed). `agent-evaluate` at `*/15 13-21 * * 1-5`. | MEASURED |

### 2.3 Line drifts found

1. `technicalCalculations.js:76-78` → the RSI seed the step function must copy spans `:76-81` (split at `:76-77`, SMA seed at `:80-81`). Cosmetic.
2. The contract's §5.1 import `getSessionForDate(etDate)` names a function that does not exist; §3 below.
3. `agent-evaluate.test.js:868` pins the exact source of the §11 call site (not named in the contract); moved in the §11 commit.

No cited anchor is missing. No fenced file needs editing.

---

## 3. `marketSchedule.js` — what exists, what §5.1 needs

**Exports at HEAD** (`api/_utils/marketSchedule.js`, 371 lines): `MAINTAINED_HOLIDAY_YEARS` `:77`, `getETDate` `:96`, `formatDateString` `:100`, `isMarketHoliday` `:111`, `isTradingDay` `:121`, `isEarlyCloseDay` `:128`, `isTodayHoliday` `:133`, `getPreviousTradingDay` `:141`, `isMarketOpen` `:158`, `getMarketState` `:180`, `getNextMarketOpen` `:218`, `getNextMarketClose` `:258`, `getTimeUntilNextOpen` `:296`, `getEffectiveTTL` `:312`, `getEffectiveTTLMs` `:345`, `isPreMarketWindow` `:359`. Tables: `NYSE_HOLIDAYS_2026` `:32-43`, `NYSE_EARLY_CLOSE_2026` `:46-49`, `NYSE_HOLIDAYS_2027` `:52-63`, `NYSE_EARLY_CLOSE_2027` `:68-70`; `EARLY_CLOSE_HOUR = 13` `:28`. All VERIFIED.

**§5.1 needs, per ET date:** trading-day-or-not, `openMs`, `closeMs` (13:00 ET on early-close days), `sessionLenMin` (390 / 210), the previous session for seeding/validation, and a hard `calendar_missing` outside the maintained horizon. None of the existing exports return instants for an arbitrary date; `getNextMarketClose` is now-relative; `getPreviousTradingDay` walks the holiday table with a host-local `Date` (date-only arithmetic, so tz-safe).

**Canonical copy:** `api/_utils/marketSchedule.js` (header `:12-16`). Eight sibling holiday lists exist (Addenda item 5); six are early-close-blind; none is the source of record. The build adds to the canonical file:
- `getSessionForDate(etDate)` → `{ etDate, isTradingDay, isEarlyClose, openMs, closeMs, sessionLenMin }` or `null` when `etDate`'s year ∉ `MAINTAINED_HOLIDAY_YEARS` (→ `calendar_missing`); instants computed with `Intl` (`America/New_York`) — never a hand-rolled offset.
- `getPreviousSessionDate(etDate)` → the previous trading day string by the same tables (a thin, string-only walk beside `getPreviousTradingDay`).

Both are pure (no `Date.now()`), and every intraday module takes `nowMs` as an argument.

---

## 4. The Live v2 shape (§4) — what this session can and cannot say

- **Cannot verify:** `https://eodhd.com/…` is refused by the egress proxy (curl `CONNECT tunnel failed, response 403`; WebFetch `EGRESS_BLOCKED`). No vendor page was read; no API call made.
- **Carried as ASSUMED, verbatim from the contract (G7, §4):** `GET /api/us-quote-delayed?s=…` (≤ 20 tickers, 1 unit each) returns `{ data: { [symbol]: { symbol, lastTradePrice, lastTradeTime (ms), size, open, high, low, volume, averageVolume, previousClosePrice, change, changePercent, timestamp (s) } } }`; a requested symbol absent from `data` is silently omitted.
- **What the build does with the uncertainty:** the adapter is written against exactly those names; a response with no `data` object is an adapter anomaly (`shape_unexpected`, every requested symbol counted `missing`, no update) rather than a crash; the unit tests fix the parse against the contract's field list; the report's smoke section names "first production sweep returns `anomalies.missing === 0` for a known-good symbol" as the vendor-shape check the founder observes on day 1 (crons do not run on preview, BUILD_RULES §6).
- The crypto Live v1 shape (`code, close, timestamp, volume, previousClose, change, change_p`) matches what `fetchRealTimePrice` already consumes at `marketDataCache.js:750-757` (VERIFIED in-repo, not against the vendor).

---

## 5. Items the build will state as intentional or assumed (so the review can refute them)

1. Fixture materialised at the canonical path from the founder's `.md` upload (§1).
2. `getSessionForDate` / `getPreviousSessionDate` added to the canonical `marketSchedule.js` (§3).
3. Live v2 field names ASSUMED (§4).
4. Boolean pins in `src/config/intradayFlags.test.js` (scanned by `flagPinGuard.test.js`), enum pin in `src/config/intradayPriceSourceFlags.test.js`; `INTRADAY_AGENT_USE_ENABLED` and `INTRADAY_RISK_ACTIVATION_ENABLED` registered in `DARK_BY_DESIGN` (stage 2 / stage 4 gates); the two "live in build 1" flags ship `false` as ordinary pinned flags the founder flips after the day-1 smoke.
5. §8.4 vintages additions gated on `INTRADAY_DIAGNOSTIC_ENABLED` to keep the pinned flag-off block byte-identical.
6. `vercel.json` goes 39 → 41 entries under contract G10 (100/project); BUILD_RULES §6's "40" is the §16 hygiene item.
7. `agent-evaluate.test.js:868` source pin moved in the §11 commit.

**STOP conditions checked:** no cited anchor missing (none); vendor shape not shown to differ (unverifiable, not different); no fenced file required (none). Proceeding to the build.
