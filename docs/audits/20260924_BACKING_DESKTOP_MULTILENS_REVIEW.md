# Backing desktop layouts — multi-lens adversarial review record

**Branch:** `backing/desktop` (the task prompt's name) · **Reviewed commit:** `40596357` (the build: `524c7e5d` pins, `43d10db6` the landing's door, `7211f374` the desktop screen, `14298380` the guard suites, `c1ba06b1` the preview's desktop toggle, `288f002b` the screenshots, `40596357` the importer ratchet) · **Fix commits:** `f1e61e91`, `2348ff0d`, `1b0030fa`, `6db2f842`, `48a9529b`, `6a93aa2b`, `559d690b`, `55fc5dff`, `42602f44` (the preview's default frame, the rows for WIRE-2, the screenshots re-rendered) · **Base:** `main` (`97693a41`). `main` has since moved to `987a9a68` with three docs-only commits (the Cockpit spec). They touch none of this branch's files and no code, so the goldens generated on `97693a41` are still `main`'s code.
**Rule:** BUILD_RULES §2 — review is mandatory at ≥10 files or ≥1,500 lines (this branch at `40596357`: 55 files, +4,364 / −261; with the fixes: 57 files, +5,197 / −288 at `42602f44`, before this record). Multi-lens, adversarial, independently refuted, `vite build` run, mutation-checked, written down.
**Reviewer isolation (founder ruling, Sep 2, 2026):** every lens and every refuter worked on its own `git archive 40596357` extraction under the session scratchpad (`review/lens-{seal,dark,placement,wiring}`, `review/refute-{seal,dark,placement,wiring}`), `node_modules` symlinked, read-only on git and on the shared working tree; probes and mutants ran only in those trees, and every mutated file was restored byte-identical (sha256 recorded in each report; each reviewer re-verified its whole tree against the commit's blobs at the end). A read-only `git archive main` extraction (`review/base-main`) served comparisons. The coordinator's mutation pass — the mutating lens — ran LAST, after every lens and refuter had reported, each mutant on its own `git archive` extraction under `scratchpad/mut/` (`run_mutants.py`). No reviewer wrote to `/home/user/TradeSeven`. Disclosed by the reviewers themselves: vitest's gitignored timing cache under the shared `node_modules` symlink was refreshed by some runs; lens-seal briefly kept a backup file in the shared `review/` folder (outside every tree) and deleted it; refute-seal created and removed an empty directory beside its tree.

## 0. Verdict

| | |
| --- | --- |
| **"Leak the sealed pool through a wider layout"** (the founder's brief for one lens) | **The desktop Backing screen holds the seal.** Leaky pod, pool, team, stake, card, in-play and stake-reply data rendered byte-equal to the same render with the leaked fields removed — every desktop strip kind, ten window states the build's rows did not picture, and a 43-step walk of the real `BackingScreen viewport="desktop"` (every tab, every seat, the control, Confirm with a leaky reply). The one open-pool exposure on a desktop surface is **pre-existing** (SEAL-1: the trainer stats fold the trainer's own open pool — PR 5's server fold, byte-identical on `main`; the desktop mount adds no viewer) — **reported as a gate for the activation PR**. Two defence-in-depth gaps closed in this branch (SEAL-2's blind order check, SEAL-3's reveal gate). |
| **Dark byte-identity and mobile byte-identity** (prompt §A, §F) | **Hold.** Both golden files regenerated on `main`'s code are byte-identical to the committed ones (lens-dark, refute-dark, and the coordinator's own regeneration — 94/94 rows); a 5,260-step differential walk, branch vs `main` (plain, StrictMode, seeded fuzz over 13 setups), found one difference outside animation noise (DARK-R-1, unreachable — fixed). Nothing backing renders or reads while dark; a 240-click / 720-key fuzz never opened the desktop host. The mobile funnel is now pinned per view, before the dedup, against a golden generated on `main` (DARK-1 / DARK-R-2). |
| **Placement and emphasis** (prompt §B–§E) | **The strip's place holds** (centre column, directly under the picker and Auto-draft card / the seated hero, above "Watch a live game" and the bracket line, nothing reserved, nothing moved; "Ranked entry · ET" not built; no animation anywhere). **Fixed:** the "Back a team →" action was missing while the window was open for every returning backer Monday–Friday and for a viewer whose own pool closed at its fire (PLACE-1, MAJOR); at the desktop widths below 1440 the strip covered lobby controls (PLACE-2, MAJOR) and the stacked Backing screen painted columns over each other (PLACE-3, MAJOR); narrow-width clipping (PLACE-4, PLACE-6); keyboard focus and dialog basics (PLACE-7). The 1440×900 contract held throughout: the three lines and Confirm on screen, unscrolled. |
| **Wiring and scope** ("layout only") | **Fixed:** the desktop screen had made the results read lazy — the settle-on-read retry's cadence, a data-path change (WIRE-1, MAJOR); the results table headed "Pays ×" over staked BP on void and settling pools (WIRE-6, MAJOR); the section jumped as data landed, card visits were mis-measured, the results section flashed a false alert, a seated viewer's strip mounted twice (WIRE-2/3/5/7). No hook, service or endpoint changed; the `services` seams are unused in production. |
| **Findings** | **24 findings and 5 notes** across the four lenses. The refuters **CONFIRMED 23**, **PARTIALLY REFUTED 1** (WIRE-3: its (b) half, which was applied and then reverted) and refuted one factual sub-claim (PLACE-5); they **added 10** (§3, §5). The five in-branch MAJORs are all fixed: PLACE-1, PLACE-2, PLACE-3, WIRE-1 and WIRE-6. One MAJOR is pre-existing (SEAL-1); it is reported as a gate for the activation PR. |
| **Fixed in this branch** | Every in-branch CONFIRMED finding is fixed, and every behavioural fix carries a row; every row was mutation-checked (§6). CSS-only fixes were measured in Chromium (§7). |
| **For separate tasking** | SEAL-1 (+ SEAL-R-2) — **gate for the activation PR**; SEAL-4; the lobby's pre-existing overlap at ≤1024px with the flag off; the League's missing focus styles (PLACE-7's pre-existing half); the mobile strip below "Watch a live game" (N3, frozen by §A); the strip not re-reading after a stake or the close (PRE-1 / N4); WIRE-R-2. (§9) |
| **For the founder** | The Monday–Friday "Back a team →" ruling (PLACE-1 / OBS-1); SEAL-1 as an activation gate; the trainer stats' desktop home is the mobile view, not the attached design's closed-weeks layout (SEAL-R-1); the strip's width is the picker's, not the whole column above ~1576px (N1). (§10) |

## 1. Scope

The prescribed "Backing Desktop Build (layout only)", flags `false` throughout: desktop layouts for every Backing surface to the attached desktop design (`docs/design/backing/desktop/`, committed byte-for-byte), reusing the hooks, services and copy the mobile build ships — the strip's desktop door in `LeagueLobbyDesktop`'s centre column (§B, §C), the settled decisions (§D), the desktop Backing screen (§E), every mobile guarantee at desktop width (§F), the dev preview's desktop toggle and 1440×900 screenshots (§G).

Not in scope and not reviewed as this PR's: any endpoint, service, data or logic change; the activation PR; the gate-3 honesty fixes; tournament code; any fenced file (none touched).

## 2. Method

| Lens | Brief | Tree | Agents |
| --- | --- | --- | --- |
| SEAL | The founder's: "leak the sealed pool through a wider layout" — every desktop surface and state, hidden channels (attributes, aria, keys, overflow), orders or emphasis derived from a sealed figure, leaky replies byte-compared with clean ones | `lens-seal` | 1 reviewer |
| DARK | The two founder guarantees — mobile byte-identical to today; with the flag false the desktop League renders exactly as today — and the standing dark rule; the goldens' provenance and whether their rows can fail | `lens-dark` | 1 reviewer |
| PLACEMENT | Design fidelity: the strip's place and emphasis (§B–§D), the desktop screen (§E), every desktop width the app serves (769px up), accessibility basics | `lens-placement` | 1 reviewer |
| WIRING | Lifecycle, telemetry, labels (D-af), display agreement (§9), tokens (§10/§11), and "layout only" scope — any data or logic change in disguise | `lens-wiring` | 1 reviewer |
| REFUTATION | Every finding of each lens handed to a refuter on its own tree, instructed to refute it with a concrete repro and to assess each proposed fix against the build's constraints | `refute-{seal,dark,placement,wiring}` | 4 refuters |
| MUTATION | The prompt's four required checks, then a per-fix guard check for every review fix (§4, §6) — last, on its own trees | `mut/*` | the coordinator |

Eight agents in total. The full reports stayed in the session scratchpad (`review/reports/`, outside the repository); each is reproduced in substance below. The refuters also cross-checked the in-flight fix commits in the shared tree (read-only) and reported on them (PLACE-R-3, DARK-R-2) — both were acted on.

## 3. Findings and dispositions

Severities as the refuter left them. **Disposition** is the coordinator's; the CONFIRMED / REFUTED split is §5. Three findings surfaced in more than one lens and are disposed once: OBS-1 = PLACE-1 = WIRE-8 (the "Back a team" action), OBS-2 = PLACE-5 = WIRE-4 (the window chip).

### SEAL lens — "leak the sealed pool through a wider layout" — 6 findings; refuter: 6 CONFIRMED, 0 REFUTED; refuter added 3

Clean (lens, re-verified by the refuter — its five probes re-ran 25/25): every desktop strip kind over leaky pods/pools/teams/stakes/in-play pools; ten window states the build's rows did not picture (list / card / control with no stakes, attestation, eligibility loading, top-up and at the cap, wallet unknown, the viewer's own pod, an in-play list naming an OPEN pool); the card projection's own leaky fields (`card.pool`, per-team figures, last week's CLOSED pool beside this week's OPEN one); a 43-step walk of the real `BackingScreen viewport="desktop"` including Confirm with a leaky stake reply — "steps differing leaky vs clean: none"; Your Backing never lists an open pool; no `title`, CSS `content`, keyframe or animation in the changed files; every `data-*` / `aria-*` a status, kind, id, preset or label.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| SEAL-1 | MAJOR (pre-existing) | The trainer stats fold the trainer's OWN team's live figures while its pool is open — "Backers on you 4", "BP backed on you 1,250", moving stake by stake — now also in their desktop home beside the pitch (`IdentityPanel.jsx:106`). Root: `computeTrainerStats` (`api/_utils/backingStats.js`) never reads the pool's status; PR 5's suites pin it. | CONFIRMED, **stronger**: the desktop design this PR commits forbids it in so many words — "Closed weeks only — this week's pool is sealed even to the trainer" — as do spec §3, Amendment B D-q, the original brief §3 and prompt §F. Pre-existing and byte-identical on `main`; the desktop mount adds no viewer (the reply is the viewer's own); dark today. The fold fix reds exactly 3 pinned rows in 2 server suites — not layout-only. | **REPORTED — a gate for the activation PR** (§9); disclosed in the PR. Holding back the desktop mount would contradict §E and protect no one (refuter). |
| SEAL-2 | MINOR (test) | The desktop seal rows could not see an ORDER derived from a sealed figure: the fixture was already "most backed first", so a desktop-only sort survived all 13 suites. | CONFIRMED; the lens's two-pod fix is incomplete — still blind to an ascending ("quietest first") sort. The fixture must be non-monotone. | **FIXED** (`f1e61e91`) — three pods (pots 1,234 · 4,321 · 321) and four seats (777 · 555 · 888 · 666), non-monotone both ways; the strip row gets two staked pods, the smaller pot first. Both sort mutants red (§6). |
| SEAL-3 | NOTE | Both results cards revealed a pool's figures on their presence alone (`Number.isFinite(pod.potTotal)`), not on its closure. | CONFIRMED as defence in depth — four server layers keep an open pool out of every results reply; the gate moves no golden. | **FIXED** (`1b0030fa`) — revealed only once closed; row: an open pool with figures renders byte-equal to the sealed pool, on both cards. |
| SEAL-4 | NOTE (pre-existing) | `BackersCall` clamps the count to the reply's own `floor`, not to three — `{count:47, floor:47}` prints "Backers 47 of 47". | CONFIRMED, unreachable (the server always writes floor 3); `BackingParts.jsx` is byte-identical to `main`. | **REPORTED for separate tasking.** |
| OBS-1 | → PLACE-1 | — | CONFIRMED, both states reachable | See PLACE-1. |
| OBS-2 | → PLACE-5 | — | CONFIRMED, broader (SEAL-R-3) | See PLACE-5. |
| SEAL-R-1 | NOTE (disclosure) | The desktop trainer-stats home is the mobile view (`StatsEntryView` / `TrainerStats`), not the attached design's closed-weeks layout (a sealed "This week" row, "Last week", "Season"); a faithful port needs per-week data the reply does not carry. | raised by the refuter | **DISCLOSED** (PR body, §10); belongs with SEAL-1's follow-up. |
| SEAL-R-2 | MINOR (for the follow-up) | SEAL-1's fold fix alone would make the panel say "Nobody has backed your team yet." while four have — pair it with the design's sealed row; never key that row on whether stakes exist. | raised by the refuter | **REPORTED with SEAL-1** (§9). |
| SEAL-R-3 | NOTE | The window chip mirrored the viewer's own pod ("Closes Wed 7:00 PM ET" above pods closing Sunday), and a viewer whose only pod had locked saw none while the window was open. | raised by the refuter | **FIXED with PLACE-5** (`f1e61e91`). |

### DARK lens — the flag-off guarantee and mobile byte-identity — 1 finding; refuter: CONFIRMED; the CLEAN verdict SURVIVED with one correction; refuter added 2

Clean (lens; attacked by the refuter over 5,260 differential steps, branch vs `main`, plain / StrictMode / seeded fuzz across 13 setups): mobile markup and behaviour equal `main`'s in every pinned row and in 30+ unpinned states; with the flag false the desktop League's markup and behaviour equal `main`'s in every state (seated statuses, pre-open, tabs, docked pods, Spectate, slot claim/leave, Auto-draft, signed out, user switch); no backing hook, read, emit or `data-backing` node while dark; a 240-click / 720-key fuzz never opened the desktop host (the flag-on fuzz finds backing nodes — not vacuous); `DeskLobby`'s extraction keeps effect order, subscription and state lifetime; both goldens regenerate byte-identical on `main`'s code (and hold under three other timezones). The only behavioural delta: a stats-tab switch no longer re-invokes the two stats hooks (the tab state moved into the child view) — fewer renders, no read.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| DARK-1 | MINOR | The branch rewired the mobile funnel's effects (what is "on screen", their dependencies, the moved `results_viewed`), and no test pinned which mobile view emits which event — the lens silenced two mobile events and 19 suites stayed green. | CONFIRMED — with every mobile funnel event on the wrong view the WHOLE suite stayed green (777 files). No live defect (identical telemetry, before and after the dedup). | **FIXED** (`f1e61e91`, refined `6a93aa2b`) — two journeys in `backingMobilePin.test.jsx` (list → card → control → card → list → a second card; data landing after mount), every emit captured BEFORE the dedup, per view step, the readable steps in a golden generated on `main`'s code (the 94 markup rows reproduced there unchanged). The refuter's design, mutation-tested by it (6 of 6 classes red); the coordinator's pass: §6. |
| DARK-R-1 | NOTE | `DeskLobby`'s `uid = null` default flipped the signed-out slot picker ("Leave" ↔ "Claim seat") for a seat whose `odUserId` is null — the lens's fixture masked it. | raised by the refuter; unreachable (the endpoint needs auth; seats are uid strings) | **FIXED** (`6a93aa2b`) — no default; the refuter's trial: branch equals `main` in all 10 cases. |
| DARK-R-2 | MINOR (test, on the in-flight fix) | The first DARK-1 row (`f1e61e91`) recorded one flat list at the sink, after the dedup — green under a wrong-view mutation. | raised by the refuter at `48a9529b` | **FIXED** (`6a93aa2b`) — see DARK-1. |

### PLACEMENT lens — the strip's door and emphasis, the desktop screen — 7 findings + 5 notes; refuter: 7 CONFIRMED (one sub-claim of PLACE-5 refuted), notes confirmed; refuter added 3

Clean (lens, re-verified): §B — the strip directly after the Auto-draft card (not seated) or the hero (seated), always above "Watch a live game" and the bracket line, never in a rail, exactly one strip on every lit landing and none dark, no reserved space, the Training tab without a strip (as mobile), "Ranked entry · ET" not built; §C — the accent edge in every state, no pot / per-team figure / urgency copy, `animation-name: none` on every element of the strip and the host on all pages, zero running animations; §D — "Watch a live game" still only spectates, desktop pod rows read "Tap a seat · Predictions"; §E — 400 / flex / 380 columns, the stake control, the attestation and the top-up line in the right column, Your Backing and the results whole-screen, the private record beside the pitch; the host covers the sidebar at every width.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| PLACE-1 | MAJOR | The desktop strip hid "Back a team →" while the window was open: every returning backer Monday–Friday (spec V1.3 §4 opens next week's window at Monday 00:00 ET, so it is open through the in-play week, and the WEEK state outranks it), and a viewer whose only stake closed at a slot fire while lobby pools stayed open ("Closed · plays Monday"). The screen treated the window as open for the same data (§9). | CONFIRMED; the lens's derivation missed the fire-closed half — "any listed pool open" is the fact. Recommends option (a): a sibling pill (the WEEK strip keeps opening Your Backing). | **FIXED** (`f1e61e91`, `48a9529b`) — `backingWindow(pods)` (any listed pool open) gates the action; the action is the strip card's SECOND button (never nested) and opens the window; the strip's own button opens the section its state names (`stripSection`). Rows: the preview's week landing (strip → Your Backing, pill → the window), the lit lobby (pill → window; a dialog), unit rows. **For the founder** (§10): the Monday–Friday reading. |
| PLACE-2 | MAJOR (root cause pre-existing) | At the lobby's ≤1180px layouts the new centre strip overflowed its grid row and was painted over "Open my game", "Your group" and follow rows — clicks landed on the strip. Root: `LD_STYLE` (unchanged) keeps `min-height: 0` on the reflowed grid items. | CONFIRMED in the App's real frame (both sidebar states; the default is collapsed, 64px); lit-only emission keeps the dark goldens; **the lens's rule over-corrected** — the right rail unrolled and pushed "Open my game" below the fold. | **FIXED** (`f1e61e91`, refined `6a93aa2b` per PLACE-R-3) — the lit strip slot brings `.ld-grid > .ld-center, .ld-grid > .ld-rail-left { min-height: auto }` at ≤1180px (not the right rail); the flag-off lobby is untouched. Measured clean 769–1920px, both frames (§7). The flag-off overlap at ≤1024px is REPORTED. |
| PLACE-3 | MAJOR | The Backing screen's window section was unusable at 769–980px: the stacked columns (~266px each) painted over each other; seats and "Back …" were covered. | CONFIRMED; the rule works; add scroll-into-view (stacked, the card sits ~1,625px down). | **FIXED** (`f1e61e91`, `6a93aa2b`) — `min-height: auto` on the stacked columns; a seat pick scrolls the card into view and "Back" the control, only when stacked. Measured: 0 overlaps / 0 covered at 769–980 (§7). |
| PLACE-4 | MINOR | In a narrow centre column (1181–1366px) the strip's close clipped, its eyebrow shrank to 0px, the week's team names rendered 0–9px wide, "250 BP" clipped. | CONFIRMED; the lens's fix partial. | **FIXED** (`f1e61e91`, `6a93aa2b`) — the header wraps, the close may wrap, the week grid never outgrows its box, stake rows wrap. Clean at every width in the default frame. With the sidebar expanded, some names are cut short with an ellipsis at 1181–1235px only (a 165–219px strip). No figure and no action is clipped (§7). |
| PLACE-5 | MINOR | The window column's close chip took its gate from one state and its words from another: "Settles after Friday's close" beside open pools (§9). | CONFIRMED; **one sub-claim refuted** (the window's close does appear — on each pod row); the lens's text fix covered the WEEK case only. | **FIXED** (`f1e61e91`) — gate and words from `backingWindow`: the window's own close (the latest open close). Row (`2348ff0d`). |
| PLACE-6 | MINOR | At 981–1280px side-by-side content truncated: the team card's two-layer tape ("Held all…"), the results table's winner ("Ke…"). | CONFIRMED; book fix must spare the 1440 split; the results fix `minmax(140px, …)` did not work. | **FIXED** (`f1e61e91` book at ≤1365px; `6a93aa2b` results — the record aside goes under the results at ≤1180px). Measured: no truncation 981–1440 (§7). |
| PLACE-7 | MINOR (pattern pre-existing) | Keyboard focus on the new strip and the screen's controls was invisible (`all: unset`); the full-window host had no dialog semantics, Escape or focus. | CONFIRMED; the lens's global rule would restyle mobile and flag-off surfaces — scope it. | **FIXED** (`f1e61e91` — `role="dialog"`, `aria-modal`, label, Escape, focus on open; `6a93aa2b` — focus rings scoped to the lit strip card and the Backing screen). Verified: strip, pill and back link ring; "Ranked" untouched (§7). The League-wide half is REPORTED. |
| N1 | NOTE | The strip is the picker's width (560px), not the whole centre column above ~1576px. | CONFIRMED | **For the founder** (§10). |
| N2 | NOTE (pre-existing) | No bracket funnel renders on the desktop lobby at all; "nothing else moves" holds (the strip's top is identical with and without a bracket). | CONFIRMED | RECORDED. |
| N3 | NOTE (pre-existing) | On mobile the strip still sits below "Watch a live game" — frozen by §A. | CONFIRMED | **REPORTED.** |
| N4 | NOTE (pre-existing) | Nothing re-reads the pod list at the close, so "Back a team" stays until a reload. | CONFIRMED | **REPORTED** with PRE-1. |
| N5 | NOTE | "Backing open · N pods" counts pods, not backers. | CONFIRMED | RECORDED. |
| PLACE-R-1 | NOTE | The harness framed the League with the sidebar expanded (220px); App defaults to collapsed (64px). | raised by the refuter | **FIXED** (`6a93aa2b`) — the screenshots use the default frame. |
| PLACE-R-2 | MINOR | = SEAL-R-3 (the chip read the viewer's Wednesday pod). | raised by the refuter | **FIXED** (`f1e61e91`). |
| PLACE-R-3 | MAJOR / MINOR (on the in-flight fixes, `6db2f842`) | The adopted lobby rule over-corrected (right rail); the results fix was ineffective; the strip still clipped at 1181–1210px (expanded); no focus ring yet. | raised by the refuter | **FIXED** (`6a93aa2b`). |

### WIRING lens — lifecycle, telemetry, labels, scope — 10 findings; refuter: 9 CONFIRMED, 1 PARTIALLY REFUTED; refuter added 2

Clean (lens, re-verified): nothing under `src/hooks/`, `src/services/` or `api/` changed; `deriveStripState` unchanged; `screenStateLine` is `main`'s `headerLine` moved verbatim; the `services` seams (LiveDraftPicker, AutoDraftFallback, SlotCenter, StakeControl) are never passed in production and the default path is identical (one schedule fetch over four renders); mobile telemetry unchanged; the per-session dedup holds; every team name on every desktop surface is the resolver's `label` (the raw-id guard's desktop rows are not vacuous); `weekCardModel` / `resultsCardModel` feed both layouts; no raw colour and no inline `transition={{` in any added src line; `lint:gate` and `vite build` exit 0.

| ID | Sev | Finding | Refuter | Disposition |
| --- | --- | --- | --- | --- |
| WIRE-1 | MAJOR | The desktop screen read `GET /api/backing/results` only when its tab opened — the settle-on-read path, the only automatic retry for a pool whose Friday settlement failed: a changed fetch cadence, a data-path change. | CONFIRMED, **wider**: also the §7 refund routing for a pod voided or expired after its pool closed; a regression against `main`'s own desktop screen, which read it on open; `backingDark.test.jsx` pinned the lazy read. | **FIXED** (`f1e61e91`) — read on open, as mobile; `results_viewed` fires only once the results are on screen; the pin now expects one read. |
| WIRE-2 | MINOR | The desktop section was re-derived from live state until a tab was clicked: the screen opened on the window and jumped, `window_viewed` depended on arrival order, a snapshot could pull the viewer off what they read, and a card was left open off-screen. | CONFIRMED (all four symptoms); the seed-from-strip alternative works. | **FIXED** (`f1e61e91`, `48a9529b`) — the strip names the section it opens and the screen holds it while its snapshots land; leaving the window by any route closes the card. Row: the desktop funnel (opened on Your Backing from the first frame). |
| WIRE-3 | MINOR | `team_card_opened` on desktop: (a) re-clicking the open seat recorded a truncated visit and the dedup dropped the real one; (b) "Back" recorded the visit while the card was still displayed beside the control. | **PARTIALLY REFUTED** — (a) CONFIRMED; (b) REFUTED: the event's contract ends a visit on leaving the card "to the list, the control or a closed screen", as mobile measures it. | (a) **FIXED** (`f1e61e91`) — re-selecting the open card is a no-op; the visit is keyed on its seat. (b) the lens's fix was applied in `f1e61e91` and **REVERTED** per the refuter in `2348ff0d` — one meaning for the dwell. Row: the desktop funnel (62 s recorded, never the 2 s before a re-click). |
| WIRE-4 | MINOR | = PLACE-5. | CONFIRMED | **FIXED** with PLACE-5. |
| WIRE-5 | MINOR | Entering the results committed a false `role="alert"` "Stats are unavailable right now." and "No completed pools yet…" before the reads answered. | CONFIRMED (the stats half also on the click path). | **FIXED** (`f1e61e91`) — "nothing yet" and "unavailable" only after a read answers; rows (`6db2f842`). |
| WIRE-6 | MAJOR | The desktop results table headed its last column "Pays ×" over the team's staked BP on every unsettled pool — "Pays ×: 150 BP / 0 BP" on a void pool (§9; the HON-2 / HON-6 class). | CONFIRMED with the real server projection over real close / refund / settle documents. | **FIXED** (`f1e61e91`) — the head reads "BP backed" until the pool settles; row per outcome. |
| WIRE-7 | MINOR | A seated viewer's lobby mounted the strip under the slot picker, then again under the waiting room — 2 pod-list requests and 10 stake listeners (`main`: 1 and 5). | CONFIRMED; the seat-known gate is valid (it runs no backing hook while dark); hoisting the hooks into the lobby is not (hooks above the flag read). | **FIXED** (`f1e61e91`) — the strip mounts once the seat subscription has answered (it always answers). Row: one pod-list read, one strip. Cost recorded: one extra lobby render per signed-in viewer (no markup change) and the strip's first read waits for the seat. |
| WIRE-8 | = PLACE-1 | — | CONFIRMED reachable | See PLACE-1. |
| PRE-1 | NOTE (pre-existing) | The landing strip behind the Backing host never re-reads after the host closes — stale after a stake. Identical on `main`. | CONFIRMED | **REPORTED.** |
| NOTE-1 | NOTE | A raw hex accent in the screenshot harness. | CONFIRMED | **FIXED** (`f1e61e91`) — the teal token. |
| WIRE-R-1 | MINOR | The desktop rail's resting copy said "Window open" / "Read a team, back it with 1,000 free points…" with no pool open. | raised by the refuter | **FIXED** (`2348ff0d`) — it follows `backingWindow`; row. |
| WIRE-R-2 | NOTE (pre-existing) | A live stake on a pod voided or expired after its pool closed is classified "window" — "Closed · plays Monday" for a pod that will never play. | raised by the refuter | **REPORTED**; WIRE-1's fix restores the desktop screen's self-heal (the results read refunds it). |

## 4. The prompt's four mutation checks

Each mutant on its own `git archive 6a93aa2b` extraction (`scratchpad/mut/run_mutants.py`: the edit applied as an exact string replacement asserted to match once, the named suites run with the JSON reporter, the tree discarded). The table counts every red row. The list under it names, in bold, the rows each check asks about.

| # | Mutant | Result |
| --- | --- | --- |
| R1a | (1) a per-team share on the desktop pod list while open — the shared seat row | **RED** — 3 red / 19 green: `PodList.test.jsx` 3 red — "…ean one) the whole window — the pod list, no card yet — is the clean window, byte for byte" and 2 more |
| R1b | (1) a per-team share on the desktop pod list while open — the desktop pods column only | **RED** — 3 red / 19 green: `PodList.test.jsx` 3 red — "…ean one) the whole window — the pod list, no card yet — is the clean window, byte for byte" and 2 more |
| R2a | (2) the strip mounted below "Watch a live game" (seated: the waiting room) | **RED** — 2 red / 62 green: `backingLanding.test.jsx` 2 red — "…under the waiting room's headline and hero, above "Watch a live game" and the bracket line" and 1 more |
| R2b | (2') the strip mounted below the bracket line (not seated: the slot picker's centre) | **RED** — 2 red / 6 green: `backingLanding.test.jsx` 2 red — "…ntre, directly under the draft-slot picker and the Auto-draft card, above the bracket line" and 1 more |
| R3a | (3) the mobile layout changed — the mobile strip's head 16px → 17px | **RED** — 25 red / 26 green: `backingMobilePin.test.jsx` 25 red — "…landings, unseated and seated LeagueHome · no-bracket · flag on · unseated (server render)" and 24 more |
| R3b | (3') the mobile layout changed — the mobile Confirm's padding 14 → 15 | **RED** — 5 red / 46 green: `backingMobilePin.test.jsx` 5 red — "…list, a card, the stake control a seat → its card → the stake control (attested, then not)" and 4 more |
| R4a | (4) the flag false but the desktop strip renders (the gate bypassed for `wide`) | **RED** — 6 red / 50 green: `backingDark.test.jsx` 6 red — "…tity panel, the pod sheet — opens NO backing read and makes NO request (DARK-3 / DARK-R-1)" and 5 more |
| R4b | (4') the flag false but a desktop backing element renders — the stats view mounted ungated beside the pitch | **RED** — 4 red / 52 green: `backingDark.test.jsx` 4 red — "…es render nothing: ScoutingLine is empty and the identity panel carries no backing element" and 3 more |
| R4c | (4'') the flag false but the desktop Backing screen renders (the gate bypassed for viewport=desktop) | **RED** — 2 red / 54 green: `backingDark.test.jsx` 2 red — "…ctly while dark renders nothing and opens NO read — the desktop layout rides the same gate" and 1 more |

Beside the rows (prompt §3):
1. **A per-team share visible on the desktop pod list while open** — R1a (the seat row, shared) and R1b (the desktop pods column alone): **the seal rows red** — `PodList.test.jsx` "SEAL at desktop width … the whole window … is the clean window, byte for byte" (list, card, control): 3 red each.
2. **The strip mounted below "Watch a live game"** — R2a: **the placement rows red** — `backingLanding.test.jsx` "desktop — seated: … above "Watch a live game" and the bracket line" and "a bracket exists … desktop — unseated and seated"; R2b (below the bracket line, not seated): the unseated rows red.
3. **The mobile layout changed** — R3a (the mobile strip's head) and R3b (the mobile Confirm's padding): **the mobile-pin rows red** — 25 and 5 rows of `backingMobilePin.test.jsx`, the golden-keys row among them.
4. **The flag false but a desktop backing element renders** — R4a (the desktop strip past its gate), R4b (the stats view ungated beside the pitch), R4c (the desktop screen past its gate): **the dark rows red** — `backingDark.test.jsx` "flag OFF — the desktop League renders EXACTLY as today (golden, generated on main before the desktop build)" (all four lobby rows under R4a; the identity-panel row under R4b), the no-read rows, and the gate's source row.

## 5. CONFIRMED / REFUTED

| Lens | Findings | CONFIRMED | REFUTED | Refuter added |
| --- | --- | --- | --- | --- |
| SEAL | 6 (SEAL-1…4, OBS-1, OBS-2) | 6 (SEAL-2's proposed fix shown incomplete; SEAL-1's shown to need a companion) | 0 | 3 — SEAL-R-1, SEAL-R-2, SEAL-R-3 |
| DARK | 1 (DARK-1) + the CLEAN claims C1–C13 | 1; the CLEAN verdict survived with one correction | 0 | 2 — DARK-R-1, DARK-R-2 (on the in-flight fix) |
| PLACEMENT | 7 (PLACE-1…7) + 5 notes | 7 (PLACE-5 in part: its "the window's close appears nowhere" refuted); the 5 notes confirmed | 1 sub-claim | 3 — PLACE-R-1, PLACE-R-2, PLACE-R-3 (on the in-flight fixes) |
| WIRING | 10 (WIRE-1…8, PRE-1, NOTE-1) | 9 + WIRE-3(a) | WIRE-3(b) (**PARTIALLY REFUTED**) | 2 — WIRE-R-1, WIRE-R-2 |
| **Total** | **24 + 5 notes** | **23, and WIRE-3 in part** | **1 finding in part, 1 sub-claim** | **10** |

A review that never refutes itself has not been run adversarially: here the refuters refuted WIRE-3(b) (the coordinator had already applied it — reverted in `2348ff0d`) and a factual sub-claim of PLACE-5, showed three proposed fixes incomplete or wrong (SEAL-2's, PLACE-2's over-correcting rule, PLACE-6's results column), and twice caught the coordinator's own in-flight fixes short (PLACE-R-3, DARK-R-2).

## 6. Per-fix guard checks

Each behavioural review fix was reverted on its own `git archive` extraction (`6a93aa2b`; the two WIRE-2 re-runs and F-WIRE3-both at `55fc5dff`), the guarding suites run, the tree discarded. A fix counts as guarded only when a NAMED row goes red.

| Mutant | What it reverts | Result |
| --- | --- | --- |
| F-WIRE6 | WIRE-6 reverted: the last head is "Pays ×" whatever the outcome | **RED** — 1 red / 19 green: `BackingResultsCard.test.jsx` 1 red — "…he cells are staked BP and the head says so (WIRE-6: a void or settling team never "pays")" |
| F-SEAL3 | SEAL-3 reverted: revealed on a figure's presence alone | **RED** — 1 red / 19 green: `BackingResultsCard.test.jsx` 1 red — "…ers byte-equal to the sealed pool, on both cards — the reveal waits for the close (SEAL-3)" |
| F-SEAL2-desc | SEAL-2: a desktop-only "most backed first" order (pods by pot, seats by stake, descending) | **RED** — 3 red / 19 green: `PodList.test.jsx` 3 red — "…ean one) the whole window — the pod list, no card yet — is the clean window, byte for byte" and 2 more |
| F-SEAL2-asc | SEAL-2: a desktop-only "quietest first" order (ascending) | **RED** — 3 red / 19 green: `PodList.test.jsx` 3 red — "…ean one) the whole window — the pod list, no card yet — is the clean window, byte for byte" and 2 more |
| F-PLACE1-window | PLACE-1 reverted: the pill rides the strip's own state (OPEN only), not the pod list's window | **RED** — 2 red / 33 green: `BackingPreviewScreen.test.jsx` 2 red — "…rip in the page’s desktop landing is BackingLandingStrip’s wide output for the same inputs" and 1 more |
| F-PLACE1-section | PLACE-1: the pill opens the strip's own section, not the window | **RED** — 1 red / 90 green: `BackingPreviewScreen.test.jsx` 1 red — "…ction its state names — while its "Back a team" action opens the WINDOW (PLACE-1 / WIRE-2)" |
| F-WIRE2-strip | WIRE-2: the strip's button names no section (the screen re-derives while its snapshots land) | **RED** — 1 red / 90 green: `BackingPreviewScreen.test.jsx` 1 red — "…ction its state names — while its "Back a team" action opens the WINDOW (PLACE-1 / WIRE-2)" |
| F-WIRE2-latch | WIRE-2 reverted: the screen ignores the section it was opened on (re-run at `55fc5dff`, with the rows added for it) | **RED** — 1 red / 108 green: `backingMobilePin.test.jsx` 1 red — "…by "Back a team" on a Monday–Friday viewer: the WINDOW, never the week its state points to" |
| F-WIRE2-card | WIRE-2: leaving the window by an implicit route leaves the card open (re-run at `55fc5dff`, with the rows added for it) | **RED** — 1 red / 143 green: `backingMobilePin.test.jsx` 1 red — "…the data (none asked for) closes the open card — the window comes back without a stale one" |
| F-WIRE3a | WIRE-3(a) reverted: re-selecting the open seat is a new visit | **GREEN** — 0 red / 51 green |
| F-WIRE3-key | WIRE-3: the visit keyed on the view object again (deps [view] — the pre-fix shape) | **GREEN** — 0 red / 51 green |
| F-WIRE3-both | WIRE-3(a) both guards reverted: a new view object on every seat click AND the visit keyed on the view object (run at `55fc5dff`) | **RED** — 1 red / 52 green: `backingMobilePin.test.jsx` 1 red — "…w, a card, a re-click, Back, back to the card, another card, the results, the window again" |
| F-WIRE1 | WIRE-1 reverted: the desktop results read only while the section is open | **RED** — 1 red / 55 green: `backingDark.test.jsx` 1 red — "…ctly while dark renders nothing and opens NO read — the desktop layout rides the same gate" |
| F-WIRE5-results | WIRE-5 reverted (results): the empty state before any answer | **RED** — 1 red / 21 green: `PodList.test.jsx` 1 red — "…r the reads are enabled (no answer yet, not loading) is LOADING — no alert, no empty state" |
| F-WIRE5-stats | WIRE-5 reverted (stats): the unavailable alert before any answer | **RED** — 1 red / 21 green: `PodList.test.jsx` 1 red — "…r the reads are enabled (no answer yet, not loading) is LOADING — no alert, no empty state" |
| F-PLACE5 | PLACE-5 / WIRE-4 / OBS-2 reverted: the chip reads the viewer's strip line | **RED** — 1 red / 21 green: `PodList.test.jsx` 1 red — "…the week) sees the WINDOW's close beside open pools — never "Settles after Friday's close"" |
| F-WIRER1 | WIRE-R-1 reverted: the rail always rests on "Window open" | **RED** — 1 red / 21 green: `PodList.test.jsx` 1 red — "…ool: no chip, and the rail rests on the quiet line — never "Window open" over no open pool" |
| F-WIRE7 | WIRE-7 reverted: the strip mounts before the seat is known (twice for a seated viewer) | **RED** — 1 red / 55 green: `backingDark.test.jsx` 1 red — "…e pod-list read, not one under the slot picker and another under the waiting room (WIRE-7)" |
| F-PLACE7-esc | PLACE-7 reverted: Escape does not close the host | **RED** — 1 red / 55 green: `backingDark.test.jsx` 1 red — "…r the strip's own section — and the host is a dialog that Escape closes (PLACE-1, PLACE-7)" |
| F-PLACE7-focus | PLACE-7 reverted: the host does not take focus | **RED** — 1 red / 55 green: `backingDark.test.jsx` 1 red — "…r the strip's own section — and the host is a dialog that Escape closes (PLACE-1, PLACE-7)" |
| F-PLACE2 | PLACE-2 reverted: the lit slot brings no lobby rule | **RED** — 3 red / 5 green: `backingLanding.test.jsx` 3 red — "…ntre, directly under the draft-slot picker and the Auto-draft card, above the bracket line" and 2 more |
| F-PLACE2-rail | PLACE-2 over-correction back: the lobby rule unrolls the right rail too | **RED** — 1 red / 7 green: `backingLanding.test.jsx` 1 red — "…ntre, directly under the draft-slot picker and the Auto-draft card, above the bracket line" |
| F-DARK1-A | DARK-1 M-A: the mobile list events fire on the card view instead of the list | **RED** — 2 red / 49 green: `backingMobilePin.test.jsx` 2 red — "…top review record) list → card → stake → card → list → a second card left inside one frame" and 1 more |
| F-DARK1-B | DARK-1 M-B: results_viewed silenced on mobile | **RED** — 3 red / 48 green: `backingMobilePin.test.jsx` 3 red — "…top review record) list → card → stake → card → list → a second card left inside one frame" and 2 more |
| F-DARK1-D | DARK-1 M-D: window_viewed loses its data dependencies | **RED** — 1 red / 50 green: `backingMobilePin.test.jsx` 1 red — "…ktop review record) data arriving while the list is off screen — the effects' dependencies" |
| F-DARK1-E | DARK-1 M-E: Your Backing's event 'shown' on every mobile view | **RED** — 2 red / 49 green: `backingMobilePin.test.jsx` 2 red — "…top review record) list → card → stake → card → list → a second card left inside one frame" and 1 more |
| F-DARKR1 | DARK-R-1 back: DeskLobby defaults uid to null | **GREEN** — 0 red / 99 green |

Three mutants stay GREEN, each accounted for:
- **F-WIRE3a** and **F-WIRE3-key** are the two independent guards of WIRE-3(a): the no-op on re-selecting the open card, and the visit keyed on its seat. Either one alone keeps the 62 s visit whole, so each is green when reverted by itself. Reverting BOTH (**F-WIRE3-both**) turns the desktop funnel row red. Green by design.
- **F-DARKR1** — DARK-R-1 has no row: it is unreachable with production-shaped data (the slot endpoint requires auth; seats are uid strings). The refuter's trial showed the branch equal to `main` in all ten crafted cases with the fix.

Two more were GREEN in the first pass and are red now:
- **F-WIRE2-latch** and **F-WIRE2-card**: no row could see the screen ignoring the section it was opened on (the funnel row asked for the section the data pointed to anyway), or the card surviving an implicit section change. Two rows were added (`55fc5dff`); both mutants now red.

CSS-only fixes (PLACE-2's rule, PLACE-3, PLACE-4, PLACE-6, PLACE-7's rings) are verified by measurement in Chromium (§7); a jsdom row cannot lay out. PLACE-2's rule and its "never the right rail" are pinned as markup (F-PLACE2, F-PLACE2-rail red).

## 7. Browser measurements (Chromium, the reviewers' own probes, at the final code)

Pages were rendered by the screenshot harness and by refute-placement's two-frame probe in a scratch copy of the final tree, lit and dark. There are two frames: the sidebar collapsed (App's default, 64px) and expanded (220px). No component file changed after `6a93aa2b`; the later commits touch the dev preview, a test file and the screenshots.

- **Lobby overlap (PLACE-2):** every lit landing (seated or not × bracket or none, both frames) at 769, 900, 1024, 1100, 1180, 1181, 1280, 1366, 1440 and 1920px, 80 renders in all: **no control covered, no horizontal scroll**. Every covered control measured is on a FLAG-OFF page at ≤1024px: 72 hits across 17 renders. That is the lobby's pre-existing overlap on `main`, reported in §9.
- **The stacked Backing screen (PLACE-3):** the stake, top-up and attestation pages at 769, 900 and 980px — **0 overlaps, 0 covered controls**.
- **Truncation (PLACE-6):** the results and the veteran card at 981, 1024, 1100, 1180, 1181, 1280, 1366 and 1440px — **nothing truncated**.
- **The strip in a narrow column (PLACE-4):** the whole strip card was measured, the strip and its "Back a team" pill together (the coordinator's variant of refute-placement's probe, which had scanned the strip's own button only). Widths: 1181, 1200, 1210, 1220–1240 in 5px steps, 1280, 1366, 1440 and 1920px. The default frame is clean at every width. With the sidebar expanded (a 165–219px strip), names are cut short with an ellipsis and nothing else happens:
  - the eyebrow "Window open" at 1181px (74 of 79px shown);
  - the week's team names up to 1200px;
  - the staked pod's name "Vanguard" up to 1235px.

  The pill (142px), every figure and every action stay whole. The strip card is clean from 1240px.
- **Focus (PLACE-7):** keyboard focus on the strip, its "Back a team" action and the screen's back link draws a 2px ring; an ordinary lobby control ("Ranked") is unchanged.
- **The 1440×900 contract (§E):** `shoot.mjs` measured the stake control's three lines and Confirm on screen, the column unscrolled — stake 282–499 / 511–557px, top-up 378–595 / 607–663px — at the reviewed commit and again at the final one.

## 8. Verification

All at `42602f44`, the code this record reviews. The record's own commit adds only this file.

- **`npm run test:run`**: exit 0. The `Test Files` line, verbatim:

  ```
   Test Files  777 passed | 3 skipped (780)
        Tests  15307 passed | 64 skipped (15371)
  ```

  The three skipped files are pre-existing and untouched here: `firestore.rules.emulator.test.js` (it needs the emulator) and the two `src/screens/__golden__/capture*` scripts.
- **`npx vite build`**: exit 0 (3,923 modules). Every warning it prints is pre-existing:
  - four CSS-syntax warnings from the untouched `battleArena.css` and `league.css`;
  - Rollup's dynamic-import and chunk-size notices.
- **`npm run lint:gate`**: exit 0 (`--max-warnings 0`).
- **`git diff --stat 97693a41 42602f44`**: 57 files changed, 5,197 insertions, 288 deletions. With this record it is 58 files.
- **Fence (BUILD_RULES §1)**: no fenced file edited and no fenced function called.
- **Out of bounds**: no file under `api/`, and no `.env`, Vercel or Firestore file.
- **Flags**: `BACKING_BETA_ENABLED` stays `false` (`src/config/featureFlags.js:2531`, untouched).
- **Goldens**: `__fixtures__/backingMobilePin.golden.json` and `__fixtures__/backingDark.golden.json` were regenerated on `main`'s code. Both are byte-identical to the committed files. That includes the coordinator's regeneration after the funnel rows: 94 markup rows unchanged.

## 9. Reported for separate tasking (BUILD_RULES §3 — found, not fixed here)

1. **SEAL-1 (+ SEAL-R-2) — a gate for the activation PR.** `computeTrainerStats` (`api/_utils/backingStats.js`) folds live stakes on OPEN pools into "Backers on you", "BP backed on you" and "In play on you" — the trainer sees their own team's sealed book, stake by stake. Forbidden by the attached desktop design ("Closed weeks only — this week's pool is sealed even to the trainer"), spec §3, Amendment B D-q, the original brief §3 and prompt §F. The fix is server-side (skip open-pool stakes; 3 pinned rows in `backingStats.test.js` / `trainer-stats.test.js` move) and must come with the design's sealed "This week" row — never keyed on whether stakes exist — or the panel says "Nobody has backed your team yet." while four have. Pre-existing on mobile (`EquipStation`); dark today.
2. **SEAL-4:** `BackersCall` (`BackingParts.jsx`) clamps the §B6 count to the reply's own `floor`, not to three — `Math.min(count, VALIDITY_MIN_BACKERS)`. Unreachable with honest data.
3. **The lobby's pre-existing overlap (PLACE-2's root):** `LD_STYLE` (`LeagueLobbyDesktop.jsx`) keeps `min-height: 0` on its reflowed grid items at ≤1180px; with the flag off, at ≤1024px the centre covers "Open my game" and the right rail covers left-rail rows (measured, §7). The fix this branch emits while lit (`.ld-center`, `.ld-rail-left` → `min-height: auto`) belongs in `LD_STYLE` itself.
4. **PLACE-7's pre-existing half:** no `:focus-visible` rule anywhere in League CSS (`all: unset` + `.lg-tap` hides focus on `MyGameBar`, `DeskTabBar`, the mobile strip…); `LDFocus` (Spectate's overlay) has no dialog semantics.
5. **N3:** the mobile strip still sits below "Watch a live game" (`LeagueLobbyRedesign.jsx`) — the desktop brief calls it a known issue to resolve on both; prompt §A froze mobile.
6. **PRE-1 / N4:** the landing strip never re-reads after the Backing host closes (stale after a stake), nor at the close (a "Back a team" left on screen past Sunday 23:59 ET until a reload). Identical on `main`.
7. **WIRE-R-2:** a live stake on a pod voided or expired after its pool closed is classified "window" (`deriveStripState`) — "Closed · plays Monday" for a pod that will never play. Identical on `main`; the results read (now on open, WIRE-1) refunds it and self-heals the screen.

## 10. For the founder

- **"While the window is open" on Monday–Friday (PLACE-1 / OBS-1).** Spec V1.3 §4 opens next week's window at Monday 00:00 ET, so it is open through the whole in-play week — the design assumed it never is ("Pools open Fri 4:00 PM ET"; "Mon to Fri · read only"). This branch reads §C literally: the returning backer's week strip carries the "Back a team →" action (it opens the window), while the strip itself still opens Your Backing (§E). If you want the Monday–Friday strip quiet, it is one condition (`backingWindow` gating the action) — the refuters trialled both readings.
- **SEAL-1 is an activation gate** (§9 item 1) — the trainer stats show the trainer's own open pool. Not a desktop regression (mobile shows the same numbers today, dark), but it contradicts the design you attached.
- **The trainer stats' desktop home is the mobile view** (SEAL-R-1): the attached design's closed-weeks layout ("This week · SEALED", "Last week", "Season") needs per-week data the stats reply does not carry — not buildable layout-only.
- **"Full centre-column width" (N1):** the strip is the width of the draft-slot picker and the Auto-draft card it sits under (560px); the centre column is wider than that only above ~1576px (default frame). Say if you meant the whole column.
- **Screenshots** are framed as App lays out a desktop width by default: the sidebar collapsed to its 64px rail.
- **New visible copy** (all in `backingCopy.js`, lexicon-clean): "Back a team"; the top-up panel's "Your stake on {label}", "Adding", "New total", "of 500", "+{n}"; the results table's heads "Team", "Backers", "Share", "Pays ×" — and "BP backed" before a pool settles.
