# RECAP RESTORATION — DISCOVERY RULINGS (FOUNDER, JUL 30 2026)

**Relayed verbatim. Governs together with spec V1.1 + the discovery-lite report. Commit this memo, both spec versions, and the discovery report to `docs/` as the first build commit (standing D-P2-11 rule). Build may then begin.**

**R-B1 — Econ operand source of record: EODHD economic-events fetch, net-new (option a).** Sonar-verification (b) rejected — it neither fixes the actuals-null silence nor constitutes ground truth (LLM-vs-LLM is a second opinion, not proof). Build the fetch on the `fetchEarningsCalendarEODHD.js` pattern. **Founder deliverable:** CC writes a read-only capture script (shortlist pattern, `--env-file=.env.local`); founder runs it and returns the raw response, which becomes R2's provenance fixture. **Contingency: if the endpoint is unavailable on the plan → HARD STOP, re-scope with founder. No improvised fallback.** Sonar remains for the preview path only; the recap path becomes deterministic end-to-end.

**R-B1a — Operand-reliability layer (founder-raised: both EODHD and Sonar calendars have shown historical unreliability).** Two deterministic mechanisms, in scope: **(i) Plausibility gate at ingestion** — every operand must parse AND `|actual − estimate|` must fall within a per-category structural band (CC proposes the band table in the build report from category units/typical ranges; bands are deliberately loose — they exist to catch series/unit mis-mapping, the classic aggregator failure, not to adjudicate close calls). Violation → new skip-taxonomy code `operand_implausible`, story held, loud log. Same band applied to earnings surprise %. **(ii) Settle delay** — econ recap eligibility = release time + one cron tick (~30 min), letting initial-posting revisions settle. **Scope boundary, stated to prevent lock confusion: these are PUBLICATION gates upstream of everything — they are NOT adapter tolerances; F-M4's locked tolerances are untouched, no `adapterVersion` interaction.** Explicitly rejected: dual-source-of-record voting (two imperfect sources don't make truth — founder's prior experiment confirms). Register, post-gate, in order of rigor if memos show plausible-but-wrong values passing the band: Sonar concurrence as logged ADVISORY only (P9: observes, never gates); agency-direct APIs (BLS/BEA) as true econ ground truth. Note: date-unreliability is already structurally mitigated by R-A1 (arrays own the set/dates; EODHD supplies numbers for known events only) and by the actual-present data-gate + morning window on earnings (±1 day report-date drift self-heals). The founder's R2 capture run doubles as the freshness test on current EODHD econ quality — eyeball it together before the fetch is built.

**R-A1 — Tier-1 authority: the DRB `macroCalendar` arrays, full category set, + jobless claims added as a category.** The feed-driven `isTier1Event` (impact/keyword) is retired for the recap gate; membership in the array set IS the classification. Priority ordering when multiple uncovered: high-impact categories first. Jobless claims is intended Tier-1 (already `JOBLESS_CLAIMS` in the locked subjectRef enum); weekly cadence → R9 strong form with ≤1-week bound. The duplicated `isTier1Event` copies → register (unchanged for other consumers this arc). Both open feed questions are mooted.

**R-B3 — C8(c) superseded; the FINAL-LOCK §3 join stands.** Do NOT re-key the consensus write. The locked invariant is self-consistency (writer and adapter share one UTC expression evaluated at the same instant) and it holds across the midnight boundary by construction. Replacement acceptance row: **UTC-midnight-boundary fixture** — the 8pm-ET fire's operand write and story land coherently and the adapter join succeeds. Seed-ET-vs-append-UTC divergence stays on the register (cosmetic doc-splitting, not join failure). The receipt-bucket half of C8 stands exactly as discovered: firing-scoped, B5-immutable, never re-keyed.

**R-B2 — C2 re-scoped to BOTH: (i) fix every UTC-"today" derivation to ET via `deriveMarketDate` (`generate-recap.js:68/356`, `generate-econ.js:394`, and the recap dedup boundary); (ii) the prior-session capability ships via a CRON RE-AIM** — one of the five evening firings moves to pre-market ET (~13:00 UTC; CC proposes the exact expression with a fixture). Entry count unchanged → C3 intact. The morning fire runs prior-ET-session AMC + same-day BMO through `getPreviousTradingDay`, with `assertMaintainedYear` ported onto that walker path (2028 gap closed in scope, one line).

**R-B5 — F2 confirmed on all three, labeling generalized:** the operand label is a deterministic function of `(beforeAfterMarket, session-relation-to-reportDate)` — same-evening AMC → "into-earnings (pre-reaction session)"; next-morning AMC → early-reaction, may say so. Surfacing `beforeAfterMarket` into prompt + top-level story metadata is IN scope. Reaction-day move as a new snapshot field → post-gate register (version bump + adapter change). Fix the `:176` mislabel comment.

**R-B4 — Referent dedup schema addition APPROVED, top-level only:** `referentDate` (+ existing symbol/eventName) as top-level story fields — **never inside `dataSnapshot`** (C1 photographs snapshot key sets; top-level placement preserves the freeze). Composite index declared in `firestore.indexes.json`. Dedup query: (type, referent, referentDate, non-superseded), pre-model-call, zero model calls on hit — per C8(a)/(b). S3's multi-day 5× and S5's boundary mismatch both close under this one mechanism.

**R-B6 — F1 amendments accepted:** distinct `fetch_failed` code at both outer catches (Sonar `generate-econ.js:174`, EODHD `generate-recap.js:380`); S3 dual-count (`events.length` + `releasedTier1.length`) logged on EVERY firing including the zero path, single line.

**R-B7 — ONE pre-window epoch reset, not two.** Part D steps 2–3 misread the standing Jul-29 ruling: deferred exemplars (neta econ_recap, doug earnings_recap) are **post-gate** — they land after the gate passes as the already-priced deliberate reset. Sequence of record: mini-arc PR (6→7) + N2 exemplar PR merge → R9 liveness observed → two-period window opens → gate passes → deferred exemplars later. Budget +23 Haiku/day: accepted. Flag posture (on-but-silent, no dependency on `WIRE_WRITES_ENABLED`): confirmed. "R9 observation window" ≡ the V1.2 N3.4 two-period gate window preceded by R9 liveness: equivalence confirmed.

**Register additions from Part E:** all seven filed as-is; item 1 (EST-hardcoded dedup boundary) is partially consumed by R-B2(i) where it touches this arc's files — the remaining sites stay register.

*RECAP_RESTORATION_DISCOVERY_RULINGS_JUL30_2026.md*
