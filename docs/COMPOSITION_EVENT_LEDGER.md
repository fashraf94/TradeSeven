# Composition Event Ledger — V1.0 (living manifest)

**What this is:** the single accumulating list of everything the `ARCHETYPE_IDENTITY_VERSION` composition event must carry. The event is ONE deliberate, founder-gated, adversarially-reviewed identity-version bump (Map Disposition Ruling D2 — no piecemeal edits to hashed registry inputs, ever). Anything discovered later that touches a hashed input gets ADDED HERE, not fixed inline. Update this file whenever an item joins.

## Cargo (as of July 29, 2026)
1. **The authored cell matrix replaces the stored compat map** as registry content — the program's plan of record (Disposition D1).
2. **Schema extension:** the runtime verdict set gains `tension` with its Guide-§4 treatment fields (guidance ref, `narrowedParams`), and the offer surfaces gain per-archetype parameter-domain restriction — the product build the audit's V12 proved missing (`narrowedParams`/`paramBounds` have zero code occurrences today).
3. **The 17 core_conflict-class corrections** from the audit's flag table (wrong silent permissions — the dangerous direction).
4. **The four family re-filings** (audit V10: `tv-15`, `i-09` out of `high_volatility`; `i-10` out of `momentum_breakout`; per the audit's own definitions).
5. **Seed-map reconciliation** forced by the strict seeded-rule invariant (audit V6: 39 offerable cells CI-locked against reclassification — the invariant must be answered, not bypassed).
6. **Default-trait seeding substitutions** (small-fixes arc check-0 stop, Jul 29): diversifier's four hidden seeds (`tv-04`, `mb-05`, `gs-05`, `gs-06`) and guardian's deprecated `risk-single-stock-limit` → supported, stored-native, same-family substitutes, proposed at event time for founder review under the regenerated snapshot.
7. **presentationHash implementation** (Sheet C C-3's dual-hash lane) so display-class fields never again force identity events — the honesty-hygiene arc's supportStatus map and this ledger's future cousins get a formal home.

## Preconditions before the event can execute
- Cell matrix complete for all authorable columns (Diversifier's 95 cells rejoin after `SECTOR_CAP_MODE='enforce'` is live and verified — which is therefore ALSO a precondition, sequenced first).
- The audit's 121 tension-class flags dispositioned through family-batch authoring (Disposition D3).
- Event-time adversarial review of the full cargo, then founder merge — the same ceremony as every activation, at the largest scale the program has.
