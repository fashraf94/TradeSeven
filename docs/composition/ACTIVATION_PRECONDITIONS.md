# Composition — Activation Preconditions Ledger (Sol design review, adjudicated)

**Date:** Aug 6, 2026 · **Source:** Sol's PR 2 design review + PR 3 pre-activation review, founder-adjudicated · **Verdict of record:** **PR 2 and PR 3 merge as-is and run DARK — both verdicts explicitly permit dark operation.** Every blocker below is a **PR 4 precondition to activation** (the PR-3 rows are now closed), not a merge defect. This ledger is the tracking document: each item carries its closing PR and the acceptance test that proves it closed. **PR 4 tasking follows on this ledger.**

**Reading rule:** an item is CLOSED only when its acceptance test exists, fails under the item's defect (mutation-checked), and passes at the closing PR's HEAD. No flag in the flag-ownership table leaves `off`/dark until every item whose closing PR has merged is green — and no epoch close or activation runs until ALL items are green.

---

## PR 3 preconditions — **CLOSED** (landed at the CompiledBuild / compiler boundary PR, merged dark)

All five closed per the reading rule (tests exist, fail under their defects, green at PR-3 HEAD `344c9a71`; §2 review `docs/audits/20260806_COMPOSITION_PR3_CODE_REVIEW.md`). Each row's PR-4 hardening — surfaced by the §2 review and Sol's pre-activation pass — is carried in the PR-4-EXT table below.

| # | Requirement | Acceptance test | PR-4 hardening (→ EXT row) |
|---|---|---|---|
| **B8** ✅ | **Per-writer behavioral fence suite** — parameterized over the ENTIRE census (11 settings endpoints, deploy gate, provisioners, background loops, CLI scripts): drive each writer under (a) a **closed** epoch and (b) a **mismatched** epoch, and assert all four: rejection (the class's sentinel — 409 / skip / abort), **zero writes**, **no compiled-build mutation**, **no success response**. Extends the behavioral proof from 2 writers to every censused writer — retiring the lens-1 C5 residue for good. | `composition.fenceBehavior.test.js` (45 rows; every endpoint has an OPEN-epoch anti-vacuity control proving it writes when admitted — §2 review F2 fix) | **B8-FINAL** — re-run at the activation SHA + external-admin inventory |
| **B3** (test form) ✅ | **Deny-by-default protected-store AST scan** — an acorn AST scan of `api/` + `scripts/` flagging EVERY Firestore-shaped write (`set`/`update`/`create`/`delete`/`add`, tx/batch/computed-method variants) whose collection resolves to a protected store, failing CI unless the call site is on an **explicit allowlist** at its **pinned site count** (§2 review F3a). **TEST FORM only — NOT the capability refactor.** | `compositionProtectedStores.scan.test.js` + `compositionProtectedStoresAllowlist.json` (158 keys / 211 sites, count-pinned both-direction ratchet; documented resolution limits) | **B3-EXT** — one-level helper-parameter data-flow |
| **M11** ✅ | **One-hop-resolved forbidden imports** — the forbidden-reads guard resolves each prompt-surface file's one-hop import list and bans the compat surface on those modules too (the transitive-shim hole). | `compositionForbiddenReads.test.js` one-hop rows (a scaffolded re-export shim imported by an assembler fails) | — (two-hop excluded by design; noted) |
| **M12** ✅ | **Base64url entry keys + the semantic-vs-run hash split** — entry doc IDs become `base64url(entryKey)` (injective); `overlayContentHash` splits into `semanticHash` (runId-independent) and `runHash` (runId-pinned). | `composition.pr3.acceptance.test.js` (legacy-collision → distinct ids; equal semanticHash / distinct runHash across runs; scan writes both) | — |
| **B5** ✅ | **The generation-consistent shared production loader CONTRACT** — ONE loader: transaction + **full-descriptor-tuple seqlock** (§2 review F3 ABA fix), returns `{state, generation}`, every derived write stamped; malformed descriptor fails closed (§2 review F4). | `compositionProductionLoader.contract.test.js` (interleaved-bump retry, ABA rollback+reactivation, torn-read exhaustion, malformed-fail-closed, stamp tripwire) | **B1-EXT** (override-revision token) + **B4** (generation monotonicity) |

## PR 4 preconditions (close at the activation/runbook PR)

| # | Requirement | Acceptance test |
|---|---|---|
| **B1** | **`expectedEpochId` binding on every identity write** — fence validation compares the in-tx-read epochId against the writer's **first-observed epochId, pinned across transaction retries** (a retry that observes a different epoch REJECTS — it must not silently revalidate against the new epoch); and **absent epoch doc FAILS CLOSED once an activation record exists** (fail-open remains only in the pre-activation dark world). | Fence rows: a simulated tx retry re-reading a different epochId rejects with zero writes; absent-doc + no activation record admits (dark compat, byte-identical today); absent-doc + activation record present rejects |
| **B2** | **Lease/commit-fencing for the nontransactional provisioners** — `casualClone.ensureCasualClone` and `trainingClone.provisionTrainingClones` move from entry-guard-only to a mechanism that makes the WRITES themselves fenced: a registered lease the §8 close drains, or transactional/create-fenced commits — so a provisioner that read "open" cannot land writes after the watermark. | Provisioner rows: a provisioner holding a lease acquired pre-close cannot commit post-close (write rejected or lease drain blocks the close until it exits); interrupted provisioning remains re-entrant (the existing subcollections-first order preserved) |
| **B4** | **The full activation DESCRIPTOR** — the activation record carries the complete tuple `{activeEpochId, activationGeneration, candidateStateId, semanticHash, identityVersionTarget}` and **rollback repoints the COMPLETE tuple atomically** (never epochId alone). The design note §2 activation-record row is aligned to the spec's descriptor shape as part of this item. **Generation monotonicity (PR-3 design review, seqlock-ABA finding): the activation writer MUST mint strictly increasing `activationGeneration` values — a rollback repoints the tuple but never reuses a generation number for a different candidate.** (The B5 loader's seqlock independently compares the full tuple, so it does not DEPEND on monotonicity — but stamped derived writes do: a reused generation number would let a stale stamp pass reader validation.) | Activation rows: rollback asserts every tuple member repoints together in one transaction; a partial repoint is unrepresentable (single-doc descriptor) or rejected; a re-activation after rollback mints generation MAX+1, never a reused number (asserted); A48 (the activation-record property row deferred from PR 2) lands here against the full descriptor |
| **M6** | **Activation-time candidate verification** — the activation transaction verifies the candidate namespace BEFORE ratifying: entry IDs are **create-only** (no entry was overwritten since apply), **exact `entryCount` match**, **recomputed content hash equals the stored hash**, and **no stale extra entries** in the collection. | Activation rows, one per defect class: missing entry / extra entry / count drift / hash drift each ABORT the activation with nothing repointed |
| **B9** | **Rules deploy-record-smoke gate** — the §8 runbook requires a recorded `firestore.rules` deploy (deploy record: SHA + timestamp + operator) AND a green emulator smoke run (`compositionEpochDenials` suite) at that recorded SHA **before** `COMPOSITION_EPOCH_FENCE_ENABLED` flips — closing the "rules inert until Console deploy" gap (lens-1 C6) with an auditable gate instead of trust. | Runbook gate: the flip checklist item fails without a deploy record naming the deployed rules SHA + the smoke result; the smoke suite runs against the DEPLOYED text, not the repo text |
| **M10** | **Observe-evidence scope language** — the runbook's observe-window requirement states its true scope: PR 2 instruments **equip-bundle only**; the flip checklist must name which boundaries produced observe evidence and which activate on B8's behavioral suite alone. | Runbook text row reviewed at PR 4; the checklist template carries a per-boundary evidence column |

## PR 4 preconditions — EXTENDED (Sol PR-3 pre-activation review, founder-adjudicated Aug 6, 2026)

The PR-3 §2 review + Sol's pre-activation pass surfaced hardenings that are activation-blocking but out of PR-3 scope. Same reading rule; each EXT row supersedes/extends its base row where noted.

| # | Requirement | Acceptance test |
|---|---|---|
| **B1-EXT** (extends B1/B5) | **The full consistency token + the derived-write census.** Three parts: (1) **`activationGeneration` strictly increases** on every activation / rollback / reactivation — **no full-tuple reuse EVER** (a rollback repoints the tuple; a reactivation mints MAX+1, never a prior number). (2) **`overrideRevision`** (or `epochStateHash`) **joins the consistency token**, incremented **atomically** with every logical mutation of the mutable active-epoch override layer — so a mid-read override change at the SAME generation still forces a seqlock retry (generation alone can't see an override edit). (3) **EVERY persisted derived write** either **commit-validates the descriptor transactionally** OR **carries the full source tuple with reader rejection** — the `decide.js` splice pattern (ruling of record below), generalized to all derived writers. | (1) monotonicity: `activate→rollback→reactivate` asserts strictly-increasing generation and zero tuple reuse; (2) override-revision: any override-layer mutation increments `overrideRevision` in-tx, and the B5 loader's seqlock retries when it changes mid-read even at fixed generation; (3) **a CI derived-write census** enumerates every writer that persists composition-derived state and proves each does (a) in-tx descriptor validation OR (b) full-tuple stamp + reader-side rejection — a derived writer doing neither fails |
| **FC-1-CLOSE** (closes PR-3 review FC-1) | **`createAgentBattle` cutover atomicity.** The fenced battle writer **pins the full descriptor BEFORE manifest resolution, RE-VALIDATES at commit, and STAMPS the battle manifest with the source generation + semantic identity.** An activation interleaved across the battle write yields a **wholly-A or wholly-B** manifest — never A's compiledBuild with B's advisory slice. Readers **tolerate both `compositionCompat` shapes** (present, and legacy-absent). *(This is the §1 concept-fence contact the PR-3 review named: the battle-doc shape changes through non-fenced `resolvedAgentManifest.js` at the flip — closed here under §7 sign-off.)* | A cutover-interleaving test (activation lands between pin and commit) proves the manifest is wholly one generation, stamped with `sourceGeneration` + `semanticHash`; a commit-time descriptor mismatch aborts/retries the battle write; a reader handles a battle with the slice and one without |
| **B3-EXT** (extends B3 — modified from Sol) | **One-level helper-parameter data-flow.** Extend the deny-by-default scan **exactly one hop**: an exported helper whose **parameter flows into a write method on a protected store** (`function f(ref){ ref.set(…) }`) is itself a scanned write-helper; its **call sites** passing a protected-store ref are allowlisted **with per-callsite counts**. Closes the PR-3 review F3b/design-F1 ref-as-parameter blind spot. **NOT full data-flow analysis, NOT the capability refactor — the one-level fallback only.** | A helper taking a ref param that writes is detected; a NEW call site passing an `agents`/`rules`/`bundles`/compiled-build ref into it fails CI until allowlisted at its count; a two-hop chain remains a documented limit (the census chokepoints + B8 + rules layer cover it) |
| **B8-FINAL** (extends step 0 / B8) | **Activation-SHA reconciliation + external-path inventory.** At the exact activation candidate SHA: (1) the A46 census + B3 + B3-EXT scans re-run green, **every newly-discovered writer reconciled** (the PR #716 casualClone interception is the precedent); (2) **one behavioral fence row per DISTINCT write implementation** (dedup by mechanism, not per endpoint — the B8 45-row suite is already near this); (3) an **operational inventory doc of external admin write paths** — Firebase Console, VPS scripts, service accounts — enumerated with each path's **out-of-band pause step** (these cannot be code-fenced; the runbook pauses them). | Census/scan green at the deploy SHA with a recorded reconciliation; a behavioral row exists per distinct write mechanism; `docs/composition/EXTERNAL_ADMIN_WRITE_PATHS.md` (or equivalent) exists and every listed path has a runbook pause entry |
| **A7-LOCK** (extends A7) | **Version freeze + corpus-wide legality parity.** For the activation event: **registry / template / kernel versions are FROZEN** (ARCHETYPE_IDENTITY_VERSION, RULE_LIBRARY_VERSION, the candidate manifestHash, and the `compositionEnforcement` kernel content hash), all **pinned in the activation evidence**. A **corpus-wide differential legality test** at the activation SHA proves the compile-boundary verdict **equals** the equip/save kernel verdict for **every** equippable rule × archetype × the ledger domains (the PR-3 review F2 agreement, proven corpus-wide — not just on fixtures). *(Single-source consolidation of the two legality call sites is filed POST-event.)* | The activation evidence doc pins all four version/hash values; a drift between evidence and HEAD fails the gate; the corpus differential test finds zero compile-vs-kernel verdict disagreements across the full 95×5 universe at the seeded + boundary param values |
| **M7-E2E** (extends M7) | **Worst-case FULL-REQUEST budget, per assembler.** A fixture **per assembler** (draft `buildStrategyUserPrompt`, eval `buildAgentIdentityBlock` + `buildEvalSystemPrompt` + `buildLiveContextBlock` + tool schema) with **max advisory payload + max live-context + tool schema + all framing**, measured, asserting the whole request fits the model context with an **explicit stated headroom margin**. Closes the PR-3 review F4 scope gap (M7 measured system+identity only). | Per-assembler worst-case fixtures assert `input + EVAL_MAX_OUTPUT_TOKENS < contextLimit − statedHeadroom`; a runaway (duplicated block, advisory double-append at scale) fails with margin to spare |
| **FINAL-DRYRUN** (hard gate) | **Founder-ratified dry-run at the candidate SHA.** The runbook `--apply` step is **blocked** until a migration dry-run at the **exact PR-4 candidate SHA** is produced AND the **founder records ratification of the exact counts** (affected agents / entries / report classes). **Required because A11 changed the migration population** — the six former `needsBinding` rows now resolve to `param_out_of_domain` clamp entries, so D1's ratified 15-entry count is stale. | The `--apply` gate refuses without a dry-run report at the candidate SHA + a recorded founder ratification of its exact counts (the D1-ratification pattern, re-run at the final SHA) |
| **BARE-CELL-INVARIANT** (CI) | **Bare domain ⇒ exactly one bindable param.** A CI invariant, strengthening the PR-3 A11 row (which only forbade bare-domain-on-multi-param) into positive form: **every** candidate cell whose `narrowedParams` is a BARE domain (not param-keyed) must sit on a rule with **EXACTLY ONE** bindable template param — a bare domain on a **zero-param or multi-param** rule fails CI. | `archetypeCompatibilityCandidate.test.js` invariant: for every cell, bare `narrowedParams` ⇒ the rule's forgeKnowledgeBase template has exactly one distinct param key; a zero-param or multi-param bare cell fails |
| **B4-TRAIT** (Sol B4 — **CLOSED at PR 3.5**, `claude/composition-pr3-5-trait-channel`) | **CLOSED — founder trait rulings, Aug 6 2026 (Sol's B4 resolved via UNIFIED HOST PROJECTION, its preferred fix — not the zero-count route, which the NONZERO gate refuted).** Delivered dark at PR 3.5: (1) `projectHostedRuleDocs` — the ONE selection kernel (projectActiveRules; bundle + trait channels, deduped, host provenance) now feeds the migration planner, the residual scanner, AND the candidate compiler's input (`buildProjectedCompileInputs`); trait-hosted tension rules carry advisories, and A15/quarantine/A13-exactly-once extend over the trait channel; the freshness vector gains `projectedRulesHash`. (2) The three violating ladders re-authored inside their adjudicated domains — never widened, never clamped (patient-holder/mb-01 minutes 30/45/60; dual-conviction/tv-10 fund_score 71/75/80; trend-rider/tv-01 stretched 80/77/75 — the "analyst-only" premise was DISPROVEN: tv-01/momentum_chaser violated at dominant) — with ARCHETYPE_IDENTITY_VERSION 1→2 per the registry lock's remedy. (3) Three CI invariants landed: (a) no default trait hosts core_conflict/deferred for its own archetype; (b) every ladder seeds in-domain at every strength (authored RED against the pre-repair ladders, green after — the fail-then-green history is the proof); (c) trait-hosted tensions render their advisories end-to-end (compile → manifest → assembler). | (a)+(b) `src/data/archetypeDefaultTraits.composition.test.js` (kernel-imported predicates, no hand copies); (c) the end-to-end row in `compositionAdvisoryRender.activation.test.js` (mutation: severing the trait channel in the selection kernel fails it); unified-projection rows in `compileBuild.candidate.test.js` (trait advisory + provenance, A7-over-trait using the pre-repair analyst value as the fixture, draft-bundle universe, dedupe-exactly-once, vector-hash movement) |

## §8 runbook — step 0 (named by founder adjudication)

> **STEP 0 — census re-run at the exact merged SHA.** Before any epoch close or activation step, re-run the full A46 census suite (`compositionWriterCensus.test.js`) and the B3 protected-store scan **at the SHA actually deployed**, and record SHA + result in the runbook log. The census is a build-time artifact; writers merged between the census's last green run and the deploy SHA (the PR #716 casualClone interception is the precedent) MUST be caught before the fence goes live, not after.

## Ruling of record — `decide.js` (REVERSED, Aug 6, 2026)

The prior ruling ("DERIVED classification ACCEPTED; no PR-4 splice required") is **REVERSED by the Sol adjudication**: the DERIVED classification **stands for dark operation only**. **The PR-4 fenced splice is now REQUIRED** — epoch/generation validation before the `activeRules` write, plus a **generation stamp** on the written projection, with readers rejecting stale-generation projections.

**Recorded reason — Sol's cross-generation counterexample:** the transitive-fence argument freezes the projection's inputs *within* an epoch; it says nothing across an **activation boundary**. A decide.js window in flight during activation can read the authority stores pre-activation (generation N−1), then commit its re-derived `activeRules` projection *after* generation N activates — persisting an old-identity projection past the watermark with nothing marking it stale, indistinguishable to readers from a current one. Input-purity does not survive a generation flip mid-window; only write-time validation plus a reader-checked stamp does.

- **Closing item (PR 4):** the sanctioned fenced splice (§7-gated fence contact, founder-approved by this ruling) — validate epoch + generation before the write, stamp `projectionGeneration` on the written value; the deploy path and assemblers reject a stamp ≠ current generation.
- **Acceptance test:** a projection write carrying generation N−1 attempted after activation of N is rejected at the write; a stale-stamped projection already persisted is rejected by the reader path (both directions covered).
- Census row updated in the same commit as this ledger (`compositionWriterCensus.json` `derivedClassified[0].founderRuling`).

## Trait ruling of record — Sol's B4 conditional count (**NONZERO → RE-RULE**, Aug 6, 2026)

**Question (founder):** across the six `ARCHETYPE_DEFAULT_TRAITS` sets, grading each default trait's hosted rules against the trait's OWN archetype (candidate registry, post-substitution). ZERO ⇒ close B4 via a birth-only-channel argument + a CI invariant; NONZERO ⇒ report + STOP + re-rule.

**Method:** the real candidate kernel (`checkCandidateTraitLegality`, `compositionEnforcement.js`) at the actual birth strength (`moderate` — `seedDefaultTraits.js:36` default, `AgentCreationFlow.jsx` passes none), plus the cell-state tally. **Verified by 7 independent paths** (raw-text registry parse, ESM-import recompute, both adjudication ledgers, live runtime gate, worktree base-diff, two adversarial refutation agents) — all agree, high confidence.

**Result — NONZERO.** Two figures, kept precise (a framing correction the verification forced):

1. **Cell-state grades: 20 tension, 0 core_conflict, 0 deferred** (of 46 hosted triples: 13 native, 4 neutral, 20 tension, 9 no-cell). ⚠️ **The 20 tension pairs are DESIGNED-COMPATIBLE and legal/non-blocking** under the kernel (`isBlockingViolation` fires only on core_conflict / deferred / param_out_of_domain) — they are **not** "20 conflicts." Zero born-with rule is core_conflict or deferred for its own archetype.
2. **The load-bearing NONZERO — 2 genuinely BLOCKING `param_out_of_domain` births at moderate, both analyst:**
   - `trait-dual-conviction / tv-10` — seeded `fund_score=65`, analyst narrowed domain `{min:71, max:85}` → `65 < 71`, OUT.
   - `trait-patient-holder / mb-01` — seeded `minutes=90`, analyst narrowed domain `{min:15, max:60}` → `90 > 60`, OUT.
   - Per-strength blocking floor: **subtle=1, moderate=2, dominant=2 — never 0** (the two offenders pull opposite ways on the strength axis, so no uniform strength clears both).

**CLOSURE (PR 3.5, `claude/composition-pr3-5-trait-channel`):** the founder ruled — profiles re-authored (never widen, never clamp), unified host projection adopted, three CI invariants landed. See the B4-TRAIT row above (CLOSED). The re-authored values are subject to founder ratification at merge.

**Why this REFUTED the birth-only-channel closure of B4** *(the state of record at the STOP, before the trait rulings — retained as history)*: birth is confirmed the *sole* channel injecting these raw out-of-domain values — the seeder (`buildSeedPlan` → `createRule`) persists the profile values faithfully and **never clamps** (clamping lives only in the migration planner). So the analyst was *born*, at the default strength, holding two rules illegal under its own archetype's narrowed domains. The birth-only argument was supposed to show birth only ever seeds compatible rules; it did the opposite. B4 was therefore held OPEN at that point; the founder's trait rulings then resolved it via the unified host projection + the ladder repair — **see the CLOSURE line above and the B4-TRAIT row (CLOSED at PR 3.5)**.

*(The literal-ZERO route exists only under the LEGACY production map — no `tension` state, no param-domain concept — but that grades the pre-activation live surface, a different question than the PR-4 candidate gate B4 is about; citing it here would be dishonest.)*

## Discrepancy resolved — `needsBinding` "still 6" (**pre-PR-3 checkout, no real gap**)

The founder's re-scan showing `needsBinding = 6` is a **pre-PR-3 checkout**, not a PR-3 gap. Verified (incl. a worktree at the base):

- **At the pre-PR-3 base `0b400974`:** 10 bare-domain-on-multi-param cells across **6 distinct rules** — `alloc-sector-cap`, `alloc-sector-minimum`, `gs-02`, `mb-11`, `th-05`, `tv-12` (the count the founder saw is these 6 rules).
- **At PR-3 HEAD:** **0** — the A11 binding restoration param-keyed all 10 coordinates (the remaining 15 bare-domain cells are all on single-param rules, which bind cleanly and never go ambiguous). Confirmed 1:1: the 10 base coordinates are exactly the 10 HEAD now binds.
- The migration planner maps a bare domain on a multi-param rule → `ambiguous_domain_binding` → `needsBinding`; a param-keyed domain does not. So the class is genuinely closed at HEAD.
- **Consequence (already a hard gate — FINAL-DRYRUN):** those 6 rules now resolve to `param_out_of_domain` **clamp** entries wherever a hosting agent's persisted value is out of the now-bound domain — so D1's ratified 15-entry count is stale and `--apply` must re-ratify a fresh dry-run at the candidate SHA.

---

# PR 4 — DELIVERY RECORD (branch `claude/composition-pr4-identity-event`, Aug 7 2026)

**Founder rulings of record (Aug 7 2026 — the answers to the PR-4 discovery STOP; verbatim in the session log):**

- **Q1 — `boundaryStateVersion` (V0.9 §3 authorial intent, now written):** the integer version of the per-boundary enforcement-state SET. 1 at first activation; ANY change to the active-boundary set or its semantics mints a NEW `activationGeneration` (the descriptor never mutates in place; B1-EXT no-tuple-reuse applies); rollback carries the PRIOR value (it travels with its descriptor); A34's check = each boundary's compile-time `SUPPORTED_BOUNDARY_STATE_VERSIONS` declaration vs the record's value, unsupported ⇒ that boundary rejects the request (`compositionWriteEpoch.js`).
- **Q2 — A24 (text of record; A24 IS B10's test):** after the candidate defaults object lands, new births are byte-identical to pre-PR births (the object unreachable from every birth path — the traitLibrary chokepoint); births change ONLY once the activation record selects the candidate; proven both sides; existing agents untouched in both worlds (A12 restated at this boundary).
- **Q3 — substitution policy for non-offerable seeds:** supported + offerable + NATIVE for the host (candidate-registry-native where cells exist; the STORED map for the reserved diversifier column); criterion = the trait's stated identity function (RULE_FAMILIES a tie-breaker only); no native fit ⇒ DROP the seed and reshape with honest copy (WS1 precedent) — never seed hidden/deprecated, never stretch semantics; ladders satisfy invariant (b) wherever cells exist; exact proposals tabled, founder ratifies before they land.
- **Confirmations:** the descriptor is the **7-field union incl. `activeEpochId`** (the ledger governs where the item-2 list was incomplete); `identityVersionTarget` → `activeIdentityVersion` is a **clean rename** (B4 alignment clause — shipped loader, run docs, design note; nothing validates the old field, FINAL-DRYRUN re-stamps).
- **F2 — first-activation rollback (ruled Aug 7, post-review): GENESIS DESCRIPTOR, not fix-forward.** Before first activation, a runbook step writes generation 1 = the genesis descriptor `{activeIdentityVersion: LIVE, boundaryStateVersion: 1, candidateStateId: 'genesis', semanticHash: null-sentinel, activeEpochId: the open epoch, overrideRevision: 0}` — no overlay participation; the resolver under genesis reads base only (today's semantics made explicit). First real activation = generation 2. **Rollback is TOTAL:** atomic repoint to the prior descriptor at every generation, no special case, no tuple reuse. Delivered: `writeGenesisDescriptor` (epoch-paired, first-write-only, reserved ids), the loader's genesis base-only short-circuit + half-genesis-is-malformed pair check, rollback-to-genesis + birth-parity + base-only rows; runbook step 1 sequences genesis explicitly BEFORE the epoch close, paired with the epoch doc so B1's absent-doc-fails-closed arms coherently.
- **L1-1 — the client birth read (ruled Aug 7): NO client gate** — the flagless record read is the A48 design and stays. The merge-to-rules-deploy denied-read window closes OPERATIONALLY: the founder deploys the updated rules immediately after merge (runbook step −1), so the window is minutes; the read stays time-boxed (1.5 s, timeout resolves LIVE).

**Sol's pre-activation review (Aug 8) — all 12 findings folded (core mechanisms explicitly accepted; the findings concentrated in the runbook/activation state machine):**

- **#11 REPORT (genesis-present contract sweep — what was found):** ONE real inference existed: the compile chokepoint's `candidateMode` defaulted to the FLAG (`COMPOSITION_COMPILED_IDENTITY_ENABLED`), so post-rollback-to-genesis (flag standing, F5) endpoint compiles would have used candidate cells while the record selected live. **Fixed: the selection is RECORD-SCOPED** (`resolveCandidateModeInTx` — flag = dark switch only, zero reads dark; lit, candidate cells ONLY when the record's `activeIdentityVersion` = 3), threaded through all ten settings endpoints + the deploy gate; the candidate pipeline is scoped explicitly to `{candidateStateId, activeIdentityVersion: 3}` (runbook step 5); genesis-present + record-absent + v3-record endpoint rows added. Everything else checked CLEAN: the fence's `bothAbsent` tolerance (pre-genesis lit window only, inert post-genesis), the B1 absent-doc arm (record-presence-based, correct), `selectIdentityVersion`/client resolver (absent→live = fail-safe direction; genesis returns live identically).
- **#5 REPORT (closed-epoch --apply): the mode DID NOT EXIST** — `migration-scan --apply` required an open epoch at every iteration, contradicting the runbook's closed-epoch step 3. **Built:** `--during-close` explicit authorization → `assertClosedEpochCandidateWindow` (a DEDICATED inverse guard: epoch doc present + `'closed'`; open/closing/absent refuse `candidate_window_not_closed`; tested) + runtime path-assertion of every apply write into `compositionCandidateState/*`. PR 2's general guard untouched.
- **#3:** drain refuses expired-unreleased leases (`StuckProvisionerLeaseError`, holders named); explicit attributed resolution (`resolveStuckProvisionerLease`, dead-holder-only); purge narrowed to released-only. **#2:** THE ROLLBACK PROTOCOL (runbook section) + the interleaving row (pre-rollback pin rejects at the epoch belt while closed AND at the descriptor belt after reopen) + the battle rule proven on the locked-manifest branch. **#10:** the scan detects destructured/extracted write methods (`extract:*` sites, deny-by-default; typeof feature-detects excluded as unwritable; repo pinned at zero occurrences). **#4/#6/#7/#8/#9:** runbook re-sequenced as stated (pause+ack before closing; 8A/8B split with named probe identities; post-watermark battle-drain hard gate; deploy→preflight→SHA-pin→old-invocation drain; pre-genesis deployed-lambda snapshot smoke). **#1:** the rollback guarantee restated (TOTAL through 8A; 8B probe-enumerable + named hand reconciliation; selector-total after unfreeze) + the honest-divergence regression row (a v3-born agent's base docs persist across rollback — recorded, not asserted equal).
- **#12 FILED (post-event prerequisite):** any ARBITRARY-generation rollback claim requires **immutable per-revision override snapshots / a frozen final epoch revision** (without them, a restored intermediate generation's override layer is not byte-identical to that generation as-lived). Until built, rollback-to-genesis (2 → 1) is the only cross-generation restoration claimed, for THIS event only.

**Sol's second re-review (Aug 8) — 9 findings accepted, all folded (10 of the prior 12 confirmed fully closed; residual = rollback execution + temporary write windows):**

- **#1 CLIENT-SDK EPOCH BINDING — verified ABSENT, built.** The rules' old `epochWriteOpen()` checked only `state == 'open'`; no token existed. Now: `captureWriteEpochToken()` (compositionIdentityClient) reads `composition/writeEpoch.epochId` WHEN THE MUTATION IS FORMED (time-boxed; read failure → tokenless → post-genesis DENIED, fail closed); all **eleven** client-SDK identity-write sites (forgeService ×8, agentService.createAgent, useTraits param save, useForge rename) stamp `writeEpochId` via `withEpochToken`; the renamed `epochWriteAdmitted()` requires token EQUALITY on a present doc (absent doc = pre-genesis fail-open, byte-identical). Emulator rows: the E0-formed mutation submitted after E1 opens is DENIED; tokenless denied; matching token admitted. This is B1's "every identity write" closed on the one path no server pin can see.
- **#4 PROBE-ONLY GATE — mechanically enforced.** A new epoch state **`'probe'`** with `probeIdentities: [...]`: the server chokepoint (`validateWriteEpochInTx`, the provisioner-lease acquisition) admits ONLY a threaded, listed actor (`actor` threaded through all 11 settings endpoints, decide's projection commit, the deploy gate, casualClone) — everything unthreaded/unlisted rejects `probe_only` fail-closed; the rules layer requires `request.auth.uid ∈ probeIdentities` + the current token. Negative controls: server (probe + non-probe identity ⇒ 409, zero writes) and emulator (unlisted uid denied; listed + stale token denied). Reused verbatim by Rollback-B.
- **#5 MALFORMED FAILS CLOSED at the compile boundary.** `resolveCandidateModeInTx`'s four-state contract aligned with the loader: no record → LIVE; valid genesis/v2 → LIVE; valid v3 → CANDIDATE; **present-but-malformed or unrecognized version → REJECT** (the loader's per-field throw propagates; a valid record naming an uncompilable version throws too — never a silent cell-source guess). Endpoint mutation rows: malformed record ⇒ non-200 + zero writes; v5 record ⇒ same.
- **#6 BIRTH PROVENANCE — stamped, not narrowed.** Every fresh born-with seed stamps `identityVersionAtBirth` + `activationGenerationAtBirth`: server paths from their pinned descriptor (`birthProvenanceStamp` — dark ⇒ zero new keys, A23), the client birth from the record read (absent ⇒ no keys), clone paths INHERIT the source's stamps (`INHERITED_LOADOUT_FIELDS`; a reseeded training clone restamps at its own pin). The rollback reconciliation QUERIES these fields (`activationGenerationAtBirth >= <rolled-from generation>`), never trait-id inference. The honest-divergence row stays.
- **#8 NAMESPACE-BELT MUTATION ROW — built.** The apply writer extracted to `compositionCandidateApply.js` (entries-first/sentinel-last preserved); its mutation row redirects the write set to `agents/*` and proves the abort lands BEFORE any Firestore write (zero writes; cited next to #5 in the runbook step 3).
- **Runbook (#2/#3/#7/#9):** the rollback protocol's numbered order now pauses + positively acknowledges ALL external admin BEFORE `state:'closing'`; verification splits **Rollback-A** (target epoch closed, read-only loader/descriptor/seed-resolution checks) / **Rollback-B** (the probe gate, provenance-queried reconciliation, real birth/compile probes) mirroring 8A/8B on the same mechanism; step 1's VERIFY replaces the post-close probe birth with the NON-WRITING seed-plan resolution check; the run log is strict-ordered (−1 → … → 8A → 8B → 9) with R1–R8 rollback rows appended on invocation.

**Sol's confirmation pass (Aug 8) — two blockers (defects in the prior round's fixes) + one major + one note + one minor, all folded. Everything else CLEARED (probe state survived attack; malformed fail-closed accepted; --during-close closed):**

- **BL1 — THE WRITE-FENCE INCARNATION (the ABA's third appearance, inside our own client token):** `epochId` cannot carry both "which epoch" and "which incarnation" — a rollback reopening the restored epoch id would have re-admitted tokens/pins formed under its earlier life. **`fenceGeneration`** added to `composition/writeEpoch`: increments every time a quiesced/closed world becomes writable again (initial open = 1; close→reopen same epoch = 2; a rollback reopen = next; probe→open RETAINS — no intervening close/watermark), computed MECHANICALLY by `transitionWriteEpoch` (every runbook epoch write goes through it). Clients pin the TUPLE (`writeEpochId` + `writeFenceGeneration`, captured at formation; the rules require both equal); server transactions pin the tuple across retries. **Both regression rows exactly as specified:** (a) emulator — a token at E0/inc1 is DENIED after a rollback reopens E0 as a later incarnation, same epoch id; (b) server — a tx pinned E0/inc1 whose retry observes E0/inc2 rejects `epoch_changed_across_retry`. Resolver semantics, genesis, and historical descriptors untouched.
- **BL2 — BIRTH PROVENANCE MEANS BIRTH (fields split):** a NEW clone object stamps its OWN creation descriptor (`identityVersionAtBirth`/`activationGenerationAtBirth` = the current record); lineage moved to **`loadoutSourceIdentityVersion`/`loadoutSourceActivationGeneration`** (from the source's birth stamp; refreshed on re-sync); a RE-SYNC of an existing clone preserves its original birth stamp (the birth fields left the inherited/re-sync copy set — only an actual new birth stamps fresh). **IMMUTABILITY (the trust point, taken):** the Firestore create rule validates the client stamp against the LIVE activation descriptor (`birthProvenanceValid()` — forged v2/gen1 under a v3 record DENIED; stampless post-genesis DENIED; pre-genesis the fields must be absent), and updates cannot alter it (the agents update allowlist never admitted the fields — emulator-proven). The v3-clone-of-v2-source row stamps generation 2 with v2/gen-1 lineage and IS found by the reconciliation query.
- **MAJOR (taken):** the runbook names the provenance-queried reconciliation as **the ONLY intentional admin-write exception while Rollback-B is active**, with its TARGET COUNT recorded in the log before it runs — any other admin write in the window is a defect.
- **NOTE (taken, fail-closed unweakened):** the compile boundary's malformed/unrecognized-descriptor rejection is operationally LOUD and distinguishable — its own error class/code (`compile_boundary_descriptor_rejected`) + a dedicated `COMPILE BOUNDARY FAIL-CLOSED` log line (asserted in the endpoint row), never blended into validation-409 noise.
- **MINOR #9:** the stale duplicate run-log block deleted — the artifact now matches the strict-order claim.

**B4 tuple, ALIGNED (supersedes the tuple text in the B4 row above per its own alignment clause):** `{activeIdentityVersion, boundaryStateVersion, activeEpochId, candidateStateId, semanticHash, activationGeneration, overrideRevision}` — `compositionProductionLoader.ACTIVATION_DESCRIPTOR_FIELDS` is the list of record.

**SCOPE FINDING (cargo item 6):** the RED-control predicate proves the live DEFAULT_TRAITS seed **SEVEN** non-offerable rules — item 6's five PLUS `momentum_chaser/t-09` and `contrarian/tv-07` (hidden_absent_substrate). Both ride the same policy under the 3.5 "premise disproven" precedent; **all seven substitutions await founder ratification at merge** (the proposal table: `traitLibraryCandidate.js` header + the STOP report).

## Per-row delivery (reading rule: each row CLOSES when its acceptance test exists, fails under the defect — mutation-checked at the §2 review — and is green at the merged HEAD)

| Row | Delivered by | Acceptance artifact |
|---|---|---|
| **B1** | `validateWriteEpochInTx` epochPin + absent-doc-fails-closed-post-activation | `compositionActivationService.test.js` B1 rows |
| **B2** | `compositionProvisionerLease.js` + casualClone/trainingClone wiring | `compositionProvisionerLease.test.js` (9 rows) |
| **B4** | `compositionActivationService.js` (7-field writer, monotonicity, atomic-tuple rollback via history) | service suite B4 rows (A48 lands there) |
| **M6** | in-tx candidate verification (count/hash/id/extras) | service suite M6 rows — one per defect class |
| **B9** | RULES_DEPLOY_RECORD.json (unfilled) + check-rules-deploy-gate.js + COMPOSITION_RULES_TEXT_PATH | `compositionRunbookGates.test.js` |
| **M10** | runbook per-boundary evidence checklist | ACTIVATION_RUNBOOK.md §M10 |
| **B1-EXT** | monotonicity + overrideRevision in the seqlock token + `bumpOverrideRevisionInTx` + the derived-write census | loader contract suite + service suite + `compositionDerivedWrites.census.test.js` |
| **FC-1-CLOSE** | pin → stamp → commit-revalidate in `agentBattleService.js` via `compositionGenerationFence.js`; manifest/slice stamp pair; reader tolerance + fail-closed mismatch | `compositionGenerationFence.test.js` |
| **decide.js ruling (REVERSED)** | the §7-signed projection splice, both directions | same suite, write + reader rows |
| **B3-EXT** | one-level helper-param data-flow in the scanner (per-callsite counts) | `compositionProtectedStores.scan.test.js` B3-EXT rows |
| **B8-FINAL** | `preflight-at-sha.js` (SHA-pinned harness) + `EXTERNAL_ADMIN_WRITE_PATHS.md` | gates suite + runbook step 0 (the census re-run itself is runbook-time) |
| **A7-LOCK** | `ACTIVATION_EVIDENCE.json` freeze + corpus-wide compile-vs-kernel differential | `composition.a7lock.test.js` (mutation-checked in-session) |
| **M7-E2E** | per-assembler worst-case full-request fixtures (eval 8,576 / draft 3,526 est.) | `composition.m7e2eBudget.test.js` |
| **FINAL-DRYRUN** | the `--apply` ratification gate is a RUNBOOK step (step 2) — **not closeable at merge by design** | ACTIVATION_RUNBOOK.md step 2 |
| **BARE-CELL-INVARIANT** | positive-form invariant + synthetic controls | `archetypeCompatibilityCandidate.test.js` |
| **Cargo item 6 (+scope finding)** | `traitLibraryCandidate.js` (candidate object; 7 substitutions ACROSS 5 hosts, ratification pending) + A24 wiring (registry catalog, server seeding, client resolver, rules read clause) | `traitLibraryCandidate.composition.test.js` + `compositionBirthSwitch.test.js` |
| **D14/D15 (flip obligations)** | ON-state suite promoted (record-gated framing); endpoint fakes reconciled (activation doc, persistent subcollections, tx.get(collection), delegating tx.set); F7 candidate round-trip rows | `compositionAdvisoryRender.activation.test.js` + `composition.endpoints.test.js` F7 rows |
| **Runbook (§8/§10)** | steps 0–9 + §10 positive/negative checks + ACTION_COPY + M7 live + M10 checklist + run log | `docs/composition/ACTIVATION_RUNBOOK.md` |

**Still open at merge, BY DESIGN (runbook-time):** FINAL-DRYRUN ratification; the B8-FINAL census re-run at the deployed SHA; the B9 record fill; every §10 observation; the X6 base-metadata arc (separately sequenced — no gate-green claimed).
