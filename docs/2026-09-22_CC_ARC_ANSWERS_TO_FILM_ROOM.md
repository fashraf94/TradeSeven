# Command Center arc → Film Room chat: answers for the Build A spec (HEAD pass)

**Date:** 2026-09-22 · **From:** the Command Center arc (arc draft by Fable; HEAD pass by Claude Code, read-only) · **Answers:** `FILM_ROOM_TO_CC_ARC_SPEC_INPUT_REQUEST_20260922.md`
**HEAD:** `463a375fc4192c51dfa1cce669ff8a5a13305c6a` — `origin/main` after `git fetch origin` (recorded per BUILD_RULES §3), the merge of PR #886, 2026-09-21. **Every `file:line` in this document is at that SHA and was read in this session** (VERIFIED) unless marked *production* (a live Firestore document or census count, not re-verifiable from the tree) or *draft citation* (a document the draft names that is not in the tree). Branch `claude/cc-arc-answers-film-room-tz3wch`, cut from `origin/main` at HEAD, clean tree; no source file changed; no test run; no PR.
**What this document is:** the arc draft `CC_ARC_ANSWERS_TO_FILM_ROOM_DRAFT_20260922.md` with every "verify at HEAD" / "HEAD pass" marker replaced by `file:line`, every claim marked, and the four open items filled (§F). The A–E structure, the confirmed / partial / not covered / not ours labels and every **ruling** are the draft's and are unchanged. Where HEAD contradicts a ruling's premise, it is said under the ruling and the ruling is left for the arc.
**Legend — draft labels:** **confirmed** · **partial** · **not covered** · **not ours** (routed) · **ruling** (arc disposition, approve-by-default for Flash). **HEAD-pass markers:** **VERIFIED** — read at HEAD, as the draft said · **CORRECTED** — HEAD says otherwise, the truth follows · **NOT FOUND** — the thing does not exist at HEAD.
**Label collision, stated once:** the Film Room handback's C-1…C-8 (armed state, eligible alternatives, risk numbers, post-sale prices, day close, structured condition, review grounding, retire writers) and the capture spec's C-1…C-10 (completeness, score, privacy, retention, manifest, alternatives, armed state, exits, timing, identity) are two numberings. This document writes **handback C-n** and **spec C-n** wherever both could be meant.

---

## 0. Executive verdict — what HEAD changed in the draft

| # | Draft said | HEAD says | Marker |
|---|---|---|---|
| 1 | Tick capture is merged **dark**, flip pending (A1, E3). | **Lit.** `TICK_CAPTURE_ENABLED = true` (`src/config/featureFlags.js:2692`), flipped by `ed16c3f3` on Sep 21 (merged in PR #884, `3753af78`). Every admitted tick since then writes `agentBattles/{id}/ticks/{tickId}` and `…/tickBodies/{tickId}`. | **CORRECTED** |
| 2 | B2 (the shared directive transaction) merged; every directive response attests `persisted`/`charged`; a read-side bench cap of five since B2 (B1, B3, E3). | **Not merged.** `origin/claude/b2-directive-transaction` is 13 commits ahead of `main`, tip `5ce73eb9` (Sep 13), base `c35f9a59` (Sep 12). `api/_utils/directiveTransaction.js` does not exist at HEAD; `chat.js` files a directive with a plain `battleRef.update` (`api/agent/chat.js:1071-1083`); nothing on the wire says `persisted` or `charged`; `selectFlagged` is uncapped (`src/screens/battleView/selectBench.js:157-170`). The forensics fields **are** at HEAD (they came with the fit-check build, not B2). | **CORRECTED** |
| 3 | `DIRECTIVE_FIT_CHECK_ENABLED` flip PR issued Sep 18; state unknown (B2). | Still **`false`** (`featureFlags.js:887`). The flip was **halted** on Sep 18: four red rows in files the flip map never named plus one real prompt-coherence defect on the unknown-archetype path; the flag was left unchanged and the halt report merged (`docs/audits/20260918_FLIP_DIRECTIVE_FIT_CHECK.md:1-20`). Still registered dark-by-design (`src/config/flagPinGuard.test.js:172`). | **VERIFIED (false) · halt noted** |
| 4 | `ANTICIPATION_THRESHOLD_LINT_MODE` `'shadow'` flip prompt was ready; state unknown (B3). | **`'off'`** (`featureFlags.js:2563`). No flip commit exists on any origin branch. At `'on'` there is **no durable drop receipt**; the only receipts are a console line and the GCS shadow record (§B3). | **VERIFIED (off) · receipt NOT FOUND** |
| 5 | Evaluation entries "now carry" the eight intraday pointer fields (E3). | They carry them **only under `INTRADAY_DIAGNOSTIC_ENABLED`**, which is `false` (`featureFlags.js:2605`); the composer is inside that branch (`api/cron/agent-evaluate.js:3696-3738`). No entry at HEAD carries them. | **CORRECTED** |
| 6 | A non-triggered tick writes nothing; capture "may" record it (A3). | It writes no evaluation entry (`agent-evaluate.js:2295-2315`) — but with capture lit it writes a tick record with `exitReason: 'no_trigger'`, `stageReached: 'trigger_evaluated'`, `model.outcome: 'not_attempted'`, the risk verdicts and the scores (`:2286-2313`, `:1566-1577`, `:1037-1045`). "Not evaluated" is recordable now. | **CORRECTED (upward)** |
| 7 | `budget_skipped`: entry or counter only, unknown (A3). | Entry **and** feed beat **and** `cronErrors` row **and** a tick record (`model.outcome: 'failed'`, `failureClass: 'budget_skipped'`). No tick stamps. §A3. | **filled** |
| 8 | Deferred battle: nothing written (A3). | Holds. Pre-admission, no `tickSeq`, no document (`agent-evaluate.js:390-396`; `api/_utils/tickCapture/captureConfig.js:69-73`). The deferred-beat PR exists on no branch. | **VERIFIED** |
| 9 | C-5 (per-day close snapshot, persisted result): unknown (A1, A4). | **Not covered.** No `scoreState.dayClose[]`, no `scoreState.result` anywhere in `api/` or `src/`. `agent-daily-scores` writes badge points per day, never `currentScore`; completion derives `result` and writes it to agent stats and a feed sentence, never to the battle document. §A4. | **NOT FOUND** |
| 10 | C-1 armed state, C-3 risk numbers: unknown (A1). | C-1 **partial** (per-symbol verdict `{action, reason}` + guardrail facts per tick; no per-mechanism four-state model). C-3 **not covered** on any record (the permanent record admits ids only; the numbers survive as English in a capped feed message and, partly, in the entry's evidence stamp). §F1. | **filled** |
| 11 | `useAgentBattle.js:49` subscribes to the whole document (D5). | The subscription is `:26-28`; `:32` spreads the whole document; `:49` is the `statusFeed` derivation the audit cited. Same fact, wrong line. | **CORRECTED (line)** |
| 12 | `agent-batch-review` reads are governed by the intraday allowlists (E1). | It reads entries wholesale (`api/cron/agent-batch-review.js:189`); it is diagnostic-free **by construction** (diagnostics live in the `intradayViews` subcollection), not by an allowlist. The §9.2 allowlists are on the four prompt-feeding readers. | **CORRECTED (mechanism)** |

Everything else in the draft is VERIFIED at HEAD, with lines, below.

### 0.1 Flag states at HEAD (`src/config/featureFlags.js`)

| Flag | Value | Line | Note |
|---|---|---|---|
| `DIRECTIVE_FIT_CHECK_ENABLED` | `false` | `:887` | Flip halted Sep 18 (§B2). Dark-by-design entry `flagPinGuard.test.js:172`. |
| `ANTICIPATION_THRESHOLD_LINT_MODE` | `'off'` | `:2563` | Modes `['off','shadow','on']` `:2566`. Pinned directly (string). |
| `TICK_CAPTURE_ENABLED` | **`true`** | `:2692` | Flipped `ed16c3f3`, Sep 21. `false` is now the rollback (`:2684-2686`). |
| `INTRADAY_COLLECT_ENABLED` | `true` | `:2587` | Flipped `39e5c48a`, Sep 21 (`:2581-2584`). |
| `INTRADAY_DIAGNOSTIC_ENABLED` | `false` | `:2605` | Gates the `intradayViews` write and the eight pointer fields. |
| `INTRADAY_AGENT_USE_ENABLED` / `INTRADAY_PRICE_SOURCE` / `INTRADAY_RISK_ACTIVATION_ENABLED` | `false` / `'legacy'` / `false` | `:2606-2649` | Stages 2–4, dark by design (`flagPinGuard.test.js:79`, `:81`). |
| `VOICE_GROUNDING_MODE` | `'shadow'` | `:2271` | Walk order `['off','shadow','canary','on']` `:2274`. |
| `SHOW_IT_ENABLED` | `false` | `:2420` | Dark-by-design `flagPinGuard.test.js:123`. |
| `SHADOW_ASSEMBLY_ENABLED` | `false` | `:1482` | Dark-by-design `flagPinGuard.test.js:176`. |

`vercel.json` carries **41** cron entries (counted from the `crons` array; BUILD_RULES §6 says 41/100).

---

## A. The record the tape reads

**A1 — What the merged capture build covers. filled — see §F1 for the per-item table.**
The tick-capture build is merged to `main` and **lit**, not dark: Stage A (`dde8f682`) and Stage C (`d26ee1c6`) merged in PR #882 (`03c82131`, Sep 21, branch `claude/funny-heisenberg-7aaptc`); the flip `ed16c3f3` merged in PR #884 (`3753af78`, Sep 21). **CORRECTED:** the draft's "dark behind `TICK_CAPTURE_ENABLED`, flip pending" — the flag reads `true` at `src/config/featureFlags.js:2692`, and its pin asserts it (`src/config/tickCaptureFlags.test.js:31-35`). It was built in the Jev chat from `docs/CAPTURE_BUILD_SCOPE_AMENDMENT_A_V1_1_SEP19_2026.md` and spec V1.3 — `docs/specs/CAPTURE_BUILD_SPEC_V1_3.md` (90 lines) — with **V1.4 present** at `docs/specs/CAPTURE_BUILD_SPEC_V1_4.md` (V1.3 verbatim plus the five founder-approved amendments at `:102-108`: `failed` as the fifth check status; overhead measured by telemetry; received bytes hashed; the allowlist by path and shape; request-local means async-chain-local). Its Phase 0 is `docs/audits/20260920_PHASE0_TICK_CAPTURE.md`; its build report is `docs/audits/20260921_BUILD_TICK_CAPTURE.md` (517 lines). The spec's own scope words are as the draft listed them (spec V1.4 `:29-47`). What the build records for the **handback's** C-1, C-2, C-3 and C-5 is answered item by item in §F1: **C-1 partial, C-2 partial (thin), C-3 not covered, C-5 not covered.** The spec itself moved two of them out by name: "Unified armed state and exit levels → Exit-dials arc, with G-1" and "Full alternatives enumeration → Tier 2, with J2" (spec V1.4 `:91-98`). **Owner note** stands: the build report and V1.4 are the Jev chat's; both are on `main` now, so the Film Room chat can be sent the paths above.

**A2 — Shape and retention. filled — see §F2.** The 120-day TTL is real and is on **`tickBodies` only**: `expireAt = capturedAt + 120 days` (`api/_utils/tickCapture/captureConfig.js:63`; `api/_utils/tickCapture/captureWriter.js:170`, `:181`), declared as a TTL field in `firestore.indexes.json:612-630` (`"ttl": true`), which commit `d1a8babe` (PR #883, `44c8fd82`, Sep 21) transcribed **from the live production config** — so the production TTL policy is enabled, per that commit's own message. The permanent `ticks` document has no TTL. Paths, keys, the `status` vocabularies (including `failed`), and the per-field homes are in §F2. **Ruling** (unchanged): the Film Room derives and stores its own per-day tape record before bodies expire; §F2 confirms that **nothing the tape named lives only in a TTL'd body** — the two exceptions are the exact request/response bytes and the pre-lint candidate list, neither of which the tape needs. The finding for the capture build's owner, from §F2: the tape's per-check facts on the battle document (`heard`, `evidence`, `candidates`, `scores`) sit in `evaluations[]`, which is capped at 150 entries (`api/cron/agent-evaluate.js:3938`) — the permanent tick record is the uncapped home for the scores and the directive facts, but **not** for `candidates` (no `candidates` path exists in the permanent allowlist, `api/_utils/tickCapture/captureSerializer.js:63-185`).

**A3 — Ticks that build no prompt, and skipped or deferred battles. partial → filled.**
- **Deferred battles: not covered, VERIFIED.** The handler loop breaks when elapsed time exceeds `TIME_BUDGET_MS` (290 s, `api/cron/agent-evaluate.js:170`) with a console line and `summary.skipped += remaining` (`:390-396`); `processAgentBattle` is never called for the tail, so no lock, no `tickSeq`, no entry, no beat, no document. The capture build states the same by design: lease refusal and the scheduler's budget deferral are pre-admission and "deliberately absent" from the exit map (`api/_utils/tickCapture/captureConfig.js:69-73`; spec V1.4 `:43`). The scaling Phase 0 is on `main` (`docs/audits/20260918_PHASE0_EVAL_CRON_SCALING.md:16`, `:84`), and its R2 still holds: `summary.skipped` conflates lock-skips (`agent-evaluate.js:691-694`) with deferrals (`:394`), and the response is not persisted. The **instrumentation-and-deferred-beat PR is on no origin branch** (§F4) — **not built**, as the draft said.
- **Budget-skipped wakes: filled.** `shouldStartHaikuCall` (`api/_utils/agentEvalTransport.js:167-176`) is consulted once, before the build (`agent-evaluate.js:2371`). When it says no, the tick sets `haikuFailure.failureClass = 'budget_skipped'` and `fallbackHold = true` (`:2386-2393`) and continues down the **normal write path**. It writes: an **evaluation entry** (`:3618`) with the placeholder rationale "Evaluation skipped — cron budget too low to start Haiku call. Defaulting to HOLD." (`:3627-3630`), `haikuError` (`:3657`) and `holdKind: 'default_failure'` (`:3678`); an **`eval_degraded` status-feed beat** (`:3872-3893`); a **`cronState.cronErrors` row** (`:3998-4006`); `evalSeq` advances (`:3986`); `consecutiveEvalFailures` passes through unchanged (`:3978-3981`); `totalHaikuCalls` does not increment (`:3964`) and `lastEvalStartedAt` is not refreshed (`:3969`). With capture lit it also writes a **tick record** with `model.outcome: 'failed'`, `model.failureClass: 'budget_skipped'` (`:2395-2397`), `exitReason: 'completed'`, and `body.status: 'skipped'` (nothing was dispatched, `captureWriter.js:400-401`). It carries **no tick stamps** — the composer is gated on `promptBuilt` (`:3762`; `api/_utils/tickStamps.js:314-320`), so `heard`, `evidence`, `vintages` and `candidates` are absent, and the client treats it as such (`src/screens/battleView/deriveHeard.js:32-35`). So `budget_skipped` is **counted, recorded, and visible** — it is not a counter-only outcome.
- **Non-triggered ticks: CORRECTED (upward).** When the trigger gate does not fire, the tick returns early after one `battleRef.update(scoreUpdate)` (`agent-evaluate.js:2295-2315`, the update at `:2308`): the scores (`:1026-1030`), `cronState.triggerGatePassCount` (`:2302`), the cron memory via `finalizeCronState` (`:2303`) and any status-feed entries already accumulated (`:2304-2307`). **No evaluation entry, no `evalSeq` advance.** The draft's inference from the fair-rotation key was right about the entry. But with capture lit the tick **is** recorded: `tickCapture.exit('no_trigger')` (`:2312`) after `stage('trigger_evaluated')` (`:2287`), on a record that already holds the risk verdicts (`:1566-1577`), the scores and the universe (`:1037-1046`), `model.outcome: 'not_attempted'` (the context default, `api/_utils/tickCapture/captureContext.js:144`) and every check at `not_evaluated` (`captureWriter.js:139-145`). "No check woke" has a durable, queryable record per tick — by presence of `exitReason === 'no_trigger'` in `agentBattles/{id}/ticks`.
- **Ruling — what the tape renders.** Unchanged, and the premise now holds better than the draft assumed: *no check woke* is readable from the tick record (`exitReason: 'no_trigger'`), *check skipped · budget* from the entry (`haikuError.failureClass === 'budget_skipped'`) or the tick record (`model.failureClass`), and *check deferred · budget* still has no record until the deferred beat lands. "No record for this check" remains the render for a minted-but-missing tick — the export measures those as gaps against `cronState.tickSeq` (`api/_utils/tickCapture/captureCoverage.js:12`, `:48`).

**A4 — The score source. partial → ruling; premise VERIFIED, C-5 NOT FOUND.**
- Same-day close is **not persisted** on the battle document: VERIFIED at HEAD. The per-tick score fields are `scoreState.activeScore / bankedScore / currentScore / opponentScore / lastScoredAt` (`agent-evaluate.js:1026-1030`), `peakScore / peakScoreAt` (`:1051-1052`), `evaluationCount / holdCount` (`:3951-3954`), and `tradeCount` in the fenced executor (`api/_utils/agentSwapExecution.js:363`, read-only). The fenced initial shape (`api/_utils/agentBattleService.js:274-286`, read-only) has exactly those plus `dailyScores` and `bankedBadgePoints`. **`scoreState.dayClose[]` and `scoreState.result`: NOT FOUND** — no writer or reader in `api/` or `src/`. (The draft's "Forge Record Phase 0 (Sep 12, `main` @ `27f42c07`)" is a *draft citation*; no document with that name is in `docs/` at HEAD.) DCR-001 is real and unchanged: completion reads the **cached** `fresh.scoreState.currentScore` inside its transaction (`agent-evaluate.js:5727-5729`; `docs/audits/2026-09-20_DATA_CONSISTENCY_CONCURRENCY_RETRY_AUDIT.md:92-102`). The one-tick lag after a forced exit is the eval-fix T1 item (`docs/audits/20260919_BUILD_EVAL_FIX_INTEGRATION.md`, commit `9d06c0fd`). The Sep 18 production observation of an empty `dailyScores` map is *production* and consistent with the writer below.
- **What `agent-daily-scores` writes, and when.** Schedule `45 1 * * 2-6` UTC (`vercel.json:65-66`) = 21:45 ET during EDT (the header comment at `api/cron/agent-daily-scores.js:4` says 8:45 PM, an EST reading). Per **still-active** battle only (`findActiveAgentBattles`, `:23`, `:279`) it writes, in one update (`:171-194`): `scoreState.bankedBadgePoints.total` (increment) and `.breakdown.{dayKey} = { points, badges, recordedAt }` (`:176-181`); **`scoreState.dailyScores.{dayKey} = { badgePoints, recorded: true, recordedAt, recordedBy: 'cron' }`** (`:182-187`); the `thresholdHistory` reset (`:174`); the cleared `swapPrice`/`swappedInDay` on the portfolio (`:175`); `timing.currentTradingDay` and `timing.lastDailyResetAt` (`:188-189`). It fetches day-end prices first (`:248-266`) and re-scores **badge points** against them — it never reads or writes `currentScore`, `activeScore` or `bankedScore`. So `dailyScores[dayKey]` is a badge-points receipt, not a close snapshot, and a battle that completes before 01:45 UTC never gets one. Step order relative to valuation: after its own price fetch, after the evaluator's last tick of the day (`agent-evaluate` runs `*/15 13-21 * * 1-5` UTC, `vercel.json:157-158`), before nothing else — no final valuation exists to be after.
- **Completion.** `completeBattle` (`agent-evaluate.js:5692`) writes `status: 'completed'`, `completedAt`, `pendingReflection`, `reviewPending`, `cronState.evaluatingAt: null` and a `battle_complete` feed entry carrying `score` (`:5812-5847`; the entry at `:5840-5846`), plus `completionContext` for tournament battles (`:5850-5852`). `result` (win/loss/draw, derived at `:5637`) goes to **agent stats only** (`:5903`) — **it is never written on the battle document**; it survives as the feed sentence at `:5842`.
- **The `evaluationCount` cap defect, located:** `evaluations` is `slice(-150)` at `agent-evaluate.js:3938` and `scoreState.evaluationCount` is written as `evaluations.length` at `:3951`, so it saturates at 150. (`evalId` no longer does — §E3.) The same length-derivation exists at `api/agent/reflect.js:127` and `api/_utils/shadowAssemblyCapture.js:430`.
- **The closest per-day score series that exists:** the permanent tick record's `scores.{active, banked, total, opponent, bankedBadgePoints}` — the fields **exactly as this tick wrote them** (`captureWriter.js:314-320`; writer `agent-evaluate.js:1039-1045`), one per admitted tick, uncapped and un-expiring. A rider can reduce that to "last check of the day" but it is a last-tick value, not a post-valuation close.
- **Ruling.** Unchanged. The premise holds: C-5 is **not in the merged capture build**, so this arc builds it. HEAD adds one fact for the design: the rider should not hang off `agent-daily-scores`'s per-battle loop without noting that the loop is active-battles-only and runs after a Guard-2 price fetch (`agent-daily-scores.js:252-266`) — a completed single-day battle is invisible to it.

## B. The player's call

**B1 — `battle.directive` at HEAD. confirmed; field presence VERIFIED; the writer CORRECTED.**
- `battle.directive = { text, expiry, directiveThreadId, createdAt, adjustmentId?, canonicalTextVersion? }` — the one slot shape, `api/_utils/directiveFiling.js:50-61` (`buildDirectiveSlot`); the id and version ride it only when the gate minted them (`:56-59`). **`text` is the canonical text**: the gate commits `text: canonical` (`api/_utils/directiveGate.js:166-176`). Latest wins by construction — the whole object is set (`directiveFiling.js:46-48`). The draft's production field list `{ adjustmentId, canonicalTextVersion, createdAt, directiveThreadId, expiry, text }` matches. VERIFIED.
- **Writers, CORRECTED:** two, neither a B2 transaction. (1) The chat turn: `api/agent/chat.js:1071-1083` — a plain `battleRef.update` that appends the exchange (`chatExchanges: FieldValue.arrayUnion(exchange)`, `:1072`), charges the budget (`:1077`), and sets `directive: buildDirectiveSlot(...)` only when a thread id was minted (`:1080-1082`; the mint is `randomUUID()` at `:1009`, only when `lintedHasDirective && lintedDirective`). (2) The chip route `api/agent/file-directive.js` — the one **transaction** at HEAD (`:196`), writing the same slot and record (`:274-276`, `:283-284`) — but it returns 404 unless `getVoiceGroundingMode(uid) === 'on'` (`:172-174`), so it is **dark at `'shadow'`**. The shared-transaction refactor, the `persisted`/`charged` attestation and the response reading are all on the unmerged B2 branch (§F4).
- **The player's own words are `chatExchanges[i].userMessage`** (`chat.js:1028`, the sanitized message). The exchange fields at HEAD (`:1027-1067`): `userMessage`, `agentResponse`, `scratchpad`, `hasDirective`, `directive` (`buildDirectiveRecord` → `{ text, expiry, directiveThreadId, adjustmentId?, canonicalTextVersion? }`, `directiveFiling.js:29-43`), `directiveThreadId`, `suggestedActions`, `elicitationTarget`, `timestamp`, `mode`, `groupId` (tournament only, `:1052-1054`), **`archetypeGate`** (`:1056`), `researchLint` (`:1061`, only when withheld), `groundingVersion` (`:1066`, grounded turns only). **`archetypeGate = { classification, selectedAdjustmentId, status, repairUsed, originalUserAsk, counterOfferText, rejectionReason }`** (`directiveGate.js:251-290`, fields at `:257-260` and the forensics spread at `:284`), plus `fitCheck: { expected, quoted }` **only** on a `fit_mismatch` (`:285-287`). The three forensics fields are "ADDITIVE AND ALWAYS ON, deliberately NOT behind `DIRECTIVE_FIT_CHECK_ENABLED`" (`:261-262`), sanitized through the chat-text path (`:242-248`), and are taken from the **first** proposal, before any repair (`:281-283`). **Ruling** unchanged: the card shows `userMessage`, never `originalUserAsk`. VERIFIED that `originalUserAsk` is never read into `directive.text` (`:12-13`).
- **Heard stamps on evaluation entries:** `evaluations[i].heard = { directiveThreadId, suppressed }` — composed by `deriveHeardStamp` from the cron's own `resolveControls` on the in-memory battle (`api/_utils/tickStamps.js:138-151`; `suppressed ∈ null | 'malformed' | 'mode_not_enforce' | 'epoch_killed'`, `:108`), spread onto the entry at `agent-evaluate.js:3802-3821` under `TICK_STAMPS_ENABLED && promptBuilt` (`:3762`). `evaluations[i].directiveThreadId` and `ignoredDirectiveIds[]` are the **model's echo** (`:3636`, `:3634`) — the basis of Acted, never Heard (`tickStamps.js:13-21`). `controlEpochLog[] = { epochKey, modes, suppressedDirectiveIds, suppressedLeanIds, at }` (`api/_utils/controlSuppressionTelemetry.js:30`) — appended by `recordControlEpochIfNeeded` with `arrayUnion` (`:234`), called once per battle per mode-epoch from `agent-evaluate.js:1486-1503`. The status-feed `directiveThreadId` is on the proposal and swap/hold entries (`:3573`, `:3593`), again the model's echo. First heard = the first entry after `createdAt` whose `heard.directiveThreadId` matches — the client's rule is "last entry per thread wins", presence-gated, and a filing that lands mid-tick is stamped on the **next** decided entry (`src/screens/battleView/deriveHeard.js:37-42`). VERIFIED.
- **Also durable, and new since the draft:** the permanent tick record carries the **same resolution** as ids and hashes — `controls.directiveThreadId`, `directiveAdjustmentId`, `directiveCanonicalTextVersion`, `directiveTextHash`, `directiveSuppressed`, `controlEpoch` (`captureWriter.js:270-286`; composed at `agent-evaluate.js:4224-4249` from the one `controlResolution` the prompt was rendered from, `:3798-3800`) — so Heard for a check also exists **uncapped**, on `ticks/{tickId}`, as `controls.directiveThreadId` with `directiveSuppressed === null`. The rendered directive **text** goes to the TTL body only (`tickBodies.controlsAsRendered.directiveText`, `captureWriter.js:191`; `agent-evaluate.js:4252-4259`) — but the same canonical text is durable on `battle.directive.text` and on the exchange.
- **Ruling, restated from Sep 16:** unchanged — two things, may state the gap, may show the reply as a third, may not excuse the gap.

**B2 — The gate discovery and the fit check. confirmed for the facts; flag state VERIFIED `false`; the halt is the news.**
- The Sep 14 and Sep 18 incidents and the root cause are as the draft has them; the root-cause statement is written into the gate itself (`api/_utils/directiveGate.js:87-96`: membership was proved, fit never was). The Phase 0 record is on `main` (`docs/audits/20260915_PHASE0_DIRECTIVE_GATE.md`; the draft's `f2ce8881` is the tip of its branch `origin/claude/phase0-directive-gate`, one docs commit ahead). Incident details are *production*.
- **`DIRECTIVE_FIT_CHECK_ENABLED = false`** (`featureFlags.js:887`). **The flip did not happen.** `docs/audits/20260918_FLIP_DIRECTIVE_FIT_CHECK.md:1-20`: the flip session's prescribed stop condition fired — the suite reds **19 rows across 6 files**, the flip map's exact headline numbers, but four of the rows are in files the map never names, and one is a real prompt-coherence defect on the unknown-archetype path (its §5.2), a founder ruling (its §7). The precondition passed (`VOICE_GROUNDING_MODE = 'shadow'`). The branch `claude/flip-directive-fit-check` carries the report only and is merged as such. **Standing rule VERIFIED** in the flag's own docstring: never lit while grounding is `'canary'` or `'on'`, because the grounded prompt is not transformed and every typed-path filing would become `fit_mismatch` (`featureFlags.js:813-833`).
- **What a filing carries once the fit check is on — VERIFIED with one CORRECTION.** Under the flag, after membership passes, the reply must contain the canonical text verbatim (whitespace-normalized, **case-sensitive**: `directiveGate.js:124-129`, `:152`); otherwise the verdict is `{ directive: null, hasDirective: false, status: 'fit_mismatch', fitCheck: { expected: canonical, quoted: false } }` (`:152-161`). **Written:** the exchange with `hasDirective: false`, `directive: null`, `archetypeGate.status: 'fit_mismatch'`, `archetypeGate.fitCheck` (`:285-287`), and the always-on forensics. **Not written:** `battle.directive` — the slot write is conditional on a minted thread id (`chat.js:1080-1082`, `:1009`). **CORRECTED:** the draft's "`rejectionReason` set" — `rejectionReason` is the model's stated reason from its first proposal (`:242-248`), present or null on **any** outcome; the fit check does not set it. The field the fit check adds is `fitCheck`. On the wire a `fit_mismatch` reports exactly what every null-write turn reports: `directiveStatus: 'no_change'` + the no-change line (`:69-73`, `:108-110`). **Stated limit, at HEAD:** both client surfaces gate that line on the grounding marker, so at `'shadow'` the player sees the over-claiming reply with no card and no line (`:112-123`) — the Film Room card reading `archetypeGate.status` would be the first surface to show it.
- **The card's three states — VERIFIED, with a fourth to absorb.** *committed* (`status: 'committed'`, `:162-178`), *no change* (`classification ∈ core_conflict | user_lever | research_only` → `status: 'no_change'`, `:141-145`), *not filed* (`fit_mismatch`). A **fourth** status exists at HEAD: when the one-shot repair still yields no valid proposal, `status` is the repair reason — `'no_proposal'` or `'invalid_id'` (`:140`, `:179`, `:353`) — also a null write, reported on the wire as `no_change`. The card's "no change" state should read `status ∈ {no_change, no_proposal, invalid_id}` as one bucket. `classification` vocabulary: `in_archetype | flex | core_conflict | user_lever | research_only` (`:75`). The forensics block is present in all states.

**B3 — Anticipation entries. partial; fields VERIFIED; the cap and the receipt CORRECTED / NOT FOUND.**
- **Fields.** On `chatExchanges[i]` (the shipped path, `api/_utils/voiceLayerAnticipation.js:351-369`): `messageType: 'anticipation'` (`:361`), `anticipationSource: 'haiku'` (`:362`), `anticipationContext = { symbol, direction, threshold, evaluationId }` (`:363-368`), `elicitationTarget: 'anticipation'`, `userMessage: null`, written by a `chatExchanges` `arrayUnion` only — never the status feed (`:379-381`). (The grounded composer, live only at grounding `'on'`, writes `{ symbol, direction, evaluationId, slot }` with no `threshold`, `:117-139`.) On the evaluation entry, **`candidates[] = { symbol, direction, signalSummary, threshold, signalSource? }`** (`api/_utils/tickStamps.js:293-309`; `CANDIDATE_FIELDS` `:103-105`), spread with the other stamps at `agent-evaluate.js:3802-3805` from the **linted** array (the same reference while the lint is `'off'`). `threshold` is **prose** — `strOrNull(c.threshold)`. The `evaluationId` join is by `evalId` (`voiceLayerAnticipation.js:60-61`). VERIFIED.
- **"Read-side bench cap of five since B2": CORRECTED — NOT at HEAD.** `selectFlagged` returns every `potential_entry` candidate in roster order, uncapped (`src/screens/battleView/selectBench.js:157-170`). The cap (`FLAGGED_DISPLAY_CAP = 5`, a display rule) is B2 commit E on the unmerged branch (§F4).
- **`ANTICIPATION_THRESHOLD_LINT_MODE = 'off'`** (`featureFlags.js:2563`); no `'shadow'` flip exists on any branch. Semantics VERIFIED in the cron: `lintEnforcing` / `lintActive` (`agent-evaluate.js:2769-2770`); the present-signals map is built from the tick's own in-memory objects (`:2773-2779`); a failing candidate at `'shadow'` is logged and kept, at `'on'` it is **dropped from both sites** — the anticipation queue and the `candidates[]` stamp (`:2782-2830`). **The receipts at `'on'`: a console line** (`:2804-2807`) **and a GCS shadow record** — `logAnticipation({ errorStep: 'threshold_absent_signal', errorReason, absent, lintMode, dropped: lintEnforcing, timestamp, candidate })` (`:2808-2830`), which is `appendToStream('anticipation', …)` to the GCS bucket (`api/_utils/shadowLogger.js:154`, `:44-60`) — fire-and-forget, resolving `false` when GCS is disabled. **No durable drop receipt exists**: `dropReceipt` matches nothing in `api/` or `src/`; nothing is written to the battle document or to the tick record for a drop. The cron says so itself: "The 'on' flip PR owes a durable receipt on the battle doc itself; see the build report's §5 flip precondition" (`:2797-2803`). One partial, TTL'd trace does exist: the tick body's `originalToolResult` is copied **before** the lint runs (`:2547`, `captureWriter.js:187`), so the pre-lint `anticipationCandidates` survive in `tickBodies/{tickId}` for 120 days, unlabeled. **Ruling** unchanged — the tape shows a dropped plan only from a receipt, never reconstructs one; the premise is confirmed: **at `'on'` today there is no receipt to show from**, so the tape would show nothing for a dropped plan until the flip PR writes one.
- **C-6 structured condition: not covered, VERIFIED.** `threshold` is a string at every site above; the lint parses signal names out of prose (`api/_utils/anticipationThresholdLint.js`, named at `featureFlags.js:2531`). **Ruling** unchanged.

## C. Open dispositions from handback §8

**C1.** C-6 — above. C-8 (retire the `proposedRules` and `forgeSuggestions[]` writers) — **not ours**, Forge Record stream. At HEAD the `forgeSuggestions` writer is live in review mode: `agents/{agentId}.forgeSuggestions` via `arrayUnion` at `api/agent/chat.js:1093-1097` (beside `lessons`). Route unchanged.

**C2.** G-1 → exit-dials arc, **confirmed**; the "zero deployed guardrails ever" premise is the Sep 17 census recorded at `docs/audits/20260919_BUILD_EVAL_FIX_INTEGRATION.md:382` (*production*: zero agents with a deployed guardrail, zero guardrail exits across 468 battles / 402 trades). G-2 — **this arc's, not started**; what HEAD shows: a pending gameplan meeting makes the tick run the deterministic pass only and exit before the model call (`agent-evaluate.js:2133-2146`, capture exit `gameplan_pending`), so "a pending meeting blocks the day's evaluation" is the code's behavior; the "approval card unmounted" half is the Film Room Phase 0B finding on its **unmerged** branch (§F4). G-3 — **partial**: `cautiousRegister` is per archetype in `src/data/archetypeAdjustments.js:89`, `:116`, `:143`, `:175`, `:203` (the draft's path `archetypeAdjustments.js` is under `src/data/`, not `api/_utils/`); **`exitBounds`: NOT FOUND** in `api/` or `src/` at HEAD. Disposition unchanged. H-2 — Film Room's own.

**C3.** Held-position parity — **not started** (no branch, §F4); fenced (`api/_utils/agentEvalPromptAssembly.js`, BUILD_RULES §1). What bench names get today, VERIFIED in the fenced assembler (read-only): `buildBenchTechnicalBlock` (`:1535`) renders RSI (`:1610-1612`), MACD with cross state (`:1614-1620`), BB %B (`:1634-1642`), the ATR regime (`:1645`) and RVOL from the volume profile (`:1652-1658`) — the draft's four. Held names reach the decider as the ATR multiple, VWAP deviation, the regime word and the risk verdict (the evidence stamp mirrors exactly that, `tickStamps.js:22-34`, `:192-209`; "the prompt renders `rsPercentile` for BENCH names only", `:28-32`). **Ruling** unchanged.

**C4.** `VOICE_GROUNDING_MODE = 'shadow'` (`featureFlags.js:2271`); the 20-pair count is *production*. Review-mode grounding (C-7) — NOT FOUND on any branch; **not on any queue**. Unchanged.

## D. Facts the Film Room plans to cite

1. **Confirmed, and sharpen it — VERIFIED where the tree can.** The validator runs `*/30 10-16 * * 2-6` UTC (`vercel.json:165-166`), so its first attempt is **10:00 UTC = 6:00 AM EDT**; it stamps `firstPublishHourUtc` from `getUTCHours()` at the first publish (`api/_utils/intraday/validationRunner.js:136`) into `intradayValidation/{etDate}` (`:106`; `api/_utils/intraday/intradayStore.js:30`, `:38`). The value `10` for 2026-09-21 is *production*. It fetches the actionable set, held ∪ bench of active battles (`api/_utils/intraday/universe.js:34`). The founder's Sep 18 bars test and "5 units per symbol" are *production*/vendor facts. Coordination note stands.
2. **Wrong — VERIFIED at HEAD.** `intradaySnapshots/latest` is one document overwritten per sweep; `intradayCalcState/{etDate}/actionable/{sym}` holds the per-minute ring for the **actionable** tier only (`intradayStore.js:7-12`, `:24-38`); the universe tier (`UNIVERSE_STOCKS`, `universe.js:16`) is swept every `UNIVERSE_CADENCE_MIN = 5` minutes (`api/_utils/intradayConfig.js:11`) into the single latest snapshot. Build 1 does not replace handback C-4. Unchanged.
3. **Confirmed.** 41 entries in `vercel.json` (§0.1); BUILD_RULES §6 reads 41/100.
4. **Confirmed.** `trades[]` is `slice(-50)` in the fenced executor (`api/_utils/agentSwapExecution.js:354`, read-only; DCR-012 at `docs/audits/2026-09-20_DATA_CONSISTENCY_CONCURRENCY_RETRY_AUDIT.md:252-262`). The trade entry shape is `{ symbolOut, symbolIn, name, tier, slotIndex, entryPrice, exitPrice, lockedPoints, lockedGainPct, swappedOutAt, swapDay, isCrypto, direction, ...evaluationMetadata, snapshot }` (`:255-273`); a risk-manager exit's metadata adds `evaluationId: risk_{reason}_{symbol}_{ms}`, `exitReason`, `source` (`agent-evaluate.js:1748-1757`). **The id fields, named:** on the tick record, `tickId = ${battleId}:${tickSeq}` (`api/_utils/tickCapture/captureContext.js:130`) and `actions[].actionId = ${tickId}:${n}` (`:249`) with `{ kind, source, exitReason, symbolOut, symbolIn, swappedOutAt, lockedPoints, entryPrice, committed }` (`:246-261`; `source ∈ risk_manager | archetype | haiku | guardrail | gameplan_meeting`, `captureConfig.js:174-180`); the join to the trade entry is `(battleId, swappedOutAt, symbolOut, symbolIn)` (spec V1.4 `:47`). On the learning receipt, `learningReceipts/{battleId}/receipts/{agentId}_seq{receiptSeq}` (`api/_utils/learning/captureReceipt.js:406-411`, create-only `:420`) with `receiptSeq = tradeCount + 1`, `symbolIn`, `symbolOut`, `source`, `exitReason`, `timestamp` (`api/_utils/learning/learningSchemas.js:122-134`). Agree: source swaps from the tick record's `actions[]` (uncapped, permanent) or the receipts.
5. **Confirmed; line CORRECTED.** `src/hooks/useAgentBattle.js:26-28` — `doc(db, 'agentBattles', id)` + `onSnapshot`; `:32` spreads the whole document into state. `:49` is the `statusFeed` derivation (PLC-008's citation, `docs/audits/2026-09-20_FANTASYTRADES_PERFORMANCE_LATENCY_COST_AUDIT.md:526-534`). The intraday `intradayViews` subcollection is the precedent: one `getDoc` on panel open, never a subscription (`src/screens/battleView/useIntradayView.js:31`). Unchanged.
6. **Confirmed by documents;** the counts are *production*: 96 `vwap_failure` exits, all May 6 – Jun 12 (`docs/20260919_ASTRA_TRADING_BRAIN_JEV_DISCOVERY_ADJUDICATION_V1_1.md:48`); zero deployed guardrails (`20260919_BUILD_EVAL_FIX_INTEGRATION.md:382`). The two census scripts are on unmerged branches (§F4). Extension VERIFIED: `INTRADAY_RISK_ACTIVATION_ENABLED = false` and dark by design (`flagPinGuard.test.js:81`), so the VWAP floor (`api/_utils/agentRiskManager.js:138-147`, fenced) and the trail stop (`:164-171`) stay as they are — bust/ATR-ladder (`:127-134`), LOCK (`:149-161`), stagnation (`:185-192`) and model swaps are the live exits. Unchanged.

## E. Fence and sequencing

**E1.** `api/_utils/tickStamps.js` is non-fenced (absent from BUILD_RULES §1) — the Film Room reads. `api/cron/agent-evaluate.js` — the intraday hook (`:3686-3738`), the capture hooks (`:195` through `:4280`) and `evalSeq` (`:2731-2741`, `:3986`) all landed in one week; **no Film Room rider there.** `api/cron/agent-batch-review.js` — read-only for the Film Room; **CORRECTED (mechanism):** it reads the day's entries wholesale (`:189`, `battle.evaluations.filter(e => e.day === currentDay)`) and stays diagnostic-free **by construction** because diagnostics live in `intradayViews`, not on the entry (`INTRADAY_DATA_BUILD_1_CONTRACT_V1_1.md:208-209`); the explicit allowlists are on the four prompt-feeding readers (e.g. `voiceLayerAnticipation.js:60-61`). The `agent-daily-scores` rider — see A4. `chat.js` — read-only. **Ruling** unchanged: readers + a per-day tape record written by a flag-gated rider + a screen behind its own flag is fence-free.

**E2.** **Ruling** unchanged: the rider on its own cron after `agent-daily-scores`. 41/100 slots. No objection to the spec going to Sol now, provided it cites **this** document.

**E3 — since Sep 15, VERIFIED with three corrections.** The fit check and threshold lint built dark Sep 16–17 (`ac5f8ebf`, `3f7a39f1`) and **both are still dark** (§0.1). **CORRECTED: B2 has not merged** (§0 row 2) — the forensics fields on `chatExchanges[].archetypeGate` are at HEAD from the fit-check build (`directiveGate.js:261-262`), the attestation is not. Eval transport hygiene: `promptBuiltAt`, `buildMs`, `callMs` on every entry (`agent-evaluate.js:3664-3666`), `haikuError.timeoutKind` (`:3657`; `agentEvalTransport.js:117`). `SHADOW_ASSEMBLY_ENABLED = false` (`featureFlags.js:1482`; `docs/audits/20260912_FLIP_SHADOW_ASSEMBLY_OFF.md`). The eval small-fixes integration (Sep 20, PR #875 `75b89b9a`): `holdKind` (`:3678`), `guardrailFault` as its own field (`:3681`, never merged into `haikuError`, `:2341-2343`), `refresh_failed` (`:2372-2385`), the T1 snapshot rebuild after a forced exit (`9d06c0fd`). **`evalId` is monotonic via `cronState.evalSeq` — VERIFIED merged:** commit `929624ca` in PR #885 (`1740996d`, Sep 21); mint at `:2731-2741`, written on the same update as the append at `:3982-3986`. `scoreState.evaluationCount` still saturates at 150 (`:3938`, `:3951`). The capture build merged **and lit** Sep 21 (§A1). The intraday arc: build 1 merged Sep 19 (PR #871), collection lit Sep 21 (`39e5c48a`), calcVersion 2 merged Sep 21 (PR #887, `1dcdfe75`; `intradayConfig.js:102`; its §2 review is PR #886 = HEAD). **CORRECTED:** evaluation entries carry `intradaySnapshotId`, `intradayGeneration`, `intradayViewRef`, `intradayViewStatus`, `intradayEvaluatedAt`, `intradayPolicyVersion`, `decisionStartedAt`, `decisionCompletedAt` (`api/_utils/intraday/evaluatorHook.js:63-76`) **only once `INTRADAY_DIAGNOSTIC_ENABLED` flips** — the composer is inside that branch (`agent-evaluate.js:3696`, `:3728-3738`); at HEAD no entry has them and no `agentBattles/{id}/intradayViews/{evalId}` document is written (`:3713`; `evaluatorHook.js:17`). The subcollection's rules block is in place: owner read, client write denied (`firestore.rules:448-452`). **Ruling for the tape** unchanged. The "integrity findings register" is the four Astra audits on `main` (`docs/audits/2026-09-19_ASTRA_RUNTIME_STATE_INTEGRITY_AUDIT.md`, `2026-09-20_DATA_CONSISTENCY_CONCURRENCY_RETRY_AUDIT.md`, `2026-09-20_FANTASYTRADES_PERFORMANCE_LATENCY_COST_AUDIT.md`, `2026-09-20_ASTRA_SECURITY_ABUSE_AUDIT.md`); no single register file exists. DCR-001 is Package A (`…RETRY_AUDIT.md:466`).

---

## F. Items the HEAD pass filled

### F1 — A1: capture build coverage of the handback items

All writers are in `api/cron/agent-evaluate.js` unless named; all documents are `agentBattles/{battleId}/ticks/{tickId}` (permanent) unless named; the composer is `api/_utils/tickCapture/captureWriter.js` and the admission allowlist is `api/_utils/tickCapture/captureSerializer.js:63-185` (default deny, `:27-31`).

| Handback item | Coverage | Writer · path · fields | What is not there |
|---|---|---|---|
| **C-1** armed state per mechanism per held symbol per check; trigger gate opened; guardrails evaluated | **partial** | **Per-symbol risk verdict, every admitted tick that reaches the risk pass** (it runs before the trigger gate): `ticks.risk.verdicts.{sym} = { action, reason }`, `risk.evaluatedCount`, `risk.lockedCount`, `risk.forcedExitCount` — writer `:1566-1577` (read off the map `evaluateRisk` filled at `:1543-1552`); composed `captureWriter.js:244-249`; allowlist `captureSerializer.js:94-98`. `action ∈ HOLD | EMERGENCY_SWAP | SWAP_OUT | TRAIL_STOP | LOCK`, `reason ∈ bust_avoidance | vwap_failure | threshold_proximity | stepped_trail | stagnation | null` (`api/_utils/agentRiskManager.js:127-196`, fenced, read-only). **Guardrails:** `ticks.guardrail = { evaluated, deployedCount, overrideCount, sourceNote, faultClass, suppressionPassRan, suppressionPassFaulted }` — writer `:2996-3003` (`evaluated` = not refresh-failed AND deployed guardrails > 0 or a sector-slot observe cap), fault `:2984`; composed `captureWriter.js:231-239`. **Trigger gate:** `ticks.exitReason = 'no_trigger'` with `stageReached = 'trigger_evaluated'` when it did not open (`:2286-2313`); on a triggered tick the entry's `triggers[]` (`:3641`). Also on prompt-built ticks: `evaluations[].evidence.{sym}.risk = { action, reason? }` (`api/_utils/tickStamps.js:160-165`, `:211`). | **The four-state model per mechanism is not recorded.** The record holds one verdict per symbol, not a state per mechanism (bust / VWAP / trail / LOCK / stagnation): a mechanism that did not fire cannot be told apart as *evaluated-and-held* versus *disarmed* (e.g. the trail stop is silent both when `atrX < trailATR` and when `sma20_5m` is absent, `agentRiskManager.js:164-171`). No verdicts on `degraded_quotes` (`:869-870`) or `cpu_passive` (`:1091`) exits. The spec moved this out by name: "Unified armed state and exit levels → Exit-dials arc, with G-1" (spec V1.4 `:41`, `:94`; Phase 0 `20260920_PHASE0_TICK_CAPTURE.md:356`). |
| **C-2** eligible alternatives at swap time (bench composition, cooldowns, hot-bench additions) | **partial (thin)** | **The gates a live proposal met:** `ticks.checks.{proposedPairValidation, lock, distressedVeto, hurdle, swapCap, conviction, reservation, execution} = { status, result, stage, symbolOut, symbolIn, reason }` — `status ∈ evaluated | bypassed | not_evaluated | unknown | failed`, `result ∈ passed | blocked | faulted` (`captureConfig.js:120-149`); writers `:3017-3023` (lock), `:3036-3044` (distressed veto), `:3052-3059` (pair validation), `:3065-3071` (conviction, always `unknown` — verdict discarded inside the fenced validator), `:3130`, `:3138` (hurdle, swap cap), `:3244` (reservation), `:3256`, `:3288`, `:3446-3450` (execution); untouched checks default to `not_evaluated` (`captureWriter.js:139-145`). `ticks.universeSize` (`captureWriter.js:221`) and `manifest.evidenceKeys` = the held symbols (`:3862`). | **The bench roster, cooldowns and hot-bench names at swap time are not persisted.** Held ∪ bench ∪ candidate sets are collected (`:1046`, `:2288-2292`) only to admit symbols through the serializer (`captureSerializer.js:16-19`) and reach the document as a **count**. The fenced picker's rejected alternatives are `unknown` by ruling ("Full alternatives enumeration → Tier 2, with J2", spec V1.4 `:39`, `:96`). The live bench is `battle.portfolio.bench` (mutable, no history); the model-flagged names per check are `evaluations[].candidates[]` (B3). |
| **C-3** risk boundary numbers per exit (bust buffer, entry-based ATR multiple, trail level, 5-min SMA, VWAP streak and dead band, guardrail threshold and actual) | **not covered** | Nothing on the tick record: the allowlist admits `risk.verdicts.*.action` and `.reason` as ids only (`captureSerializer.js:94-95`); a number in a verdict would be rejected to the TTL body. **Where the numbers exist at HEAD, none of it a per-exit record:** (a) the risk manager's `detail` prose (`agentRiskManager.js:132`, `:145`, `:158`, `:169`, `:191`) → the status-feed message `Risk: ${detail}` (`:1845-1858`, `citedRules: [reason]`, `triggeredBy: risk_{reason}`) — English, in a feed capped at 50/100 (`:805`); (b) `evaluations[].evidence.{sym}.atrX` (the entry-based ATR multiple), `vwapDev`, `px`, `chg`, `bbPct`, `nr7`, `regime` (`tickStamps.js:192-209`) — prompt-built ticks only, capped 150; (c) `evaluations[].guardrailOverrides[] = { threshold, actual, action, symbol, replacementSymbol, … }` (`:3651`; `api/_utils/agentGuardrails.js:175-182`, `:538-546`, fenced) — real fields, but zero deployed guardrails exist (D6); (d) the learning receipt's `guardrailReplay` **null-flags** `highWaterMark`, `trailActivation`, `trailStepLevel` as "NOT stored" (`learningSchemas.js:155-164`). | **Not stored anywhere per exit:** the bust buffer (`bustBuffer`, preset-driven, default −0.85, `agentRiskManager.js:120`), the trail level (`trailStopATR`, default 1.5, `:122`), the 5-minute SMA20 value (`intradaySnapshot.sma20_5m`, consumed at `:165`, never stamped), the VWAP streak (`vwapTicks[sym]`, a running counter overwritten each tick through `finalizeCronState`, `:2303`) and the dead band (`presetConfig.risk.vwapDeadBandPct`, default 0.5, `:1513`). The thresholds are derivable after the fact from the battle's `strategyPreset` and the preset config version; the per-tick actuals are not. Owner per the spec: exit-dials arc (spec V1.4 `:41`). |
| **C-5** per-day close snapshot of `currentScore` and its terms; persisted `result` at completion | **not covered** | Per-tick only: `ticks.scores = { active, banked, total, opponent, bankedBadgePoints }` exactly as the tick wrote them (`:1039-1045`; `captureWriter.js:314-320`) — uncapped, permanent. `evaluations[].scores = { active, banked, total }` (`:3642-3646`, capped 150). `agent-daily-scores` writes `scoreState.dailyScores.{dayKey} = { badgePoints, recorded, recordedAt, recordedBy }` (`api/cron/agent-daily-scores.js:182-187`) — badge points, active battles only. | **NOT FOUND:** `scoreState.dayClose[]`, `scoreState.result`, any per-day `currentScore` snapshot, any post-valuation write. Completion writes `status: 'completed'`, `completedAt` and a `battle_complete` feed entry with `score` (`:5812-5847`); `result` goes to agent stats only (`:5903`). See A4. |

### F2 — A2: the captured record's shape and retention

**Paths and keys.** `agentBattles/{battleId}/ticks/{tickId}` (permanent, no free text) and `agentBattles/{battleId}/tickBodies/{tickId}` (all text, TTL), written in **one atomic batch** (`captureWriter.js:531-534`, `:546`) so a tick is captured whole or not at all (`:6-11`). `tickId = ${battleId}:${tickSeq}` (`captureContext.js:130`); `tickSeq` is minted inside the existing admission transaction, `cronState.tickSeq + 1`, committed with the lock (`agent-evaluate.js:679-688`) — the record's denominator. `evalId` rides both documents alongside (`captureWriter.js:179`, `:212`) and is null on ticks that never composed an entry (`no_trigger` and the other early exits). `actions[].actionId = ${tickId}:${n}` (`captureContext.js:249`). Subcollection names: `captureConfig.js:66-67`. Schema version 1 (`:38`).

**The permanent document's fields** (`captureWriter.js:207-337`, admitted by `captureSerializer.js:63-185`): `schemaVersion, tickId, battleId, tickSeq, evalId, agentId, ownerId, gameMode, day, battlePhase, capturedAt, stageReached, exitReason, universeSize`; `model.{outcome, failureClass, invalidField, timeoutKind, attempted, dispatched}`; `guardrail.{faultClass, sourceNote, overrideCount, evaluated, deployedCount, suppressionPassRan, suppressionPassFaulted}`; `checks.{name}.{status, result, stage, symbolOut, symbolIn, reason}`; `risk.{verdicts.{sym}.{action, reason}, lockedCount, forcedExitCount, evaluatedCount}`; `decision.{original, final, originalSymbolOut, originalSymbolIn, finalSymbolOut, finalSymbolIn, conviction, tier, holdKind, downgraded, validationErrorCount, replacedByDeterministic}`; `actions[]` (above); `controls.{rendered, suppressedControlCount, activeRuleRendered, directiveThreadId, directiveAdjustmentId, directiveCanonicalTextVersion, directiveTextHash, directiveSuppressed, standingLeanIds[], standingLeanVersions[], standingLeanTextHashes[], activeRuleIds[], activeRuleTextHashes[], equippedConfigHash, controlEpoch}`; `manifest.{evidenceKeys[], vintages{}, vintageLabels{}, notRenderedFields[]}`; `callEnvelope.{requestedModel, returnedModel, temperature, maxOutputTokens, inputTokens, outputTokens, buildMs, callMs, httpStatus, promptBuiltAt}`; `body.{status, incomplete, requestSha256, responseSha256, requestBytes, responseBytes, expireAt}`; `scores.{active, banked, total, opponent, bankedBadgePoints}`; `capture.{preCommitMs, bodyMs, deadlineMs, minRemainingBudgetMs, remainingBudgetMs, disposition, rejectedFieldCount}`.

**The body document's fields** (`captureWriter.js:174-204`): `schemaVersion, tickId, battleId, tickSeq, evalId, capturedAt, expireAt`; `request.{body, bytes, sha256, truncated}`; `response.{body, bytes, sha256, truncated, status, returnedModel}`; `copyError`; `originalToolResult` (the model's proposal exactly as returned, before any deterministic replacement — includes its `anticipationCandidates`, `rationale`, `hypothesis`); `finalToolResult`; `controlsAsRendered.{directiveText, standingLeanTexts[]}`; `controlSourceText`; `faults.{model, guardrail, capture}`; `validationErrors[]`; `rejectedFields[]`; and, when capped, `shedFields[]`, `bodyIncomplete`.

**`status` values a reader can see** (`captureConfig.js`): `model.outcome ∈ ok | failed | not_attempted` (`:108`), with `model.failureClass` carrying the shipped classes unchanged — `timeout | build_timeout | truncated_response | budget_skipped | invalid_tool_result | refresh_failed`, an HTTP status string, an error constructor name, or `unknown` (`:101-106`); `checks.*.status ∈ evaluated | bypassed | not_evaluated | unknown | failed` (`:134` — **`failed`** = an executor or check that was called and threw, V1.4 amendment 1, `:126-133`); `checks.*.result ∈ passed | blocked | faulted` (`:149`); `guardrail.faultClass ∈ guardrail_error | null` (`:118`); `exitReason ∈ degraded_quotes | cpu_passive | proposal_pending | gameplan_pending | gameplan_created | no_trigger | completed | tick_error` (`:75-84`); `stageReached ∈ admitted … finalized` (`:87-99`); `body.status ∈ written | truncated | copy_failed | skipped` (`:158`); `capture.disposition` is always `written` on a document that exists (`captureWriter.js:334`) — `skipped_budget`, `timed_out`, `write_failed`, `serialize_failed` (`captureConfig.js:165-171`) are dispositions the finalizer returns to the log, never a document (`captureWriter.js:479-495`, `:553`); a timed-out commit is *unknown* until the export checks presence (`:17-21`). The battle document's own `status` is `active | completed` (`agent-evaluate.js:5813`).

**Retention — what expires and what persists.**
- **TTL (120 days from capture):** `tickBodies` only — `expireAt` (`captureConfig.js:63`; `captureWriter.js:170`), TTL declared and matching production (`firestore.indexes.json:612-630`; commit `d1a8babe`). The unrelated `signalDropCache.expiresAt` is the only other TTL field (`:671-689`).
- **Persistent:** `ticks` (no TTL, no cap); `agentBattles/{id}/intradayViews/{evalId}` (no TTL; written only once the diagnostic flag flips); `learningReceipts/{battleId}/receipts/*` (no TTL, create-only); the battle document itself. **Capped, not expiring, on the battle document:** `evaluations[]` 150 (`agent-evaluate.js:3938`), `statusFeed[]` 100 with an agent, 50 without (`:805`), `trades[]` 50 (`agentSwapExecution.js:354`), `cronState.cronErrors[]` 20 (`:3996`); `chatExchanges[]` is uncapped (`chat.js:1072`).
- **Client access:** `ticks` and `tickBodies` are **denied to every client** by the root default-deny — no `match` block exists for either (`firestore.rules` has none; build report `docs/audits/20260921_BUILD_TICK_CAPTURE.md:236-244`; acceptance `test/rules/tickCaptureDenials.rules.mjs`). A Film Room **screen** cannot subscribe to them; a server-side rider (Admin SDK) can, and the export script already reads them that way (`scripts/export-tick-capture.js:1-40`). `intradayViews` is owner-readable (`firestore.rules:448-452`).

**Every field the Film Room named, and its persistent home** (the request's list: score bridge, swap replay, who made each exit, directive card, plans beside prices, the per-day tape):

| Tape needs | Persistent home(s) | TTL-only? |
|---|---|---|
| Score per check | `ticks.scores.*` (permanent, per admitted tick); `evaluations[].scores` (capped); `scoreState.*` (live) | No |
| Swap replay, who made each exit | `ticks.actions[].{source, exitReason, symbolOut, symbolIn, swappedOutAt, lockedPoints, entryPrice, committed}` (permanent); `trades[]` with `exitReason`/`source` from the cron's metadata (capped 50); learning receipts (`source`, `exitReason`, `receiptSeq`) | No |
| Directive card (player text, canonical text, thread, filing status, forensics) | `chatExchanges[].{userMessage, agentResponse, directive, directiveThreadId, archetypeGate}` (uncapped); `battle.directive` (latest only); `ticks.controls.directive*` ids/hashes (permanent) | No — only the *rendered* text copy is TTL'd (`tickBodies.controlsAsRendered`), and it duplicates the battle document |
| Heard / Saw per check | `evaluations[].heard`, `.evidence`, `.vintages` (capped 150); `ticks.controls.{directiveThreadId, directiveSuppressed}` (permanent, the same resolution) | No |
| Plans beside prices (`candidates[]`, `threshold`) | `evaluations[].candidates[]` (capped 150) — **not on the permanent tick record** (no `candidates` path in the allowlist) | Partly: the pre-lint copy is only in `tickBodies.originalToolResult` |
| Model rationale / hypothesis | `evaluations[].{rationale, hypothesis}` (capped 150) | The verbatim response is TTL-only; the entry's copy persists |
| Not-evaluated / skipped / deferred | `ticks.exitReason`, `ticks.model.*` (permanent); `evaluations[].haikuError` (capped); deferred: nothing | No |
| Exact request and response bytes | `tickBodies.request/response` | **Yes** — and the tape does not need them |

**Confirmed:** nothing the tape named lives only in a TTL'd body. **Finding for the capture build's owner** (per the A2 ruling): the tape's per-check facts that exist only on `evaluations[]` — `candidates`, `heard`, `evidence`, `rationale` — are capped at 150 entries, so on a multi-day battle the earliest checks fall off the battle document while their tick records persist without those fields. The Film Room's per-day record, written the same night, is what keeps them; that is the ruling's design and the reason it is right.

**What the tape may rely on for any check captured while the flag is on:** `tickId`, `tickSeq`, `capturedAt`, `stageReached`, `exitReason`, `model.*`, `checks.*`, `risk.*`, `scores.*`, `actions[]`, `controls.*`, `body.status` — present on every document that exists (defaults are explicit, `captureWriter.js:207-337`); `evalId`, `day`, `battlePhase` only on ticks that reached `decision_resolved` (`agent-evaluate.js:3837`). Coverage is **best-effort with countable gaps**: a capture that skips for budget (`< 10 s` remaining, `captureConfig.js:44`), times out (3 s deadline, `:41`) or fails leaves a minted `tickSeq` with no document, which the export reports as a gap against `cronState.tickSeq` (`captureCoverage.js:12`, `:48`). The tape must treat a missing `tickSeq` as "no record for this check", exactly as the A3 ruling says.

### F3 — Flag states at HEAD

See §0.1. All seven requested flags plus `SHADOW_ASSEMBLY_ENABLED` and the intraday stage flags, with lines.

### F4 — Unmerged branches that bear on an answer

Scanned every `origin/*` branch ahead of `origin/main` for source diffs touching `evalSeq`, `tickSeq`, `deferred`, `dayClose`, `tape`, `TICK_CAPTURE` and for any commit after Sep 13. No branch touches `evalSeq`, `dayClose` or a tape rider in source. The ones that bear on an answer:

| Branch | Tip · date · ahead | What it carries | What it would change if merged |
|---|---|---|---|
| `origin/claude/b2-directive-transaction` | `5ce73eb9` · Sep 13 · 13 commits, 21 files (+5090/−305), base `c35f9a59` (Sep 12) | New `api/_utils/directiveTransaction.js`; `api/agent/chat.js`, `api/agent/file-directive.js`, `api/_utils/agentChatBudget.js`, `api/_utils/compositionProtectedStoresAllowlist.json`, `src/screens/battleView/selectBench.js`; docs `20260913_BUILD_B2_DIRECTIVE_TRANSACTION.md`, `20260913_PHASE0_B2_SHOWIT_PREREQS.md` (neither on `main`) | **B1:** the typed chat path files through the same transaction as the chip path (status and budget re-read inside the write). **E3:** every response attests `persisted` / `charged` (commit `4cb20114`); the client reads the attestation (`39f17671`). **B3:** `FLAGGED_DISPLAY_CAP = 5` at the read (`5715668f`). Nine days behind a `main` that since rewrote `chat.js` and `directiveGate.js` (fit check Sep 16–17, cautious register Sep 17) — it will not merge clean; its build report's one STOP-class item (a commit that landed whose reply was lost, its §7) is unresolved. |
| `origin/claude/flip-directive-fit-check` | merged (report only) | `docs/audits/20260918_FLIP_DIRECTIVE_FIT_CHECK.md` | Nothing further; the flag is unchanged. **B2.** |
| `origin/claude/epic-bell-stwcxu` | `d75c35bd` · Sep 15 · 1 docs commit | `docs/audits/20260915_PHASE0B_FILM_ROOM_DISCOVERY.md` (886 lines; read-only discovery at `a007a2b5`) | No code. Its findings match this pass at HEAD: the result is never written to the battle document; stop prices, trail levels and high-water marks are nowhere on the trade; a sold stock's later prices are never written down; the gameplan approval UI is unreachable (its A1, B2, B4). **A4, C2 (G-2), F1 C-3.** It is the Film Room's own record and should be merged so a later session can cite it. |
| `origin/claude/stoic-cray-wwhf0n` · `origin/claude/vwap-exit-dating-census-2sdebc` | `5d90d260` · `d9b57917` · Sep 17 · 1 commit each | `scripts/exit-dials-live-census.js`, `scripts/vwap-exit-dating-census.js` (read-only census scripts) | No product change; they are the basis of the D6 counts. |
| `origin/claude/elegant-mccarthy-vwxcog` | `2722dfa0` · Sep 21 · 1 docs commit | §2 review of the intraday follow-up PR | No code. **E3** context only. |
| `origin/claude/phase0-eval-cron-scaling` | ahead 0 | — | Merged; its report is on `main`. **The deferred-beat / instrumentation PR it recommends (`…SCALING.md:271`, `:298`) exists on no branch.** |

**NOT FOUND on any branch:** a `scoreState.dayClose` writer, a completion `result` on the battle document, a `dropReceipt`, a deferred beat, an `intradayViews`-style subcollection for evaluation entries, a held-position parity change, a review-mode grounding path, a tape rider.

---

## Method (BUILD_RULES §3)

`git fetch origin` was the first command of the session. HEAD `463a375f` = `origin/main`. The branch `claude/cc-arc-answers-film-room-tz3wch` was cut from it (the task named `docs/cc-arc-answers-to-film-room`; the session's designated branch was used and is reported as such). Working tree clean at cut. Every line cited was read in this session with `sed -n`/`grep -n` over the checkout; fenced files (`agentRiskManager.js`, `agentGuardrails.js`, `agentSwapExecution.js`, `agentBattleService.js`, `agentEvalPromptAssembly.js`) were **read, never edited**. No test was run (none was needed for a read-only pass). Unmerged-branch claims come from `git diff origin/main...<branch>` and `git show <branch>:<path>`. Production values (a live battle document, census counts, a validator's recorded hour) are marked *production* and were not read. One file is added by this branch: this document.
