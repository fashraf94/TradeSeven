{
  "_schema": "Motion guard baseline (spec V1 acceptance row A5, Delight Task 3 Phase 3). Per migrated file: `count` is the number of raw `transition={{ ... }}` opener literals ALLOWED in that file (the ratchet floor), and `authority` says why. New motion consumes src/theme/motion.js; a raw transition literal added to a guarded file trips the guard. The guarded-file list EXPANDS as surfaces migrate (adoption is per-surface and D3-gated). Regen after a deliberate change: GENERATE_MOTION_GUARD_BASELINE=1 npx vitest run src/theme/motion.guard.test.js, then commit this file in the same commit.",
  "guarded": {
    "src/components/Forge/ParamControls/ParamToggle.jsx": {
      "count": 0,
      "authority": "Phase 2 pilot — migrated to the `snappy` token (transition={snappy}); zero raw transition literals remain."
    },
    "src/screens/battleView/ChatSheet.jsx": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/TapeCards.jsx": {
      "count": 0,
      "authority": "Phase A2 (A2.2) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/LandingWash.jsx": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/ThisTurnStrip.jsx": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/TurnLine.jsx": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/WhyPanel.jsx": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/battleViewCopy.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/buildTape.js": {
      "count": 0,
      "authority": "Phase A2 (A2.2) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/deriveReceipts.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/deriveTurnLine.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/selectDeployPlan.js": {
      "count": 0,
      "authority": "Phase A2 (A2.1b) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/selectSymbolRoster.js": {
      "count": 0,
      "authority": "Phase A2 (A2.3) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); a pure selector with no motion at all."
    },
    "src/screens/battleView/scopeTape.js": {
      "count": 0,
      "authority": "Phase A2 (A2.3) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); a pure selector with no motion at all."
    },
    "src/screens/battleView/PeekStrip.jsx": {
      "count": 0,
      "authority": "Phase A2 (A2.4) — the peek strip does not animate at all (seed §A2.4: the line updates when the tape does, with no motion between); zero raw transition literals from birth."
    },
    "src/screens/battleView/derivePeekLine.js": {
      "count": 0,
      "authority": "Phase A2 (A2.4) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); a pure selector with no motion at all."
    },
    "src/screens/battleView/landing.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/selectWhyState.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/useChatSheet.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/useCoarseNow.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/useContentStable.js": {
      "count": 0,
      "authority": "Phase A (A4 review L5-N6) — a Battle View controller surface written on the vocabulary (motionToken / named tokens); zero raw transition literals from birth."
    },
    "src/screens/battleView/ArenaHeader.jsx": {
      "count": 0,
      "authority": "Phase A3 (D-96) — the character pane's arena header, written on the vocabulary from birth: its two transitions are ONE motionToken('smooth', { reducedMotion }) identifier, shared by the mount-in and the bar, so reduced motion resolves both to `instant`. Zero raw transition literals."
    },
    "src/screens/battleView/computeTugOfWarWidth.js": {
      "count": 0,
      "authority": "Phase A3 (D-96) — the character pane's arena header, written on the vocabulary from birth: its two transitions are ONE motionToken('smooth', { reducedMotion }) identifier, shared by the mount-in and the bar, so reduced motion resolves both to `instant`. Zero raw transition literals."
    },
    "src/screens/battleView/CharacterAvatar.jsx": {
      "count": 0,
      "authority": "Phase A3 (D-91, D-98) — the character on the board. Its ONE animation is the bubble's arrival fade, motionToken('fade', { reducedMotion }) keyed on the tape entry's id so it plays once per entry; reduced motion skips `initial` outright. No idle motion of any kind. Zero raw transition literals."
    },
    "src/screens/battleView/deriveBubble.js": {
      "count": 0,
      "authority": "Phase A3 (D-91, D-98) — the character on the board. Its ONE animation is the bubble's arrival fade, motionToken('fade', { reducedMotion }) keyed on the tape entry's id so it plays once per entry; reduced motion skips `initial` outright. No idle motion of any kind. Zero raw transition literals."
    },
    "src/screens/battleView/CharacterPane.jsx": {
      "count": 0,
      "authority": "Phase A3 (D-93) — the pane. Its one transition is motionToken('smooth', { reducedMotion }) on the region's arrival; the section change is a `display` swap, not an animation, because the chat must not be unmounted (hazard 45). Zero raw transition literals."
    },
    "src/screens/battleView/useCharacterPane.js": {
      "count": 0,
      "authority": "Phase A3 (D-93) — the pane. Its one transition is motionToken('smooth', { reducedMotion }) on the region's arrival; the section change is a `display` swap, not an animation, because the chat must not be unmounted (hazard 45). Zero raw transition literals."
    },
    "src/screens/battleView/PaneBench.jsx": {
      "count": 0,
      "authority": "Phase A3 (D-92) — Bench. It has NO motion at all: the section is a list of the decider's own sentences and nothing on it animates. Zero raw transition literals."
    },
    "src/screens/battleView/selectBench.js": {
      "count": 0,
      "authority": "Phase A3 (D-92) — Bench. It has NO motion at all: the section is a list of the decider's own sentences and nothing on it animates. Zero raw transition literals."
    },
    "src/screens/battleView/PaneTape.jsx": {
      "count": 0,
      "authority": "Phase A3 (D-94) — Tape. No motion of its own: the shipped TradeCard carries whatever the stream gives it, and the activity log is a plain show/hide with no AnimatePresence (the overlay's height tween did not come across). Zero raw transition literals."
    },
    "src/screens/battleView/PaneOverflow.jsx": {
      "count": 0,
      "authority": "Phase A3 (D-95) — the pane's overflow. No motion: the menu opens and closes with no transition, because it is a control surface and the seed's motion rules reserve movement for EVENTS (D-97). Zero raw transition literals."
    }
  }
}
