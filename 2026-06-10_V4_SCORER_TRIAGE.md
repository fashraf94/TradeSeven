BaggerBomb V4 Nightly Scorer — Scoring-Divergence Triage Report

[Transcription note — not part of the original: this file is a verbatim transcription of the founder's paste of the original Claude Code session output (June 10–11, 2026). Formatting reflects that paste. Line anchors cited within refer to repo code at HEAD f12f852, not to lines of this file. The fix this triage prescribed was implemented in commit 5432c7f6 and merged to main.]

1. Preamble
Branch: claude/epic-volta-nla260 · HEAD: f12f852a0ae87ab11c4a70a753aac8e0d4b9a992 · Tree: clean · Timestamp: 2026-06-10 ~23:50 UTC.

Clone deepening (founder-approved): the environment provided a shallow clone (245 commits, boundary ≈ 2026-05-13), which blocked Q4. Per founder approval during planning, I ran git fetch --unshallow — a .git-only history download; no working-tree files, branches, or code were touched (tree re-confirmed clean). Full history (2,469 commits) was then available. No other writes of any kind were performed.

Drift notes on cited anchors — all re-verified at HEAD: divergent constants at baggerbomb-v4-daily-scores.js:90-100 and local calculateAssetScore at :103-136 — exact, no drift (VERIFIED, firsthand). Canonical client constants src/constants/baggerBombScoring.js:33-50 (points) and :56-60 (conviction) — the task's :33-60 range holds (VERIFIED, firsthand). Server mirror agentScoring.js:13-35 — exact (VERIFIED, firsthand, prior session at same HEAD). Client direction handling baggerBombUtils.js:538-552 — within the actual :539-552 block (VERIFIED, firsthand).

2. Verdict: STALE-COPY BUG
More precisely: a born-divergent wrong re-implementation in a backup path — never canonical at any point in repo history. The evidence trail (all VERIFIED from deepened history):

Canonical predates the cron. The 15/30/50 / −10/−20/−35 points at 1.0/1.5/2.0 breakpoints already existed in the client before the cron was written (git show 7549e65f^:src/utils/baggerBombUtils.js:19-26; multipliers since their first introduction in 4da2e6c8, 2026-01-30), and were centralized on 2026-02-09 by 7549e65f — "fix: standardize BaggerBomb scoring constants across all game modes" — explicit single-source-of-truth intent.
The cron was born divergent nine days later. 49759920 (2026-02-18, "multi-day score carryover for BaggerBomb V4 battles") created baggerbomb-v4-daily-scores.js with today's exact flat map (creation-commit lines :106-112 identical to HEAD :93-100).
It is a backup that contradicts its primary — in the same commit. The creation message designates the cron a "server-side backup" of the client-side banker created alongside it; that banker imported and used canonical calculateAssetScoreV3 from day one (git show 49759920:src/services/dailyScoringV4Service.js:17, :95). A deliberate rebalance would have changed both; a backup that disagrees with its primary is definitionally wrong.
The contamination source is identified. The creation message says the cron followed "the snake-draft-daily-scores pattern." Snake Draft — a different game mode — used per-event flat scoring baggerBombs * 15 / busts * -7.5, introduced 2026-02-03 (141597bb). The flat 15/−7.5 values are Snake Draft's rulebook, not V4's.
The 1.0/2.0/3.0 breakpoints match no rulebook that ever existed. A repo-wide pickaxe for min: 3.0/tenBagger: 3 hits only the cron's own creation commit; the client has had 1.0/1.5/2.0 in every version since 2026-01-30.
The identical bug class was found and fixed in the sibling — this copy was missed. 018f909c (2026-03-10): "fix: replace duplicated cron scoring function with shared calculator — Remove local calculateAssetScore() from snake-draft-daily-scores.js — Fixes bust point mismatch: cron was using −7.5, client uses −10 — Single source of truth for Snake Draft scoring logic." The V4 cron's local copy of the same pattern was not included.
No stated intent anywhere. No comment, doc, spec, or commit message expresses a V4 flat-points rebalance (repo-wide grep + full-history log). Per the task: absence of intent is itself a finding.
The direction gap is a second, later-onset stale-port: the cron (Feb 18) predates the March 2026 short-scoring work; when direction handling was added to calculateAssetScoreV3 and the V4 swap path, the cron's local scorer was never updated (:103 takes no direction input; no negation anywhere in :103-136).
3. Severity: live-and-diverging-for-real-users (backup-path conditioned)
The cron is scheduled and live: vercel.json:34-35, 15 1 * * 2-6. It selects real-money-game docs: battles where _v == 4 AND state.status == 'active', excluding isTraining (:331-342). VERIFIED. Whether any qualifying battle is active right now is not provable read-only (no Firestore access) — ASSUMED that V4 battles occur, given seven user-visible consumers of its outputs.
Crucial nuance: banking is dual-writer with a recorded-flag mutex. The client-side primary banks with canonical math (dailyScoringV4Service.js:107, idempotency :157-158, recorded: true at :260); the cron backup banks with divergent math only for day-slots the client missed (cron idempotency :183). Divergent values therefore enter the record only on player-days where no client performed day-end banking (e.g., nobody had the battle open at the day transition) — a common case for multi-day battles, but not universal. VERIFIED mechanism; frequency ASSUMED.
When the cron banks, the divergent values are not cosmetic: they flow verbatim into the displayed total (useBaggerBombBattleV4.js:465-467, :484-486), the dashboard (useDashboardScores.js:171-172, plus liveScore write-back :503-523), and — sharpest — final winner determination and XP: processV4BattleCompletion → calculateV4FinalScores sums the banked days (dailyScoringV4Service.js:338-349) and creatorWon = creatorScore >= opponentScore (App.jsx:5184-5197). A divergently-banked day can flip a battle outcome. VERIFIED end to end.
Magnitude is material at higher multipliers: at ≥2.0× ATR, canonical cumulative badges pay +95 (15+30+50) where the cron banks +30; at ≤−2.0×, canonical −65 vs cron −15. Busts are systematically under-penalized (−7.5 vs −10 at the first threshold). VERIFIED arithmetic from both constant sets (baggerBombScoring.js:33-50; cron :93-100, cumulative loop :116-125).
4. Shorts exposure: YES — crypto positions only
Reachable today: the asset picker renders a SHORT button for crypto rows only (AssetPickerModal.jsx:78, :155) → swapServiceV4.js:309-312 writes direction onto crypto incoming assets → battle create/join preserve it (firebaseService.js:1805, :2056: direction: asset.direction || 'long'). The V4 swap service header documents shorts as a supported feature (swapServiceV4.js:12-13). The cron scores such a position as a long: its calculateAssetScore(asset, priceChange, history) has no direction parameter and computes multiplier = priceChange / baseATR and basePoints = priceChange * 10 * tierMultiplier un-negated (:103-108) — a winning crypto short banks as a loss (and earns bust badges) on cron-banked days. All VERIFIED. Note the asymmetry: closed trades and client banking score shorts correctly (canonical scorer), so the error is confined to cron-banked day snapshots of held short positions.

5. Per-question findings
Q1 — Live? Yes, by construction. Entry vercel.json:34-35 (15 1 * * 2-6 ≈ Mon–Fri evening ET). Auth: vercel-cron header or CRON_SECRET (:309-313); trading-day guard (:318-325). Query/filters as in §3. Writes (activity evidence): state.dailyScores.day{N} with recorded/recordedAt/recordedBy:'cron' (:287-292), state.bankedBadgePoints.{role}.total increment + per-day breakdown (:239-243), {role}.history reset with archived dailyThresholds (:246-267), swap-metadata cleanup on portfolios (:269-283). All VERIFIED firsthand. Proof of recent production runs requires Firestore — ASSUMED.

Q2 — One rulebook displayed, another banked? DISPLAY-VS-BANKED DIVERGENCE (conditional on cron banking). Intraday, players see canonical math: the V4 hook calls calculateAssetScoreV3 (useBaggerBombBattleV4.js:20, :368, :436-452) on canonical constants (baggerBombScoring.js:33-60 via baggerBombUtils.js). The displayed total then folds banked days in verbatim — myTotalScore = bankedScore + live + closedTradePoints (:484-486), bankedScore = Σ dailyScores.day{N}.{role}.activeScore (dailyScoringV4Service.js:54-68). The cron's banked activeScore is the sum of its divergent totalPoints (cron :218-225), so on cron-banked days yesterday's canonical on-screen points are re-derived overnight under the flat map — e.g., a displayed +45 badge stack at 1.5× ATR re-banks as +15. bankedBadgePoints is display-only diagnostics, not double-counted (useBaggerBombBattleV4.js:474-477 "diagnostic — for ScoreBreakdownPopover"; shown at BaggerBombBattleView.jsx:695). On client-banked days the books match (same canonical scorer). All VERIFIED.

Q3 — Shorts reachable? Yes (crypto-only), per §4. Stored data retains direction on portfolio positions (cron deep-copies and preserves all asset fields, :271-283; flattenPortfolio spreads ...asset, :78-87) and on closed trades (swapServiceV4.js:261: direction: outAsset.direction || null). VERIFIED.

Q4 — Stale or deliberate? Stale-copy bug; full evidence trail in §2. VERIFIED via deepened history.

Q5 — Blast radius. Exposure window: 2026-02-18 (cron creation, 49759920) → present, ≈16 weeks ≈ 80 trading days (VERIFIED window; affected-battle/doc counts unknowable read-only — ASSUMED indeterminate). Affected population: multi-day, non-training _v==4 battles whose day-ends were cron-banked. Downstream consumption: banked values persist in battle docs and propagate at completion into the result object (winner/loser/margin/XP, App.jsx:5184-5215) and user XP — i.e., not terminal; they touch records users keep. Dashboards and modals read them live (useDashboardScores.js:171-172, useBaggerBombCardScore.js:9, DailyScoresModal.jsx:103,117). compute-rankings.js does not consume them (VERIFIED). Retroactive identification is feasible in principle: cron-banked days are stamped recordedBy: 'cron' (:291) vs the client's recordedBy: source (dailyScoringV4Service.js:262); each banked day stores assetScores and closingPrices (:224-228, :287-292), and short positions are identifiable via persisted direction on portfolios and closed trades. Feasibility only — proposing nothing.

6. Context for the tournament build's records (§7 rider)
The collapse-to-one-source rider should treat the V4 cron's values as a defect to converge, not a mode config to preserve: the flat map was never a sanctioned rulebook (born of cross-game contamination from Snake Draft, breakpoints that never existed anywhere, contradicting its own same-commit primary), and the repo already contains the exact remediation precedent — 018f909c replaced the sibling cron's local scorer with the shared baggerBombCalculator under a "single source of truth" rationale. The rider's copy inventory should also note that the V4 cron's local scorer additionally lacks the March 2026 direction handling and the thresholdPriceChange parameter, so convergence there is not just constant-swapping but scorer-sourcing. Context only — no implementation implied here.

7. HARD STOP
Triage complete. No files created or modified, no branches, no fixes — the only environment change is the founder-approved git fetch --unshallow recorded in the preamble. Founder review follows.
